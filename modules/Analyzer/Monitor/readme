- 检查目录结构 ：确认了main.go和baserun包的存在
- 查看代码内容 ：分析了main.go的功能和baserun包的实现
- 初始化Go模块 ：执行 go mod init batchattach 创建了go.mod文件
- 修改导入路径 ：将main.go中的远程包导入 github.com/Yinzhongkan399/GoServerPS/baserun 改为本地包 batchattach/baserun
- 更新依赖 ：执行 go mod tidy 整理模块依赖
- 编译并运行 ：执行 go build -o batchattach main.go && ./batchattach 编译并运行程序

生成一段代码,使其先编译main.go的内容,以生成对应
生成一段代码，使其编译并读取/.cache/kProberFunc.c的

go generate ./tcxprober/tcxProber.go && CGO_ENABLED=1 go build && sudo ./batchattach


我希望将项目分别编译为三个文件，第一个文件为现在的main的内容，编译过程为
go generate ./tcxprober/tcxProber.go && CGO_ENABLED=1 go build
最后以管理员权限执行这个文件。 
然后，编译kbatch

以AttachAndRunProber.py中的数据库处理逻辑，修改kbatch.go中的数据拉取和保存过程。使得其中数据以相同的方法处理和存储到同名的数据库中。

以kbatch/kbatch.go和tcxprober/tcxProber.go的数据库操作为基础，静态校验server/database.go中的数据读取过程。

请为我在test中增加测试用例，测试server中的各个接口功能正常性，测试用例可使用python完成

请为我在test中增加测试用例，测试数据库中的各个表的数据状态，测试用例可使用python完成

我希望将server中的端口打包为可供智能体调用的skill，该怎么做？