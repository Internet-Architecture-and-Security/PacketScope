
//go:build ignore
#include "./vmlinux.h"
#include <bpf/bpf_helpers.h>
#include <bpf/bpf_endian.h>
#include <bpf/bpf_tracing.h>
#include <bpf/bpf_core_read.h>

typedef __u64 u64;
typedef __u8 u8;
typedef __u32 u32;
typedef __u16 u16;

struct SkProbe
{
	u32 pid;
	u32 padding32;
	u64 kernelTime;
	// char comm[TASK_COMM_LEN];
	// BPF info above
    u64 FuncID;
    u64 ret;
    u64 family;
    u64 dport;
    u64 lport;
    u32 ipv4__sendaddr;
    u32 ipv4__recvaddr;
    u8 ipv6__sendaddr[16];
    u8 ipv6__recvaddr[16];
};

struct packet_metadata
{
    u64 isPacket;
    u64 timestamp;
    u64 pid;
    u64 FuncID;
    u64 payloadlen;
    u8 payloadHdr[58];
};
struct {
    __uint(type, BPF_MAP_TYPE_RINGBUF);
    __uint(max_entries, 1 << 24);
} events SEC(".maps");

struct {
    __uint(type, BPF_MAP_TYPE_RINGBUF);
    __uint(max_entries, 1 << 24);
} SpecEvents SEC(".maps");
// BPF_RINGBUF_OUTPUT(events, 512);
// BPF_RINGBUF_OUTPUT(SpecEvents, 128);


char LICENSE[] SEC("license") = "GPL";


SEC("kprobe/ip_rcv_core")
int BPF_KPROBE(KtprobeipVZVXrcvVZVXcore,struct sk_buff *skb)
{
struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
if(!data){{return 0;}}
struct packet_metadata *pdata = bpf_ringbuf_reserve(&SpecEvents, sizeof(struct packet_metadata), 0);
if(!pdata){
    bpf_ringbuf_discard(data, 0);
    return 0;
}
//struct sock *sk = skb->sk;
data->ret = 0;
data->FuncID=200000;
pdata->FuncID=200000;
data->kernelTime = bpf_ktime_get_ns();
data->pid = bpf_get_current_pid_tgid();
pdata->pid=data->pid;
u64 plen = 0;
bpf_core_read(&plen, sizeof(plen), &skb->len);
pdata->payloadlen = plen;
plen&=0xfff;
pdata->timestamp = data->kernelTime;
//@len: Length of actual data
if(plen>0){
    void *data_ptr = NULL;
    bpf_core_read(&data_ptr, sizeof(data_ptr), &skb->data);
    if (data_ptr) {
        bpf_probe_read_kernel(&pdata->payloadHdr, plen > sizeof(pdata->payloadHdr) ? sizeof(pdata->payloadHdr) : plen, data_ptr);
    }
}
bpf_ringbuf_submit(pdata, 0);
bpf_ringbuf_submit(data, 0);
return 0;
}
SEC("kretprobe/ip_rcv_core")
int BPF_KRETPROBE(KtretprobeipVZVXrcvVZVXcore)
{
struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
if(!data){{return 0;}}
data->FuncID=200000;
data->kernelTime = bpf_ktime_get_ns();
data->pid=bpf_get_current_pid_tgid();
data->ret=1;
bpf_ringbuf_submit(data, 0);
return 0;
}
SEC("kprobe/ip6_rcv_core")
int BPF_KPROBE(Ktprobeip6VZVXrcvVZVXcore,struct sk_buff *skb)
{
struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
if(!data){{return 0;}}
struct packet_metadata *pdata = bpf_ringbuf_reserve(&SpecEvents, sizeof(struct packet_metadata), 0);
if(!pdata){
    bpf_ringbuf_discard(data, 0);
    return 0;
}
// struct sock *sk = skb->sk;
data->ret = 0;
data->FuncID=200001;
pdata->FuncID=200001;
data->kernelTime = bpf_ktime_get_ns();
data->pid = bpf_get_current_pid_tgid();
pdata->pid=data->pid;
u64 plen = 0;
bpf_core_read(&plen, sizeof(plen), &skb->len);
pdata->payloadlen = plen;
pdata->isPacket = 1;
plen&=0xfff;
pdata->timestamp = data->kernelTime;
//@len: Length of actual data
if(plen>0){
    void *data_ptr = NULL;
    bpf_core_read(&data_ptr, sizeof(data_ptr), &skb->data);
    if (data_ptr) {
        bpf_probe_read_kernel(&pdata->payloadHdr, plen > sizeof(pdata->payloadHdr) ? sizeof(pdata->payloadHdr) : plen, data_ptr);
    }
}
bpf_ringbuf_submit(pdata, 0);                  
bpf_ringbuf_submit(data, 0);
return 0;
}
SEC("kretprobe/ip6_rcv_core")
int BPF_KRETPROBE(Ktretprobeip6VZVXrcvVZVXcore)
{
struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
if(!data){{return 0;}}
data->FuncID=200001;
data->kernelTime = bpf_ktime_get_ns();
data->pid=bpf_get_current_pid_tgid();
data->ret=1;
bpf_ringbuf_submit(data, 0);
return 0;
}

SEC("kprobe/icmp_push_reply")
int BPF_KPROBE(KtprobeicmpVZVXpushVZVXreply)
{
   struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){{return 0;}}
    data->kernelTime = bpf_ktime_get_ns();
    data->pid = bpf_get_current_pid_tgid();
    data->FuncID=200002;
    data->ret=0;
    
    // 从寄存器中获取sock指针（在x86_64上，第一个参数通常在rdi寄存器中）
    struct sock *sk = (struct sock *)PT_REGS_PARM1(ctx);
    
    // 验证sk指针是否可读
    if (sk == NULL) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    
    u16 dport;
    if (bpf_probe_read_kernel(&dport, sizeof(dport), &sk->__sk_common.skc_dport) != 0) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    data->dport = (dport >> 8) | ((dport << 8) & 0xff00);
    
    u16 lport;
    if (bpf_probe_read_kernel(&lport, sizeof(lport), &sk->__sk_common.skc_num) != 0) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    data->lport = (lport >> 8) | ((lport << 8) & 0xff00);
    
    u32 family;
    if (bpf_probe_read_kernel(&family, sizeof(family), &sk->__sk_common.skc_family) != 0) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    
    if (family == 10) /* AF_INET6 */
    {
        data->family = 6;
        if (bpf_probe_read_kernel(&data->ipv6__recvaddr, sizeof(data->ipv6__recvaddr), 
                             &sk->__sk_common.skc_v6_daddr.in6_u.u6_addr8) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
        if (bpf_probe_read_kernel(&data->ipv6__sendaddr, sizeof(data->ipv6__sendaddr), 
                             &sk->__sk_common.skc_v6_rcv_saddr.in6_u.u6_addr8) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
    }
    else
    {
        data->family = 4;
        if (bpf_probe_read_kernel(&data->ipv4__recvaddr, sizeof(data->ipv4__recvaddr), 
                             &sk->__sk_common.skc_daddr) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
        if (bpf_probe_read_kernel(&data->ipv4__sendaddr, sizeof(data->ipv4__sendaddr), 
                             &sk->__sk_common.skc_rcv_saddr) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
    }
    
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmp_push_reply")
int BPF_KRETPROBE(KtretprobeicmpVZVXpushVZVXreply)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){{return 0;}}
    data->FuncID=200002;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kprobe/raw_sendmsg")
int BPF_KPROBE(KtproberawVZVXsendmsg)
{
     struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){{return 0;}}
    data->kernelTime = bpf_ktime_get_ns();
    data->pid = bpf_get_current_pid_tgid();
    data->FuncID=200003;
    data->ret=0;
    
    // 从寄存器中获取sock指针（在x86_64上，第一个参数通常在rdi寄存器中）
    struct sock *sk = (struct sock *)PT_REGS_PARM1(ctx);
    
    // 验证sk指针是否可读
    if (sk == NULL) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    
    u16 dport;
    if (bpf_probe_read_kernel(&dport, sizeof(dport), &sk->__sk_common.skc_dport) != 0) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    data->dport = (dport >> 8) | ((dport << 8) & 0xff00);
    
    u16 lport;
    if (bpf_probe_read_kernel(&lport, sizeof(lport), &sk->__sk_common.skc_num) != 0) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    data->lport = (lport >> 8) | ((lport << 8) & 0xff00);
    
    u32 family;
    if (bpf_probe_read_kernel(&family, sizeof(family), &sk->__sk_common.skc_family) != 0) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    
    if (family == 10) /* AF_INET6 */
    {
        data->family = 6;
        if (bpf_probe_read_kernel(&data->ipv6__recvaddr, sizeof(data->ipv6__recvaddr), 
                             &sk->__sk_common.skc_v6_daddr.in6_u.u6_addr8) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
        if (bpf_probe_read_kernel(&data->ipv6__sendaddr, sizeof(data->ipv6__sendaddr), 
                             &sk->__sk_common.skc_v6_rcv_saddr.in6_u.u6_addr8) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
    }
    else
    {
        data->family = 4;
        if (bpf_probe_read_kernel(&data->ipv4__recvaddr, sizeof(data->ipv4__recvaddr), 
                             &sk->__sk_common.skc_daddr) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
        if (bpf_probe_read_kernel(&data->ipv4__sendaddr, sizeof(data->ipv4__sendaddr), 
                             &sk->__sk_common.skc_rcv_saddr) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
    }
    
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/raw_sendmsg")
int BPF_KRETPROBE(KtretproberawVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){{return 0;}}
    data->FuncID=200003;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/rawv6_sendmsg")
int BPF_KPROBE(Ktproberawv6VZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){{return 0;}}
    data->kernelTime = bpf_ktime_get_ns();
    data->pid = bpf_get_current_pid_tgid();
    data->FuncID=200004;
    data->ret=0;
    
    // 从寄存器中获取sock指针（在x86_64上，第一个参数通常在rdi寄存器中）
    struct sock *sk = (struct sock *)PT_REGS_PARM1(ctx);
    
    // 验证sk指针是否可读
    if (sk == NULL) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    
    u16 dport;
    if (bpf_probe_read_kernel(&dport, sizeof(dport), &sk->__sk_common.skc_dport) != 0) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    data->dport = (dport >> 8) | ((dport << 8) & 0xff00);
    
    u16 lport;
    if (bpf_probe_read_kernel(&lport, sizeof(lport), &sk->__sk_common.skc_num) != 0) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    data->lport = (lport >> 8) | ((lport << 8) & 0xff00);
    
    u32 family;
    if (bpf_probe_read_kernel(&family, sizeof(family), &sk->__sk_common.skc_family) != 0) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    
    if (family == 10) /* AF_INET6 */
    {
        data->family = 6;
        if (bpf_probe_read_kernel(&data->ipv6__recvaddr, sizeof(data->ipv6__recvaddr), 
                             &sk->__sk_common.skc_v6_daddr.in6_u.u6_addr8) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
        if (bpf_probe_read_kernel(&data->ipv6__sendaddr, sizeof(data->ipv6__sendaddr), 
                             &sk->__sk_common.skc_v6_rcv_saddr.in6_u.u6_addr8) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
    }
    else
    {
        data->family = 4;
        if (bpf_probe_read_kernel(&data->ipv4__recvaddr, sizeof(data->ipv4__recvaddr), 
                             &sk->__sk_common.skc_daddr) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
        if (bpf_probe_read_kernel(&data->ipv4__sendaddr, sizeof(data->ipv4__sendaddr), 
                             &sk->__sk_common.skc_rcv_saddr) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
    }
    
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/rawv6_sendmsg")
int BPF_KRETPROBE(Ktretproberawv6VZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){{return 0;}}
    data->FuncID=200004;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kprobe/udp_sendmsg")
int BPF_KPROBE(KtprobeudpVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){{return 0;}}
    data->kernelTime = bpf_ktime_get_ns();
    data->pid = bpf_get_current_pid_tgid();
    data->FuncID=200005;
    data->ret=0;
    
    // 从寄存器中获取sock指针（在x86_64上，第一个参数通常在rdi寄存器中）
    struct sock *sk = (struct sock *)PT_REGS_PARM1(ctx);
    
    // 验证sk指针是否可读
    if (sk == NULL) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    
    u16 dport;
    if (bpf_probe_read_kernel(&dport, sizeof(dport), &sk->__sk_common.skc_dport) != 0) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    data->dport = (dport >> 8) | ((dport << 8) & 0xff00);
    
    u16 lport;
    if (bpf_probe_read_kernel(&lport, sizeof(lport), &sk->__sk_common.skc_num) != 0) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    data->lport = (lport >> 8) | ((lport << 8) & 0xff00);
    
    u32 family;
    if (bpf_probe_read_kernel(&family, sizeof(family), &sk->__sk_common.skc_family) != 0) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    
    if (family == 10) /* AF_INET6 */
    {
        data->family = 6;
        if (bpf_probe_read_kernel(&data->ipv6__recvaddr, sizeof(data->ipv6__recvaddr), 
                             &sk->__sk_common.skc_v6_daddr.in6_u.u6_addr8) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
        if (bpf_probe_read_kernel(&data->ipv6__sendaddr, sizeof(data->ipv6__sendaddr), 
                             &sk->__sk_common.skc_v6_rcv_saddr.in6_u.u6_addr8) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
    }
    else
    {
        data->family = 4;
        if (bpf_probe_read_kernel(&data->ipv4__recvaddr, sizeof(data->ipv4__recvaddr), 
                             &sk->__sk_common.skc_daddr) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
        if (bpf_probe_read_kernel(&data->ipv4__sendaddr, sizeof(data->ipv4__sendaddr), 
                             &sk->__sk_common.skc_rcv_saddr) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
    }
    
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_sendmsg")
int BPF_KRETPROBE(KtretprobeudpVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){{return 0;}}
    data->FuncID=200005;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kprobe/udpv6_sendmsg")
int BPF_KPROBE(Ktprobeudpv6VZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){{return 0;}}
    data->kernelTime = bpf_ktime_get_ns();
    data->pid = bpf_get_current_pid_tgid();
    data->FuncID=200006;
    data->ret=0;
    
    // 从寄存器中获取sock指针（在x86_64上，第一个参数通常在rdi寄存器中）
    struct sock *sk = (struct sock *)PT_REGS_PARM1(ctx);
    
    // 验证sk指针是否可读
    if (sk == NULL) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    
    u16 dport;
    if (bpf_probe_read_kernel(&dport, sizeof(dport), &sk->__sk_common.skc_dport) != 0) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    data->dport = (dport >> 8) | ((dport << 8) & 0xff00);
    
    u16 lport;
    if (bpf_probe_read_kernel(&lport, sizeof(lport), &sk->__sk_common.skc_num) != 0) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    data->lport = (lport >> 8) | ((lport << 8) & 0xff00);
    
    u32 family;
    if (bpf_probe_read_kernel(&family, sizeof(family), &sk->__sk_common.skc_family) != 0) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    
    if (family == 10) /* AF_INET6 */
    {
        data->family = 6;
        if (bpf_probe_read_kernel(&data->ipv6__recvaddr, sizeof(data->ipv6__recvaddr), 
                             &sk->__sk_common.skc_v6_daddr.in6_u.u6_addr8) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
        if (bpf_probe_read_kernel(&data->ipv6__sendaddr, sizeof(data->ipv6__sendaddr), 
                             &sk->__sk_common.skc_v6_rcv_saddr.in6_u.u6_addr8) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
    }
    else
    {
        data->family = 4;
        if (bpf_probe_read_kernel(&data->ipv4__recvaddr, sizeof(data->ipv4__recvaddr), 
                             &sk->__sk_common.skc_daddr) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
        if (bpf_probe_read_kernel(&data->ipv4__sendaddr, sizeof(data->ipv4__sendaddr), 
                             &sk->__sk_common.skc_rcv_saddr) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
    }
    
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udpv6_sendmsg")
int BPF_KRETPROBE(Ktretprobeudpv6VZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){{return 0;}}
    data->FuncID=200006;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kprobe/tcp_sendmsg")
int BPF_KPROBE(KtprobetcpVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){{return 0;}}
    data->kernelTime = bpf_ktime_get_ns();
    data->pid = bpf_get_current_pid_tgid();
    data->FuncID=200007;
    data->ret=0;
    
    // 从寄存器中获取sock指针（在x86_64上，第一个参数通常在rdi寄存器中）
    struct sock *sk = (struct sock *)PT_REGS_PARM1(ctx);
    
    // 验证sk指针是否可读
    if (sk == NULL) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    
    u16 dport;
    if (bpf_probe_read_kernel(&dport, sizeof(dport), &sk->__sk_common.skc_dport) != 0) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    data->dport = (dport >> 8) | ((dport << 8) & 0xff00);
    
    u16 lport;
    if (bpf_probe_read_kernel(&lport, sizeof(lport), &sk->__sk_common.skc_num) != 0) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    data->lport = (lport >> 8) | ((lport << 8) & 0xff00);
    
    u32 family;
    if (bpf_probe_read_kernel(&family, sizeof(family), &sk->__sk_common.skc_family) != 0) {
        bpf_ringbuf_discard(data, 0);
        return 0;
    }
    
    if (family == 10) /* AF_INET6 */
    {
        data->family = 6;
        if (bpf_probe_read_kernel(&data->ipv6__recvaddr, sizeof(data->ipv6__recvaddr), 
                             &sk->__sk_common.skc_v6_daddr.in6_u.u6_addr8) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
        if (bpf_probe_read_kernel(&data->ipv6__sendaddr, sizeof(data->ipv6__sendaddr), 
                             &sk->__sk_common.skc_v6_rcv_saddr.in6_u.u6_addr8) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
    }
    else
    {
        data->family = 4;
        if (bpf_probe_read_kernel(&data->ipv4__recvaddr, sizeof(data->ipv4__recvaddr), 
                             &sk->__sk_common.skc_daddr) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
        if (bpf_probe_read_kernel(&data->ipv4__sendaddr, sizeof(data->ipv4__sendaddr), 
                             &sk->__sk_common.skc_rcv_saddr) != 0) {
            bpf_ringbuf_discard(data, 0);
            return 0;
        }
    }
    
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kretprobe/tcp_sendmsg")
int BPF_KRETPROBE(KtretprobetcpVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){{return 0;}}
    data->FuncID=200007;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_rcv")
int BPF_KPROBE(KtprobeipVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=300000;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_rcv")
int BPF_KRETPROBE(KtretprobeipVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=300000;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kprobe/ipv6_rcv")
int BPF_KPROBE(Ktprobeipv6VZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=300001;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_rcv")
int BPF_KRETPROBE(Ktretprobeipv6VZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=300001;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kprobe/ip_list_rcv")
int BPF_KPROBE(KtprobeipVZVXlistVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=300002;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_list_rcv")
int BPF_KRETPROBE(KtretprobeipVZVXlistVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=300002;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kprobe/ipv6_list_rcv")
int BPF_KPROBE(Ktprobeipv6VZVXlistVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=300003;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_list_rcv")
int BPF_KRETPROBE(Ktretprobeipv6VZVXlistVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=300003;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__icmp_send")
int BPF_KPROBE(KtprobeVZVXVZVXicmpVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46364;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__icmp_send")
int BPF_KRETPROBE(KtretprobeVZVXVZVXicmpVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46364;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip6addrlbl_add")
int BPF_KPROBE(KtprobeVZVXVZVXip6addrlblVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46422;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip6addrlbl_add")
int BPF_KRETPROBE(KtretprobeVZVXVZVXip6addrlblVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46422;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ipv6_addr_label")
int BPF_KPROBE(KtprobeVZVXVZVXipv6VZVXaddrVZVXlabel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46424;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ipv6_addr_label")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipv6VZVXaddrVZVXlabel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46424;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ipv6_dev_ac_dec")
int BPF_KPROBE(KtprobeVZVXVZVXipv6VZVXdevVZVXacVZVXdec)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46426;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ipv6_dev_ac_dec")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipv6VZVXdevVZVXacVZVXdec)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46426;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ipv6_dev_ac_inc")
int BPF_KPROBE(KtprobeVZVXVZVXipv6VZVXdevVZVXacVZVXinc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46427;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ipv6_dev_ac_inc")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipv6VZVXdevVZVXacVZVXinc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46427;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ipv6_sock_ac_close")
int BPF_KPROBE(KtprobeVZVXVZVXipv6VZVXsockVZVXacVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46428;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ipv6_sock_ac_close")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipv6VZVXsockVZVXacVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46428;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__lock_sock")
int BPF_KPROBE(KtprobeVZVXVZVXlockVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46536;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__lock_sock")
int BPF_KRETPROBE(KtretprobeVZVXVZVXlockVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46536;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__lock_sock_fast")
int BPF_KPROBE(KtprobeVZVXVZVXlockVZVXsockVZVXfast)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46538;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__lock_sock_fast")
int BPF_KRETPROBE(KtretprobeVZVXVZVXlockVZVXsockVZVXfast)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46538;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ppp_xmit_process")
int BPF_KPROBE(KtprobeVZVXVZVXpppVZVXxmitVZVXprocess)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46678;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ppp_xmit_process")
int BPF_KRETPROBE(KtretprobeVZVXVZVXpppVZVXxmitVZVXprocess)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46678;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_icmp_send")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXicmpVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46878;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_icmp_send")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXicmpVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46878;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tls_alert_recv")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtlsVZVXalertVZVXrecv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46961;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tls_alert_recv")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtlsVZVXalertVZVXrecv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46961;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tls_alert_send")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtlsVZVXalertVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46962;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tls_alert_send")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtlsVZVXalertVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46962;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_xdp_devmap_xmit")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXxdpVZVXdevmapVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46981;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_xdp_devmap_xmit")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXxdpVZVXdevmapVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=46981;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__release_sock")
int BPF_KPROBE(KtprobeVZVXVZVXreleaseVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47038;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__release_sock")
int BPF_KRETPROBE(KtretprobeVZVXVZVXreleaseVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47038;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sk_attach_prog")
int BPF_KPROBE(KtprobeVZVXVZVXskVZVXattachVZVXprog)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47144;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sk_attach_prog")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskVZVXattachVZVXprog)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47144;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sk_backlog_rcv")
int BPF_KPROBE(KtprobeVZVXVZVXskVZVXbacklogVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47146;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sk_backlog_rcv")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskVZVXbacklogVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47146;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sk_dst_check")
int BPF_KPROBE(KtprobeVZVXVZVXskVZVXdstVZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47150;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sk_dst_check")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskVZVXdstVZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47150;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sk_flush_backlog")
int BPF_KPROBE(KtprobeVZVXVZVXskVZVXflushVZVXbacklog)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47151;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sk_flush_backlog")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskVZVXflushVZVXbacklog)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47151;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sk_free")
int BPF_KPROBE(KtprobeVZVXVZVXskVZVXfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47152;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sk_free")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskVZVXfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47152;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sk_mem_raise_allocated")
int BPF_KPROBE(KtprobeVZVXVZVXskVZVXmemVZVXraiseVZVXallocated)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47154;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sk_mem_raise_allocated")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskVZVXmemVZVXraiseVZVXallocated)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47154;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sk_mem_reclaim")
int BPF_KPROBE(KtprobeVZVXVZVXskVZVXmemVZVXreclaim)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47156;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sk_mem_reclaim")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskVZVXmemVZVXreclaim)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47156;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sk_mem_reduce_allocated")
int BPF_KPROBE(KtprobeVZVXVZVXskVZVXmemVZVXreduceVZVXallocated)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47157;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sk_mem_reduce_allocated")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskVZVXmemVZVXreduceVZVXallocated)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47157;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sk_mem_schedule")
int BPF_KPROBE(KtprobeVZVXVZVXskVZVXmemVZVXschedule)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47159;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sk_mem_schedule")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskVZVXmemVZVXschedule)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47159;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sk_receive_skb")
int BPF_KPROBE(KtprobeVZVXVZVXskVZVXreceiveVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47161;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sk_receive_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskVZVXreceiveVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47161;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_header_pointer")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXheaderVZVXpointer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47163;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_header_pointer")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXheaderVZVXpointer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47163;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_recv_udp")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXrecvVZVXudp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47165;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_recv_udp")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXrecvVZVXudp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47165;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sock_cmsg_send")
int BPF_KPROBE(KtprobeVZVXVZVXsockVZVXcmsgVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47173;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sock_cmsg_send")
int BPF_KRETPROBE(KtretprobeVZVXVZVXsockVZVXcmsgVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47173;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sock_queue_rcv_skb")
int BPF_KPROBE(KtprobeVZVXVZVXsockVZVXqueueVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47174;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sock_queue_rcv_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXsockVZVXqueueVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47174;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sock_set_timestamps")
int BPF_KPROBE(KtprobeVZVXVZVXsockVZVXsetVZVXtimestamps)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47176;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sock_set_timestamps")
int BPF_KRETPROBE(KtretprobeVZVXVZVXsockVZVXsetVZVXtimestamps)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47176;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sock_wfree")
int BPF_KPROBE(KtprobeVZVXVZVXsockVZVXwfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47178;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sock_wfree")
int BPF_KRETPROBE(KtretprobeVZVXVZVXsockVZVXwfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47178;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__tcp_ack_snd_check")
int BPF_KPROBE(KtprobeVZVXVZVXtcpVZVXackVZVXsndVZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47246;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__tcp_ack_snd_check")
int BPF_KRETPROBE(KtretprobeVZVXVZVXtcpVZVXackVZVXsndVZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47246;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__tcpv6_gso_segment_csum")
int BPF_KPROBE(KtprobeVZVXVZVXtcpv6VZVXgsoVZVXsegmentVZVXcsum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47248;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__tcpv6_gso_segment_csum")
int BPF_KRETPROBE(KtretprobeVZVXVZVXtcpv6VZVXgsoVZVXsegmentVZVXcsum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47248;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__udp4_lib_err")
int BPF_KPROBE(KtprobeVZVXVZVXudp4VZVXlibVZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47763;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__udp4_lib_err")
int BPF_KRETPROBE(KtretprobeVZVXVZVXudp4VZVXlibVZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47763;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__udp4_lib_lookup")
int BPF_KPROBE(KtprobeVZVXVZVXudp4VZVXlibVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47765;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__udp4_lib_lookup")
int BPF_KRETPROBE(KtretprobeVZVXVZVXudp4VZVXlibVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47765;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__udp4_lib_mcast_deliver")
int BPF_KPROBE(KtprobeVZVXVZVXudp4VZVXlibVZVXmcastVZVXdeliver)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47767;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__udp4_lib_mcast_deliver")
int BPF_KRETPROBE(KtretprobeVZVXVZVXudp4VZVXlibVZVXmcastVZVXdeliver)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47767;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__udp4_lib_rcv")
int BPF_KPROBE(KtprobeVZVXVZVXudp4VZVXlibVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47769;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__udp4_lib_rcv")
int BPF_KRETPROBE(KtretprobeVZVXVZVXudp4VZVXlibVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47769;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__udp_disconnect")
int BPF_KPROBE(KtprobeVZVXVZVXudpVZVXdisconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47771;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__udp_disconnect")
int BPF_KRETPROBE(KtretprobeVZVXVZVXudpVZVXdisconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47771;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__udp_enqueue_schedule_skb")
int BPF_KPROBE(KtprobeVZVXVZVXudpVZVXenqueueVZVXscheduleVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47772;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__udp_enqueue_schedule_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXudpVZVXenqueueVZVXscheduleVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=47772;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/aa_unix_label_sk_perm")
int BPF_KPROBE(KtprobeaaVZVXunixVZVXlabelVZVXskVZVXperm)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=48225;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/aa_unix_label_sk_perm")
int BPF_KRETPROBE(KtretprobeaaVZVXunixVZVXlabelVZVXskVZVXperm)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=48225;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/aa_unix_sock_perm")
int BPF_KPROBE(KtprobeaaVZVXunixVZVXsockVZVXperm)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=48233;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/aa_unix_sock_perm")
int BPF_KRETPROBE(KtretprobeaaVZVXunixVZVXsockVZVXperm)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=48233;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ack_update_msk")
int BPF_KPROBE(KtprobeackVZVXupdateVZVXmsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=48310;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ack_update_msk")
int BPF_KRETPROBE(KtretprobeackVZVXupdateVZVXmsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=48310;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/audit_log_task")
int BPF_KPROBE(KtprobeauditVZVXlogVZVXtask)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=50048;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/audit_log_task")
int BPF_KRETPROBE(KtretprobeauditVZVXlogVZVXtask)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=50048;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/cubictcp_acked")
int BPF_KPROBE(KtprobecubictcpVZVXacked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53121;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/cubictcp_acked")
int BPF_KRETPROBE(KtretprobecubictcpVZVXacked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53121;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/cubictcp_cong_avoid")
int BPF_KPROBE(KtprobecubictcpVZVXcongVZVXavoid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53122;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/cubictcp_cong_avoid")
int BPF_KRETPROBE(KtretprobecubictcpVZVXcongVZVXavoid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53122;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/cubictcp_cwnd_event")
int BPF_KPROBE(KtprobecubictcpVZVXcwndVZVXevent)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53124;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/cubictcp_cwnd_event")
int BPF_KRETPROBE(KtretprobecubictcpVZVXcwndVZVXevent)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53124;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/cubictcp_init")
int BPF_KPROBE(KtprobecubictcpVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53125;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/cubictcp_init")
int BPF_KRETPROBE(KtretprobecubictcpVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53125;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/cubictcp_recalc_ssthresh")
int BPF_KPROBE(KtprobecubictcpVZVXrecalcVZVXssthresh)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53126;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/cubictcp_recalc_ssthresh")
int BPF_KRETPROBE(KtretprobecubictcpVZVXrecalcVZVXssthresh)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53126;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/cubictcp_state")
int BPF_KPROBE(KtprobecubictcpVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53128;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/cubictcp_state")
int BPF_KRETPROBE(KtretprobecubictcpVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53128;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/current_check_access_socket")
int BPF_KPROBE(KtprobecurrentVZVXcheckVZVXaccessVZVXsocket)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53134;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/current_check_access_socket")
int BPF_KRETPROBE(KtretprobecurrentVZVXcheckVZVXaccessVZVXsocket)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53134;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/dcb_getrewr_prio_dscp_mask_map")
int BPF_KPROBE(KtprobedcbVZVXgetrewrVZVXprioVZVXdscpVZVXmaskVZVXmap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53195;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/dcb_getrewr_prio_dscp_mask_map")
int BPF_KRETPROBE(KtretprobedcbVZVXgetrewrVZVXprioVZVXdscpVZVXmaskVZVXmap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53195;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/dcb_getrewr_prio_pcp_mask_map")
int BPF_KPROBE(KtprobedcbVZVXgetrewrVZVXprioVZVXpcpVZVXmaskVZVXmap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53197;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/dcb_getrewr_prio_pcp_mask_map")
int BPF_KRETPROBE(KtretprobedcbVZVXgetrewrVZVXprioVZVXpcpVZVXmaskVZVXmap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53197;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/dcb_ieee_getapp_default_prio_mask")
int BPF_KPROBE(KtprobedcbVZVXieeeVZVXgetappVZVXdefaultVZVXprioVZVXmask)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53200;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/dcb_ieee_getapp_default_prio_mask")
int BPF_KRETPROBE(KtretprobedcbVZVXieeeVZVXgetappVZVXdefaultVZVXprioVZVXmask)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53200;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/dcb_ieee_getapp_dscp_prio_mask_map")
int BPF_KPROBE(KtprobedcbVZVXieeeVZVXgetappVZVXdscpVZVXprioVZVXmaskVZVXmap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53202;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/dcb_ieee_getapp_dscp_prio_mask_map")
int BPF_KRETPROBE(KtretprobedcbVZVXieeeVZVXgetappVZVXdscpVZVXprioVZVXmaskVZVXmap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53202;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/dcb_ieee_getapp_mask")
int BPF_KPROBE(KtprobedcbVZVXieeeVZVXgetappVZVXmask)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53203;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/dcb_ieee_getapp_mask")
int BPF_KRETPROBE(KtretprobedcbVZVXieeeVZVXgetappVZVXmask)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53203;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/dcb_ieee_getapp_prio_dscp_mask_map")
int BPF_KPROBE(KtprobedcbVZVXieeeVZVXgetappVZVXprioVZVXdscpVZVXmaskVZVXmap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53204;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/dcb_ieee_getapp_prio_dscp_mask_map")
int BPF_KRETPROBE(KtretprobedcbVZVXieeeVZVXgetappVZVXprioVZVXdscpVZVXmaskVZVXmap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=53204;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/dummy_ip6_datagram_recv_ctl")
int BPF_KPROBE(KtprobedummyVZVXip6VZVXdatagramVZVXrecvVZVXctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55087;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/dummy_ip6_datagram_recv_ctl")
int BPF_KRETPROBE(KtretprobedummyVZVXip6VZVXdatagramVZVXrecvVZVXctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55087;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/dummy_ipv6_chk_addr")
int BPF_KPROBE(KtprobedummyVZVXipv6VZVXchkVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55089;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/dummy_ipv6_chk_addr")
int BPF_KRETPROBE(KtretprobedummyVZVXipv6VZVXchkVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55089;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/dummy_ipv6_icmp_error")
int BPF_KPROBE(KtprobedummyVZVXipv6VZVXicmpVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55091;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/dummy_ipv6_icmp_error")
int BPF_KRETPROBE(KtretprobedummyVZVXipv6VZVXicmpVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55091;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/dummy_ipv6_recv_error")
int BPF_KPROBE(KtprobedummyVZVXipv6VZVXrecvVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55093;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/dummy_ipv6_recv_error")
int BPF_KRETPROBE(KtretprobedummyVZVXipv6VZVXrecvVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55093;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/espintcp_close")
int BPF_KPROBE(KtprobeespintcpVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55763;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/espintcp_close")
int BPF_KRETPROBE(KtretprobeespintcpVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55763;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/espintcp_data_ready")
int BPF_KPROBE(KtprobeespintcpVZVXdataVZVXready)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55764;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/espintcp_data_ready")
int BPF_KRETPROBE(KtretprobeespintcpVZVXdataVZVXready)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55764;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/espintcp_destruct")
int BPF_KPROBE(KtprobeespintcpVZVXdestruct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55765;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/espintcp_destruct")
int BPF_KRETPROBE(KtretprobeespintcpVZVXdestruct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55765;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/espintcp_init_sk")
int BPF_KPROBE(KtprobeespintcpVZVXinitVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55768;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/espintcp_init_sk")
int BPF_KRETPROBE(KtretprobeespintcpVZVXinitVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55768;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/espintcp_parse")
int BPF_KPROBE(KtprobeespintcpVZVXparse)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55770;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/espintcp_parse")
int BPF_KRETPROBE(KtretprobeespintcpVZVXparse)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55770;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/espintcp_poll")
int BPF_KPROBE(KtprobeespintcpVZVXpoll)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55772;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/espintcp_poll")
int BPF_KRETPROBE(KtretprobeespintcpVZVXpoll)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55772;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/espintcp_push_msgs")
int BPF_KPROBE(KtprobeespintcpVZVXpushVZVXmsgs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55773;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/espintcp_push_msgs")
int BPF_KRETPROBE(KtretprobeespintcpVZVXpushVZVXmsgs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55773;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/espintcp_push_skb")
int BPF_KPROBE(KtprobeespintcpVZVXpushVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55774;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/espintcp_push_skb")
int BPF_KRETPROBE(KtretprobeespintcpVZVXpushVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55774;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/espintcp_queue_out")
int BPF_KPROBE(KtprobeespintcpVZVXqueueVZVXout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55775;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/espintcp_queue_out")
int BPF_KRETPROBE(KtretprobeespintcpVZVXqueueVZVXout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55775;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/espintcp_rcv")
int BPF_KPROBE(KtprobeespintcpVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55777;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/espintcp_rcv")
int BPF_KRETPROBE(KtretprobeespintcpVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55777;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/espintcp_recvmsg")
int BPF_KPROBE(KtprobeespintcpVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55779;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/espintcp_recvmsg")
int BPF_KRETPROBE(KtretprobeespintcpVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55779;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/espintcp_release")
int BPF_KPROBE(KtprobeespintcpVZVXrelease)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55780;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/espintcp_release")
int BPF_KRETPROBE(KtretprobeespintcpVZVXrelease)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55780;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/espintcp_sendmsg")
int BPF_KPROBE(KtprobeespintcpVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55782;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/espintcp_sendmsg")
int BPF_KRETPROBE(KtretprobeespintcpVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55782;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/espintcp_sendskmsg_locked")
int BPF_KPROBE(KtprobeespintcpVZVXsendskmsgVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55784;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/espintcp_sendskmsg_locked")
int BPF_KRETPROBE(KtretprobeespintcpVZVXsendskmsgVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55784;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/espintcp_write_space")
int BPF_KPROBE(KtprobeespintcpVZVXwriteVZVXspace)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55786;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/espintcp_write_space")
int BPF_KRETPROBE(KtretprobeespintcpVZVXwriteVZVXspace)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=55786;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/hook_socket_bind")
int BPF_KPROBE(KtprobehookVZVXsocketVZVXbind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58054;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/hook_socket_bind")
int BPF_KRETPROBE(KtretprobehookVZVXsocketVZVXbind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58054;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/hook_socket_connect")
int BPF_KPROBE(KtprobehookVZVXsocketVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58055;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/hook_socket_connect")
int BPF_KRETPROBE(KtretprobehookVZVXsocketVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58055;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmp6_send")
int BPF_KPROBE(Ktprobeicmp6VZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58537;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmp6_send")
int BPF_KRETPROBE(Ktretprobeicmp6VZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58537;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmp_build_probe")
int BPF_KPROBE(KtprobeicmpVZVXbuildVZVXprobe)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58539;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmp_build_probe")
int BPF_KRETPROBE(KtretprobeicmpVZVXbuildVZVXprobe)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58539;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmp_discard")
int BPF_KPROBE(KtprobeicmpVZVXdiscard)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58541;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmp_discard")
int BPF_KRETPROBE(KtretprobeicmpVZVXdiscard)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58541;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmp_echo")
int BPF_KPROBE(KtprobeicmpVZVXecho)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58542;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmp_echo")
int BPF_KRETPROBE(KtretprobeicmpVZVXecho)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58542;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmp_err")
int BPF_KPROBE(KtprobeicmpVZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58544;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmp_err")
int BPF_KRETPROBE(KtretprobeicmpVZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58544;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmp_global_allow")
int BPF_KPROBE(KtprobeicmpVZVXglobalVZVXallow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58546;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmp_global_allow")
int BPF_KRETPROBE(KtretprobeicmpVZVXglobalVZVXallow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58546;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmp_global_consume")
int BPF_KPROBE(KtprobeicmpVZVXglobalVZVXconsume)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58547;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmp_global_consume")
int BPF_KRETPROBE(KtretprobeicmpVZVXglobalVZVXconsume)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58547;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmp_glue_bits")
int BPF_KPROBE(KtprobeicmpVZVXglueVZVXbits)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58549;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmp_glue_bits")
int BPF_KRETPROBE(KtretprobeicmpVZVXglueVZVXbits)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58549;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmp_ndo_send")
int BPF_KPROBE(KtprobeicmpVZVXndoVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58552;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmp_ndo_send")
int BPF_KRETPROBE(KtretprobeicmpVZVXndoVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58552;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmp_out_count")
int BPF_KPROBE(KtprobeicmpVZVXoutVZVXcount)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58554;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmp_out_count")
int BPF_KRETPROBE(KtretprobeicmpVZVXoutVZVXcount)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58554;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmp_rcv")
int BPF_KPROBE(KtprobeicmpVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58557;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmp_rcv")
int BPF_KRETPROBE(KtretprobeicmpVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58557;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmp_redirect")
int BPF_KPROBE(KtprobeicmpVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58558;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmp_redirect")
int BPF_KRETPROBE(KtretprobeicmpVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58558;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmp_reply")
int BPF_KPROBE(KtprobeicmpVZVXreply)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58560;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmp_reply")
int BPF_KRETPROBE(KtretprobeicmpVZVXreply)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58560;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmp_route_lookup")
int BPF_KPROBE(KtprobeicmpVZVXrouteVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58562;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmp_route_lookup")
int BPF_KRETPROBE(KtretprobeicmpVZVXrouteVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58562;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmp_sk_init")
int BPF_KPROBE(KtprobeicmpVZVXskVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58563;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmp_sk_init")
int BPF_KRETPROBE(KtretprobeicmpVZVXskVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58563;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmp_socket_deliver")
int BPF_KPROBE(KtprobeicmpVZVXsocketVZVXdeliver)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58565;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmp_socket_deliver")
int BPF_KRETPROBE(KtretprobeicmpVZVXsocketVZVXdeliver)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58565;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmp_timestamp")
int BPF_KPROBE(KtprobeicmpVZVXtimestamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58566;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmp_timestamp")
int BPF_KRETPROBE(KtretprobeicmpVZVXtimestamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58566;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmp_unreach")
int BPF_KPROBE(KtprobeicmpVZVXunreach)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58567;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmp_unreach")
int BPF_KRETPROBE(KtretprobeicmpVZVXunreach)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58567;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmpv4_global_allow")
int BPF_KPROBE(Ktprobeicmpv4VZVXglobalVZVXallow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58569;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmpv4_global_allow")
int BPF_KRETPROBE(Ktretprobeicmpv4VZVXglobalVZVXallow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58569;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmpv4_xrlim_allow")
int BPF_KPROBE(Ktprobeicmpv4VZVXxrlimVZVXallow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58571;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmpv4_xrlim_allow")
int BPF_KRETPROBE(Ktretprobeicmpv4VZVXxrlimVZVXallow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58571;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmpv6_echo_reply")
int BPF_KPROBE(Ktprobeicmpv6VZVXechoVZVXreply)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58573;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmpv6_echo_reply")
int BPF_KRETPROBE(Ktretprobeicmpv6VZVXechoVZVXreply)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58573;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmpv6_err")
int BPF_KPROBE(Ktprobeicmpv6VZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58575;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmpv6_err")
int BPF_KRETPROBE(Ktretprobeicmpv6VZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58575;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmpv6_flow_init")
int BPF_KPROBE(Ktprobeicmpv6VZVXflowVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58578;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmpv6_flow_init")
int BPF_KRETPROBE(Ktretprobeicmpv6VZVXflowVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58578;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmpv6_getfrag")
int BPF_KPROBE(Ktprobeicmpv6VZVXgetfrag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58579;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmpv6_getfrag")
int BPF_KRETPROBE(Ktretprobeicmpv6VZVXgetfrag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58579;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmpv6_global_allow")
int BPF_KPROBE(Ktprobeicmpv6VZVXglobalVZVXallow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58581;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmpv6_global_allow")
int BPF_KRETPROBE(Ktretprobeicmpv6VZVXglobalVZVXallow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58581;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmpv6_notify")
int BPF_KPROBE(Ktprobeicmpv6VZVXnotify)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58584;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmpv6_notify")
int BPF_KRETPROBE(Ktretprobeicmpv6VZVXnotify)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58584;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmpv6_param_prob_reason")
int BPF_KPROBE(Ktprobeicmpv6VZVXparamVZVXprobVZVXreason)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58586;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmpv6_param_prob_reason")
int BPF_KRETPROBE(Ktretprobeicmpv6VZVXparamVZVXprobVZVXreason)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58586;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmpv6_push_pending_frames")
int BPF_KPROBE(Ktprobeicmpv6VZVXpushVZVXpendingVZVXframes)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58588;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmpv6_push_pending_frames")
int BPF_KRETPROBE(Ktretprobeicmpv6VZVXpushVZVXpendingVZVXframes)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58588;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmpv6_rcv")
int BPF_KPROBE(Ktprobeicmpv6VZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58589;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmpv6_rcv")
int BPF_KRETPROBE(Ktretprobeicmpv6VZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58589;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmpv6_route_lookup")
int BPF_KPROBE(Ktprobeicmpv6VZVXrouteVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58591;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmpv6_route_lookup")
int BPF_KRETPROBE(Ktretprobeicmpv6VZVXrouteVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58591;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmpv6_xrlim_allow")
int BPF_KPROBE(Ktprobeicmpv6VZVXxrlimVZVXallow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58593;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmpv6_xrlim_allow")
int BPF_KRETPROBE(Ktretprobeicmpv6VZVXxrlimVZVXallow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58593;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet6_csk_route_req")
int BPF_KPROBE(Ktprobeinet6VZVXcskVZVXrouteVZVXreq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58744;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet6_csk_route_req")
int BPF_KRETPROBE(Ktretprobeinet6VZVXcskVZVXrouteVZVXreq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58744;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet6_csk_route_socket")
int BPF_KPROBE(Ktprobeinet6VZVXcskVZVXrouteVZVXsocket)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58746;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet6_csk_route_socket")
int BPF_KRETPROBE(Ktretprobeinet6VZVXcskVZVXrouteVZVXsocket)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58746;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet6_csk_update_pmtu")
int BPF_KPROBE(Ktprobeinet6VZVXcskVZVXupdateVZVXpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58748;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet6_csk_update_pmtu")
int BPF_KRETPROBE(Ktretprobeinet6VZVXcskVZVXupdateVZVXpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58748;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet6_csk_xmit")
int BPF_KPROBE(Ktprobeinet6VZVXcskVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58750;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet6_csk_xmit")
int BPF_KRETPROBE(Ktretprobeinet6VZVXcskVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58750;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_lhash2_bucket_sk")
int BPF_KPROBE(KtprobeinetVZVXlhash2VZVXbucketVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58831;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_lhash2_bucket_sk")
int BPF_KRETPROBE(KtretprobeinetVZVXlhash2VZVXbucketVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58831;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_lookup_run_sk_lookup")
int BPF_KPROBE(KtprobeinetVZVXlookupVZVXrunVZVXskVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58837;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_lookup_run_sk_lookup")
int BPF_KRETPROBE(KtretprobeinetVZVXlookupVZVXrunVZVXskVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58837;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_steal_sock")
int BPF_KPROBE(KtprobeinetVZVXstealVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58855;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_steal_sock")
int BPF_KRETPROBE(KtretprobeinetVZVXstealVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=58855;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/io_link_skb")
int BPF_KPROBE(KtprobeioVZVXlinkVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59338;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/io_link_skb")
int BPF_KRETPROBE(KtretprobeioVZVXlinkVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59338;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_err_gen_icmpv6_unreach")
int BPF_KPROBE(Ktprobeip6VZVXerrVZVXgenVZVXicmpv6VZVXunreach)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59548;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_err_gen_icmpv6_unreach")
int BPF_KRETPROBE(Ktretprobeip6VZVXerrVZVXgenVZVXicmpv6VZVXunreach)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59548;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6addrlbl_add")
int BPF_KPROBE(Ktprobeip6addrlblVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59550;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6addrlbl_add")
int BPF_KRETPROBE(Ktretprobeip6addrlblVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59550;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6addrlbl_del")
int BPF_KPROBE(Ktprobeip6addrlblVZVXdel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59552;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6addrlbl_del")
int BPF_KRETPROBE(Ktretprobeip6addrlblVZVXdel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59552;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6addrlbl_dump")
int BPF_KPROBE(Ktprobeip6addrlblVZVXdump)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59553;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6addrlbl_dump")
int BPF_KRETPROBE(Ktretprobeip6addrlblVZVXdump)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59553;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6addrlbl_fill")
int BPF_KPROBE(Ktprobeip6addrlblVZVXfill)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59555;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6addrlbl_fill")
int BPF_KRETPROBE(Ktretprobeip6addrlblVZVXfill)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59555;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6addrlbl_get")
int BPF_KPROBE(Ktprobeip6addrlblVZVXget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59557;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6addrlbl_get")
int BPF_KRETPROBE(Ktretprobeip6addrlblVZVXget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59557;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6addrlbl_net_exit")
int BPF_KPROBE(Ktprobeip6addrlblVZVXnetVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59558;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6addrlbl_net_exit")
int BPF_KRETPROBE(Ktretprobeip6addrlblVZVXnetVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59558;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6addrlbl_net_init")
int BPF_KPROBE(Ktprobeip6addrlblVZVXnetVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59559;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6addrlbl_net_init")
int BPF_KRETPROBE(Ktretprobeip6addrlblVZVXnetVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59559;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6addrlbl_newdel")
int BPF_KPROBE(Ktprobeip6addrlblVZVXnewdel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59560;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6addrlbl_newdel")
int BPF_KRETPROBE(Ktretprobeip6addrlblVZVXnewdel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59560;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_sk_ioctl")
int BPF_KPROBE(Ktprobeip6mrVZVXskVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59562;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_sk_ioctl")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXskVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59562;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_fib_net_exit")
int BPF_KPROBE(KtprobeipVZVXfibVZVXnetVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59566;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_fib_net_exit")
int BPF_KRETPROBE(KtretprobeipVZVXfibVZVXnetVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59566;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_forward")
int BPF_KPROBE(KtprobeipVZVXforward)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59567;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_forward")
int BPF_KRETPROBE(KtretprobeipVZVXforward)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59567;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_forward_finish")
int BPF_KPROBE(KtprobeipVZVXforwardVZVXfinish)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59568;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_forward_finish")
int BPF_KRETPROBE(KtretprobeipVZVXforwardVZVXfinish)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59568;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_icmp_error_rfc4884")
int BPF_KPROBE(KtprobeipVZVXicmpVZVXerrorVZVXrfc4884)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59570;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_icmp_error_rfc4884")
int BPF_KRETPROBE(KtretprobeipVZVXicmpVZVXerrorVZVXrfc4884)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59570;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_rt_ioctl")
int BPF_KPROBE(KtprobeipVZVXrtVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59572;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_rt_ioctl")
int BPF_KRETPROBE(KtretprobeipVZVXrtVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59572;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_valid_fib_dump_req")
int BPF_KPROBE(KtprobeipVZVXvalidVZVXfibVZVXdumpVZVXreq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59576;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_valid_fib_dump_req")
int BPF_KRETPROBE(KtretprobeipVZVXvalidVZVXfibVZVXdumpVZVXreq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59576;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipe_digest_audit")
int BPF_KPROBE(KtprobeipeVZVXdigestVZVXaudit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59582;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipe_digest_audit")
int BPF_KRETPROBE(KtretprobeipeVZVXdigestVZVXaudit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59582;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_ac_destroy_dev")
int BPF_KPROBE(Ktprobeipv6VZVXacVZVXdestroyVZVXdev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59604;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_ac_destroy_dev")
int BPF_KRETPROBE(Ktretprobeipv6VZVXacVZVXdestroyVZVXdev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59604;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_addr_label")
int BPF_KPROBE(Ktprobeipv6VZVXaddrVZVXlabel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59606;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_addr_label")
int BPF_KRETPROBE(Ktretprobeipv6VZVXaddrVZVXlabel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59606;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_chk_acast_addr")
int BPF_KPROBE(Ktprobeipv6VZVXchkVZVXacastVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59613;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_chk_acast_addr")
int BPF_KRETPROBE(Ktretprobeipv6VZVXchkVZVXacastVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59613;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_chk_acast_addr_src")
int BPF_KPROBE(Ktprobeipv6VZVXchkVZVXacastVZVXaddrVZVXsrc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59614;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_chk_acast_addr_src")
int BPF_KRETPROBE(Ktretprobeipv6VZVXchkVZVXacastVZVXaddrVZVXsrc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59614;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_dev_ac_dec")
int BPF_KPROBE(Ktprobeipv6VZVXdevVZVXacVZVXdec)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59616;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_dev_ac_dec")
int BPF_KRETPROBE(Ktretprobeipv6VZVXdevVZVXacVZVXdec)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59616;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_icmp_sysctl_init")
int BPF_KPROBE(Ktprobeipv6VZVXicmpVZVXsysctlVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59622;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_icmp_sysctl_init")
int BPF_KRETPROBE(Ktretprobeipv6VZVXicmpVZVXsysctlVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59622;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_sock_ac_close")
int BPF_KPROBE(Ktprobeipv6VZVXsockVZVXacVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59626;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_sock_ac_close")
int BPF_KRETPROBE(Ktretprobeipv6VZVXsockVZVXacVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59626;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_sock_ac_drop")
int BPF_KPROBE(Ktprobeipv6VZVXsockVZVXacVZVXdrop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59628;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_sock_ac_drop")
int BPF_KRETPROBE(Ktretprobeipv6VZVXsockVZVXacVZVXdrop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59628;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_sock_ac_join")
int BPF_KPROBE(Ktprobeipv6VZVXsockVZVXacVZVXjoin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59629;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_sock_ac_join")
int BPF_KRETPROBE(Ktretprobeipv6VZVXsockVZVXacVZVXjoin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=59629;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mctp_getsockopt")
int BPF_KPROBE(KtprobemctpVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=60843;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mctp_getsockopt")
int BPF_KRETPROBE(KtretprobemctpVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=60843;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mctp_recvmsg")
int BPF_KPROBE(KtprobemctpVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=60853;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mctp_recvmsg")
int BPF_KRETPROBE(KtretprobemctpVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=60853;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mctp_sendmsg")
int BPF_KPROBE(KtprobemctpVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=60856;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mctp_sendmsg")
int BPF_KRETPROBE(KtretprobemctpVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=60856;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mctp_setsockopt")
int BPF_KPROBE(KtprobemctpVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=60858;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mctp_setsockopt")
int BPF_KRETPROBE(KtretprobemctpVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=60858;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mctp_sk_close")
int BPF_KPROBE(KtprobemctpVZVXskVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=60859;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mctp_sk_close")
int BPF_KRETPROBE(KtretprobemctpVZVXskVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=60859;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mctp_sk_destruct")
int BPF_KPROBE(KtprobemctpVZVXskVZVXdestruct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=60860;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mctp_sk_destruct")
int BPF_KRETPROBE(KtretprobemctpVZVXskVZVXdestruct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=60860;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mctp_sk_hash")
int BPF_KPROBE(KtprobemctpVZVXskVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=60863;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mctp_sk_hash")
int BPF_KRETPROBE(KtretprobemctpVZVXskVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=60863;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mctp_sk_init")
int BPF_KPROBE(KtprobemctpVZVXskVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=60864;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mctp_sk_init")
int BPF_KRETPROBE(KtretprobemctpVZVXskVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=60864;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mctp_sk_unhash")
int BPF_KPROBE(KtprobemctpVZVXskVZVXunhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=60865;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mctp_sk_unhash")
int BPF_KRETPROBE(KtretprobemctpVZVXskVZVXunhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=60865;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_established_options")
int BPF_KPROBE(KtprobemptcpVZVXestablishedVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61204;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_established_options")
int BPF_KRETPROBE(KtretprobemptcpVZVXestablishedVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61204;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_established_options_add_addr")
int BPF_KPROBE(KtprobemptcpVZVXestablishedVZVXoptionsVZVXaddVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61205;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_established_options_add_addr")
int BPF_KRETPROBE(KtretprobemptcpVZVXestablishedVZVXoptionsVZVXaddVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61205;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_established_options_dss")
int BPF_KPROBE(KtprobemptcpVZVXestablishedVZVXoptionsVZVXdss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61207;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_established_options_dss")
int BPF_KRETPROBE(KtretprobemptcpVZVXestablishedVZVXoptionsVZVXdss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61207;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_established_options_mp")
int BPF_KPROBE(KtprobemptcpVZVXestablishedVZVXoptionsVZVXmp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61208;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_established_options_mp")
int BPF_KRETPROBE(KtretprobemptcpVZVXestablishedVZVXoptionsVZVXmp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61208;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_established_options_mp_fail")
int BPF_KPROBE(KtprobemptcpVZVXestablishedVZVXoptionsVZVXmpVZVXfail)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61210;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_established_options_mp_fail")
int BPF_KRETPROBE(KtretprobemptcpVZVXestablishedVZVXoptionsVZVXmpVZVXfail)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61210;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_established_options_rst")
int BPF_KPROBE(KtprobemptcpVZVXestablishedVZVXoptionsVZVXrst)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61211;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_established_options_rst")
int BPF_KRETPROBE(KtretprobemptcpVZVXestablishedVZVXoptionsVZVXrst)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61211;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_fastopen_subflow_synack_set_params")
int BPF_KPROBE(KtprobemptcpVZVXfastopenVZVXsubflowVZVXsynackVZVXsetVZVXparams)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61213;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_fastopen_subflow_synack_set_params")
int BPF_KRETPROBE(KtretprobemptcpVZVXfastopenVZVXsubflowVZVXsynackVZVXsetVZVXparams)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61213;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_get_options")
int BPF_KPROBE(KtprobemptcpVZVXgetVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61215;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_get_options")
int BPF_KRETPROBE(KtretprobemptcpVZVXgetVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61215;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_get_reset_option")
int BPF_KPROBE(KtprobemptcpVZVXgetVZVXresetVZVXoption)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61217;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_get_reset_option")
int BPF_KRETPROBE(KtretprobemptcpVZVXgetVZVXresetVZVXoption)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61217;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_incoming_options")
int BPF_KPROBE(KtprobemptcpVZVXincomingVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61219;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_incoming_options")
int BPF_KRETPROBE(KtretprobemptcpVZVXincomingVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61219;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_parse_option")
int BPF_KPROBE(KtprobemptcpVZVXparseVZVXoption)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61221;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_parse_option")
int BPF_KRETPROBE(KtretprobemptcpVZVXparseVZVXoption)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61221;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_syn_options")
int BPF_KPROBE(KtprobemptcpVZVXsynVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61223;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_syn_options")
int BPF_KRETPROBE(KtretprobemptcpVZVXsynVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61223;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_synack_options")
int BPF_KPROBE(KtprobemptcpVZVXsynackVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61225;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_synack_options")
int BPF_KRETPROBE(KtretprobemptcpVZVXsynackVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61225;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_update_rcv_data_fin")
int BPF_KPROBE(KtprobemptcpVZVXupdateVZVXrcvVZVXdataVZVXfin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61227;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_update_rcv_data_fin")
int BPF_KRETPROBE(KtretprobemptcpVZVXupdateVZVXrcvVZVXdataVZVXfin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61227;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_write_options")
int BPF_KPROBE(KtprobemptcpVZVXwriteVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61229;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_write_options")
int BPF_KRETPROBE(KtretprobemptcpVZVXwriteVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61229;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ncsi_send_cmd_nl")
int BPF_KPROBE(KtprobencsiVZVXsendVZVXcmdVZVXnl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61370;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ncsi_send_cmd_nl")
int BPF_KRETPROBE(KtretprobencsiVZVXsendVZVXcmdVZVXnl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61370;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ncsi_send_netlink_err")
int BPF_KPROBE(KtprobencsiVZVXsendVZVXnetlinkVZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61372;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ncsi_send_netlink_err")
int BPF_KRETPROBE(KtretprobencsiVZVXsendVZVXnetlinkVZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61372;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ncsi_send_netlink_rsp")
int BPF_KPROBE(KtprobencsiVZVXsendVZVXnetlinkVZVXrsp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61374;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ncsi_send_netlink_rsp")
int BPF_KRETPROBE(KtretprobencsiVZVXsendVZVXnetlinkVZVXrsp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61374;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ncsi_send_netlink_timeout")
int BPF_KPROBE(KtprobencsiVZVXsendVZVXnetlinkVZVXtimeout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61375;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ncsi_send_netlink_timeout")
int BPF_KRETPROBE(KtretprobencsiVZVXsendVZVXnetlinkVZVXtimeout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61375;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ncsi_set_channel_mask_nl")
int BPF_KPROBE(KtprobencsiVZVXsetVZVXchannelVZVXmaskVZVXnl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61376;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ncsi_set_channel_mask_nl")
int BPF_KRETPROBE(KtretprobencsiVZVXsetVZVXchannelVZVXmaskVZVXnl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61376;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ncsi_set_package_mask_nl")
int BPF_KPROBE(KtprobencsiVZVXsetVZVXpackageVZVXmaskVZVXnl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61378;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ncsi_set_package_mask_nl")
int BPF_KRETPROBE(KtretprobencsiVZVXsetVZVXpackageVZVXmaskVZVXnl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61378;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ncsi_xmit_cmd")
int BPF_KPROBE(KtprobencsiVZVXxmitVZVXcmd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61384;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ncsi_xmit_cmd")
int BPF_KRETPROBE(KtretprobencsiVZVXxmitVZVXcmd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61384;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ndisc_alloc_skb")
int BPF_KPROBE(KtprobendiscVZVXallocVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61401;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ndisc_alloc_skb")
int BPF_KRETPROBE(KtretprobendiscVZVXallocVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61401;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ndisc_recv_na")
int BPF_KPROBE(KtprobendiscVZVXrecvVZVXna)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61434;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ndisc_recv_na")
int BPF_KRETPROBE(KtretprobendiscVZVXrecvVZVXna)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61434;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ndisc_recv_ns")
int BPF_KPROBE(KtprobendiscVZVXrecvVZVXns)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61435;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ndisc_recv_ns")
int BPF_KRETPROBE(KtretprobendiscVZVXrecvVZVXns)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61435;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ndisc_recv_rs")
int BPF_KPROBE(KtprobendiscVZVXrecvVZVXrs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61436;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ndisc_recv_rs")
int BPF_KRETPROBE(KtretprobendiscVZVXrecvVZVXrs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61436;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ndisc_send_na")
int BPF_KPROBE(KtprobendiscVZVXsendVZVXna)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61440;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ndisc_send_na")
int BPF_KRETPROBE(KtretprobendiscVZVXsendVZVXna)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61440;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ndisc_send_ns")
int BPF_KPROBE(KtprobendiscVZVXsendVZVXns)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61442;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ndisc_send_ns")
int BPF_KRETPROBE(KtretprobendiscVZVXsendVZVXns)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61442;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ndisc_send_redirect")
int BPF_KPROBE(KtprobendiscVZVXsendVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61444;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ndisc_send_redirect")
int BPF_KRETPROBE(KtretprobendiscVZVXsendVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61444;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ndisc_send_rs")
int BPF_KPROBE(KtprobendiscVZVXsendVZVXrs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61446;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ndisc_send_rs")
int BPF_KRETPROBE(KtretprobendiscVZVXsendVZVXrs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61446;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ndisc_send_skb")
int BPF_KPROBE(KtprobendiscVZVXsendVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61448;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ndisc_send_skb")
int BPF_KRETPROBE(KtretprobendiscVZVXsendVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61448;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ndisc_send_unsol_na")
int BPF_KPROBE(KtprobendiscVZVXsendVZVXunsolVZVXna)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61449;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ndisc_send_unsol_na")
int BPF_KRETPROBE(KtretprobendiscVZVXsendVZVXunsolVZVXna)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61449;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlbl_cipsov4_add")
int BPF_KPROBE(KtprobenetlblVZVXcipsov4VZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61507;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlbl_cipsov4_add")
int BPF_KRETPROBE(KtretprobenetlblVZVXcipsov4VZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61507;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlbl_cipsov4_list")
int BPF_KPROBE(KtprobenetlblVZVXcipsov4VZVXlist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61513;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlbl_cipsov4_list")
int BPF_KRETPROBE(KtretprobenetlblVZVXcipsov4VZVXlist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61513;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlbl_cipsov4_listall")
int BPF_KPROBE(KtprobenetlblVZVXcipsov4VZVXlistall)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61514;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlbl_cipsov4_listall")
int BPF_KRETPROBE(KtretprobenetlblVZVXcipsov4VZVXlistall)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61514;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlbl_cipsov4_remove")
int BPF_KPROBE(KtprobenetlblVZVXcipsov4VZVXremove)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61517;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlbl_cipsov4_remove")
int BPF_KRETPROBE(KtretprobenetlblVZVXcipsov4VZVXremove)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61517;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/nf_ip6_check_hbh_len")
int BPF_KPROBE(KtprobenfVZVXip6VZVXcheckVZVXhbhVZVXlen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61551;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/nf_ip6_check_hbh_len")
int BPF_KRETPROBE(KtretprobenfVZVXip6VZVXcheckVZVXhbhVZVXlen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61551;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/nf_ip6_checksum")
int BPF_KPROBE(KtprobenfVZVXip6VZVXchecksum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61553;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/nf_ip6_checksum")
int BPF_KRETPROBE(KtretprobenfVZVXip6VZVXchecksum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61553;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/nf_ip_checksum")
int BPF_KPROBE(KtprobenfVZVXipVZVXchecksum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61554;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/nf_ip_checksum")
int BPF_KRETPROBE(KtretprobenfVZVXipVZVXchecksum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61554;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/pad_compress_skb")
int BPF_KPROBE(KtprobepadVZVXcompressVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61881;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/pad_compress_skb")
int BPF_KRETPROBE(KtretprobepadVZVXcompressVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=61881;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ping_v6_sendmsg")
int BPF_KPROBE(KtprobepingVZVXv6VZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=62839;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ping_v6_sendmsg")
int BPF_KRETPROBE(KtretprobepingVZVXv6VZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=62839;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/pipefs_init_fs_context")
int BPF_KPROBE(KtprobepipefsVZVXinitVZVXfsVZVXcontext)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=62866;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/pipefs_init_fs_context")
int BPF_KRETPROBE(KtretprobepipefsVZVXinitVZVXfsVZVXcontext)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=62866;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ppp_send_frame")
int BPF_KPROBE(KtprobepppVZVXsendVZVXframe)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=63368;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ppp_send_frame")
int BPF_KRETPROBE(KtretprobepppVZVXsendVZVXframe)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=63368;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ppp_start_xmit")
int BPF_KPROBE(KtprobepppVZVXstartVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=63373;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ppp_start_xmit")
int BPF_KRETPROBE(KtretprobepppVZVXstartVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=63373;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ppp_xmit_process")
int BPF_KPROBE(KtprobepppVZVXxmitVZVXprocess)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=63384;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ppp_xmit_process")
int BPF_KRETPROBE(KtretprobepppVZVXxmitVZVXprocess)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=63384;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/pskb_may_pull_reason")
int BPF_KPROBE(KtprobepskbVZVXmayVZVXpullVZVXreason)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=63631;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/pskb_may_pull_reason")
int BPF_KRETPROBE(KtretprobepskbVZVXmayVZVXpullVZVXreason)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=63631;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/release_sock")
int BPF_KPROBE(KtprobereleaseVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=64227;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/release_sock")
int BPF_KRETPROBE(KtretprobereleaseVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=64227;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sch_frag_xmit")
int BPF_KPROBE(KtprobeschVZVXfragVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=64668;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sch_frag_xmit")
int BPF_KRETPROBE(KtretprobeschVZVXfragVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=64668;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sch_frag_xmit_hook")
int BPF_KPROBE(KtprobeschVZVXfragVZVXxmitVZVXhook)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=64670;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sch_frag_xmit_hook")
int BPF_KRETPROBE(KtretprobeschVZVXfragVZVXxmitVZVXhook)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=64670;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/seg6_hmac_validate_skb")
int BPF_KPROBE(Ktprobeseg6VZVXhmacVZVXvalidateVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65158;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/seg6_hmac_validate_skb")
int BPF_KRETPROBE(Ktretprobeseg6VZVXhmacVZVXvalidateVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65158;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_netlbl_inet_csk_clone")
int BPF_KPROBE(KtprobeselinuxVZVXnetlblVZVXinetVZVXcskVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65189;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_netlbl_inet_csk_clone")
int BPF_KRETPROBE(KtretprobeselinuxVZVXnetlblVZVXinetVZVXcskVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65189;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_netlbl_sctp_sk_clone")
int BPF_KPROBE(KtprobeselinuxVZVXnetlblVZVXsctpVZVXskVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65192;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_netlbl_sctp_sk_clone")
int BPF_KRETPROBE(KtretprobeselinuxVZVXnetlblVZVXsctpVZVXskVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65192;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_netlbl_skbuff_getsid")
int BPF_KPROBE(KtprobeselinuxVZVXnetlblVZVXskbuffVZVXgetsid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65197;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_netlbl_skbuff_getsid")
int BPF_KRETPROBE(KtretprobeselinuxVZVXnetlblVZVXskbuffVZVXgetsid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65197;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_netlbl_skbuff_setsid")
int BPF_KPROBE(KtprobeselinuxVZVXnetlblVZVXskbuffVZVXsetsid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65199;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_netlbl_skbuff_setsid")
int BPF_KRETPROBE(KtretprobeselinuxVZVXnetlblVZVXskbuffVZVXsetsid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65199;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_netlbl_sock_genattr")
int BPF_KPROBE(KtprobeselinuxVZVXnetlblVZVXsockVZVXgenattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65201;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_netlbl_sock_genattr")
int BPF_KRETPROBE(KtretprobeselinuxVZVXnetlblVZVXsockVZVXgenattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65201;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_netlbl_sock_rcv_skb")
int BPF_KPROBE(KtprobeselinuxVZVXnetlblVZVXsockVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65203;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_netlbl_sock_rcv_skb")
int BPF_KRETPROBE(KtretprobeselinuxVZVXnetlblVZVXsockVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65203;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_netlbl_socket_connect")
int BPF_KPROBE(KtprobeselinuxVZVXnetlblVZVXsocketVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65205;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_netlbl_socket_connect")
int BPF_KRETPROBE(KtretprobeselinuxVZVXnetlblVZVXsocketVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65205;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_netlbl_socket_connect_locked")
int BPF_KPROBE(KtprobeselinuxVZVXnetlblVZVXsocketVZVXconnectVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65206;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_netlbl_socket_connect_locked")
int BPF_KRETPROBE(KtretprobeselinuxVZVXnetlblVZVXsocketVZVXconnectVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65206;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_netlbl_socket_post_create")
int BPF_KPROBE(KtprobeselinuxVZVXnetlblVZVXsocketVZVXpostVZVXcreate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65208;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_netlbl_socket_post_create")
int BPF_KRETPROBE(KtretprobeselinuxVZVXnetlblVZVXsocketVZVXpostVZVXcreate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65208;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_netlbl_socket_setsockopt")
int BPF_KPROBE(KtprobeselinuxVZVXnetlblVZVXsocketVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65209;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_netlbl_socket_setsockopt")
int BPF_KRETPROBE(KtretprobeselinuxVZVXnetlblVZVXsocketVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65209;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/set_xfrm_gro_udp_encap_rcv")
int BPF_KPROBE(KtprobesetVZVXxfrmVZVXgroVZVXudpVZVXencapVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65613;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/set_xfrm_gro_udp_encap_rcv")
int BPF_KRETPROBE(KtretprobesetVZVXxfrmVZVXgroVZVXudpVZVXencapVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=65613;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_alloc")
int BPF_KPROBE(KtprobeskVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66060;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_alloc")
int BPF_KRETPROBE(KtretprobeskVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66060;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_attach_filter")
int BPF_KPROBE(KtprobeskVZVXattachVZVXfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66064;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_attach_filter")
int BPF_KRETPROBE(KtretprobeskVZVXattachVZVXfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66064;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_capable")
int BPF_KPROBE(KtprobeskVZVXcapable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66068;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_capable")
int BPF_KRETPROBE(KtretprobeskVZVXcapable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66068;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_clear_memalloc")
int BPF_KPROBE(KtprobeskVZVXclearVZVXmemalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66069;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_clear_memalloc")
int BPF_KRETPROBE(KtretprobeskVZVXclearVZVXmemalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66069;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_clone_lock")
int BPF_KPROBE(KtprobeskVZVXcloneVZVXlock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66071;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_clone_lock")
int BPF_KRETPROBE(KtretprobeskVZVXcloneVZVXlock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66071;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_common_release")
int BPF_KPROBE(KtprobeskVZVXcommonVZVXrelease)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66072;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_common_release")
int BPF_KRETPROBE(KtretprobeskVZVXcommonVZVXrelease)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66072;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_destruct")
int BPF_KPROBE(KtprobeskVZVXdestruct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66073;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_destruct")
int BPF_KRETPROBE(KtretprobeskVZVXdestruct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66073;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_detach_filter")
int BPF_KPROBE(KtprobeskVZVXdetachVZVXfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66074;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_detach_filter")
int BPF_KRETPROBE(KtretprobeskVZVXdetachVZVXfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66074;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_dst_check")
int BPF_KPROBE(KtprobeskVZVXdstVZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66075;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_dst_check")
int BPF_KRETPROBE(KtretprobeskVZVXdstVZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66075;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_error_report")
int BPF_KPROBE(KtprobeskVZVXerrorVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66076;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_error_report")
int BPF_KRETPROBE(KtretprobeskVZVXerrorVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66076;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_filter_charge")
int BPF_KPROBE(KtprobeskVZVXfilterVZVXcharge)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66078;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_filter_charge")
int BPF_KRETPROBE(KtretprobeskVZVXfilterVZVXcharge)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66078;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_filter_trim_cap")
int BPF_KPROBE(KtprobeskVZVXfilterVZVXtrimVZVXcap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66083;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_filter_trim_cap")
int BPF_KRETPROBE(KtretprobeskVZVXfilterVZVXtrimVZVXcap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66083;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_filter_uncharge")
int BPF_KPROBE(KtprobeskVZVXfilterVZVXuncharge)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66085;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_filter_uncharge")
int BPF_KRETPROBE(KtretprobeskVZVXfilterVZVXuncharge)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66085;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_free")
int BPF_KPROBE(KtprobeskVZVXfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66086;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_free")
int BPF_KRETPROBE(KtretprobeskVZVXfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66086;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_fullsock")
int BPF_KPROBE(KtprobeskVZVXfullsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66088;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_fullsock")
int BPF_KRETPROBE(KtretprobeskVZVXfullsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66088;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_get_filter")
int BPF_KPROBE(KtprobeskVZVXgetVZVXfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66090;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_get_filter")
int BPF_KRETPROBE(KtretprobeskVZVXgetVZVXfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66090;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_get_meminfo")
int BPF_KPROBE(KtprobeskVZVXgetVZVXmeminfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66092;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_get_meminfo")
int BPF_KRETPROBE(KtretprobeskVZVXgetVZVXmeminfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66092;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_getsockopt")
int BPF_KPROBE(KtprobeskVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66094;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_getsockopt")
int BPF_KRETPROBE(KtretprobeskVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66094;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_ioctl")
int BPF_KPROBE(KtprobeskVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66095;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_ioctl")
int BPF_KRETPROBE(KtretprobeskVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66095;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_leave_memory_pressure")
int BPF_KPROBE(KtprobeskVZVXleaveVZVXmemoryVZVXpressure)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66096;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_leave_memory_pressure")
int BPF_KRETPROBE(KtretprobeskVZVXleaveVZVXmemoryVZVXpressure)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66096;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_lookup")
int BPF_KPROBE(KtprobeskVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66098;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_lookup")
int BPF_KRETPROBE(KtretprobeskVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66098;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_mc_loop")
int BPF_KPROBE(KtprobeskVZVXmcVZVXloop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66102;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_mc_loop")
int BPF_KRETPROBE(KtretprobeskVZVXmcVZVXloop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66102;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_net_capable")
int BPF_KPROBE(KtprobeskVZVXnetVZVXcapable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66106;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_net_capable")
int BPF_KRETPROBE(KtretprobeskVZVXnetVZVXcapable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66106;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_net_refcnt_upgrade")
int BPF_KPROBE(KtprobeskVZVXnetVZVXrefcntVZVXupgrade)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66107;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_net_refcnt_upgrade")
int BPF_KRETPROBE(KtretprobeskVZVXnetVZVXrefcntVZVXupgrade)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66107;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_ns_capable")
int BPF_KPROBE(KtprobeskVZVXnsVZVXcapable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66109;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_ns_capable")
int BPF_KRETPROBE(KtretprobeskVZVXnsVZVXcapable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66109;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_page_frag_refill")
int BPF_KPROBE(KtprobeskVZVXpageVZVXfragVZVXrefill)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66111;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_page_frag_refill")
int BPF_KRETPROBE(KtretprobeskVZVXpageVZVXfragVZVXrefill)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66111;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_reset_timer")
int BPF_KPROBE(KtprobeskVZVXresetVZVXtimer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66115;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_reset_timer")
int BPF_KRETPROBE(KtretprobeskVZVXresetVZVXtimer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66115;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_reuseport_attach_filter")
int BPF_KPROBE(KtprobeskVZVXreuseportVZVXattachVZVXfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66117;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_reuseport_attach_filter")
int BPF_KRETPROBE(KtretprobeskVZVXreuseportVZVXattachVZVXfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66117;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_send_sigurg")
int BPF_KPROBE(KtprobeskVZVXsendVZVXsigurg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66128;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_send_sigurg")
int BPF_KRETPROBE(KtretprobeskVZVXsendVZVXsigurg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66128;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_set_memalloc")
int BPF_KPROBE(KtprobeskVZVXsetVZVXmemalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66129;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_set_memalloc")
int BPF_KRETPROBE(KtretprobeskVZVXsetVZVXmemalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66129;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_set_peek_off")
int BPF_KPROBE(KtprobeskVZVXsetVZVXpeekVZVXoff)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66131;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_set_peek_off")
int BPF_KRETPROBE(KtretprobeskVZVXsetVZVXpeekVZVXoff)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66131;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_setsockopt")
int BPF_KPROBE(KtprobeskVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66133;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_setsockopt")
int BPF_KRETPROBE(KtretprobeskVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66133;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_setup_caps")
int BPF_KPROBE(KtprobeskVZVXsetupVZVXcaps)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66135;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_setup_caps")
int BPF_KRETPROBE(KtretprobeskVZVXsetupVZVXcaps)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66135;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_stop_timer")
int BPF_KPROBE(KtprobeskVZVXstopVZVXtimer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66145;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_stop_timer")
int BPF_KRETPROBE(KtretprobeskVZVXstopVZVXtimer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66145;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_stop_timer_sync")
int BPF_KPROBE(KtprobeskVZVXstopVZVXtimerVZVXsync)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66146;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_stop_timer_sync")
int BPF_KRETPROBE(KtretprobeskVZVXstopVZVXtimerVZVXsync)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66146;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_wait_data")
int BPF_KPROBE(KtprobeskVZVXwaitVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66148;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_wait_data")
int BPF_KRETPROBE(KtretprobeskVZVXwaitVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66148;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_clone_tx_timestamp")
int BPF_KPROBE(KtprobeskbVZVXcloneVZVXtxVZVXtimestamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66149;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_clone_tx_timestamp")
int BPF_KRETPROBE(KtretprobeskbVZVXcloneVZVXtxVZVXtimestamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66149;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_consume_udp")
int BPF_KPROBE(KtprobeskbVZVXconsumeVZVXudp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66151;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_consume_udp")
int BPF_KRETPROBE(KtretprobeskbVZVXconsumeVZVXudp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66151;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_copy_bits")
int BPF_KPROBE(KtprobeskbVZVXcopyVZVXbits)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66153;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_copy_bits")
int BPF_KRETPROBE(KtretprobeskbVZVXcopyVZVXbits)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66153;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_defer_rx_timestamp")
int BPF_KPROBE(KtprobeskbVZVXdeferVZVXrxVZVXtimestamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66154;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_defer_rx_timestamp")
int BPF_KRETPROBE(KtretprobeskbVZVXdeferVZVXrxVZVXtimestamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66154;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_do_redirect")
int BPF_KPROBE(KtprobeskbVZVXdoVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66155;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_do_redirect")
int BPF_KRETPROBE(KtretprobeskbVZVXdoVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66155;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_ext_del")
int BPF_KPROBE(KtprobeskbVZVXextVZVXdel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66157;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_ext_del")
int BPF_KRETPROBE(KtretprobeskbVZVXextVZVXdel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66157;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_orphan_partial")
int BPF_KPROBE(KtprobeskbVZVXorphanVZVXpartial)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66158;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_orphan_partial")
int BPF_KRETPROBE(KtretprobeskbVZVXorphanVZVXpartial)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66158;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/smack_inet_csk_clone")
int BPF_KPROBE(KtprobesmackVZVXinetVZVXcskVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66250;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/smack_inet_csk_clone")
int BPF_KRETPROBE(KtretprobesmackVZVXinetVZVXcskVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66250;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/smack_sk_alloc_security")
int BPF_KPROBE(KtprobesmackVZVXskVZVXallocVZVXsecurity)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66333;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/smack_sk_alloc_security")
int BPF_KRETPROBE(KtretprobesmackVZVXskVZVXallocVZVXsecurity)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66333;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/smack_sk_clone_security")
int BPF_KPROBE(KtprobesmackVZVXskVZVXcloneVZVXsecurity)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66334;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/smack_sk_clone_security")
int BPF_KRETPROBE(KtretprobesmackVZVXskVZVXcloneVZVXsecurity)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66334;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/smack_socket_connect")
int BPF_KPROBE(KtprobesmackVZVXsocketVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66336;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/smack_socket_connect")
int BPF_KRETPROBE(KtretprobesmackVZVXsocketVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66336;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/smack_socket_getpeersec_dgram")
int BPF_KPROBE(KtprobesmackVZVXsocketVZVXgetpeersecVZVXdgram)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66337;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/smack_socket_getpeersec_dgram")
int BPF_KRETPROBE(KtretprobesmackVZVXsocketVZVXgetpeersecVZVXdgram)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66337;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/smack_socket_getpeersec_stream")
int BPF_KPROBE(KtprobesmackVZVXsocketVZVXgetpeersecVZVXstream)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66338;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/smack_socket_getpeersec_stream")
int BPF_KRETPROBE(KtretprobesmackVZVXsocketVZVXgetpeersecVZVXstream)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66338;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/smack_socket_post_create")
int BPF_KPROBE(KtprobesmackVZVXsocketVZVXpostVZVXcreate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66339;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/smack_socket_post_create")
int BPF_KRETPROBE(KtretprobesmackVZVXsocketVZVXpostVZVXcreate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66339;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/smack_socket_sendmsg")
int BPF_KPROBE(KtprobesmackVZVXsocketVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66340;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/smack_socket_sendmsg")
int BPF_KRETPROBE(KtretprobesmackVZVXsocketVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66340;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/smack_socket_sock_rcv_skb")
int BPF_KPROBE(KtprobesmackVZVXsocketVZVXsockVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66341;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/smack_socket_sock_rcv_skb")
int BPF_KRETPROBE(KtretprobesmackVZVXsocketVZVXsockVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66341;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/smack_socket_socketpair")
int BPF_KPROBE(KtprobesmackVZVXsocketVZVXsocketpair)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66342;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/smack_socket_socketpair")
int BPF_KRETPROBE(KtretprobesmackVZVXsocketVZVXsocketpair)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66342;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/smack_unix_may_send")
int BPF_KPROBE(KtprobesmackVZVXunixVZVXmayVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66359;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/smack_unix_may_send")
int BPF_KRETPROBE(KtretprobesmackVZVXunixVZVXmayVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66359;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/smk_ipv4_check")
int BPF_KPROBE(KtprobesmkVZVXipv4VZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66386;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/smk_ipv4_check")
int BPF_KRETPROBE(KtretprobesmkVZVXipv4VZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66386;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/smk_skb_to_addr_ipv6")
int BPF_KPROBE(KtprobesmkVZVXskbVZVXtoVZVXaddrVZVXipv6)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66392;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/smk_skb_to_addr_ipv6")
int BPF_KRETPROBE(KtretprobesmkVZVXskbVZVXtoVZVXaddrVZVXipv6)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66392;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_bind_add")
int BPF_KPROBE(KtprobesockVZVXbindVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66440;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_bind_add")
int BPF_KRETPROBE(KtretprobesockVZVXbindVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66440;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_bindtoindex")
int BPF_KPROBE(KtprobesockVZVXbindtoindex)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66442;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_bindtoindex")
int BPF_KRETPROBE(KtretprobesockVZVXbindtoindex)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66442;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_bindtoindex_locked")
int BPF_KPROBE(KtprobesockVZVXbindtoindexVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66444;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_bindtoindex_locked")
int BPF_KRETPROBE(KtretprobesockVZVXbindtoindexVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66444;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_cmsg_send")
int BPF_KPROBE(KtprobesockVZVXcmsgVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66446;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_cmsg_send")
int BPF_KRETPROBE(KtretprobesockVZVXcmsgVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66446;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_common_getsockopt")
int BPF_KPROBE(KtprobesockVZVXcommonVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66447;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_common_getsockopt")
int BPF_KRETPROBE(KtretprobesockVZVXcommonVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66447;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_common_recvmsg")
int BPF_KPROBE(KtprobesockVZVXcommonVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66449;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_common_recvmsg")
int BPF_KRETPROBE(KtretprobesockVZVXcommonVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66449;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_common_setsockopt")
int BPF_KPROBE(KtprobesockVZVXcommonVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66450;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_common_setsockopt")
int BPF_KRETPROBE(KtretprobesockVZVXcommonVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66450;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_copy")
int BPF_KPROBE(KtprobesockVZVXcopy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66452;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_copy")
int BPF_KRETPROBE(KtretprobesockVZVXcopy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66452;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_def_destruct")
int BPF_KPROBE(KtprobesockVZVXdefVZVXdestruct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66455;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_def_destruct")
int BPF_KRETPROBE(KtretprobesockVZVXdefVZVXdestruct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66455;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_def_error_report")
int BPF_KPROBE(KtprobesockVZVXdefVZVXerrorVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66456;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_def_error_report")
int BPF_KRETPROBE(KtretprobesockVZVXdefVZVXerrorVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66456;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_def_wakeup")
int BPF_KPROBE(KtprobesockVZVXdefVZVXwakeup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66458;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_def_wakeup")
int BPF_KRETPROBE(KtretprobesockVZVXdefVZVXwakeup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66458;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_def_write_space")
int BPF_KPROBE(KtprobesockVZVXdefVZVXwriteVZVXspace)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66459;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_def_write_space")
int BPF_KRETPROBE(KtretprobesockVZVXdefVZVXwriteVZVXspace)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66459;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_devmem_dontneed")
int BPF_KPROBE(KtprobesockVZVXdevmemVZVXdontneed)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66461;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_devmem_dontneed")
int BPF_KRETPROBE(KtretprobesockVZVXdevmemVZVXdontneed)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66461;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_edemux")
int BPF_KPROBE(KtprobesockVZVXedemux)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66462;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_edemux")
int BPF_KRETPROBE(KtretprobesockVZVXedemux)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66462;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_efree")
int BPF_KPROBE(KtprobesockVZVXefree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66463;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_efree")
int BPF_KRETPROBE(KtretprobesockVZVXefree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66463;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_enable_timestamp")
int BPF_KPROBE(KtprobesockVZVXenableVZVXtimestamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66465;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_enable_timestamp")
int BPF_KRETPROBE(KtretprobesockVZVXenableVZVXtimestamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66465;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_gen_put")
int BPF_KPROBE(KtprobesockVZVXgenVZVXput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66468;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_gen_put")
int BPF_KRETPROBE(KtretprobesockVZVXgenVZVXput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66468;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_gettstamp")
int BPF_KPROBE(KtprobesockVZVXgettstamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66472;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_gettstamp")
int BPF_KRETPROBE(KtretprobesockVZVXgettstamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66472;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_init_data")
int BPF_KPROBE(KtprobesockVZVXinitVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66474;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_init_data")
int BPF_KRETPROBE(KtretprobesockVZVXinitVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66474;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_init_data_uid")
int BPF_KPROBE(KtprobesockVZVXinitVZVXdataVZVXuid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66476;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_init_data_uid")
int BPF_KRETPROBE(KtretprobesockVZVXinitVZVXdataVZVXuid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66476;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_inuse_exit_net")
int BPF_KPROBE(KtprobesockVZVXinuseVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66477;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_inuse_exit_net")
int BPF_KRETPROBE(KtretprobesockVZVXinuseVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66477;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_inuse_get")
int BPF_KPROBE(KtprobesockVZVXinuseVZVXget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66478;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_inuse_get")
int BPF_KRETPROBE(KtretprobesockVZVXinuseVZVXget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66478;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_inuse_init_net")
int BPF_KPROBE(KtprobesockVZVXinuseVZVXinitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66479;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_inuse_init_net")
int BPF_KRETPROBE(KtretprobesockVZVXinuseVZVXinitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66479;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_ioctl_inout")
int BPF_KPROBE(KtprobesockVZVXioctlVZVXinout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66481;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_ioctl_inout")
int BPF_KRETPROBE(KtretprobesockVZVXioctlVZVXinout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66481;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_kfree_s")
int BPF_KPROBE(KtprobesockVZVXkfreeVZVXs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66483;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_kfree_s")
int BPF_KRETPROBE(KtretprobesockVZVXkfreeVZVXs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66483;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_kmalloc")
int BPF_KPROBE(KtprobesockVZVXkmalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66485;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_kmalloc")
int BPF_KRETPROBE(KtretprobesockVZVXkmalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66485;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_kmemdup")
int BPF_KPROBE(KtprobesockVZVXkmemdup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66487;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_kmemdup")
int BPF_KRETPROBE(KtretprobesockVZVXkmemdup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66487;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_kzfree_s")
int BPF_KPROBE(KtprobesockVZVXkzfreeVZVXs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66488;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_kzfree_s")
int BPF_KRETPROBE(KtretprobesockVZVXkzfreeVZVXs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66488;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_no_accept")
int BPF_KPROBE(KtprobesockVZVXnoVZVXaccept)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66492;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_no_accept")
int BPF_KRETPROBE(KtretprobesockVZVXnoVZVXaccept)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66492;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_no_bind")
int BPF_KPROBE(KtprobesockVZVXnoVZVXbind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66494;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_no_bind")
int BPF_KRETPROBE(KtretprobesockVZVXnoVZVXbind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66494;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_no_connect")
int BPF_KPROBE(KtprobesockVZVXnoVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66496;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_no_connect")
int BPF_KRETPROBE(KtretprobesockVZVXnoVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66496;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_no_getname")
int BPF_KPROBE(KtprobesockVZVXnoVZVXgetname)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66498;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_no_getname")
int BPF_KRETPROBE(KtretprobesockVZVXnoVZVXgetname)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66498;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_no_ioctl")
int BPF_KPROBE(KtprobesockVZVXnoVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66499;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_no_ioctl")
int BPF_KRETPROBE(KtretprobesockVZVXnoVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66499;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_no_linger")
int BPF_KPROBE(KtprobesockVZVXnoVZVXlinger)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66500;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_no_linger")
int BPF_KRETPROBE(KtretprobesockVZVXnoVZVXlinger)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66500;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_no_listen")
int BPF_KPROBE(KtprobesockVZVXnoVZVXlisten)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66501;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_no_listen")
int BPF_KRETPROBE(KtretprobesockVZVXnoVZVXlisten)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66501;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_no_mmap")
int BPF_KPROBE(KtprobesockVZVXnoVZVXmmap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66503;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_no_mmap")
int BPF_KRETPROBE(KtretprobesockVZVXnoVZVXmmap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66503;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_no_recvmsg")
int BPF_KPROBE(KtprobesockVZVXnoVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66505;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_no_recvmsg")
int BPF_KRETPROBE(KtretprobesockVZVXnoVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66505;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_no_sendmsg")
int BPF_KPROBE(KtprobesockVZVXnoVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66507;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_no_sendmsg")
int BPF_KRETPROBE(KtretprobesockVZVXnoVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66507;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_no_sendmsg_locked")
int BPF_KPROBE(KtprobesockVZVXnoVZVXsendmsgVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66509;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_no_sendmsg_locked")
int BPF_KRETPROBE(KtretprobesockVZVXnoVZVXsendmsgVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66509;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_no_shutdown")
int BPF_KPROBE(KtprobesockVZVXnoVZVXshutdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66510;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_no_shutdown")
int BPF_KRETPROBE(KtretprobesockVZVXnoVZVXshutdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66510;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_no_socketpair")
int BPF_KPROBE(KtprobesockVZVXnoVZVXsocketpair)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66512;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_no_socketpair")
int BPF_KRETPROBE(KtretprobesockVZVXnoVZVXsocketpair)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66512;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_ofree")
int BPF_KPROBE(KtprobesockVZVXofree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66513;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_ofree")
int BPF_KRETPROBE(KtretprobesockVZVXofree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66513;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_omalloc")
int BPF_KPROBE(KtprobesockVZVXomalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66515;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_omalloc")
int BPF_KRETPROBE(KtretprobesockVZVXomalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66515;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_pfree")
int BPF_KPROBE(KtprobesockVZVXpfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66519;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_pfree")
int BPF_KRETPROBE(KtretprobesockVZVXpfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66519;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_prot_inuse_get")
int BPF_KPROBE(KtprobesockVZVXprotVZVXinuseVZVXget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66521;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_prot_inuse_get")
int BPF_KRETPROBE(KtretprobesockVZVXprotVZVXinuseVZVXget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66521;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_put")
int BPF_KPROBE(KtprobesockVZVXput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66522;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_put")
int BPF_KRETPROBE(KtretprobesockVZVXput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66522;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_queue_rcv_skb_reason")
int BPF_KPROBE(KtprobesockVZVXqueueVZVXrcvVZVXskbVZVXreason)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66524;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_queue_rcv_skb_reason")
int BPF_KRETPROBE(KtretprobesockVZVXqueueVZVXrcvVZVXskbVZVXreason)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66524;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_recv_errqueue")
int BPF_KPROBE(KtprobesockVZVXrecvVZVXerrqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66526;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_recv_errqueue")
int BPF_KRETPROBE(KtretprobesockVZVXrecvVZVXerrqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66526;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_reserve_memory")
int BPF_KPROBE(KtprobesockVZVXreserveVZVXmemory)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66528;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_reserve_memory")
int BPF_KRETPROBE(KtretprobesockVZVXreserveVZVXmemory)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66528;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_reset_flag")
int BPF_KPROBE(KtprobesockVZVXresetVZVXflag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66529;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_reset_flag")
int BPF_KRETPROBE(KtretprobesockVZVXresetVZVXflag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66529;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_rfree")
int BPF_KPROBE(KtprobesockVZVXrfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66530;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_rfree")
int BPF_KRETPROBE(KtretprobesockVZVXrfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66530;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_set_flag")
int BPF_KPROBE(KtprobesockVZVXsetVZVXflag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66531;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_set_flag")
int BPF_KRETPROBE(KtretprobesockVZVXsetVZVXflag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66531;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_set_keepalive")
int BPF_KPROBE(KtprobesockVZVXsetVZVXkeepalive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66532;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_set_keepalive")
int BPF_KRETPROBE(KtretprobesockVZVXsetVZVXkeepalive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66532;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_set_mark")
int BPF_KPROBE(KtprobesockVZVXsetVZVXmark)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66534;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_set_mark")
int BPF_KRETPROBE(KtretprobesockVZVXsetVZVXmark)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66534;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_set_priority")
int BPF_KPROBE(KtprobesockVZVXsetVZVXpriority)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66536;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_set_priority")
int BPF_KRETPROBE(KtretprobesockVZVXsetVZVXpriority)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66536;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_set_rcvbuf")
int BPF_KPROBE(KtprobesockVZVXsetVZVXrcvbuf)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66538;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_set_rcvbuf")
int BPF_KRETPROBE(KtretprobesockVZVXsetVZVXrcvbuf)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66538;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_set_reuseaddr")
int BPF_KPROBE(KtprobesockVZVXsetVZVXreuseaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66539;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_set_reuseaddr")
int BPF_KRETPROBE(KtretprobesockVZVXsetVZVXreuseaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66539;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_set_reuseport")
int BPF_KPROBE(KtprobesockVZVXsetVZVXreuseport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66540;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_set_reuseport")
int BPF_KRETPROBE(KtretprobesockVZVXsetVZVXreuseport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66540;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_set_sndtimeo")
int BPF_KPROBE(KtprobesockVZVXsetVZVXsndtimeo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66542;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_set_sndtimeo")
int BPF_KRETPROBE(KtretprobesockVZVXsetVZVXsndtimeo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66542;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_set_timestamp")
int BPF_KPROBE(KtprobesockVZVXsetVZVXtimestamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66546;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_set_timestamp")
int BPF_KRETPROBE(KtretprobesockVZVXsetVZVXtimestamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66546;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_set_timestamping")
int BPF_KPROBE(KtprobesockVZVXsetVZVXtimestamping)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66548;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_set_timestamping")
int BPF_KRETPROBE(KtretprobesockVZVXsetVZVXtimestamping)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66548;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_setsockopt")
int BPF_KPROBE(KtprobesockVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66549;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_setsockopt")
int BPF_KRETPROBE(KtretprobesockVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66549;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_valbool_flag")
int BPF_KPROBE(KtprobesockVZVXvalboolVZVXflag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66552;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_valbool_flag")
int BPF_KRETPROBE(KtretprobesockVZVXvalboolVZVXflag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66552;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_wmalloc")
int BPF_KPROBE(KtprobesockVZVXwmalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66555;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_wmalloc")
int BPF_KRETPROBE(KtretprobesockVZVXwmalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66555;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sockopt_lock_sock")
int BPF_KPROBE(KtprobesockoptVZVXlockVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66558;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sockopt_lock_sock")
int BPF_KRETPROBE(KtretprobesockoptVZVXlockVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66558;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sockopt_release_sock")
int BPF_KPROBE(KtprobesockoptVZVXreleaseVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66561;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sockopt_release_sock")
int BPF_KRETPROBE(KtretprobesockoptVZVXreleaseVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66561;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sol_ip_sockopt")
int BPF_KPROBE(KtprobesolVZVXipVZVXsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66568;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sol_ip_sockopt")
int BPF_KRETPROBE(KtretprobesolVZVXipVZVXsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66568;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sol_ipv6_sockopt")
int BPF_KPROBE(KtprobesolVZVXipv6VZVXsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66569;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sol_ipv6_sockopt")
int BPF_KRETPROBE(KtretprobesolVZVXipv6VZVXsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66569;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sol_socket_sockopt")
int BPF_KPROBE(KtprobesolVZVXsocketVZVXsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66570;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sol_socket_sockopt")
int BPF_KRETPROBE(KtretprobesolVZVXsocketVZVXsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66570;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sol_tcp_sockopt")
int BPF_KPROBE(KtprobesolVZVXtcpVZVXsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66571;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sol_tcp_sockopt")
int BPF_KRETPROBE(KtretprobesolVZVXtcpVZVXsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66571;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/switch_task_namespaces")
int BPF_KPROBE(KtprobeswitchVZVXtaskVZVXnamespaces)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/switch_task_namespaces")
int BPF_KRETPROBE(KtretprobeswitchVZVXtaskVZVXnamespaces)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=66904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp6_gro_complete")
int BPF_KPROBE(Ktprobetcp6VZVXgroVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67247;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp6_gro_complete")
int BPF_KRETPROBE(Ktretprobetcp6VZVXgroVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67247;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp6_gro_receive")
int BPF_KPROBE(Ktprobetcp6VZVXgroVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67249;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp6_gro_receive")
int BPF_KRETPROBE(Ktretprobetcp6VZVXgroVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67249;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp6_gso_segment")
int BPF_KPROBE(Ktprobetcp6VZVXgsoVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67251;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp6_gso_segment")
int BPF_KRETPROBE(Ktretprobetcp6VZVXgsoVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67251;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ack")
int BPF_KPROBE(KtprobetcpVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67253;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ack")
int BPF_KRETPROBE(KtretprobetcpVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67253;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ack_tstamp")
int BPF_KPROBE(KtprobetcpVZVXackVZVXtstamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67255;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ack_tstamp")
int BPF_KRETPROBE(KtretprobetcpVZVXackVZVXtstamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67255;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ack_update_rtt")
int BPF_KPROBE(KtprobetcpVZVXackVZVXupdateVZVXrtt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67257;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ack_update_rtt")
int BPF_KRETPROBE(KtretprobetcpVZVXackVZVXupdateVZVXrtt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67257;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_add_reno_sack")
int BPF_KPROBE(KtprobetcpVZVXaddVZVXrenoVZVXsack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67259;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_add_reno_sack")
int BPF_KRETPROBE(KtretprobetcpVZVXaddVZVXrenoVZVXsack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67259;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_check_dsack")
int BPF_KPROBE(KtprobetcpVZVXcheckVZVXdsack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67263;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_check_dsack")
int BPF_KRETPROBE(KtretprobetcpVZVXcheckVZVXdsack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67263;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_check_sack_reordering")
int BPF_KPROBE(KtprobetcpVZVXcheckVZVXsackVZVXreordering)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67265;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_check_sack_reordering")
int BPF_KRETPROBE(KtretprobetcpVZVXcheckVZVXsackVZVXreordering)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67265;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_check_space")
int BPF_KPROBE(KtprobetcpVZVXcheckVZVXspace)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67266;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_check_space")
int BPF_KRETPROBE(KtretprobetcpVZVXcheckVZVXspace)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67266;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_clean_rtx_queue")
int BPF_KPROBE(KtprobetcpVZVXcleanVZVXrtxVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67268;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_clean_rtx_queue")
int BPF_KRETPROBE(KtretprobetcpVZVXcleanVZVXrtxVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67268;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_clear_retrans")
int BPF_KPROBE(KtprobetcpVZVXclearVZVXretrans)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67269;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_clear_retrans")
int BPF_KRETPROBE(KtretprobetcpVZVXclearVZVXretrans)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67269;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_collapse")
int BPF_KPROBE(KtprobetcpVZVXcollapse)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67271;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_collapse")
int BPF_KRETPROBE(KtretprobetcpVZVXcollapse)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67271;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_collapse_one")
int BPF_KPROBE(KtprobetcpVZVXcollapseVZVXone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67273;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_collapse_one")
int BPF_KRETPROBE(KtretprobetcpVZVXcollapseVZVXone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67273;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_conn_request")
int BPF_KPROBE(KtprobetcpVZVXconnVZVXrequest)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67275;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_conn_request")
int BPF_KRETPROBE(KtretprobetcpVZVXconnVZVXrequest)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67275;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_cwnd_reduction")
int BPF_KPROBE(KtprobetcpVZVXcwndVZVXreduction)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67277;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_cwnd_reduction")
int BPF_KRETPROBE(KtretprobetcpVZVXcwndVZVXreduction)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67277;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_data_ecn_check")
int BPF_KPROBE(KtprobetcpVZVXdataVZVXecnVZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67279;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_data_ecn_check")
int BPF_KRETPROBE(KtretprobetcpVZVXdataVZVXecnVZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67279;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_data_queue")
int BPF_KPROBE(KtprobetcpVZVXdataVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67280;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_data_queue")
int BPF_KRETPROBE(KtretprobetcpVZVXdataVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67280;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_data_queue_ofo")
int BPF_KPROBE(KtprobetcpVZVXdataVZVXqueueVZVXofo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67281;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_data_queue_ofo")
int BPF_KRETPROBE(KtretprobetcpVZVXdataVZVXqueueVZVXofo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67281;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_data_ready")
int BPF_KPROBE(KtprobetcpVZVXdataVZVXready)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67282;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_data_ready")
int BPF_KRETPROBE(KtretprobetcpVZVXdataVZVXready)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67282;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_done_with_error")
int BPF_KPROBE(KtprobetcpVZVXdoneVZVXwithVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67286;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_done_with_error")
int BPF_KRETPROBE(KtretprobetcpVZVXdoneVZVXwithVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67286;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_drop_reason")
int BPF_KPROBE(KtprobetcpVZVXdropVZVXreason)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67288;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_drop_reason")
int BPF_KRETPROBE(KtretprobetcpVZVXdropVZVXreason)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67288;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_dsack_extend")
int BPF_KPROBE(KtprobetcpVZVXdsackVZVXextend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67290;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_dsack_extend")
int BPF_KRETPROBE(KtretprobetcpVZVXdsackVZVXextend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67290;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_enter_cwr")
int BPF_KPROBE(KtprobetcpVZVXenterVZVXcwr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67291;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_enter_cwr")
int BPF_KRETPROBE(KtretprobetcpVZVXenterVZVXcwr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67291;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_enter_loss")
int BPF_KPROBE(KtprobetcpVZVXenterVZVXloss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67292;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_enter_loss")
int BPF_KRETPROBE(KtretprobetcpVZVXenterVZVXloss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67292;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_enter_recovery")
int BPF_KPROBE(KtprobetcpVZVXenterVZVXrecovery)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67294;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_enter_recovery")
int BPF_KRETPROBE(KtretprobetcpVZVXenterVZVXrecovery)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67294;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_event_data_recv")
int BPF_KPROBE(KtprobetcpVZVXeventVZVXdataVZVXrecv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67295;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_event_data_recv")
int BPF_KRETPROBE(KtretprobetcpVZVXeventVZVXdataVZVXrecv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67295;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_fastretrans_alert")
int BPF_KPROBE(KtprobetcpVZVXfastretransVZVXalert)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67297;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_fastretrans_alert")
int BPF_KRETPROBE(KtretprobetcpVZVXfastretransVZVXalert)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67297;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_fin")
int BPF_KPROBE(KtprobetcpVZVXfin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67298;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_fin")
int BPF_KRETPROBE(KtretprobetcpVZVXfin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67298;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_finish_connect")
int BPF_KPROBE(KtprobetcpVZVXfinishVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67299;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_finish_connect")
int BPF_KRETPROBE(KtretprobetcpVZVXfinishVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67299;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_get_syncookie_mss")
int BPF_KPROBE(KtprobetcpVZVXgetVZVXsyncookieVZVXmss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67301;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_get_syncookie_mss")
int BPF_KRETPROBE(KtretprobetcpVZVXgetVZVXsyncookieVZVXmss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67301;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_grow_window")
int BPF_KPROBE(KtprobetcpVZVXgrowVZVXwindow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67303;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_grow_window")
int BPF_KRETPROBE(KtretprobetcpVZVXgrowVZVXwindow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67303;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_identify_packet_loss")
int BPF_KPROBE(KtprobetcpVZVXidentifyVZVXpacketVZVXloss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67305;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_identify_packet_loss")
int BPF_KRETPROBE(KtretprobetcpVZVXidentifyVZVXpacketVZVXloss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67305;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_in_ack_event")
int BPF_KPROBE(KtprobetcpVZVXinVZVXackVZVXevent)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67307;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_in_ack_event")
int BPF_KRETPROBE(KtretprobetcpVZVXinVZVXackVZVXevent)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67307;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_init_cwnd")
int BPF_KPROBE(KtprobetcpVZVXinitVZVXcwnd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67309;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_init_cwnd")
int BPF_KRETPROBE(KtretprobetcpVZVXinitVZVXcwnd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67309;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_init_transfer")
int BPF_KPROBE(KtprobetcpVZVXinitVZVXtransfer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67311;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_init_transfer")
int BPF_KRETPROBE(KtretprobetcpVZVXinitVZVXtransfer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67311;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_initialize_rcv_mss")
int BPF_KPROBE(KtprobetcpVZVXinitializeVZVXrcvVZVXmss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67312;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_initialize_rcv_mss")
int BPF_KRETPROBE(KtretprobetcpVZVXinitializeVZVXrcvVZVXmss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67312;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_is_ulp_esp")
int BPF_KPROBE(KtprobetcpVZVXisVZVXulpVZVXesp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67313;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_is_ulp_esp")
int BPF_KRETPROBE(KtretprobetcpVZVXisVZVXulpVZVXesp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67313;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_mark_skb_lost")
int BPF_KPROBE(KtprobetcpVZVXmarkVZVXskbVZVXlost)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67314;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_mark_skb_lost")
int BPF_KRETPROBE(KtretprobetcpVZVXmarkVZVXskbVZVXlost)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67314;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_match_skb_to_sack")
int BPF_KPROBE(KtprobetcpVZVXmatchVZVXskbVZVXtoVZVXsack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67316;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_match_skb_to_sack")
int BPF_KRETPROBE(KtretprobetcpVZVXmatchVZVXskbVZVXtoVZVXsack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67316;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_measure_rcv_mss")
int BPF_KPROBE(KtprobetcpVZVXmeasureVZVXrcvVZVXmss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67317;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_measure_rcv_mss")
int BPF_KRETPROBE(KtretprobetcpVZVXmeasureVZVXrcvVZVXmss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67317;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_newly_delivered")
int BPF_KPROBE(KtprobetcpVZVXnewlyVZVXdelivered)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67319;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_newly_delivered")
int BPF_KRETPROBE(KtretprobetcpVZVXnewlyVZVXdelivered)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67319;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_non_congestion_loss_retransmit")
int BPF_KPROBE(KtprobetcpVZVXnonVZVXcongestionVZVXlossVZVXretransmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67320;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_non_congestion_loss_retransmit")
int BPF_KRETPROBE(KtretprobetcpVZVXnonVZVXcongestionVZVXlossVZVXretransmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67320;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ofo_queue")
int BPF_KPROBE(KtprobetcpVZVXofoVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67321;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ofo_queue")
int BPF_KRETPROBE(KtretprobetcpVZVXofoVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67321;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_oow_rate_limited")
int BPF_KPROBE(KtprobetcpVZVXoowVZVXrateVZVXlimited)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67323;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_oow_rate_limited")
int BPF_KRETPROBE(KtretprobetcpVZVXoowVZVXrateVZVXlimited)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67323;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_parse_options")
int BPF_KPROBE(KtprobetcpVZVXparseVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67329;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_parse_options")
int BPF_KRETPROBE(KtretprobetcpVZVXparseVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67329;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_process_tlp_ack")
int BPF_KPROBE(KtprobetcpVZVXprocessVZVXtlpVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67331;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_process_tlp_ack")
int BPF_KRETPROBE(KtretprobetcpVZVXprocessVZVXtlpVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67331;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_prune_ofo_queue")
int BPF_KPROBE(KtprobetcpVZVXpruneVZVXofoVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67333;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_prune_ofo_queue")
int BPF_KRETPROBE(KtretprobetcpVZVXpruneVZVXofoVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67333;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_prune_queue")
int BPF_KPROBE(KtprobetcpVZVXpruneVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67335;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_prune_queue")
int BPF_KRETPROBE(KtretprobetcpVZVXpruneVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67335;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_queue_rcv")
int BPF_KPROBE(KtprobetcpVZVXqueueVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67337;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_queue_rcv")
int BPF_KRETPROBE(KtretprobetcpVZVXqueueVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67337;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_rbtree_insert")
int BPF_KPROBE(KtprobetcpVZVXrbtreeVZVXinsert)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67339;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_rbtree_insert")
int BPF_KRETPROBE(KtretprobetcpVZVXrbtreeVZVXinsert)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67339;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_rcv_established")
int BPF_KPROBE(KtprobetcpVZVXrcvVZVXestablished)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67340;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_rcv_established")
int BPF_KRETPROBE(KtretprobetcpVZVXrcvVZVXestablished)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67340;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_rcv_fastopen_synack")
int BPF_KPROBE(KtprobetcpVZVXrcvVZVXfastopenVZVXsynack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67342;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_rcv_fastopen_synack")
int BPF_KRETPROBE(KtretprobetcpVZVXrcvVZVXfastopenVZVXsynack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67342;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_rcv_sne_update")
int BPF_KPROBE(KtprobetcpVZVXrcvVZVXsneVZVXupdate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67344;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_rcv_sne_update")
int BPF_KRETPROBE(KtretprobetcpVZVXrcvVZVXsneVZVXupdate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67344;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_rcv_space_adjust")
int BPF_KPROBE(KtprobetcpVZVXrcvVZVXspaceVZVXadjust)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67345;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_rcv_space_adjust")
int BPF_KRETPROBE(KtretprobetcpVZVXrcvVZVXspaceVZVXadjust)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67345;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_rcv_spurious_retrans")
int BPF_KPROBE(KtprobetcpVZVXrcvVZVXspuriousVZVXretrans)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67346;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_rcv_spurious_retrans")
int BPF_KRETPROBE(KtretprobetcpVZVXrcvVZVXspuriousVZVXretrans)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67346;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_rcv_state_process")
int BPF_KPROBE(KtprobetcpVZVXrcvVZVXstateVZVXprocess)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67348;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_rcv_state_process")
int BPF_KRETPROBE(KtretprobetcpVZVXrcvVZVXstateVZVXprocess)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67348;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_rcv_synrecv_state_fastopen")
int BPF_KPROBE(KtprobetcpVZVXrcvVZVXsynrecvVZVXstateVZVXfastopen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67349;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_rcv_synrecv_state_fastopen")
int BPF_KRETPROBE(KtretprobetcpVZVXrcvVZVXsynrecvVZVXstateVZVXfastopen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67349;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_rcv_synsent_state_process")
int BPF_KPROBE(KtprobetcpVZVXrcvVZVXsynsentVZVXstateVZVXprocess)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67351;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_rcv_synsent_state_process")
int BPF_KRETPROBE(KtretprobetcpVZVXrcvVZVXsynsentVZVXstateVZVXprocess)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67351;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_rcvbuf_grow")
int BPF_KPROBE(KtprobetcpVZVXrcvbufVZVXgrow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67352;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_rcvbuf_grow")
int BPF_KRETPROBE(KtretprobetcpVZVXrcvbufVZVXgrow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67352;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_rearm_rto")
int BPF_KPROBE(KtprobetcpVZVXrearmVZVXrto)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67353;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_rearm_rto")
int BPF_KRETPROBE(KtretprobetcpVZVXrearmVZVXrto)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67353;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_reqsk_record_syn")
int BPF_KPROBE(KtprobetcpVZVXreqskVZVXrecordVZVXsyn)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67355;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_reqsk_record_syn")
int BPF_KRETPROBE(KtretprobetcpVZVXreqskVZVXrecordVZVXsyn)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67355;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_reset")
int BPF_KPROBE(KtprobetcpVZVXreset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67356;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_reset")
int BPF_KRETPROBE(KtretprobetcpVZVXreset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67356;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_rtt_estimator")
int BPF_KPROBE(KtprobetcpVZVXrttVZVXestimator)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67358;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_rtt_estimator")
int BPF_KRETPROBE(KtretprobetcpVZVXrttVZVXestimator)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67358;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_sack_compress_send_ack")
int BPF_KPROBE(KtprobetcpVZVXsackVZVXcompressVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67359;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_sack_compress_send_ack")
int BPF_KRETPROBE(KtretprobetcpVZVXsackVZVXcompressVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67359;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_sack_new_ofo_skb")
int BPF_KPROBE(KtprobetcpVZVXsackVZVXnewVZVXofoVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67360;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_sack_new_ofo_skb")
int BPF_KRETPROBE(KtretprobetcpVZVXsackVZVXnewVZVXofoVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67360;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_sacktag_one")
int BPF_KPROBE(KtprobetcpVZVXsacktagVZVXone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67362;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_sacktag_one")
int BPF_KRETPROBE(KtretprobetcpVZVXsacktagVZVXone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67362;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_sacktag_walk")
int BPF_KPROBE(KtprobetcpVZVXsacktagVZVXwalk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67364;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_sacktag_walk")
int BPF_KRETPROBE(KtretprobetcpVZVXsacktagVZVXwalk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67364;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_sacktag_write_queue")
int BPF_KPROBE(KtprobetcpVZVXsacktagVZVXwriteVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67366;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_sacktag_write_queue")
int BPF_KRETPROBE(KtretprobetcpVZVXsacktagVZVXwriteVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67366;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_send_challenge_ack")
int BPF_KPROBE(KtprobetcpVZVXsendVZVXchallengeVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67367;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_send_challenge_ack")
int BPF_KRETPROBE(KtretprobetcpVZVXsendVZVXchallengeVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67367;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_send_dupack")
int BPF_KPROBE(KtprobetcpVZVXsendVZVXdupack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67368;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_send_dupack")
int BPF_KRETPROBE(KtretprobetcpVZVXsendVZVXdupack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67368;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_send_rcvq")
int BPF_KPROBE(KtprobetcpVZVXsendVZVXrcvq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67369;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_send_rcvq")
int BPF_KRETPROBE(KtretprobetcpVZVXsendVZVXrcvq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67369;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_shift_skb_data")
int BPF_KPROBE(KtprobetcpVZVXshiftVZVXskbVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67371;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_shift_skb_data")
int BPF_KRETPROBE(KtretprobetcpVZVXshiftVZVXskbVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67371;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_shifted_skb")
int BPF_KPROBE(KtprobetcpVZVXshiftedVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67373;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_shifted_skb")
int BPF_KRETPROBE(KtretprobetcpVZVXshiftedVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67373;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_simple_retransmit")
int BPF_KPROBE(KtprobetcpVZVXsimpleVZVXretransmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67374;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_simple_retransmit")
int BPF_KRETPROBE(KtretprobetcpVZVXsimpleVZVXretransmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67374;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_skb_can_collapse")
int BPF_KPROBE(KtprobetcpVZVXskbVZVXcanVZVXcollapse)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67376;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_skb_can_collapse")
int BPF_KRETPROBE(KtretprobetcpVZVXskbVZVXcanVZVXcollapse)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67376;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_skb_shift")
int BPF_KPROBE(KtprobetcpVZVXskbVZVXshift)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67378;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_skb_shift")
int BPF_KRETPROBE(KtretprobetcpVZVXskbVZVXshift)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67378;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_sndbuf_expand")
int BPF_KPROBE(KtprobetcpVZVXsndbufVZVXexpand)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67379;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_sndbuf_expand")
int BPF_KRETPROBE(KtretprobetcpVZVXsndbufVZVXexpand)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67379;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_syn_flood_action")
int BPF_KPROBE(KtprobetcpVZVXsynVZVXfloodVZVXaction)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67381;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_syn_flood_action")
int BPF_KRETPROBE(KtretprobetcpVZVXsynVZVXfloodVZVXaction)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67381;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_synack_rtt_meas")
int BPF_KPROBE(KtprobetcpVZVXsynackVZVXrttVZVXmeas)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67383;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_synack_rtt_meas")
int BPF_KRETPROBE(KtretprobetcpVZVXsynackVZVXrttVZVXmeas)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67383;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_timeout_init")
int BPF_KPROBE(KtprobetcpVZVXtimeoutVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67384;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_timeout_init")
int BPF_KRETPROBE(KtretprobetcpVZVXtimeoutVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67384;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_try_coalesce")
int BPF_KPROBE(KtprobetcpVZVXtryVZVXcoalesce)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67386;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_try_coalesce")
int BPF_KRETPROBE(KtretprobetcpVZVXtryVZVXcoalesce)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67386;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_try_keep_open")
int BPF_KPROBE(KtprobetcpVZVXtryVZVXkeepVZVXopen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67387;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_try_keep_open")
int BPF_KRETPROBE(KtretprobetcpVZVXtryVZVXkeepVZVXopen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67387;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_try_rmem_schedule")
int BPF_KPROBE(KtprobetcpVZVXtryVZVXrmemVZVXschedule)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67389;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_try_rmem_schedule")
int BPF_KRETPROBE(KtretprobetcpVZVXtryVZVXrmemVZVXschedule)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67389;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_try_to_open")
int BPF_KPROBE(KtprobetcpVZVXtryVZVXtoVZVXopen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67390;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_try_to_open")
int BPF_KRETPROBE(KtretprobetcpVZVXtryVZVXtoVZVXopen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67390;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_try_undo_loss")
int BPF_KPROBE(KtprobetcpVZVXtryVZVXundoVZVXloss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67392;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_try_undo_loss")
int BPF_KRETPROBE(KtretprobetcpVZVXtryVZVXundoVZVXloss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67392;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_try_undo_recovery")
int BPF_KPROBE(KtprobetcpVZVXtryVZVXundoVZVXrecovery)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67393;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_try_undo_recovery")
int BPF_KRETPROBE(KtretprobetcpVZVXtryVZVXundoVZVXrecovery)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67393;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_undo_cwnd_reduction")
int BPF_KPROBE(KtprobetcpVZVXundoVZVXcwndVZVXreduction)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67395;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_undo_cwnd_reduction")
int BPF_KRETPROBE(KtretprobetcpVZVXundoVZVXcwndVZVXreduction)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67395;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_update_pacing_rate")
int BPF_KPROBE(KtprobetcpVZVXupdateVZVXpacingVZVXrate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67396;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_update_pacing_rate")
int BPF_KRETPROBE(KtretprobetcpVZVXupdateVZVXpacingVZVXrate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67396;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_urg")
int BPF_KPROBE(KtprobetcpVZVXurg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67398;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_urg")
int BPF_KRETPROBE(KtretprobetcpVZVXurg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67398;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_ao_calc_key_rsk")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXaoVZVXcalcVZVXkeyVZVXrsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67402;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_ao_calc_key_rsk")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXaoVZVXcalcVZVXkeyVZVXrsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67402;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_ao_calc_key_sk")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXaoVZVXcalcVZVXkeyVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67404;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_ao_calc_key_sk")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXaoVZVXcalcVZVXkeyVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67404;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_ao_calc_key_skb")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXaoVZVXcalcVZVXkeyVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67406;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_ao_calc_key_skb")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXaoVZVXcalcVZVXkeyVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67406;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_ao_hash_skb")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXaoVZVXhashVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67410;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_ao_hash_skb")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXaoVZVXhashVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67410;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_ao_lookup")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXaoVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67412;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_ao_lookup")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXaoVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67412;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_ao_lookup_rsk")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXaoVZVXlookupVZVXrsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67414;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_ao_lookup_rsk")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXaoVZVXlookupVZVXrsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67414;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_ao_synack_hash")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXaoVZVXsynackVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67416;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_ao_synack_hash")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXaoVZVXsynackVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67416;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_parse_ao")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXparseVZVXao)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67418;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_parse_ao")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXparseVZVXao)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67418;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_validate_incoming")
int BPF_KPROBE(KtprobetcpVZVXvalidateVZVXincoming)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67420;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_validate_incoming")
int BPF_KRETPROBE(KtretprobetcpVZVXvalidateVZVXincoming)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67420;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_warn_once")
int BPF_KPROBE(KtprobetcpVZVXwarnVZVXonce)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67422;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_warn_once")
int BPF_KRETPROBE(KtretprobetcpVZVXwarnVZVXonce)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67422;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_xmit_recovery")
int BPF_KPROBE(KtprobetcpVZVXxmitVZVXrecovery)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67424;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_xmit_recovery")
int BPF_KRETPROBE(KtretprobetcpVZVXxmitVZVXrecovery)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67424;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tls_alert_recv")
int BPF_KPROBE(KtprobetlsVZVXalertVZVXrecv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67633;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tls_alert_recv")
int BPF_KRETPROBE(KtretprobetlsVZVXalertVZVXrecv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67633;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tls_alert_send")
int BPF_KPROBE(KtprobetlsVZVXalertVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67635;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tls_alert_send")
int BPF_KRETPROBE(KtretprobetlsVZVXalertVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67635;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tomoyo_socket_bind_permission")
int BPF_KPROBE(KtprobetomoyoVZVXsocketVZVXbindVZVXpermission)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67816;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tomoyo_socket_bind_permission")
int BPF_KRETPROBE(KtretprobetomoyoVZVXsocketVZVXbindVZVXpermission)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67816;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tomoyo_socket_connect_permission")
int BPF_KPROBE(KtprobetomoyoVZVXsocketVZVXconnectVZVXpermission)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67817;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tomoyo_socket_connect_permission")
int BPF_KRETPROBE(KtretprobetomoyoVZVXsocketVZVXconnectVZVXpermission)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67817;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tomoyo_socket_listen_permission")
int BPF_KPROBE(KtprobetomoyoVZVXsocketVZVXlistenVZVXpermission)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67818;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tomoyo_socket_listen_permission")
int BPF_KRETPROBE(KtretprobetomoyoVZVXsocketVZVXlistenVZVXpermission)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67818;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tomoyo_socket_sendmsg_permission")
int BPF_KPROBE(KtprobetomoyoVZVXsocketVZVXsendmsgVZVXpermission)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67819;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tomoyo_socket_sendmsg_permission")
int BPF_KRETPROBE(KtretprobetomoyoVZVXsocketVZVXsendmsgVZVXpermission)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=67819;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp4_csum_init")
int BPF_KPROBE(Ktprobeudp4VZVXcsumVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68747;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp4_csum_init")
int BPF_KRETPROBE(Ktretprobeudp4VZVXcsumVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68747;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp4_hash4")
int BPF_KPROBE(Ktprobeudp4VZVXhash4)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68748;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp4_hash4")
int BPF_KRETPROBE(Ktretprobeudp4VZVXhash4)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68748;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp4_hwcsum")
int BPF_KPROBE(Ktprobeudp4VZVXhwcsum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68750;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp4_hwcsum")
int BPF_KRETPROBE(Ktretprobeudp4VZVXhwcsum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68750;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp4_lib_lookup")
int BPF_KPROBE(Ktprobeudp4VZVXlibVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68752;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp4_lib_lookup")
int BPF_KRETPROBE(Ktretprobeudp4VZVXlibVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68752;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp4_lib_lookup2")
int BPF_KPROBE(Ktprobeudp4VZVXlibVZVXlookup2)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68754;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp4_lib_lookup2")
int BPF_KRETPROBE(Ktretprobeudp4VZVXlibVZVXlookup2)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68754;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp4_lib_lookup_skb")
int BPF_KPROBE(Ktprobeudp4VZVXlibVZVXlookupVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68756;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp4_lib_lookup_skb")
int BPF_KRETPROBE(Ktretprobeudp4VZVXlibVZVXlookupVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68756;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp4_proc_exit_net")
int BPF_KPROBE(Ktprobeudp4VZVXprocVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68758;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp4_proc_exit_net")
int BPF_KRETPROBE(Ktretprobeudp4VZVXprocVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68758;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp4_proc_init_net")
int BPF_KPROBE(Ktprobeudp4VZVXprocVZVXinitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68760;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp4_proc_init_net")
int BPF_KRETPROBE(Ktretprobeudp4VZVXprocVZVXinitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68760;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp6_csum_init")
int BPF_KPROBE(Ktprobeudp6VZVXcsumVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68762;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp6_csum_init")
int BPF_KRETPROBE(Ktretprobeudp6VZVXcsumVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68762;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp6_set_csum")
int BPF_KPROBE(Ktprobeudp6VZVXsetVZVXcsum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68764;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp6_set_csum")
int BPF_KRETPROBE(Ktretprobeudp6VZVXsetVZVXcsum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68764;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_abort")
int BPF_KPROBE(KtprobeudpVZVXabort)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68766;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_abort")
int BPF_KRETPROBE(KtretprobeudpVZVXabort)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68766;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_cmsg_send")
int BPF_KPROBE(KtprobeudpVZVXcmsgVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68768;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_cmsg_send")
int BPF_KRETPROBE(KtretprobeudpVZVXcmsgVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68768;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_connect")
int BPF_KPROBE(KtprobeudpVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68769;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_connect")
int BPF_KRETPROBE(KtretprobeudpVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68769;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_destroy_sock")
int BPF_KPROBE(KtprobeudpVZVXdestroyVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68770;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_destroy_sock")
int BPF_KRETPROBE(KtretprobeudpVZVXdestroyVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68770;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_destruct_common")
int BPF_KPROBE(KtprobeudpVZVXdestructVZVXcommon)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68771;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_destruct_common")
int BPF_KRETPROBE(KtretprobeudpVZVXdestructVZVXcommon)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68771;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_destruct_sock")
int BPF_KPROBE(KtprobeudpVZVXdestructVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68772;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_destruct_sock")
int BPF_KRETPROBE(KtretprobeudpVZVXdestructVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68772;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_disconnect")
int BPF_KPROBE(KtprobeudpVZVXdisconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68773;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_disconnect")
int BPF_KRETPROBE(KtretprobeudpVZVXdisconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68773;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_ehashfn")
int BPF_KPROBE(KtprobeudpVZVXehashfn)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68774;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_ehashfn")
int BPF_KRETPROBE(KtretprobeudpVZVXehashfn)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68774;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_err")
int BPF_KPROBE(KtprobeudpVZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68777;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_err")
int BPF_KRETPROBE(KtretprobeudpVZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68777;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_flush_pending_frames")
int BPF_KPROBE(KtprobeudpVZVXflushVZVXpendingVZVXframes)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68779;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_flush_pending_frames")
int BPF_KRETPROBE(KtretprobeudpVZVXflushVZVXpendingVZVXframes)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68779;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_get_next")
int BPF_KPROBE(KtprobeudpVZVXgetVZVXnext)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68783;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_get_next")
int BPF_KRETPROBE(KtretprobeudpVZVXgetVZVXnext)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68783;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_getsockopt")
int BPF_KPROBE(KtprobeudpVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68785;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_getsockopt")
int BPF_KRETPROBE(KtretprobeudpVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68785;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_init_sock")
int BPF_KPROBE(KtprobeudpVZVXinitVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68787;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_init_sock")
int BPF_KRETPROBE(KtretprobeudpVZVXinitVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68787;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_ioctl")
int BPF_KPROBE(KtprobeudpVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68789;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_ioctl")
int BPF_KRETPROBE(KtretprobeudpVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68789;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_lib_close")
int BPF_KPROBE(KtprobeudpVZVXlibVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68790;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_lib_close")
int BPF_KRETPROBE(KtretprobeudpVZVXlibVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68790;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_lib_get_port")
int BPF_KPROBE(KtprobeudpVZVXlibVZVXgetVZVXport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68792;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_lib_get_port")
int BPF_KRETPROBE(KtretprobeudpVZVXlibVZVXgetVZVXport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68792;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_lib_getsockopt")
int BPF_KPROBE(KtprobeudpVZVXlibVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68793;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_lib_getsockopt")
int BPF_KRETPROBE(KtretprobeudpVZVXlibVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68793;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_lib_hash")
int BPF_KPROBE(KtprobeudpVZVXlibVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68794;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_lib_hash")
int BPF_KRETPROBE(KtretprobeudpVZVXlibVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68794;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_lib_hash4")
int BPF_KPROBE(KtprobeudpVZVXlibVZVXhash4)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68796;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_lib_hash4")
int BPF_KRETPROBE(KtretprobeudpVZVXlibVZVXhash4)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68796;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_lib_lport_inuse2")
int BPF_KPROBE(KtprobeudpVZVXlibVZVXlportVZVXinuse2)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68798;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_lib_lport_inuse2")
int BPF_KRETPROBE(KtretprobeudpVZVXlibVZVXlportVZVXinuse2)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68798;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_lib_rehash")
int BPF_KPROBE(KtprobeudpVZVXlibVZVXrehash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68800;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_lib_rehash")
int BPF_KRETPROBE(KtretprobeudpVZVXlibVZVXrehash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68800;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_lib_setsockopt")
int BPF_KPROBE(KtprobeudpVZVXlibVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68802;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_lib_setsockopt")
int BPF_KRETPROBE(KtretprobeudpVZVXlibVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68802;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_lib_unhash")
int BPF_KPROBE(KtprobeudpVZVXlibVZVXunhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68803;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_lib_unhash")
int BPF_KRETPROBE(KtretprobeudpVZVXlibVZVXunhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68803;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_pernet_exit")
int BPF_KPROBE(KtprobeudpVZVXpernetVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68804;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_pernet_exit")
int BPF_KRETPROBE(KtretprobeudpVZVXpernetVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68804;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_pernet_init")
int BPF_KPROBE(KtprobeudpVZVXpernetVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68805;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_pernet_init")
int BPF_KRETPROBE(KtretprobeudpVZVXpernetVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68805;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_pre_connect")
int BPF_KPROBE(KtprobeudpVZVXpreVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68807;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_pre_connect")
int BPF_KRETPROBE(KtretprobeudpVZVXpreVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68807;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_push_pending_frames")
int BPF_KPROBE(KtprobeudpVZVXpushVZVXpendingVZVXframes)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68808;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_push_pending_frames")
int BPF_KRETPROBE(KtretprobeudpVZVXpushVZVXpendingVZVXframes)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68808;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_queue_rcv_one_skb")
int BPF_KPROBE(KtprobeudpVZVXqueueVZVXrcvVZVXoneVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68809;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_queue_rcv_one_skb")
int BPF_KRETPROBE(KtretprobeudpVZVXqueueVZVXrcvVZVXoneVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68809;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_queue_rcv_skb")
int BPF_KPROBE(KtprobeudpVZVXqueueVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68810;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_queue_rcv_skb")
int BPF_KRETPROBE(KtretprobeudpVZVXqueueVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68810;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_rcv")
int BPF_KPROBE(KtprobeudpVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68811;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_rcv")
int BPF_KRETPROBE(KtretprobeudpVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68811;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_read_skb")
int BPF_KPROBE(KtprobeudpVZVXreadVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68813;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_read_skb")
int BPF_KRETPROBE(KtretprobeudpVZVXreadVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68813;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_recvmsg")
int BPF_KPROBE(KtprobeudpVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68814;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_recvmsg")
int BPF_KRETPROBE(KtretprobeudpVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68814;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_rmem_release")
int BPF_KPROBE(KtprobeudpVZVXrmemVZVXrelease)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68816;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_rmem_release")
int BPF_KRETPROBE(KtretprobeudpVZVXrmemVZVXrelease)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68816;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_send_skb")
int BPF_KPROBE(KtprobeudpVZVXsendVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68818;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_send_skb")
int BPF_KRETPROBE(KtretprobeudpVZVXsendVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68818;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_set_csum")
int BPF_KPROBE(KtprobeudpVZVXsetVZVXcsum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68824;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_set_csum")
int BPF_KRETPROBE(KtretprobeudpVZVXsetVZVXcsum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68824;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_setsockopt")
int BPF_KPROBE(KtprobeudpVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68825;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_setsockopt")
int BPF_KRETPROBE(KtretprobeudpVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68825;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_sk_rx_dst_set")
int BPF_KPROBE(KtprobeudpVZVXskVZVXrxVZVXdstVZVXset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68827;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_sk_rx_dst_set")
int BPF_KRETPROBE(KtretprobeudpVZVXskVZVXrxVZVXdstVZVXset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68827;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_skb_destructor")
int BPF_KPROBE(KtprobeudpVZVXskbVZVXdestructor)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68828;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_skb_destructor")
int BPF_KRETPROBE(KtretprobeudpVZVXskbVZVXdestructor)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68828;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_splice_eof")
int BPF_KPROBE(KtprobeudpVZVXspliceVZVXeof)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68830;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_splice_eof")
int BPF_KRETPROBE(KtretprobeudpVZVXspliceVZVXeof)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68830;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_unicast_rcv_skb")
int BPF_KPROBE(KtprobeudpVZVXunicastVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68834;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_unicast_rcv_skb")
int BPF_KRETPROBE(KtretprobeudpVZVXunicastVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68834;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_v4_early_demux")
int BPF_KPROBE(KtprobeudpVZVXv4VZVXearlyVZVXdemux)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68835;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_v4_early_demux")
int BPF_KRETPROBE(KtretprobeudpVZVXv4VZVXearlyVZVXdemux)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68835;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_v4_get_port")
int BPF_KPROBE(KtprobeudpVZVXv4VZVXgetVZVXport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68837;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_v4_get_port")
int BPF_KRETPROBE(KtretprobeudpVZVXv4VZVXgetVZVXport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68837;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_v4_rehash")
int BPF_KPROBE(KtprobeudpVZVXv4VZVXrehash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68838;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_v4_rehash")
int BPF_KRETPROBE(KtretprobeudpVZVXv4VZVXrehash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68838;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udplite_getfrag")
int BPF_KPROBE(KtprobeudpliteVZVXgetfrag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68839;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udplite_getfrag")
int BPF_KRETPROBE(KtretprobeudpliteVZVXgetfrag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=68839;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xfrm6_ipcomp_err")
int BPF_KPROBE(Ktprobexfrm6VZVXipcompVZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=70487;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xfrm6_ipcomp_err")
int BPF_KRETPROBE(Ktretprobexfrm6VZVXipcompVZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=70487;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xfrm6_ipcomp_rcv")
int BPF_KPROBE(Ktprobexfrm6VZVXipcompVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=70488;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xfrm6_ipcomp_rcv")
int BPF_KRETPROBE(Ktretprobexfrm6VZVXipcompVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=70488;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/____ip_mc_inc_group")
int BPF_KPROBE(KtprobeVZVXVZVXVZVXVZVXipVZVXmcVZVXincVZVXgroup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=82478;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/____ip_mc_inc_group")
int BPF_KRETPROBE(KtretprobeVZVXVZVXVZVXVZVXipVZVXmcVZVXincVZVXgroup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=82478;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__genl_sk_priv_get")
int BPF_KPROBE(KtprobeVZVXVZVXgenlVZVXskVZVXprivVZVXget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83389;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__genl_sk_priv_get")
int BPF_KRETPROBE(KtretprobeVZVXVZVXgenlVZVXskVZVXprivVZVXget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83389;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__inet_csk_reqsk_queue_drop")
int BPF_KPROBE(KtprobeVZVXVZVXinetVZVXcskVZVXreqskVZVXqueueVZVXdrop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83610;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__inet_csk_reqsk_queue_drop")
int BPF_KRETPROBE(KtretprobeVZVXVZVXinetVZVXcskVZVXreqskVZVXqueueVZVXdrop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83610;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip6_append_data")
int BPF_KPROBE(KtprobeVZVXVZVXip6VZVXappendVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83652;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip6_append_data")
int BPF_KRETPROBE(KtretprobeVZVXVZVXip6VZVXappendVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83652;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip6_datagram_connect")
int BPF_KPROBE(KtprobeVZVXVZVXip6VZVXdatagramVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83653;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip6_datagram_connect")
int BPF_KRETPROBE(KtretprobeVZVXVZVXip6VZVXdatagramVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83653;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip6_del_rt")
int BPF_KPROBE(KtprobeVZVXVZVXip6VZVXdelVZVXrt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83655;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip6_del_rt")
int BPF_KRETPROBE(KtretprobeVZVXVZVXip6VZVXdelVZVXrt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83655;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip6_del_rt_siblings")
int BPF_KPROBE(KtprobeVZVXVZVXip6VZVXdelVZVXrtVZVXsiblings)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83657;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip6_del_rt_siblings")
int BPF_KRETPROBE(KtretprobeVZVXVZVXip6VZVXdelVZVXrtVZVXsiblings)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83657;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip6_dgram_sock_seq_show")
int BPF_KPROBE(KtprobeVZVXVZVXip6VZVXdgramVZVXsockVZVXseqVZVXshow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83659;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip6_dgram_sock_seq_show")
int BPF_KRETPROBE(KtretprobeVZVXVZVXip6VZVXdgramVZVXsockVZVXseqVZVXshow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83659;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip6_flush_pending_frames")
int BPF_KPROBE(KtprobeVZVXVZVXip6VZVXflushVZVXpendingVZVXframes)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83661;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip6_flush_pending_frames")
int BPF_KRETPROBE(KtretprobeVZVXVZVXip6VZVXflushVZVXpendingVZVXframes)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83661;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip6_local_out")
int BPF_KPROBE(KtprobeVZVXVZVXip6VZVXlocalVZVXout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83662;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip6_local_out")
int BPF_KRETPROBE(KtretprobeVZVXVZVXip6VZVXlocalVZVXout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83662;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip6_make_skb")
int BPF_KPROBE(KtprobeVZVXVZVXip6VZVXmakeVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83664;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip6_make_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXip6VZVXmakeVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83664;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip6_route_redirect")
int BPF_KPROBE(KtprobeVZVXVZVXip6VZVXrouteVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83666;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip6_route_redirect")
int BPF_KRETPROBE(KtretprobeVZVXVZVXip6VZVXrouteVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83666;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip6_rt_update_pmtu")
int BPF_KPROBE(KtprobeVZVXVZVXip6VZVXrtVZVXupdateVZVXpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83668;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip6_rt_update_pmtu")
int BPF_KRETPROBE(KtretprobeVZVXVZVXip6VZVXrtVZVXupdateVZVXpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83668;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip_append_data")
int BPF_KPROBE(KtprobeVZVXVZVXipVZVXappendVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83670;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip_append_data")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipVZVXappendVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83670;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip_finish_output")
int BPF_KPROBE(KtprobeVZVXVZVXipVZVXfinishVZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83671;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip_finish_output")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipVZVXfinishVZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83671;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip_flush_pending_frames")
int BPF_KPROBE(KtprobeVZVXVZVXipVZVXflushVZVXpendingVZVXframes)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83673;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip_flush_pending_frames")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipVZVXflushVZVXpendingVZVXframes)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83673;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip_local_out")
int BPF_KPROBE(KtprobeVZVXVZVXipVZVXlocalVZVXout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83674;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip_local_out")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipVZVXlocalVZVXout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83674;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip_make_skb")
int BPF_KPROBE(KtprobeVZVXVZVXipVZVXmakeVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83676;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip_make_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipVZVXmakeVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83676;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip_mc_dec_group")
int BPF_KPROBE(KtprobeVZVXVZVXipVZVXmcVZVXdecVZVXgroup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83678;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip_mc_dec_group")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipVZVXmcVZVXdecVZVXgroup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83678;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip_mc_inc_group")
int BPF_KPROBE(KtprobeVZVXVZVXipVZVXmcVZVXincVZVXgroup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83679;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip_mc_inc_group")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipVZVXmcVZVXincVZVXgroup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83679;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip_mc_join_group")
int BPF_KPROBE(KtprobeVZVXVZVXipVZVXmcVZVXjoinVZVXgroup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83681;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip_mc_join_group")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipVZVXmcVZVXjoinVZVXgroup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83681;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip_queue_xmit")
int BPF_KPROBE(KtprobeVZVXVZVXipVZVXqueueVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83683;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip_queue_xmit")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipVZVXqueueVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83683;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ipv6_dev_mc_dec")
int BPF_KPROBE(KtprobeVZVXVZVXipv6VZVXdevVZVXmcVZVXdec)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83686;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ipv6_dev_mc_dec")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipv6VZVXdevVZVXmcVZVXdec)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83686;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ipv6_dev_mc_inc")
int BPF_KPROBE(KtprobeVZVXVZVXipv6VZVXdevVZVXmcVZVXinc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83688;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ipv6_dev_mc_inc")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipv6VZVXdevVZVXmcVZVXinc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83688;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ipv6_sock_mc_close")
int BPF_KPROBE(KtprobeVZVXVZVXipv6VZVXsockVZVXmcVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83689;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ipv6_sock_mc_close")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipv6VZVXsockVZVXmcVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83689;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ipv6_sock_mc_drop")
int BPF_KPROBE(KtprobeVZVXVZVXipv6VZVXsockVZVXmcVZVXdrop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83691;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ipv6_sock_mc_drop")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipv6VZVXsockVZVXmcVZVXdrop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83691;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ipv6_sock_mc_join")
int BPF_KPROBE(KtprobeVZVXVZVXipv6VZVXsockVZVXmcVZVXjoin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83693;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ipv6_sock_mc_join")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipv6VZVXsockVZVXmcVZVXjoin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83693;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__neigh_event_send")
int BPF_KPROBE(KtprobeVZVXVZVXneighVZVXeventVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83839;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__neigh_event_send")
int BPF_KRETPROBE(KtretprobeVZVXVZVXneighVZVXeventVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83839;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__netpoll_send_skb")
int BPF_KPROBE(KtprobeVZVXVZVXnetpollVZVXsendVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83857;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__netpoll_send_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXnetpollVZVXsendVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83857;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ping_queue_rcv_skb")
int BPF_KPROBE(KtprobeVZVXVZVXpingVZVXqueueVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83989;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ping_queue_rcv_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXpingVZVXqueueVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=83989;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sock_create")
int BPF_KPROBE(KtprobeVZVXVZVXsockVZVXcreate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84451;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sock_create")
int BPF_KRETPROBE(KtretprobeVZVXVZVXsockVZVXcreate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84451;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sock_recv_cmsgs")
int BPF_KPROBE(KtprobeVZVXVZVXsockVZVXrecvVZVXcmsgs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84453;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sock_recv_cmsgs")
int BPF_KRETPROBE(KtretprobeVZVXVZVXsockVZVXrecvVZVXcmsgs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84453;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sock_recv_timestamp")
int BPF_KPROBE(KtprobeVZVXVZVXsockVZVXrecvVZVXtimestamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84454;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sock_recv_timestamp")
int BPF_KRETPROBE(KtretprobeVZVXVZVXsockVZVXrecvVZVXtimestamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84454;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sock_recv_wifi_status")
int BPF_KPROBE(KtprobeVZVXVZVXsockVZVXrecvVZVXwifiVZVXstatus)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84455;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sock_recv_wifi_status")
int BPF_KRETPROBE(KtretprobeVZVXVZVXsockVZVXrecvVZVXwifiVZVXstatus)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84455;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sock_release")
int BPF_KPROBE(KtprobeVZVXVZVXsockVZVXrelease)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84457;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sock_release")
int BPF_KRETPROBE(KtretprobeVZVXVZVXsockVZVXrelease)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84457;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sys_bind_socket")
int BPF_KPROBE(KtprobeVZVXVZVXsysVZVXbindVZVXsocket)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84507;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sys_bind_socket")
int BPF_KRETPROBE(KtretprobeVZVXVZVXsysVZVXbindVZVXsocket)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84507;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sys_listen_socket")
int BPF_KPROBE(KtprobeVZVXVZVXsysVZVXlistenVZVXsocket)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84519;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sys_listen_socket")
int BPF_KRETPROBE(KtretprobeVZVXVZVXsysVZVXlistenVZVXsocket)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84519;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sys_recvmsg_sock")
int BPF_KPROBE(KtprobeVZVXVZVXsysVZVXrecvmsgVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84527;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sys_recvmsg_sock")
int BPF_KRETPROBE(KtretprobeVZVXVZVXsysVZVXrecvmsgVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84527;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sys_sendmsg_sock")
int BPF_KPROBE(KtprobeVZVXVZVXsysVZVXsendmsgVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84532;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sys_sendmsg_sock")
int BPF_KRETPROBE(KtretprobeVZVXVZVXsysVZVXsendmsgVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84532;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sys_shutdown_sock")
int BPF_KPROBE(KtprobeVZVXVZVXsysVZVXshutdownVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84553;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sys_shutdown_sock")
int BPF_KRETPROBE(KtretprobeVZVXVZVXsysVZVXshutdownVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84553;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__tcp_ao_do_lookup")
int BPF_KPROBE(KtprobeVZVXVZVXtcpVZVXaoVZVXdoVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84571;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__tcp_ao_do_lookup")
int BPF_KRETPROBE(KtretprobeVZVXVZVXtcpVZVXaoVZVXdoVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84571;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__tcpv4_gso_segment_csum")
int BPF_KPROBE(KtprobeVZVXVZVXtcpv4VZVXgsoVZVXsegmentVZVXcsum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84575;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__tcpv4_gso_segment_csum")
int BPF_KRETPROBE(KtretprobeVZVXVZVXtcpv4VZVXgsoVZVXsegmentVZVXcsum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=84575;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__virtio_net_hdr_to_skb")
int BPF_KPROBE(KtprobeVZVXVZVXvirtioVZVXnetVZVXhdrVZVXtoVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=85086;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__virtio_net_hdr_to_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXvirtioVZVXnetVZVXhdrVZVXtoVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=85086;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__xdp_build_skb_from_frame")
int BPF_KPROBE(KtprobeVZVXVZVXxdpVZVXbuildVZVXskbVZVXfromVZVXframe)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=85296;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__xdp_build_skb_from_frame")
int BPF_KRETPROBE(KtretprobeVZVXVZVXxdpVZVXbuildVZVXskbVZVXfromVZVXframe)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=85296;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__xsk_generic_xmit")
int BPF_KPROBE(KtprobeVZVXVZVXxskVZVXgenericVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=85325;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__xsk_generic_xmit")
int BPF_KRETPROBE(KtretprobeVZVXVZVXxskVZVXgenericVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=85325;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__xsk_map_redirect")
int BPF_KPROBE(KtprobeVZVXVZVXxskVZVXmapVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=85328;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__xsk_map_redirect")
int BPF_KRETPROBE(KtretprobeVZVXVZVXxskVZVXmapVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=85328;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__xsk_rcv")
int BPF_KPROBE(KtprobeVZVXVZVXxskVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=85330;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__xsk_rcv")
int BPF_KRETPROBE(KtretprobeVZVXVZVXxskVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=85330;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__xsk_rcv_zc")
int BPF_KPROBE(KtprobeVZVXVZVXxskVZVXrcvVZVXzc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=85332;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__xsk_rcv_zc")
int BPF_KRETPROBE(KtretprobeVZVXVZVXxskVZVXrcvVZVXzc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=85332;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__xsk_sendmsg")
int BPF_KPROBE(KtprobeVZVXVZVXxskVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=85334;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__xsk_sendmsg")
int BPF_KRETPROBE(KtretprobeVZVXVZVXxskVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=85334;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__xsk_tx_release")
int BPF_KPROBE(KtprobeVZVXVZVXxskVZVXtxVZVXrelease)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=85336;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__xsk_tx_release")
int BPF_KRETPROBE(KtretprobeVZVXVZVXxskVZVXtxVZVXrelease)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=85336;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/_ipmr_fill_mroute")
int BPF_KPROBE(KtprobeVZVXipmrVZVXfillVZVXmroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=85362;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/_ipmr_fill_mroute")
int BPF_KRETPROBE(KtretprobeVZVXipmrVZVXfillVZVXmroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=85362;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/apparmor_ip_postroute")
int BPF_KPROBE(KtprobeapparmorVZVXipVZVXpostroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86653;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/apparmor_ip_postroute")
int BPF_KRETPROBE(KtretprobeapparmorVZVXipVZVXpostroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86653;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/apparmor_sk_alloc_security")
int BPF_KPROBE(KtprobeapparmorVZVXskVZVXallocVZVXsecurity)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86678;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/apparmor_sk_alloc_security")
int BPF_KRETPROBE(KtretprobeapparmorVZVXskVZVXallocVZVXsecurity)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86678;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/apparmor_sk_clone_security")
int BPF_KPROBE(KtprobeapparmorVZVXskVZVXcloneVZVXsecurity)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86679;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/apparmor_sk_clone_security")
int BPF_KRETPROBE(KtretprobeapparmorVZVXskVZVXcloneVZVXsecurity)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86679;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/apparmor_sk_free_security")
int BPF_KPROBE(KtprobeapparmorVZVXskVZVXfreeVZVXsecurity)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86680;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/apparmor_sk_free_security")
int BPF_KRETPROBE(KtretprobeapparmorVZVXskVZVXfreeVZVXsecurity)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86680;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/apparmor_sock_graft")
int BPF_KPROBE(KtprobeapparmorVZVXsockVZVXgraft)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86681;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/apparmor_sock_graft")
int BPF_KRETPROBE(KtretprobeapparmorVZVXsockVZVXgraft)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86681;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/apparmor_socket_accept")
int BPF_KPROBE(KtprobeapparmorVZVXsocketVZVXaccept)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86682;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/apparmor_socket_accept")
int BPF_KRETPROBE(KtretprobeapparmorVZVXsocketVZVXaccept)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86682;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/apparmor_socket_bind")
int BPF_KPROBE(KtprobeapparmorVZVXsocketVZVXbind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86683;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/apparmor_socket_bind")
int BPF_KRETPROBE(KtretprobeapparmorVZVXsocketVZVXbind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86683;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/apparmor_socket_connect")
int BPF_KPROBE(KtprobeapparmorVZVXsocketVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86684;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/apparmor_socket_connect")
int BPF_KRETPROBE(KtretprobeapparmorVZVXsocketVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86684;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/apparmor_socket_getpeersec_dgram")
int BPF_KPROBE(KtprobeapparmorVZVXsocketVZVXgetpeersecVZVXdgram)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86687;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/apparmor_socket_getpeersec_dgram")
int BPF_KRETPROBE(KtretprobeapparmorVZVXsocketVZVXgetpeersecVZVXdgram)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86687;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/apparmor_socket_getpeersec_stream")
int BPF_KPROBE(KtprobeapparmorVZVXsocketVZVXgetpeersecVZVXstream)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86688;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/apparmor_socket_getpeersec_stream")
int BPF_KRETPROBE(KtretprobeapparmorVZVXsocketVZVXgetpeersecVZVXstream)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86688;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/apparmor_socket_getsockname")
int BPF_KPROBE(KtprobeapparmorVZVXsocketVZVXgetsockname)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86689;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/apparmor_socket_getsockname")
int BPF_KRETPROBE(KtretprobeapparmorVZVXsocketVZVXgetsockname)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86689;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/apparmor_socket_getsockopt")
int BPF_KPROBE(KtprobeapparmorVZVXsocketVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86690;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/apparmor_socket_getsockopt")
int BPF_KRETPROBE(KtretprobeapparmorVZVXsocketVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86690;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/apparmor_socket_listen")
int BPF_KPROBE(KtprobeapparmorVZVXsocketVZVXlisten)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86691;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/apparmor_socket_listen")
int BPF_KRETPROBE(KtretprobeapparmorVZVXsocketVZVXlisten)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86691;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/apparmor_socket_post_create")
int BPF_KPROBE(KtprobeapparmorVZVXsocketVZVXpostVZVXcreate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86692;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/apparmor_socket_post_create")
int BPF_KRETPROBE(KtretprobeapparmorVZVXsocketVZVXpostVZVXcreate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86692;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/apparmor_socket_setsockopt")
int BPF_KPROBE(KtprobeapparmorVZVXsocketVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86695;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/apparmor_socket_setsockopt")
int BPF_KRETPROBE(KtretprobeapparmorVZVXsocketVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86695;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/apparmor_socket_shutdown")
int BPF_KPROBE(KtprobeapparmorVZVXsocketVZVXshutdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86696;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/apparmor_socket_shutdown")
int BPF_KRETPROBE(KtretprobeapparmorVZVXsocketVZVXshutdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86696;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/apparmor_socket_sock_rcv_skb")
int BPF_KPROBE(KtprobeapparmorVZVXsocketVZVXsockVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86697;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/apparmor_socket_sock_rcv_skb")
int BPF_KRETPROBE(KtretprobeapparmorVZVXsocketVZVXsockVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86697;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/apparmor_socket_socketpair")
int BPF_KPROBE(KtprobeapparmorVZVXsocketVZVXsocketpair)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86698;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/apparmor_socket_socketpair")
int BPF_KRETPROBE(KtretprobeapparmorVZVXsocketVZVXsocketpair)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86698;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/apparmor_unix_may_send")
int BPF_KPROBE(KtprobeapparmorVZVXunixVZVXmayVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86707;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/apparmor_unix_may_send")
int BPF_KRETPROBE(KtretprobeapparmorVZVXunixVZVXmayVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86707;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/arp_send")
int BPF_KPROBE(KtprobearpVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86844;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/arp_send")
int BPF_KRETPROBE(KtretprobearpVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86844;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/arp_send_dst")
int BPF_KPROBE(KtprobearpVZVXsendVZVXdst)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86846;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/arp_send_dst")
int BPF_KRETPROBE(KtretprobearpVZVXsendVZVXdst)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86846;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/arp_xmit")
int BPF_KPROBE(KtprobearpVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86850;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/arp_xmit")
int BPF_KRETPROBE(KtretprobearpVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86850;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/arp_xmit_finish")
int BPF_KPROBE(KtprobearpVZVXxmitVZVXfinish)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86851;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/arp_xmit_finish")
int BPF_KRETPROBE(KtretprobearpVZVXxmitVZVXfinish)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=86851;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/bq_xmit_all")
int BPF_KPROBE(KtprobebqVZVXxmitVZVXall)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=87784;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/bq_xmit_all")
int BPF_KRETPROBE(KtretprobebqVZVXxmitVZVXall)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=87784;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/cipso_v4_error")
int BPF_KPROBE(KtprobecipsoVZVXv4VZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=88721;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/cipso_v4_error")
int BPF_KRETPROBE(KtretprobecipsoVZVXv4VZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=88721;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/cipso_v4_optptr")
int BPF_KPROBE(KtprobecipsoVZVXv4VZVXoptptr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=88728;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/cipso_v4_optptr")
int BPF_KRETPROBE(KtretprobecipsoVZVXv4VZVXoptptr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=88728;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/cipso_v4_req_delattr")
int BPF_KPROBE(KtprobecipsoVZVXv4VZVXreqVZVXdelattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=88730;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/cipso_v4_req_delattr")
int BPF_KRETPROBE(KtretprobecipsoVZVXv4VZVXreqVZVXdelattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=88730;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/cipso_v4_req_setattr")
int BPF_KPROBE(KtprobecipsoVZVXv4VZVXreqVZVXsetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=88732;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/cipso_v4_req_setattr")
int BPF_KRETPROBE(KtretprobecipsoVZVXv4VZVXreqVZVXsetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=88732;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/cipso_v4_skbuff_delattr")
int BPF_KPROBE(KtprobecipsoVZVXv4VZVXskbuffVZVXdelattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=88733;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/cipso_v4_skbuff_delattr")
int BPF_KRETPROBE(KtretprobecipsoVZVXv4VZVXskbuffVZVXdelattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=88733;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/cipso_v4_skbuff_setattr")
int BPF_KPROBE(KtprobecipsoVZVXv4VZVXskbuffVZVXsetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=88735;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/cipso_v4_skbuff_setattr")
int BPF_KRETPROBE(KtretprobecipsoVZVXv4VZVXskbuffVZVXsetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=88735;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/cipso_v4_sock_delattr")
int BPF_KPROBE(KtprobecipsoVZVXv4VZVXsockVZVXdelattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=88736;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/cipso_v4_sock_delattr")
int BPF_KRETPROBE(KtretprobecipsoVZVXv4VZVXsockVZVXdelattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=88736;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/cipso_v4_sock_getattr")
int BPF_KPROBE(KtprobecipsoVZVXv4VZVXsockVZVXgetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=88738;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/cipso_v4_sock_getattr")
int BPF_KRETPROBE(KtretprobecipsoVZVXv4VZVXsockVZVXgetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=88738;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/cipso_v4_sock_setattr")
int BPF_KPROBE(KtprobecipsoVZVXv4VZVXsockVZVXsetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=88740;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/cipso_v4_sock_setattr")
int BPF_KRETPROBE(KtretprobecipsoVZVXv4VZVXsockVZVXsetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=88740;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/cipso_v4_validate")
int BPF_KPROBE(KtprobecipsoVZVXv4VZVXvalidate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=88742;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/cipso_v4_validate")
int BPF_KRETPROBE(KtretprobecipsoVZVXv4VZVXvalidate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=88742;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/cn_rx_skb")
int BPF_KPROBE(KtprobecnVZVXrxVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=88999;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/cn_rx_skb")
int BPF_KRETPROBE(KtretprobecnVZVXrxVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=88999;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/compat_sock_ioctl_trans")
int BPF_KPROBE(KtprobecompatVZVXsockVZVXioctlVZVXtrans)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=89078;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/compat_sock_ioctl_trans")
int BPF_KRETPROBE(KtretprobecompatVZVXsockVZVXioctlVZVXtrans)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=89078;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/crypto_akcipher_report")
int BPF_KPROBE(KtprobecryptoVZVXakcipherVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=89544;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/crypto_akcipher_report")
int BPF_KRETPROBE(KtretprobecryptoVZVXakcipherVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=89544;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/crypto_skcipher_report")
int BPF_KPROBE(KtprobecryptoVZVXskcipherVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=89688;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/crypto_skcipher_report")
int BPF_KRETPROBE(KtretprobecryptoVZVXskcipherVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=89688;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/devlink_fmsg_dump_skb")
int BPF_KPROBE(KtprobedevlinkVZVXfmsgVZVXdumpVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=90331;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/devlink_fmsg_dump_skb")
int BPF_KRETPROBE(KtretprobedevlinkVZVXfmsgVZVXdumpVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=90331;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/devlink_fmsg_prepare_skb")
int BPF_KPROBE(KtprobedevlinkVZVXfmsgVZVXprepareVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=90339;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/devlink_fmsg_prepare_skb")
int BPF_KRETPROBE(KtretprobedevlinkVZVXfmsgVZVXprepareVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=90339;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/do_sock_getsockopt")
int BPF_KPROBE(KtprobedoVZVXsockVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=91011;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/do_sock_getsockopt")
int BPF_KRETPROBE(KtretprobedoVZVXsockVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=91011;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/do_sock_setsockopt")
int BPF_KPROBE(KtprobedoVZVXsockVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=91013;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/do_sock_setsockopt")
int BPF_KRETPROBE(KtretprobedoVZVXsockVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=91013;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/eafnosupport_ip6_del_rt")
int BPF_KPROBE(KtprobeeafnosupportVZVXip6VZVXdelVZVXrt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=91895;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/eafnosupport_ip6_del_rt")
int BPF_KRETPROBE(KtretprobeeafnosupportVZVXip6VZVXdelVZVXrt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=91895;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/eafnosupport_ip6_mtu_from_fib6")
int BPF_KPROBE(KtprobeeafnosupportVZVXip6VZVXmtuVZVXfromVZVXfib6)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=91897;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/eafnosupport_ip6_mtu_from_fib6")
int BPF_KRETPROBE(KtretprobeeafnosupportVZVXip6VZVXmtuVZVXfromVZVXfib6)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=91897;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/eafnosupport_ipv6_dev_find")
int BPF_KPROBE(KtprobeeafnosupportVZVXipv6VZVXdevVZVXfind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=91899;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/eafnosupport_ipv6_dev_find")
int BPF_KRETPROBE(KtretprobeeafnosupportVZVXipv6VZVXdevVZVXfind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=91899;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/eafnosupport_ipv6_dst_lookup_flow")
int BPF_KPROBE(KtprobeeafnosupportVZVXipv6VZVXdstVZVXlookupVZVXflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=91901;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/eafnosupport_ipv6_dst_lookup_flow")
int BPF_KRETPROBE(KtretprobeeafnosupportVZVXipv6VZVXdstVZVXlookupVZVXflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=91901;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/eafnosupport_ipv6_fragment")
int BPF_KPROBE(KtprobeeafnosupportVZVXipv6VZVXfragment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=91903;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/eafnosupport_ipv6_fragment")
int BPF_KRETPROBE(KtretprobeeafnosupportVZVXipv6VZVXfragment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=91903;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/eafnosupport_ipv6_route_input")
int BPF_KPROBE(KtprobeeafnosupportVZVXipv6VZVXrouteVZVXinput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=91904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/eafnosupport_ipv6_route_input")
int BPF_KRETPROBE(KtretprobeeafnosupportVZVXipv6VZVXrouteVZVXinput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=91904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/genl_sk_priv_get")
int BPF_KPROBE(KtprobegenlVZVXskVZVXprivVZVXget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=94347;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/genl_sk_priv_get")
int BPF_KRETPROBE(KtretprobegenlVZVXskVZVXprivVZVXget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=94347;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/handshake_sk_destruct")
int BPF_KPROBE(KtprobehandshakeVZVXskVZVXdestruct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=94740;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/handshake_sk_destruct")
int BPF_KRETPROBE(KtretprobehandshakeVZVXskVZVXdestruct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=94740;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmp6_dst_alloc")
int BPF_KPROBE(Ktprobeicmp6VZVXdstVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95169;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmp6_dst_alloc")
int BPF_KRETPROBE(Ktretprobeicmp6VZVXdstVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95169;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/igmp6_send")
int BPF_KPROBE(Ktprobeigmp6VZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95231;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/igmp6_send")
int BPF_KRETPROBE(Ktretprobeigmp6VZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95231;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/igmp_send_report")
int BPF_KPROBE(KtprobeigmpVZVXsendVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95256;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/igmp_send_report")
int BPF_KRETPROBE(KtretprobeigmpVZVXsendVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95256;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/igmpv3_send_cr")
int BPF_KPROBE(Ktprobeigmpv3VZVXsendVZVXcr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95268;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/igmpv3_send_cr")
int BPF_KRETPROBE(Ktretprobeigmpv3VZVXsendVZVXcr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95268;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/igmpv3_send_report")
int BPF_KPROBE(Ktprobeigmpv3VZVXsendVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95270;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/igmpv3_send_report")
int BPF_KRETPROBE(Ktretprobeigmpv3VZVXsendVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95270;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/igmpv3_sendpack")
int BPF_KPROBE(Ktprobeigmpv3VZVXsendpack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95271;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/igmpv3_sendpack")
int BPF_KRETPROBE(Ktretprobeigmpv3VZVXsendpack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95271;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_csk_accept")
int BPF_KPROBE(KtprobeinetVZVXcskVZVXaccept)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95379;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_csk_accept")
int BPF_KRETPROBE(KtretprobeinetVZVXcskVZVXaccept)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95379;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_csk_bind_conflict")
int BPF_KPROBE(KtprobeinetVZVXcskVZVXbindVZVXconflict)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95381;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_csk_bind_conflict")
int BPF_KRETPROBE(KtretprobeinetVZVXcskVZVXbindVZVXconflict)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95381;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_csk_clear_xmit_timers")
int BPF_KPROBE(KtprobeinetVZVXcskVZVXclearVZVXxmitVZVXtimers)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95382;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_csk_clear_xmit_timers")
int BPF_KRETPROBE(KtretprobeinetVZVXcskVZVXclearVZVXxmitVZVXtimers)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95382;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_csk_clear_xmit_timers_sync")
int BPF_KPROBE(KtprobeinetVZVXcskVZVXclearVZVXxmitVZVXtimersVZVXsync)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95383;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_csk_clear_xmit_timers_sync")
int BPF_KRETPROBE(KtretprobeinetVZVXcskVZVXclearVZVXxmitVZVXtimersVZVXsync)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95383;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_csk_clone_lock")
int BPF_KPROBE(KtprobeinetVZVXcskVZVXcloneVZVXlock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95385;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_csk_clone_lock")
int BPF_KRETPROBE(KtretprobeinetVZVXcskVZVXcloneVZVXlock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95385;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_csk_complete_hashdance")
int BPF_KPROBE(KtprobeinetVZVXcskVZVXcompleteVZVXhashdance)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95387;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_csk_complete_hashdance")
int BPF_KRETPROBE(KtretprobeinetVZVXcskVZVXcompleteVZVXhashdance)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95387;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_csk_destroy_sock")
int BPF_KPROBE(KtprobeinetVZVXcskVZVXdestroyVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95388;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_csk_destroy_sock")
int BPF_KRETPROBE(KtretprobeinetVZVXcskVZVXdestroyVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95388;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_csk_find_open_port")
int BPF_KPROBE(KtprobeinetVZVXcskVZVXfindVZVXopenVZVXport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95390;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_csk_find_open_port")
int BPF_KRETPROBE(KtretprobeinetVZVXcskVZVXfindVZVXopenVZVXport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95390;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_csk_get_port")
int BPF_KPROBE(KtprobeinetVZVXcskVZVXgetVZVXport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95391;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_csk_get_port")
int BPF_KRETPROBE(KtretprobeinetVZVXcskVZVXgetVZVXport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95391;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_csk_init_xmit_timers")
int BPF_KPROBE(KtprobeinetVZVXcskVZVXinitVZVXxmitVZVXtimers)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95393;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_csk_init_xmit_timers")
int BPF_KRETPROBE(KtretprobeinetVZVXcskVZVXinitVZVXxmitVZVXtimers)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95393;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_csk_listen_start")
int BPF_KPROBE(KtprobeinetVZVXcskVZVXlistenVZVXstart)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95394;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_csk_listen_start")
int BPF_KRETPROBE(KtretprobeinetVZVXcskVZVXlistenVZVXstart)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95394;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_csk_listen_stop")
int BPF_KPROBE(KtprobeinetVZVXcskVZVXlistenVZVXstop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95395;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_csk_listen_stop")
int BPF_KRETPROBE(KtretprobeinetVZVXcskVZVXlistenVZVXstop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95395;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_csk_prepare_forced_close")
int BPF_KPROBE(KtprobeinetVZVXcskVZVXprepareVZVXforcedVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95396;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_csk_prepare_forced_close")
int BPF_KRETPROBE(KtretprobeinetVZVXcskVZVXprepareVZVXforcedVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95396;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_csk_rebuild_route")
int BPF_KPROBE(KtprobeinetVZVXcskVZVXrebuildVZVXroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95398;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_csk_rebuild_route")
int BPF_KRETPROBE(KtretprobeinetVZVXcskVZVXrebuildVZVXroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95398;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_csk_reqsk_queue_add")
int BPF_KPROBE(KtprobeinetVZVXcskVZVXreqskVZVXqueueVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95400;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_csk_reqsk_queue_add")
int BPF_KRETPROBE(KtretprobeinetVZVXcskVZVXreqskVZVXqueueVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95400;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_csk_reqsk_queue_drop")
int BPF_KPROBE(KtprobeinetVZVXcskVZVXreqskVZVXqueueVZVXdrop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95402;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_csk_reqsk_queue_drop")
int BPF_KRETPROBE(KtretprobeinetVZVXcskVZVXreqskVZVXqueueVZVXdrop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95402;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_csk_reqsk_queue_drop_and_put")
int BPF_KPROBE(KtprobeinetVZVXcskVZVXreqskVZVXqueueVZVXdropVZVXandVZVXput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95403;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_csk_reqsk_queue_drop_and_put")
int BPF_KRETPROBE(KtretprobeinetVZVXcskVZVXreqskVZVXqueueVZVXdropVZVXandVZVXput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95403;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_csk_reqsk_queue_hash_add")
int BPF_KPROBE(KtprobeinetVZVXcskVZVXreqskVZVXqueueVZVXhashVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95405;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_csk_reqsk_queue_hash_add")
int BPF_KRETPROBE(KtretprobeinetVZVXcskVZVXreqskVZVXqueueVZVXhashVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95405;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_csk_route_child_sock")
int BPF_KPROBE(KtprobeinetVZVXcskVZVXrouteVZVXchildVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95407;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_csk_route_child_sock")
int BPF_KRETPROBE(KtretprobeinetVZVXcskVZVXrouteVZVXchildVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95407;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_csk_route_req")
int BPF_KPROBE(KtprobeinetVZVXcskVZVXrouteVZVXreq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95409;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_csk_route_req")
int BPF_KRETPROBE(KtretprobeinetVZVXcskVZVXrouteVZVXreq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95409;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_csk_update_fastreuse")
int BPF_KPROBE(KtprobeinetVZVXcskVZVXupdateVZVXfastreuse)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95411;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_csk_update_fastreuse")
int BPF_KRETPROBE(KtretprobeinetVZVXcskVZVXupdateVZVXfastreuse)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95411;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_csk_update_pmtu")
int BPF_KPROBE(KtprobeinetVZVXcskVZVXupdateVZVXpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95412;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_csk_update_pmtu")
int BPF_KRETPROBE(KtretprobeinetVZVXcskVZVXupdateVZVXpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95412;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_reqsk_alloc")
int BPF_KPROBE(KtprobeinetVZVXreqskVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95421;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_reqsk_alloc")
int BPF_KRETPROBE(KtretprobeinetVZVXreqskVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95421;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_reqsk_clone")
int BPF_KPROBE(KtprobeinetVZVXreqskVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95423;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_reqsk_clone")
int BPF_KRETPROBE(KtretprobeinetVZVXreqskVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95423;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_sk_get_local_port_range")
int BPF_KPROBE(KtprobeinetVZVXskVZVXgetVZVXlocalVZVXportVZVXrange)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95425;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_sk_get_local_port_range")
int BPF_KRETPROBE(KtretprobeinetVZVXskVZVXgetVZVXlocalVZVXportVZVXrange)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=95425;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/io_zcrx_recv")
int BPF_KPROBE(KtprobeioVZVXzcrxVZVXrecv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96180;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/io_zcrx_recv")
int BPF_KRETPROBE(KtretprobeioVZVXzcrxVZVXrecv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96180;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/io_zcrx_recv_skb")
int BPF_KPROBE(KtprobeioVZVXzcrxVZVXrecvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96182;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/io_zcrx_recv_skb")
int BPF_KRETPROBE(KtretprobeioVZVXzcrxVZVXrecvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96182;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_append_data")
int BPF_KPROBE(Ktprobeip6VZVXappendVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96334;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_append_data")
int BPF_KRETPROBE(Ktretprobeip6VZVXappendVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96334;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_autoflowlabel")
int BPF_KPROBE(Ktprobeip6VZVXautoflowlabel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96336;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_autoflowlabel")
int BPF_KRETPROBE(Ktretprobeip6VZVXautoflowlabel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96336;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_blackhole_route")
int BPF_KPROBE(Ktprobeip6VZVXblackholeVZVXroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96338;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_blackhole_route")
int BPF_KRETPROBE(Ktretprobeip6VZVXblackholeVZVXroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96338;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_copy_metadata")
int BPF_KPROBE(Ktprobeip6VZVXcopyVZVXmetadata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96344;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_copy_metadata")
int BPF_KRETPROBE(Ktretprobeip6VZVXcopyVZVXmetadata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96344;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_create_rt_rcu")
int BPF_KPROBE(Ktprobeip6VZVXcreateVZVXrtVZVXrcu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96348;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_create_rt_rcu")
int BPF_KRETPROBE(Ktretprobeip6VZVXcreateVZVXrtVZVXrcu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96348;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_datagram_connect")
int BPF_KPROBE(Ktprobeip6VZVXdatagramVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96349;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_datagram_connect")
int BPF_KRETPROBE(Ktretprobeip6VZVXdatagramVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96349;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_datagram_connect_v6_only")
int BPF_KPROBE(Ktprobeip6VZVXdatagramVZVXconnectVZVXv6VZVXonly)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96350;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_datagram_connect_v6_only")
int BPF_KRETPROBE(Ktretprobeip6VZVXdatagramVZVXconnectVZVXv6VZVXonly)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96350;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_datagram_dst_update")
int BPF_KPROBE(Ktprobeip6VZVXdatagramVZVXdstVZVXupdate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96352;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_datagram_dst_update")
int BPF_KRETPROBE(Ktretprobeip6VZVXdatagramVZVXdstVZVXupdate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96352;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_datagram_recv_common_ctl")
int BPF_KPROBE(Ktprobeip6VZVXdatagramVZVXrecvVZVXcommonVZVXctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96353;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_datagram_recv_common_ctl")
int BPF_KRETPROBE(Ktretprobeip6VZVXdatagramVZVXrecvVZVXcommonVZVXctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96353;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_datagram_recv_ctl")
int BPF_KPROBE(Ktprobeip6VZVXdatagramVZVXrecvVZVXctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96354;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_datagram_recv_ctl")
int BPF_KRETPROBE(Ktretprobeip6VZVXdatagramVZVXrecvVZVXctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96354;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_datagram_recv_specific_ctl")
int BPF_KPROBE(Ktprobeip6VZVXdatagramVZVXrecvVZVXspecificVZVXctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96355;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_datagram_recv_specific_ctl")
int BPF_KRETPROBE(Ktretprobeip6VZVXdatagramVZVXrecvVZVXspecificVZVXctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96355;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_datagram_release_cb")
int BPF_KPROBE(Ktprobeip6VZVXdatagramVZVXreleaseVZVXcb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96356;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_datagram_release_cb")
int BPF_KRETPROBE(Ktretprobeip6VZVXdatagramVZVXreleaseVZVXcb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96356;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_datagram_send_ctl")
int BPF_KPROBE(Ktprobeip6VZVXdatagramVZVXsendVZVXctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96358;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_datagram_send_ctl")
int BPF_KRETPROBE(Ktretprobeip6VZVXdatagramVZVXsendVZVXctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96358;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_del_cached_rt")
int BPF_KPROBE(Ktprobeip6VZVXdelVZVXcachedVZVXrt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96361;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_del_cached_rt")
int BPF_KRETPROBE(Ktretprobeip6VZVXdelVZVXcachedVZVXrt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96361;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_del_rt")
int BPF_KPROBE(Ktprobeip6VZVXdelVZVXrt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96362;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_del_rt")
int BPF_KRETPROBE(Ktretprobeip6VZVXdelVZVXrt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96362;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_dst_alloc")
int BPF_KPROBE(Ktprobeip6VZVXdstVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96364;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_dst_alloc")
int BPF_KRETPROBE(Ktretprobeip6VZVXdstVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96364;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_dst_ifdown")
int BPF_KPROBE(Ktprobeip6VZVXdstVZVXifdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96372;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_dst_ifdown")
int BPF_KRETPROBE(Ktretprobeip6VZVXdstVZVXifdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96372;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_dst_lookup")
int BPF_KPROBE(Ktprobeip6VZVXdstVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96374;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_dst_lookup")
int BPF_KRETPROBE(Ktretprobeip6VZVXdstVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96374;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_dst_lookup_flow")
int BPF_KPROBE(Ktprobeip6VZVXdstVZVXlookupVZVXflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96375;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_dst_lookup_flow")
int BPF_KRETPROBE(Ktretprobeip6VZVXdstVZVXlookupVZVXflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96375;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_dst_lookup_tail")
int BPF_KPROBE(Ktprobeip6VZVXdstVZVXlookupVZVXtail)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96377;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_dst_lookup_tail")
int BPF_KRETPROBE(Ktretprobeip6VZVXdstVZVXlookupVZVXtail)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96377;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_dst_neigh_lookup")
int BPF_KPROBE(Ktprobeip6VZVXdstVZVXneighVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96378;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_dst_neigh_lookup")
int BPF_KRETPROBE(Ktretprobeip6VZVXdstVZVXneighVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96378;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_find_1stfragopt")
int BPF_KPROBE(Ktprobeip6VZVXfindVZVX1stfragopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96380;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_find_1stfragopt")
int BPF_KRETPROBE(Ktretprobeip6VZVXfindVZVX1stfragopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96380;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_finish_output")
int BPF_KPROBE(Ktprobeip6VZVXfinishVZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96381;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_finish_output")
int BPF_KRETPROBE(Ktretprobeip6VZVXfinishVZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96381;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_finish_output2")
int BPF_KPROBE(Ktprobeip6VZVXfinishVZVXoutput2)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96382;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_finish_output2")
int BPF_KRETPROBE(Ktretprobeip6VZVXfinishVZVXoutput2)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96382;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_flush_pending_frames")
int BPF_KPROBE(Ktprobeip6VZVXflushVZVXpendingVZVXframes)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96383;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_flush_pending_frames")
int BPF_KRETPROBE(Ktretprobeip6VZVXflushVZVXpendingVZVXframes)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96383;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_forward")
int BPF_KPROBE(Ktprobeip6VZVXforward)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96384;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_forward")
int BPF_KRETPROBE(Ktretprobeip6VZVXforward)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96384;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_forward_finish")
int BPF_KPROBE(Ktprobeip6VZVXforwardVZVXfinish)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96385;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_forward_finish")
int BPF_KRETPROBE(Ktretprobeip6VZVXforwardVZVXfinish)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96385;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_frag_init")
int BPF_KPROBE(Ktprobeip6VZVXfragVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96387;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_frag_init")
int BPF_KRETPROBE(Ktretprobeip6VZVXfragVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96387;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_frag_next")
int BPF_KPROBE(Ktprobeip6VZVXfragVZVXnext)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96389;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_frag_next")
int BPF_KRETPROBE(Ktretprobeip6VZVXfragVZVXnext)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96389;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_fraglist_init")
int BPF_KPROBE(Ktprobeip6VZVXfraglistVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96391;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_fraglist_init")
int BPF_KRETPROBE(Ktretprobeip6VZVXfraglistVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96391;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_fraglist_prepare")
int BPF_KPROBE(Ktprobeip6VZVXfraglistVZVXprepare)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96393;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_fraglist_prepare")
int BPF_KRETPROBE(Ktretprobeip6VZVXfraglistVZVXprepare)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96393;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_fragment")
int BPF_KPROBE(Ktprobeip6VZVXfragment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96394;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_fragment")
int BPF_KRETPROBE(Ktretprobeip6VZVXfragment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96394;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_hold_safe")
int BPF_KPROBE(Ktprobeip6VZVXholdVZVXsafe)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96396;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_hold_safe")
int BPF_KRETPROBE(Ktretprobeip6VZVXholdVZVXsafe)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96396;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_ins_rt")
int BPF_KPROBE(Ktprobeip6VZVXinsVZVXrt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96398;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_ins_rt")
int BPF_KRETPROBE(Ktretprobeip6VZVXinsVZVXrt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96398;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_link_failure")
int BPF_KPROBE(Ktprobeip6VZVXlinkVZVXfailure)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96399;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_link_failure")
int BPF_KRETPROBE(Ktretprobeip6VZVXlinkVZVXfailure)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96399;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_local_out")
int BPF_KPROBE(Ktprobeip6VZVXlocalVZVXout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96400;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_local_out")
int BPF_KRETPROBE(Ktretprobeip6VZVXlocalVZVXout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96400;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_make_skb")
int BPF_KPROBE(Ktprobeip6VZVXmakeVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96402;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_make_skb")
int BPF_KRETPROBE(Ktretprobeip6VZVXmakeVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96402;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_mc_add_src")
int BPF_KPROBE(Ktprobeip6VZVXmcVZVXaddVZVXsrc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96404;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_mc_add_src")
int BPF_KRETPROBE(Ktretprobeip6VZVXmcVZVXaddVZVXsrc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96404;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_mc_del1_src")
int BPF_KPROBE(Ktprobeip6VZVXmcVZVXdel1VZVXsrc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96406;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_mc_del1_src")
int BPF_KRETPROBE(Ktretprobeip6VZVXmcVZVXdel1VZVXsrc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96406;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_mc_del_src")
int BPF_KPROBE(Ktprobeip6VZVXmcVZVXdelVZVXsrc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96407;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_mc_del_src")
int BPF_KRETPROBE(Ktretprobeip6VZVXmcVZVXdelVZVXsrc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96407;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_mc_find_dev")
int BPF_KPROBE(Ktprobeip6VZVXmcVZVXfindVZVXdev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96409;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_mc_find_dev")
int BPF_KRETPROBE(Ktretprobeip6VZVXmcVZVXfindVZVXdev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96409;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_mc_hdr")
int BPF_KPROBE(Ktprobeip6VZVXmcVZVXhdr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96411;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_mc_hdr")
int BPF_KRETPROBE(Ktretprobeip6VZVXmcVZVXhdr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96411;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_mc_leave_src")
int BPF_KPROBE(Ktprobeip6VZVXmcVZVXleaveVZVXsrc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96413;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_mc_leave_src")
int BPF_KRETPROBE(Ktretprobeip6VZVXmcVZVXleaveVZVXsrc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96413;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_mc_msfget")
int BPF_KPROBE(Ktprobeip6VZVXmcVZVXmsfget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96415;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_mc_msfget")
int BPF_KRETPROBE(Ktretprobeip6VZVXmcVZVXmsfget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96415;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_mc_msfilter")
int BPF_KPROBE(Ktprobeip6VZVXmcVZVXmsfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96417;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_mc_msfilter")
int BPF_KRETPROBE(Ktretprobeip6VZVXmcVZVXmsfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96417;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_mc_source")
int BPF_KPROBE(Ktprobeip6VZVXmcVZVXsource)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96419;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_mc_source")
int BPF_KRETPROBE(Ktretprobeip6VZVXmcVZVXsource)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96419;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_mtu_from_fib6")
int BPF_KPROBE(Ktprobeip6VZVXmtuVZVXfromVZVXfib6)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96421;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_mtu_from_fib6")
int BPF_KRETPROBE(Ktretprobeip6VZVXmtuVZVXfromVZVXfib6)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96421;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_multipath_l3_keys")
int BPF_KPROBE(Ktprobeip6VZVXmultipathVZVXl3VZVXkeys)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96423;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_multipath_l3_keys")
int BPF_KRETPROBE(Ktretprobeip6VZVXmultipathVZVXl3VZVXkeys)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96423;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_negative_advice")
int BPF_KPROBE(Ktprobeip6VZVXnegativeVZVXadvice)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96424;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_negative_advice")
int BPF_KRETPROBE(Ktretprobeip6VZVXnegativeVZVXadvice)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96424;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_neigh_lookup")
int BPF_KPROBE(Ktprobeip6VZVXneighVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96426;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_neigh_lookup")
int BPF_KRETPROBE(Ktretprobeip6VZVXneighVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96426;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_output")
int BPF_KPROBE(Ktprobeip6VZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96427;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_output")
int BPF_KRETPROBE(Ktretprobeip6VZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96427;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_pkt_discard")
int BPF_KPROBE(Ktprobeip6VZVXpktVZVXdiscard)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96428;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_pkt_discard")
int BPF_KRETPROBE(Ktretprobeip6VZVXpktVZVXdiscard)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96428;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_pkt_discard_out")
int BPF_KPROBE(Ktprobeip6VZVXpktVZVXdiscardVZVXout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96429;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_pkt_discard_out")
int BPF_KRETPROBE(Ktretprobeip6VZVXpktVZVXdiscardVZVXout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96429;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_pkt_drop")
int BPF_KPROBE(Ktprobeip6VZVXpktVZVXdrop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96431;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_pkt_drop")
int BPF_KRETPROBE(Ktretprobeip6VZVXpktVZVXdrop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96431;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_pkt_prohibit")
int BPF_KPROBE(Ktprobeip6VZVXpktVZVXprohibit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96432;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_pkt_prohibit")
int BPF_KRETPROBE(Ktretprobeip6VZVXpktVZVXprohibit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96432;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_pkt_prohibit_out")
int BPF_KPROBE(Ktprobeip6VZVXpktVZVXprohibitVZVXout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96433;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_pkt_prohibit_out")
int BPF_KRETPROBE(Ktretprobeip6VZVXpktVZVXprohibitVZVXout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96433;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_pol_route")
int BPF_KPROBE(Ktprobeip6VZVXpolVZVXroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96435;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_pol_route")
int BPF_KRETPROBE(Ktretprobeip6VZVXpolVZVXroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96435;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_pol_route_input")
int BPF_KPROBE(Ktprobeip6VZVXpolVZVXrouteVZVXinput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96436;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_pol_route_input")
int BPF_KRETPROBE(Ktretprobeip6VZVXpolVZVXrouteVZVXinput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96436;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_pol_route_lookup")
int BPF_KPROBE(Ktprobeip6VZVXpolVZVXrouteVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96437;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_pol_route_lookup")
int BPF_KRETPROBE(Ktretprobeip6VZVXpolVZVXrouteVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96437;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_pol_route_output")
int BPF_KPROBE(Ktprobeip6VZVXpolVZVXrouteVZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96438;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_pol_route_output")
int BPF_KRETPROBE(Ktretprobeip6VZVXpolVZVXrouteVZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96438;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_push_pending_frames")
int BPF_KPROBE(Ktprobeip6VZVXpushVZVXpendingVZVXframes)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96439;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_push_pending_frames")
int BPF_KRETPROBE(Ktretprobeip6VZVXpushVZVXpendingVZVXframes)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96439;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_redirect")
int BPF_KPROBE(Ktprobeip6VZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96441;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_redirect")
int BPF_KRETPROBE(Ktretprobeip6VZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96441;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_redirect_nh_match")
int BPF_KPROBE(Ktprobeip6VZVXredirectVZVXnhVZVXmatch)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96443;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_redirect_nh_match")
int BPF_KRETPROBE(Ktretprobeip6VZVXredirectVZVXnhVZVXmatch)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96443;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_redirect_no_header")
int BPF_KPROBE(Ktprobeip6VZVXredirectVZVXnoVZVXheader)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96445;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_redirect_no_header")
int BPF_KRETPROBE(Ktretprobeip6VZVXredirectVZVXnoVZVXheader)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96445;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_route_add")
int BPF_KPROBE(Ktprobeip6VZVXrouteVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96447;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_route_add")
int BPF_KRETPROBE(Ktretprobeip6VZVXrouteVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96447;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_route_check_nh")
int BPF_KPROBE(Ktprobeip6VZVXrouteVZVXcheckVZVXnh)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96449;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_route_check_nh")
int BPF_KRETPROBE(Ktretprobeip6VZVXrouteVZVXcheckVZVXnh)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96449;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_route_del")
int BPF_KPROBE(Ktprobeip6VZVXrouteVZVXdel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96451;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_route_del")
int BPF_KRETPROBE(Ktretprobeip6VZVXrouteVZVXdel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96451;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_route_info_append")
int BPF_KPROBE(Ktprobeip6VZVXrouteVZVXinfoVZVXappend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96454;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_route_info_append")
int BPF_KRETPROBE(Ktretprobeip6VZVXrouteVZVXinfoVZVXappend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96454;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_route_info_create")
int BPF_KPROBE(Ktprobeip6VZVXrouteVZVXinfoVZVXcreate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96456;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_route_info_create")
int BPF_KRETPROBE(Ktretprobeip6VZVXrouteVZVXinfoVZVXcreate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96456;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_route_info_create_nh")
int BPF_KPROBE(Ktprobeip6VZVXrouteVZVXinfoVZVXcreateVZVXnh)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96458;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_route_info_create_nh")
int BPF_KRETPROBE(Ktretprobeip6VZVXrouteVZVXinfoVZVXcreateVZVXnh)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96458;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_route_input")
int BPF_KPROBE(Ktprobeip6VZVXrouteVZVXinput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96461;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_route_input")
int BPF_KRETPROBE(Ktretprobeip6VZVXrouteVZVXinput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96461;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_route_input_lookup")
int BPF_KPROBE(Ktprobeip6VZVXrouteVZVXinputVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96463;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_route_input_lookup")
int BPF_KRETPROBE(Ktretprobeip6VZVXrouteVZVXinputVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96463;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_route_lookup")
int BPF_KPROBE(Ktprobeip6VZVXrouteVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96465;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_route_lookup")
int BPF_KRETPROBE(Ktretprobeip6VZVXrouteVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96465;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_route_mpath_notify")
int BPF_KPROBE(Ktprobeip6VZVXrouteVZVXmpathVZVXnotify)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96467;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_route_mpath_notify")
int BPF_KRETPROBE(Ktretprobeip6VZVXrouteVZVXmpathVZVXnotify)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96467;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_route_multipath_add")
int BPF_KPROBE(Ktprobeip6VZVXrouteVZVXmultipathVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96468;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_route_multipath_add")
int BPF_KRETPROBE(Ktretprobeip6VZVXrouteVZVXmultipathVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96468;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_route_multipath_del")
int BPF_KPROBE(Ktprobeip6VZVXrouteVZVXmultipathVZVXdel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96469;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_route_multipath_del")
int BPF_KRETPROBE(Ktretprobeip6VZVXrouteVZVXmultipathVZVXdel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96469;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_route_net_exit")
int BPF_KPROBE(Ktprobeip6VZVXrouteVZVXnetVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96470;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_route_net_exit")
int BPF_KRETPROBE(Ktretprobeip6VZVXrouteVZVXnetVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96470;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_route_net_exit_late")
int BPF_KPROBE(Ktprobeip6VZVXrouteVZVXnetVZVXexitVZVXlate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96471;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_route_net_exit_late")
int BPF_KRETPROBE(Ktretprobeip6VZVXrouteVZVXnetVZVXexitVZVXlate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96471;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_route_net_init")
int BPF_KPROBE(Ktprobeip6VZVXrouteVZVXnetVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96472;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_route_net_init")
int BPF_KRETPROBE(Ktretprobeip6VZVXrouteVZVXnetVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96472;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_route_net_init_late")
int BPF_KPROBE(Ktprobeip6VZVXrouteVZVXnetVZVXinitVZVXlate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96473;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_route_net_init_late")
int BPF_KRETPROBE(Ktretprobeip6VZVXrouteVZVXnetVZVXinitVZVXlate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96473;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_route_output_flags")
int BPF_KPROBE(Ktprobeip6VZVXrouteVZVXoutputVZVXflags)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96475;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_route_output_flags")
int BPF_KRETPROBE(Ktretprobeip6VZVXrouteVZVXoutputVZVXflags)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96475;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_route_redirect")
int BPF_KPROBE(Ktprobeip6VZVXrouteVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96477;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_route_redirect")
int BPF_KRETPROBE(Ktretprobeip6VZVXrouteVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96477;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_rt_cache_alloc")
int BPF_KPROBE(Ktprobeip6VZVXrtVZVXcacheVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96479;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_rt_cache_alloc")
int BPF_KRETPROBE(Ktretprobeip6VZVXrtVZVXcacheVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96479;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_rt_copy_init")
int BPF_KPROBE(Ktprobeip6VZVXrtVZVXcopyVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96481;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_rt_copy_init")
int BPF_KRETPROBE(Ktretprobeip6VZVXrtVZVXcopyVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96481;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_rt_get_dev_rcu")
int BPF_KPROBE(Ktprobeip6VZVXrtVZVXgetVZVXdevVZVXrcu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96483;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_rt_get_dev_rcu")
int BPF_KRETPROBE(Ktretprobeip6VZVXrtVZVXgetVZVXdevVZVXrcu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96483;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_rt_update_pmtu")
int BPF_KPROBE(Ktprobeip6VZVXrtVZVXupdateVZVXpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96484;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_rt_update_pmtu")
int BPF_KRETPROBE(Ktretprobeip6VZVXrtVZVXupdateVZVXpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96484;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_send_skb")
int BPF_KPROBE(Ktprobeip6VZVXsendVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96485;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_send_skb")
int BPF_KRETPROBE(Ktretprobeip6VZVXsendVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96485;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_setup_cork")
int BPF_KPROBE(Ktprobeip6VZVXsetupVZVXcork)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96487;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_setup_cork")
int BPF_KRETPROBE(Ktretprobeip6VZVXsetupVZVXcork)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96487;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_sk_dst_lookup_flow")
int BPF_KPROBE(Ktprobeip6VZVXskVZVXdstVZVXlookupVZVXflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96489;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_sk_dst_lookup_flow")
int BPF_KRETPROBE(Ktretprobeip6VZVXskVZVXdstVZVXlookupVZVXflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96489;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_sk_dst_store_flow")
int BPF_KPROBE(Ktprobeip6VZVXskVZVXdstVZVXstoreVZVXflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96491;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_sk_dst_store_flow")
int BPF_KRETPROBE(Ktretprobeip6VZVXskVZVXdstVZVXstoreVZVXflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96491;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_sk_redirect")
int BPF_KPROBE(Ktprobeip6VZVXskVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96492;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_sk_redirect")
int BPF_KRETPROBE(Ktretprobeip6VZVXskVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96492;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_sk_update_pmtu")
int BPF_KPROBE(Ktprobeip6VZVXskVZVXupdateVZVXpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96494;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_sk_update_pmtu")
int BPF_KRETPROBE(Ktretprobeip6VZVXskVZVXupdateVZVXpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96494;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_update_pmtu")
int BPF_KPROBE(Ktprobeip6VZVXupdateVZVXpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96498;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_update_pmtu")
int BPF_KRETPROBE(Ktretprobeip6VZVXupdateVZVXpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96498;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_validate_gw")
int BPF_KPROBE(Ktprobeip6VZVXvalidateVZVXgw)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96500;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_validate_gw")
int BPF_KRETPROBE(Ktretprobeip6VZVXvalidateVZVXgw)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96500;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_xmit")
int BPF_KPROBE(Ktprobeip6VZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96502;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_xmit")
int BPF_KRETPROBE(Ktretprobeip6VZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96502;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_append_data")
int BPF_KPROBE(KtprobeipVZVXappendVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96506;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_append_data")
int BPF_KRETPROBE(KtretprobeipVZVXappendVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96506;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_build_and_send_pkt")
int BPF_KPROBE(KtprobeipVZVXbuildVZVXandVZVXsendVZVXpkt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96508;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_build_and_send_pkt")
int BPF_KRETPROBE(KtretprobeipVZVXbuildVZVXandVZVXsendVZVXpkt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96508;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_call_ra_chain")
int BPF_KPROBE(KtprobeipVZVXcallVZVXraVZVXchain)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96509;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_call_ra_chain")
int BPF_KRETPROBE(KtretprobeipVZVXcallVZVXraVZVXchain)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96509;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_check_mc_rcu")
int BPF_KPROBE(KtprobeipVZVXcheckVZVXmcVZVXrcu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96511;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_check_mc_rcu")
int BPF_KRETPROBE(KtretprobeipVZVXcheckVZVXmcVZVXrcu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96511;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_copy_metadata")
int BPF_KPROBE(KtprobeipVZVXcopyVZVXmetadata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96512;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_copy_metadata")
int BPF_KRETPROBE(KtretprobeipVZVXcopyVZVXmetadata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96512;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_do_fragment")
int BPF_KPROBE(KtprobeipVZVXdoVZVXfragment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96513;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_do_fragment")
int BPF_KRETPROBE(KtretprobeipVZVXdoVZVXfragment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96513;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_encap")
int BPF_KPROBE(KtprobeipVZVXencap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96515;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_encap")
int BPF_KRETPROBE(KtretprobeipVZVXencap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96515;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_finish_output")
int BPF_KPROBE(KtprobeipVZVXfinishVZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96518;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_finish_output")
int BPF_KRETPROBE(KtretprobeipVZVXfinishVZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96518;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_finish_output2")
int BPF_KPROBE(KtprobeipVZVXfinishVZVXoutput2)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96519;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_finish_output2")
int BPF_KRETPROBE(KtretprobeipVZVXfinishVZVXoutput2)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96519;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_flush_pending_frames")
int BPF_KPROBE(KtprobeipVZVXflushVZVXpendingVZVXframes)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96520;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_flush_pending_frames")
int BPF_KRETPROBE(KtretprobeipVZVXflushVZVXpendingVZVXframes)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96520;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_frag_init")
int BPF_KPROBE(KtprobeipVZVXfragVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96522;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_frag_init")
int BPF_KRETPROBE(KtretprobeipVZVXfragVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96522;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_frag_next")
int BPF_KPROBE(KtprobeipVZVXfragVZVXnext)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96524;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_frag_next")
int BPF_KRETPROBE(KtretprobeipVZVXfragVZVXnext)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96524;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_fraglist_init")
int BPF_KPROBE(KtprobeipVZVXfraglistVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96526;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_fraglist_init")
int BPF_KRETPROBE(KtretprobeipVZVXfraglistVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96526;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_fraglist_prepare")
int BPF_KPROBE(KtprobeipVZVXfraglistVZVXprepare)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96528;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_fraglist_prepare")
int BPF_KRETPROBE(KtretprobeipVZVXfraglistVZVXprepare)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96528;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_fragment")
int BPF_KPROBE(KtprobeipVZVXfragment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96530;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_fragment")
int BPF_KRETPROBE(KtretprobeipVZVXfragment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96530;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_generic_getfrag")
int BPF_KPROBE(KtprobeipVZVXgenericVZVXgetfrag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96531;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_generic_getfrag")
int BPF_KRETPROBE(KtretprobeipVZVXgenericVZVXgetfrag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96531;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_local_deliver")
int BPF_KPROBE(KtprobeipVZVXlocalVZVXdeliver)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96537;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_local_deliver")
int BPF_KRETPROBE(KtretprobeipVZVXlocalVZVXdeliver)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96537;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_local_deliver_finish")
int BPF_KPROBE(KtprobeipVZVXlocalVZVXdeliverVZVXfinish)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96538;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_local_deliver_finish")
int BPF_KRETPROBE(KtretprobeipVZVXlocalVZVXdeliverVZVXfinish)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96538;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_local_out")
int BPF_KPROBE(KtprobeipVZVXlocalVZVXout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96539;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_local_out")
int BPF_KRETPROBE(KtretprobeipVZVXlocalVZVXout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96539;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_ma_put")
int BPF_KPROBE(KtprobeipVZVXmaVZVXput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96540;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_ma_put")
int BPF_KRETPROBE(KtretprobeipVZVXmaVZVXput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96540;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_make_skb")
int BPF_KPROBE(KtprobeipVZVXmakeVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96542;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_make_skb")
int BPF_KRETPROBE(KtretprobeipVZVXmakeVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96542;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_add_src")
int BPF_KPROBE(KtprobeipVZVXmcVZVXaddVZVXsrc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96544;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_add_src")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXaddVZVXsrc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96544;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_check_igmp")
int BPF_KPROBE(KtprobeipVZVXmcVZVXcheckVZVXigmp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96545;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_check_igmp")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXcheckVZVXigmp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96545;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_clear_src")
int BPF_KPROBE(KtprobeipVZVXmcVZVXclearVZVXsrc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96547;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_clear_src")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXclearVZVXsrc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96547;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_del1_src")
int BPF_KPROBE(KtprobeipVZVXmcVZVXdel1VZVXsrc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96549;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_del1_src")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXdel1VZVXsrc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96549;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_del_src")
int BPF_KPROBE(KtprobeipVZVXmcVZVXdelVZVXsrc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96550;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_del_src")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXdelVZVXsrc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96550;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_destroy_dev")
int BPF_KPROBE(KtprobeipVZVXmcVZVXdestroyVZVXdev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96551;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_destroy_dev")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXdestroyVZVXdev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96551;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_down")
int BPF_KPROBE(KtprobeipVZVXmcVZVXdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96552;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_down")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96552;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_drop_socket")
int BPF_KPROBE(KtprobeipVZVXmcVZVXdropVZVXsocket)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96553;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_drop_socket")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXdropVZVXsocket)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96553;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_find_dev")
int BPF_KPROBE(KtprobeipVZVXmcVZVXfindVZVXdev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96555;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_find_dev")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXfindVZVXdev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96555;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_finish_output")
int BPF_KPROBE(KtprobeipVZVXmcVZVXfinishVZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96556;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_finish_output")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXfinishVZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96556;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_gsfget")
int BPF_KPROBE(KtprobeipVZVXmcVZVXgsfget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96557;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_gsfget")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXgsfget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96557;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_inc_group")
int BPF_KPROBE(KtprobeipVZVXmcVZVXincVZVXgroup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96559;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_inc_group")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXincVZVXgroup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96559;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_init_dev")
int BPF_KPROBE(KtprobeipVZVXmcVZVXinitVZVXdev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96560;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_init_dev")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXinitVZVXdev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96560;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_join_group")
int BPF_KPROBE(KtprobeipVZVXmcVZVXjoinVZVXgroup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96562;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_join_group")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXjoinVZVXgroup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96562;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_join_group_ssm")
int BPF_KPROBE(KtprobeipVZVXmcVZVXjoinVZVXgroupVZVXssm)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96563;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_join_group_ssm")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXjoinVZVXgroupVZVXssm)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96563;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_leave_group")
int BPF_KPROBE(KtprobeipVZVXmcVZVXleaveVZVXgroup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96564;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_leave_group")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXleaveVZVXgroup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96564;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_leave_src")
int BPF_KPROBE(KtprobeipVZVXmcVZVXleaveVZVXsrc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96566;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_leave_src")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXleaveVZVXsrc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96566;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_msfget")
int BPF_KPROBE(KtprobeipVZVXmcVZVXmsfget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96568;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_msfget")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXmsfget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96568;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_msfilter")
int BPF_KPROBE(KtprobeipVZVXmcVZVXmsfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96570;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_msfilter")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXmsfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96570;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_output")
int BPF_KPROBE(KtprobeipVZVXmcVZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96571;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_output")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96571;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_remap")
int BPF_KPROBE(KtprobeipVZVXmcVZVXremap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96572;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_remap")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXremap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96572;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_sf_allow")
int BPF_KPROBE(KtprobeipVZVXmcVZVXsfVZVXallow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96574;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_sf_allow")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXsfVZVXallow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96574;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_source")
int BPF_KPROBE(KtprobeipVZVXmcVZVXsource)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96576;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_source")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXsource)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96576;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_unmap")
int BPF_KPROBE(KtprobeipVZVXmcVZVXunmap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96577;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_unmap")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXunmap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96577;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_up")
int BPF_KPROBE(KtprobeipVZVXmcVZVXup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96578;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_up")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96578;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_validate_checksum")
int BPF_KPROBE(KtprobeipVZVXmcVZVXvalidateVZVXchecksum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96580;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_validate_checksum")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXvalidateVZVXchecksum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96580;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mr_forward")
int BPF_KPROBE(KtprobeipVZVXmrVZVXforward)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96584;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mr_forward")
int BPF_KRETPROBE(KtretprobeipVZVXmrVZVXforward)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96584;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mr_input")
int BPF_KPROBE(KtprobeipVZVXmrVZVXinput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96586;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mr_input")
int BPF_KRETPROBE(KtretprobeipVZVXmrVZVXinput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96586;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mr_output")
int BPF_KPROBE(KtprobeipVZVXmrVZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96587;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mr_output")
int BPF_KRETPROBE(KtretprobeipVZVXmrVZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96587;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mroute_getsockopt")
int BPF_KPROBE(KtprobeipVZVXmrouteVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96589;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mroute_getsockopt")
int BPF_KRETPROBE(KtretprobeipVZVXmrouteVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96589;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mroute_setsockopt")
int BPF_KPROBE(KtprobeipVZVXmrouteVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96591;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mroute_setsockopt")
int BPF_KRETPROBE(KtretprobeipVZVXmrouteVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96591;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_output")
int BPF_KPROBE(KtprobeipVZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96592;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_output")
int BPF_KRETPROBE(KtretprobeipVZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96592;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_protocol_deliver_rcu")
int BPF_KPROBE(KtprobeipVZVXprotocolVZVXdeliverVZVXrcu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96594;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_protocol_deliver_rcu")
int BPF_KRETPROBE(KtretprobeipVZVXprotocolVZVXdeliverVZVXrcu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96594;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_push_pending_frames")
int BPF_KPROBE(KtprobeipVZVXpushVZVXpendingVZVXframes)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96596;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_push_pending_frames")
int BPF_KRETPROBE(KtretprobeipVZVXpushVZVXpendingVZVXframes)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96596;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_queue_xmit")
int BPF_KPROBE(KtprobeipVZVXqueueVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96598;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_queue_xmit")
int BPF_KRETPROBE(KtretprobeipVZVXqueueVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96598;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_rcv_finish")
int BPF_KPROBE(KtprobeipVZVXrcvVZVXfinish)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96602;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_rcv_finish")
int BPF_KRETPROBE(KtretprobeipVZVXrcvVZVXfinish)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96602;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_rcv_finish_core")
int BPF_KPROBE(KtprobeipVZVXrcvVZVXfinishVZVXcore)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96604;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_rcv_finish_core")
int BPF_KRETPROBE(KtretprobeipVZVXrcvVZVXfinishVZVXcore)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96604;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_rcv_options")
int BPF_KPROBE(KtprobeipVZVXrcvVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96606;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_rcv_options")
int BPF_KRETPROBE(KtretprobeipVZVXrcvVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96606;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_reply_glue_bits")
int BPF_KPROBE(KtprobeipVZVXreplyVZVXglueVZVXbits)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96608;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_reply_glue_bits")
int BPF_KRETPROBE(KtretprobeipVZVXreplyVZVXglueVZVXbits)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96608;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_send_skb")
int BPF_KPROBE(KtprobeipVZVXsendVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96612;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_send_skb")
int BPF_KRETPROBE(KtretprobeipVZVXsendVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96612;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_send_unicast_reply")
int BPF_KPROBE(KtprobeipVZVXsendVZVXunicastVZVXreply)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96614;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_send_unicast_reply")
int BPF_KRETPROBE(KtretprobeipVZVXsendVZVXunicastVZVXreply)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96614;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_setup_cork")
int BPF_KPROBE(KtprobeipVZVXsetupVZVXcork)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96616;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_setup_cork")
int BPF_KRETPROBE(KtretprobeipVZVXsetupVZVXcork)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96616;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_skb_dst_mtu")
int BPF_KPROBE(KtprobeipVZVXskbVZVXdstVZVXmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96618;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_skb_dst_mtu")
int BPF_KRETPROBE(KtretprobeipVZVXskbVZVXdstVZVXmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96618;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_sublist_rcv")
int BPF_KPROBE(KtprobeipVZVXsublistVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96620;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_sublist_rcv")
int BPF_KRETPROBE(KtretprobeipVZVXsublistVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96620;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_cache_report")
int BPF_KPROBE(KtprobeipmrVZVXcacheVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96684;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_cache_report")
int BPF_KRETPROBE(KtretprobeipmrVZVXcacheVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96684;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_cache_resolve")
int BPF_KPROBE(KtprobeipmrVZVXcacheVZVXresolve)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96686;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_cache_resolve")
int BPF_KRETPROBE(KtretprobeipmrVZVXcacheVZVXresolve)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96686;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_cache_unresolved")
int BPF_KPROBE(KtprobeipmrVZVXcacheVZVXunresolved)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96688;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_cache_unresolved")
int BPF_KRETPROBE(KtretprobeipmrVZVXcacheVZVXunresolved)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96688;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_compat_ioctl")
int BPF_KPROBE(KtprobeipmrVZVXcompatVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96689;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_compat_ioctl")
int BPF_KRETPROBE(KtretprobeipmrVZVXcompatVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96689;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_destroy_unres")
int BPF_KPROBE(KtprobeipmrVZVXdestroyVZVXunres)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96691;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_destroy_unres")
int BPF_KRETPROBE(KtretprobeipmrVZVXdestroyVZVXunres)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96691;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_dump")
int BPF_KPROBE(KtprobeipmrVZVXdump)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96693;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_dump")
int BPF_KRETPROBE(KtretprobeipmrVZVXdump)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96693;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_fib_lookup")
int BPF_KPROBE(KtprobeipmrVZVXfibVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96696;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_fib_lookup")
int BPF_KRETPROBE(KtretprobeipmrVZVXfibVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96696;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_fill_mroute")
int BPF_KPROBE(KtprobeipmrVZVXfillVZVXmroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96698;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_fill_mroute")
int BPF_KRETPROBE(KtretprobeipmrVZVXfillVZVXmroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96698;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_fill_table")
int BPF_KPROBE(KtprobeipmrVZVXfillVZVXtable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96700;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_fill_table")
int BPF_KRETPROBE(KtretprobeipmrVZVXfillVZVXtable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96700;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_fill_vif")
int BPF_KPROBE(KtprobeipmrVZVXfillVZVXvif)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96702;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_fill_vif")
int BPF_KRETPROBE(KtretprobeipmrVZVXfillVZVXvif)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96702;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_forward_finish")
int BPF_KPROBE(KtprobeipmrVZVXforwardVZVXfinish)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96703;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_forward_finish")
int BPF_KRETPROBE(KtretprobeipmrVZVXforwardVZVXfinish)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96703;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_free_table")
int BPF_KPROBE(KtprobeipmrVZVXfreeVZVXtable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96705;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_free_table")
int BPF_KRETPROBE(KtretprobeipmrVZVXfreeVZVXtable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96705;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_get_route")
int BPF_KPROBE(KtprobeipmrVZVXgetVZVXroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96707;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_get_route")
int BPF_KRETPROBE(KtretprobeipmrVZVXgetVZVXroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96707;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_init_vif_indev")
int BPF_KPROBE(KtprobeipmrVZVXinitVZVXvifVZVXindev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96710;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_init_vif_indev")
int BPF_KRETPROBE(KtretprobeipmrVZVXinitVZVXvifVZVXindev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96710;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_ioctl")
int BPF_KPROBE(KtprobeipmrVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96712;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_ioctl")
int BPF_KRETPROBE(KtretprobeipmrVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96712;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_mfc_add")
int BPF_KPROBE(KtprobeipmrVZVXmfcVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96714;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_mfc_add")
int BPF_KRETPROBE(KtretprobeipmrVZVXmfcVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96714;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_mfc_delete")
int BPF_KPROBE(KtprobeipmrVZVXmfcVZVXdelete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96716;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_mfc_delete")
int BPF_KRETPROBE(KtretprobeipmrVZVXmfcVZVXdelete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96716;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_mr_table_iter")
int BPF_KPROBE(KtprobeipmrVZVXmrVZVXtableVZVXiter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96720;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_mr_table_iter")
int BPF_KRETPROBE(KtretprobeipmrVZVXmrVZVXtableVZVXiter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96720;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_net_exit")
int BPF_KPROBE(KtprobeipmrVZVXnetVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96721;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_net_exit")
int BPF_KRETPROBE(KtretprobeipmrVZVXnetVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96721;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_net_init")
int BPF_KPROBE(KtprobeipmrVZVXnetVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96723;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_net_init")
int BPF_KRETPROBE(KtretprobeipmrVZVXnetVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96723;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_new_table")
int BPF_KPROBE(KtprobeipmrVZVXnewVZVXtable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96725;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_new_table")
int BPF_KRETPROBE(KtretprobeipmrVZVXnewVZVXtable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96725;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_new_table_set")
int BPF_KPROBE(KtprobeipmrVZVXnewVZVXtableVZVXset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96727;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_new_table_set")
int BPF_KRETPROBE(KtretprobeipmrVZVXnewVZVXtableVZVXset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96727;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_new_tunnel")
int BPF_KPROBE(KtprobeipmrVZVXnewVZVXtunnel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96729;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_new_tunnel")
int BPF_KRETPROBE(KtretprobeipmrVZVXnewVZVXtunnel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96729;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_prepare_xmit")
int BPF_KPROBE(KtprobeipmrVZVXprepareVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96731;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_prepare_xmit")
int BPF_KRETPROBE(KtretprobeipmrVZVXprepareVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96731;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_queue_fwd_xmit")
int BPF_KPROBE(KtprobeipmrVZVXqueueVZVXfwdVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96733;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_queue_fwd_xmit")
int BPF_KRETPROBE(KtretprobeipmrVZVXqueueVZVXfwdVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96733;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_rt_fib_lookup")
int BPF_KPROBE(KtprobeipmrVZVXrtVZVXfibVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96735;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_rt_fib_lookup")
int BPF_KRETPROBE(KtretprobeipmrVZVXrtVZVXfibVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96735;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_rtm_dumplink")
int BPF_KPROBE(KtprobeipmrVZVXrtmVZVXdumplink)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96736;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_rtm_dumplink")
int BPF_KRETPROBE(KtretprobeipmrVZVXrtmVZVXdumplink)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96736;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_rtm_dumproute")
int BPF_KPROBE(KtprobeipmrVZVXrtmVZVXdumproute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96737;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_rtm_dumproute")
int BPF_KRETPROBE(KtretprobeipmrVZVXrtmVZVXdumproute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96737;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_rtm_getroute")
int BPF_KPROBE(KtprobeipmrVZVXrtmVZVXgetroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96738;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_rtm_getroute")
int BPF_KRETPROBE(KtretprobeipmrVZVXrtmVZVXgetroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96738;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_rtm_route")
int BPF_KPROBE(KtprobeipmrVZVXrtmVZVXroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96739;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_rtm_route")
int BPF_KRETPROBE(KtretprobeipmrVZVXrtmVZVXroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96739;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_rtm_valid_getroute_req")
int BPF_KPROBE(KtprobeipmrVZVXrtmVZVXvalidVZVXgetrouteVZVXreq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96740;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_rtm_valid_getroute_req")
int BPF_KRETPROBE(KtretprobeipmrVZVXrtmVZVXvalidVZVXgetrouteVZVXreq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96740;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_rule_action")
int BPF_KPROBE(KtprobeipmrVZVXruleVZVXaction)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96742;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_rule_action")
int BPF_KRETPROBE(KtretprobeipmrVZVXruleVZVXaction)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96742;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_rule_compare")
int BPF_KPROBE(KtprobeipmrVZVXruleVZVXcompare)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96744;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_rule_compare")
int BPF_KRETPROBE(KtretprobeipmrVZVXruleVZVXcompare)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96744;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_rule_configure")
int BPF_KPROBE(KtprobeipmrVZVXruleVZVXconfigure)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96746;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_rule_configure")
int BPF_KRETPROBE(KtretprobeipmrVZVXruleVZVXconfigure)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96746;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_rule_default")
int BPF_KPROBE(KtprobeipmrVZVXruleVZVXdefault)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96748;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_rule_default")
int BPF_KRETPROBE(KtretprobeipmrVZVXruleVZVXdefault)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96748;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_rule_fill")
int BPF_KPROBE(KtprobeipmrVZVXruleVZVXfill)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96750;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_rule_fill")
int BPF_KRETPROBE(KtretprobeipmrVZVXruleVZVXfill)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96750;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_rule_match")
int BPF_KPROBE(KtprobeipmrVZVXruleVZVXmatch)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96752;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_rule_match")
int BPF_KRETPROBE(KtretprobeipmrVZVXruleVZVXmatch)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96752;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_rules_dump")
int BPF_KPROBE(KtprobeipmrVZVXrulesVZVXdump)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96753;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_rules_dump")
int BPF_KRETPROBE(KtretprobeipmrVZVXrulesVZVXdump)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96753;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_rules_exit")
int BPF_KPROBE(KtprobeipmrVZVXrulesVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96754;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_rules_exit")
int BPF_KRETPROBE(KtretprobeipmrVZVXrulesVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96754;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_seq_read")
int BPF_KPROBE(KtprobeipmrVZVXseqVZVXread)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96755;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_seq_read")
int BPF_KRETPROBE(KtretprobeipmrVZVXseqVZVXread)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96755;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_sk_ioctl")
int BPF_KPROBE(KtprobeipmrVZVXskVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96756;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_sk_ioctl")
int BPF_KRETPROBE(KtretprobeipmrVZVXskVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96756;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_update_thresholds")
int BPF_KPROBE(KtprobeipmrVZVXupdateVZVXthresholds)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96758;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_update_thresholds")
int BPF_KRETPROBE(KtretprobeipmrVZVXupdateVZVXthresholds)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96758;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv4_skb_to_auditdata")
int BPF_KPROBE(Ktprobeipv4VZVXskbVZVXtoVZVXauditdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96763;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv4_skb_to_auditdata")
int BPF_KRETPROBE(Ktretprobeipv4VZVXskbVZVXtoVZVXauditdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96763;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_chk_mcast_addr")
int BPF_KPROBE(Ktprobeipv6VZVXchkVZVXmcastVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96767;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_chk_mcast_addr")
int BPF_KRETPROBE(Ktretprobeipv6VZVXchkVZVXmcastVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96767;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_dev_mc_dec")
int BPF_KPROBE(Ktprobeipv6VZVXdevVZVXmcVZVXdec)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96768;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_dev_mc_dec")
int BPF_KRETPROBE(Ktretprobeipv6VZVXdevVZVXmcVZVXdec)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96768;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_dev_mc_inc")
int BPF_KPROBE(Ktprobeipv6VZVXdevVZVXmcVZVXinc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96769;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_dev_mc_inc")
int BPF_KRETPROBE(Ktretprobeipv6VZVXdevVZVXmcVZVXinc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96769;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_icmp_error")
int BPF_KPROBE(Ktprobeipv6VZVXicmpVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96771;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_icmp_error")
int BPF_KRETPROBE(Ktretprobeipv6VZVXicmpVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96771;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_inetpeer_exit")
int BPF_KPROBE(Ktprobeipv6VZVXinetpeerVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96772;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_inetpeer_exit")
int BPF_KRETPROBE(Ktretprobeipv6VZVXinetpeerVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96772;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_inetpeer_init")
int BPF_KPROBE(Ktprobeipv6VZVXinetpeerVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96773;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_inetpeer_init")
int BPF_KRETPROBE(Ktretprobeipv6VZVXinetpeerVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96773;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_local_error")
int BPF_KPROBE(Ktprobeipv6VZVXlocalVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96775;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_local_error")
int BPF_KRETPROBE(Ktretprobeipv6VZVXlocalVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96775;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_local_rxpmtu")
int BPF_KPROBE(Ktprobeipv6VZVXlocalVZVXrxpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96777;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_local_rxpmtu")
int BPF_KRETPROBE(Ktretprobeipv6VZVXlocalVZVXrxpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96777;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_mc_dad_complete")
int BPF_KPROBE(Ktprobeipv6VZVXmcVZVXdadVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96778;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_mc_dad_complete")
int BPF_KRETPROBE(Ktretprobeipv6VZVXmcVZVXdadVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96778;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_mc_destroy_dev")
int BPF_KPROBE(Ktprobeipv6VZVXmcVZVXdestroyVZVXdev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96779;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_mc_destroy_dev")
int BPF_KRETPROBE(Ktretprobeipv6VZVXmcVZVXdestroyVZVXdev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96779;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_mc_down")
int BPF_KPROBE(Ktprobeipv6VZVXmcVZVXdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96780;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_mc_down")
int BPF_KRETPROBE(Ktretprobeipv6VZVXmcVZVXdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96780;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_mc_init_dev")
int BPF_KPROBE(Ktprobeipv6VZVXmcVZVXinitVZVXdev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96781;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_mc_init_dev")
int BPF_KRETPROBE(Ktretprobeipv6VZVXmcVZVXinitVZVXdev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96781;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_mc_remap")
int BPF_KPROBE(Ktprobeipv6VZVXmcVZVXremap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96783;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_mc_remap")
int BPF_KRETPROBE(Ktretprobeipv6VZVXmcVZVXremap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96783;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_mc_unmap")
int BPF_KPROBE(Ktprobeipv6VZVXmcVZVXunmap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96784;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_mc_unmap")
int BPF_KRETPROBE(Ktretprobeipv6VZVXmcVZVXunmap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96784;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_mc_up")
int BPF_KPROBE(Ktprobeipv6VZVXmcVZVXup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96785;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_mc_up")
int BPF_KRETPROBE(Ktretprobeipv6VZVXmcVZVXup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96785;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_proc_exit_net")
int BPF_KPROBE(Ktprobeipv6VZVXprocVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96788;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_proc_exit_net")
int BPF_KRETPROBE(Ktretprobeipv6VZVXprocVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96788;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_proc_init_net")
int BPF_KPROBE(Ktprobeipv6VZVXprocVZVXinitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96789;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_proc_init_net")
int BPF_KRETPROBE(Ktretprobeipv6VZVXprocVZVXinitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96789;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_proxy_select_ident")
int BPF_KPROBE(Ktprobeipv6VZVXproxyVZVXselectVZVXident)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96791;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_proxy_select_ident")
int BPF_KRETPROBE(Ktretprobeipv6VZVXproxyVZVXselectVZVXident)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96791;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_recv_error")
int BPF_KPROBE(Ktprobeipv6VZVXrecvVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96794;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_recv_error")
int BPF_KRETPROBE(Ktretprobeipv6VZVXrecvVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96794;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_recv_rxpmtu")
int BPF_KPROBE(Ktprobeipv6VZVXrecvVZVXrxpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96795;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_recv_rxpmtu")
int BPF_KRETPROBE(Ktretprobeipv6VZVXrecvVZVXrxpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96795;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_route_ioctl")
int BPF_KPROBE(Ktprobeipv6VZVXrouteVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96797;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_route_ioctl")
int BPF_KRETPROBE(Ktretprobeipv6VZVXrouteVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96797;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_route_sysctl_init")
int BPF_KPROBE(Ktprobeipv6VZVXrouteVZVXsysctlVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96798;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_route_sysctl_init")
int BPF_KRETPROBE(Ktretprobeipv6VZVXrouteVZVXsysctlVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96798;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_route_sysctl_table_size")
int BPF_KPROBE(Ktprobeipv6VZVXrouteVZVXsysctlVZVXtableVZVXsize)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96800;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_route_sysctl_table_size")
int BPF_KRETPROBE(Ktretprobeipv6VZVXrouteVZVXsysctlVZVXtableVZVXsize)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96800;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_select_ident")
int BPF_KPROBE(Ktprobeipv6VZVXselectVZVXident)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96802;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_select_ident")
int BPF_KRETPROBE(Ktretprobeipv6VZVXselectVZVXident)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96802;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_skb_to_auditdata")
int BPF_KPROBE(Ktprobeipv6VZVXskbVZVXtoVZVXauditdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96803;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_skb_to_auditdata")
int BPF_KRETPROBE(Ktretprobeipv6VZVXskbVZVXtoVZVXauditdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96803;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_sock_mc_close")
int BPF_KPROBE(Ktprobeipv6VZVXsockVZVXmcVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96804;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_sock_mc_close")
int BPF_KRETPROBE(Ktretprobeipv6VZVXsockVZVXmcVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96804;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_sock_mc_drop")
int BPF_KPROBE(Ktprobeipv6VZVXsockVZVXmcVZVXdrop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96805;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_sock_mc_drop")
int BPF_KRETPROBE(Ktretprobeipv6VZVXsockVZVXmcVZVXdrop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96805;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_sock_mc_join")
int BPF_KPROBE(Ktprobeipv6VZVXsockVZVXmcVZVXjoin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96806;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_sock_mc_join")
int BPF_KRETPROBE(Ktretprobeipv6VZVXsockVZVXmcVZVXjoin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96806;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_sock_mc_join_ssm")
int BPF_KPROBE(Ktprobeipv6VZVXsockVZVXmcVZVXjoinVZVXssm)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96807;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_sock_mc_join_ssm")
int BPF_KRETPROBE(Ktretprobeipv6VZVXsockVZVXmcVZVXjoinVZVXssm)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96807;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_sysctl_net_exit")
int BPF_KPROBE(Ktprobeipv6VZVXsysctlVZVXnetVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96808;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_sysctl_net_exit")
int BPF_KRETPROBE(Ktretprobeipv6VZVXsysctlVZVXnetVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96808;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_sysctl_net_init")
int BPF_KPROBE(Ktprobeipv6VZVXsysctlVZVXnetVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96809;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_sysctl_net_init")
int BPF_KRETPROBE(Ktretprobeipv6VZVXsysctlVZVXnetVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=96809;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/kernel_getsockname")
int BPF_KPROBE(KtprobekernelVZVXgetsockname)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=97249;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/kernel_getsockname")
int BPF_KRETPROBE(KtretprobekernelVZVXgetsockname)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=97249;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/kernel_recvmsg")
int BPF_KPROBE(KtprobekernelVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=97257;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/kernel_recvmsg")
int BPF_KRETPROBE(KtretprobekernelVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=97257;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/kernel_sendmsg")
int BPF_KPROBE(KtprobekernelVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=97259;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/kernel_sendmsg")
int BPF_KRETPROBE(KtretprobekernelVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=97259;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/kernel_sock_ip_overhead")
int BPF_KPROBE(KtprobekernelVZVXsockVZVXipVZVXoverhead)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=97264;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/kernel_sock_ip_overhead")
int BPF_KRETPROBE(KtretprobekernelVZVXsockVZVXipVZVXoverhead)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=97264;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/kernel_sock_shutdown")
int BPF_KPROBE(KtprobekernelVZVXsockVZVXshutdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=97266;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/kernel_sock_shutdown")
int BPF_KRETPROBE(KtretprobekernelVZVXsockVZVXshutdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=97266;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/lwtunnel_xmit")
int BPF_KPROBE(KtprobelwtunnelVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=97912;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/lwtunnel_xmit")
int BPF_KRETPROBE(KtretprobelwtunnelVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=97912;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mld_send_cr")
int BPF_KPROBE(KtprobemldVZVXsendVZVXcr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98411;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mld_send_cr")
int BPF_KRETPROBE(KtretprobemldVZVXsendVZVXcr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98411;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mld_send_initial_cr")
int BPF_KPROBE(KtprobemldVZVXsendVZVXinitialVZVXcr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98412;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mld_send_initial_cr")
int BPF_KRETPROBE(KtretprobemldVZVXsendVZVXinitialVZVXcr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98412;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mld_send_report")
int BPF_KPROBE(KtprobemldVZVXsendVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98414;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mld_send_report")
int BPF_KRETPROBE(KtretprobemldVZVXsendVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98414;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mld_sendpack")
int BPF_KPROBE(KtprobemldVZVXsendpack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98415;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mld_sendpack")
int BPF_KRETPROBE(KtretprobemldVZVXsendpack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98415;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_mib_alloc")
int BPF_KPROBE(KtprobemptcpVZVXmibVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98671;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_mib_alloc")
int BPF_KRETPROBE(KtretprobemptcpVZVXmibVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98671;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_nl_announce_doit")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXnlVZVXannounceVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98672;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_nl_announce_doit")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXnlVZVXannounceVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98672;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_nl_remove_doit")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXnlVZVXremoveVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98673;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_nl_remove_doit")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXnlVZVXremoveVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98673;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_nl_subflow_create_doit")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXnlVZVXsubflowVZVXcreateVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98674;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_nl_subflow_create_doit")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXnlVZVXsubflowVZVXcreateVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98674;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_nl_subflow_destroy_doit")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXnlVZVXsubflowVZVXdestroyVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98675;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_nl_subflow_destroy_doit")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXnlVZVXsubflowVZVXdestroyVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98675;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_remove_addr_entry")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXremoveVZVXaddrVZVXentry)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98677;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_remove_addr_entry")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXremoveVZVXaddrVZVXentry)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98677;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_token_accept")
int BPF_KPROBE(KtprobemptcpVZVXtokenVZVXaccept)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98682;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_token_accept")
int BPF_KRETPROBE(KtretprobemptcpVZVXtokenVZVXaccept)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98682;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_token_destroy")
int BPF_KPROBE(KtprobemptcpVZVXtokenVZVXdestroy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98684;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_token_destroy")
int BPF_KRETPROBE(KtretprobemptcpVZVXtokenVZVXdestroy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98684;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_token_destroy_request")
int BPF_KPROBE(KtprobemptcpVZVXtokenVZVXdestroyVZVXrequest)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98685;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_token_destroy_request")
int BPF_KRETPROBE(KtretprobemptcpVZVXtokenVZVXdestroyVZVXrequest)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98685;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_token_get_sock")
int BPF_KPROBE(KtprobemptcpVZVXtokenVZVXgetVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98689;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_token_get_sock")
int BPF_KRETPROBE(KtretprobemptcpVZVXtokenVZVXgetVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98689;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_token_iter_next")
int BPF_KPROBE(KtprobemptcpVZVXtokenVZVXiterVZVXnext)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98692;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_token_iter_next")
int BPF_KRETPROBE(KtretprobemptcpVZVXtokenVZVXiterVZVXnext)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98692;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_token_new_connect")
int BPF_KPROBE(KtprobemptcpVZVXtokenVZVXnewVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98694;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_token_new_connect")
int BPF_KRETPROBE(KtretprobemptcpVZVXtokenVZVXnewVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98694;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_token_new_request")
int BPF_KPROBE(KtprobemptcpVZVXtokenVZVXnewVZVXrequest)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98696;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_token_new_request")
int BPF_KRETPROBE(KtretprobemptcpVZVXtokenVZVXnewVZVXrequest)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98696;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_userspace_pm_append_new_local_addr")
int BPF_KPROBE(KtprobemptcpVZVXuserspaceVZVXpmVZVXappendVZVXnewVZVXlocalVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98698;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_userspace_pm_append_new_local_addr")
int BPF_KRETPROBE(KtretprobemptcpVZVXuserspaceVZVXpmVZVXappendVZVXnewVZVXlocalVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98698;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_userspace_pm_delete_local_addr")
int BPF_KPROBE(KtprobemptcpVZVXuserspaceVZVXpmVZVXdeleteVZVXlocalVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98700;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_userspace_pm_delete_local_addr")
int BPF_KRETPROBE(KtretprobemptcpVZVXuserspaceVZVXpmVZVXdeleteVZVXlocalVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98700;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_userspace_pm_dump_addr")
int BPF_KPROBE(KtprobemptcpVZVXuserspaceVZVXpmVZVXdumpVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98701;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_userspace_pm_dump_addr")
int BPF_KRETPROBE(KtretprobemptcpVZVXuserspaceVZVXpmVZVXdumpVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98701;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_userspace_pm_free_local_addr_list")
int BPF_KPROBE(KtprobemptcpVZVXuserspaceVZVXpmVZVXfreeVZVXlocalVZVXaddrVZVXlist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98702;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_userspace_pm_free_local_addr_list")
int BPF_KRETPROBE(KtretprobemptcpVZVXuserspaceVZVXpmVZVXfreeVZVXlocalVZVXaddrVZVXlist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98702;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_userspace_pm_get_addr")
int BPF_KPROBE(KtprobemptcpVZVXuserspaceVZVXpmVZVXgetVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98704;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_userspace_pm_get_addr")
int BPF_KRETPROBE(KtretprobemptcpVZVXuserspaceVZVXpmVZVXgetVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98704;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_userspace_pm_get_local_id")
int BPF_KPROBE(KtprobemptcpVZVXuserspaceVZVXpmVZVXgetVZVXlocalVZVXid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98706;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_userspace_pm_get_local_id")
int BPF_KRETPROBE(KtretprobemptcpVZVXuserspaceVZVXpmVZVXgetVZVXlocalVZVXid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98706;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_userspace_pm_is_backup")
int BPF_KPROBE(KtprobemptcpVZVXuserspaceVZVXpmVZVXisVZVXbackup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98710;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_userspace_pm_is_backup")
int BPF_KRETPROBE(KtretprobemptcpVZVXuserspaceVZVXpmVZVXisVZVXbackup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98710;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_userspace_pm_set_flags")
int BPF_KPROBE(KtprobemptcpVZVXuserspaceVZVXpmVZVXsetVZVXflags)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98712;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_userspace_pm_set_flags")
int BPF_KRETPROBE(KtretprobemptcpVZVXuserspaceVZVXpmVZVXsetVZVXflags)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98712;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mrtsock_destruct")
int BPF_KPROBE(KtprobemrtsockVZVXdestruct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98762;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mrtsock_destruct")
int BPF_KRETPROBE(KtretprobemrtsockVZVXdestruct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=98762;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/neigh_xmit")
int BPF_KPROBE(KtprobeneighVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99047;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/neigh_xmit")
int BPF_KRETPROBE(KtretprobeneighVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99047;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/net_ns_get_ownership")
int BPF_KPROBE(KtprobenetVZVXnsVZVXgetVZVXownership)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99136;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/net_ns_get_ownership")
int BPF_KRETPROBE(KtretprobenetVZVXnsVZVXgetVZVXownership)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99136;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netkit_xmit")
int BPF_KPROBE(KtprobenetkitVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99194;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netkit_xmit")
int BPF_KRETPROBE(KtretprobenetkitVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99194;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netpoll_send_skb")
int BPF_KPROBE(KtprobenetpollVZVXsendVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99268;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netpoll_send_skb")
int BPF_KRETPROBE(KtretprobenetpollVZVXsendVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99268;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netpoll_send_udp")
int BPF_KPROBE(KtprobenetpollVZVXsendVZVXudp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99270;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netpoll_send_udp")
int BPF_KRETPROBE(KtretprobenetpollVZVXsendVZVXudp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99270;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netpoll_start_xmit")
int BPF_KPROBE(KtprobenetpollVZVXstartVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99274;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netpoll_start_xmit")
int BPF_KRETPROBE(KtretprobenetpollVZVXstartVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99274;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/nh_create_ipv4")
int BPF_KPROBE(KtprobenhVZVXcreateVZVXipv4)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99347;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/nh_create_ipv4")
int BPF_KRETPROBE(KtretprobenhVZVXcreateVZVXipv4)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99347;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/nh_create_ipv6")
int BPF_KPROBE(KtprobenhVZVXcreateVZVXipv6)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99348;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/nh_create_ipv6")
int BPF_KRETPROBE(KtretprobenhVZVXcreateVZVXipv6)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99348;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/packet_getsockopt")
int BPF_KPROBE(KtprobepacketVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99747;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/packet_getsockopt")
int BPF_KRETPROBE(KtretprobepacketVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99747;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/packet_recvmsg")
int BPF_KPROBE(KtprobepacketVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99767;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/packet_recvmsg")
int BPF_KRETPROBE(KtretprobepacketVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99767;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/packet_sendmsg")
int BPF_KPROBE(KtprobepacketVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99769;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/packet_sendmsg")
int BPF_KRETPROBE(KtretprobepacketVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99769;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/packet_sendmsg_spkt")
int BPF_KPROBE(KtprobepacketVZVXsendmsgVZVXspkt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99770;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/packet_sendmsg_spkt")
int BPF_KRETPROBE(KtretprobepacketVZVXsendmsgVZVXspkt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99770;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/packet_setsockopt")
int BPF_KPROBE(KtprobepacketVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99777;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/packet_setsockopt")
int BPF_KRETPROBE(KtretprobepacketVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99777;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/packet_sock_destruct")
int BPF_KPROBE(KtprobepacketVZVXsockVZVXdestruct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99779;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/packet_sock_destruct")
int BPF_KRETPROBE(KtretprobepacketVZVXsockVZVXdestruct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99779;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/packet_sock_flag")
int BPF_KPROBE(KtprobepacketVZVXsockVZVXflag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99781;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/packet_sock_flag")
int BPF_KRETPROBE(KtretprobepacketVZVXsockVZVXflag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99781;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/packet_xmit")
int BPF_KPROBE(KtprobepacketVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99783;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/packet_xmit")
int BPF_KRETPROBE(KtretprobepacketVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=99783;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ping_init_sock")
int BPF_KPROBE(KtprobepingVZVXinitVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=101015;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ping_init_sock")
int BPF_KRETPROBE(KtretprobepingVZVXinitVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=101015;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ping_queue_rcv_skb")
int BPF_KPROBE(KtprobepingVZVXqueueVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=101021;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ping_queue_rcv_skb")
int BPF_KRETPROBE(KtretprobepingVZVXqueueVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=101021;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ping_recvmsg")
int BPF_KPROBE(KtprobepingVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=101023;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ping_recvmsg")
int BPF_KRETPROBE(KtretprobepingVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=101023;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ping_v4_sendmsg")
int BPF_KPROBE(KtprobepingVZVXv4VZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=101031;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ping_v4_sendmsg")
int BPF_KRETPROBE(KtretprobepingVZVXv4VZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=101031;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/raw_getsockopt")
int BPF_KPROBE(KtproberawVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=101987;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/raw_getsockopt")
int BPF_KRETPROBE(KtretproberawVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=101987;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/raw_hash_sk")
int BPF_KPROBE(KtproberawVZVXhashVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=101988;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/raw_hash_sk")
int BPF_KRETPROBE(KtretproberawVZVXhashVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=101988;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/raw_icmp_error")
int BPF_KPROBE(KtproberawVZVXicmpVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=101990;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/raw_icmp_error")
int BPF_KRETPROBE(KtretproberawVZVXicmpVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=101990;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/raw_rcv_skb")
int BPF_KPROBE(KtproberawVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=101999;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/raw_rcv_skb")
int BPF_KRETPROBE(KtretproberawVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=101999;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/raw_recvmsg")
int BPF_KPROBE(KtproberawVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=102000;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/raw_recvmsg")
int BPF_KRETPROBE(KtretproberawVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=102000;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/raw_send_hdrinc")
int BPF_KPROBE(KtproberawVZVXsendVZVXhdrinc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=102002;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/raw_send_hdrinc")
int BPF_KRETPROBE(KtretproberawVZVXsendVZVXhdrinc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=102002;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/raw_seticmpfilter")
int BPF_KPROBE(KtproberawVZVXseticmpfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=102009;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/raw_seticmpfilter")
int BPF_KRETPROBE(KtretproberawVZVXseticmpfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=102009;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/raw_setsockopt")
int BPF_KPROBE(KtproberawVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=102010;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/raw_setsockopt")
int BPF_KRETPROBE(KtretproberawVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=102010;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/raw_sk_init")
int BPF_KPROBE(KtproberawVZVXskVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=102011;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/raw_sk_init")
int BPF_KRETPROBE(KtretproberawVZVXskVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=102011;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/raw_unhash_sk")
int BPF_KPROBE(KtproberawVZVXunhashVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=102013;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/raw_unhash_sk")
int BPF_KRETPROBE(KtretproberawVZVXunhashVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=102013;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/reg_vif_xmit")
int BPF_KPROBE(KtproberegVZVXvifVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=102273;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/reg_vif_xmit")
int BPF_KRETPROBE(KtretproberegVZVXvifVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=102273;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/rt6_disable_ip")
int BPF_KPROBE(Ktprobert6VZVXdisableVZVXip)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=102931;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/rt6_disable_ip")
int BPF_KRETPROBE(Ktretprobert6VZVXdisableVZVXip)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=102931;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/rt6_multipath_hash")
int BPF_KPROBE(Ktprobert6VZVXmultipathVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=102956;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/rt6_multipath_hash")
int BPF_KRETPROBE(Ktretprobert6VZVXmultipathVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=102956;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/rt6_multipath_rebalance")
int BPF_KPROBE(Ktprobert6VZVXmultipathVZVXrebalance)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=102958;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/rt6_multipath_rebalance")
int BPF_KRETPROBE(Ktretprobert6VZVXmultipathVZVXrebalance)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=102958;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/rtm_to_ipmr_mfcc")
int BPF_KPROBE(KtprobertmVZVXtoVZVXipmrVZVXmfcc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103062;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/rtm_to_ipmr_mfcc")
int BPF_KRETPROBE(KtretprobertmVZVXtoVZVXipmrVZVXmfcc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103062;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/security_inet_csk_clone")
int BPF_KPROBE(KtprobesecurityVZVXinetVZVXcskVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103815;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/security_inet_csk_clone")
int BPF_KRETPROBE(KtretprobesecurityVZVXinetVZVXcskVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103815;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/security_mptcp_add_subflow")
int BPF_KPROBE(KtprobesecurityVZVXmptcpVZVXaddVZVXsubflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103889;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/security_mptcp_add_subflow")
int BPF_KRETPROBE(KtretprobesecurityVZVXmptcpVZVXaddVZVXsubflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103889;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/security_netlink_send")
int BPF_KPROBE(KtprobesecurityVZVXnetlinkVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/security_netlink_send")
int BPF_KRETPROBE(KtretprobesecurityVZVXnetlinkVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/security_sctp_sk_clone")
int BPF_KPROBE(KtprobesecurityVZVXsctpVZVXskVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103948;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/security_sctp_sk_clone")
int BPF_KRETPROBE(KtretprobesecurityVZVXsctpVZVXskVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103948;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/security_sk_alloc")
int BPF_KPROBE(KtprobesecurityVZVXskVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103979;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/security_sk_alloc")
int BPF_KRETPROBE(KtretprobesecurityVZVXskVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103979;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/security_sk_classify_flow")
int BPF_KPROBE(KtprobesecurityVZVXskVZVXclassifyVZVXflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103981;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/security_sk_classify_flow")
int BPF_KRETPROBE(KtretprobesecurityVZVXskVZVXclassifyVZVXflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103981;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/security_sk_clone")
int BPF_KPROBE(KtprobesecurityVZVXskVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103982;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/security_sk_clone")
int BPF_KRETPROBE(KtretprobesecurityVZVXskVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103982;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/security_sk_free")
int BPF_KPROBE(KtprobesecurityVZVXskVZVXfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103983;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/security_sk_free")
int BPF_KRETPROBE(KtretprobesecurityVZVXskVZVXfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103983;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/security_skb_classify_flow")
int BPF_KPROBE(KtprobesecurityVZVXskbVZVXclassifyVZVXflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103985;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/security_skb_classify_flow")
int BPF_KRETPROBE(KtretprobesecurityVZVXskbVZVXclassifyVZVXflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103985;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/security_sock_graft")
int BPF_KPROBE(KtprobesecurityVZVXsockVZVXgraft)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103986;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/security_sock_graft")
int BPF_KRETPROBE(KtretprobesecurityVZVXsockVZVXgraft)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103986;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/security_sock_rcv_skb")
int BPF_KPROBE(KtprobesecurityVZVXsockVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103987;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/security_sock_rcv_skb")
int BPF_KRETPROBE(KtretprobesecurityVZVXsockVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103987;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/security_socket_accept")
int BPF_KPROBE(KtprobesecurityVZVXsocketVZVXaccept)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103988;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/security_socket_accept")
int BPF_KRETPROBE(KtretprobesecurityVZVXsocketVZVXaccept)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103988;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/security_socket_bind")
int BPF_KPROBE(KtprobesecurityVZVXsocketVZVXbind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103989;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/security_socket_bind")
int BPF_KRETPROBE(KtretprobesecurityVZVXsocketVZVXbind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103989;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/security_socket_connect")
int BPF_KPROBE(KtprobesecurityVZVXsocketVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103990;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/security_socket_connect")
int BPF_KRETPROBE(KtretprobesecurityVZVXsocketVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103990;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/security_socket_getpeersec_stream")
int BPF_KPROBE(KtprobesecurityVZVXsocketVZVXgetpeersecVZVXstream)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103994;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/security_socket_getpeersec_stream")
int BPF_KRETPROBE(KtretprobesecurityVZVXsocketVZVXgetpeersecVZVXstream)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103994;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/security_socket_getsockname")
int BPF_KPROBE(KtprobesecurityVZVXsocketVZVXgetsockname)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103995;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/security_socket_getsockname")
int BPF_KRETPROBE(KtretprobesecurityVZVXsocketVZVXgetsockname)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103995;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/security_socket_getsockopt")
int BPF_KPROBE(KtprobesecurityVZVXsocketVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103996;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/security_socket_getsockopt")
int BPF_KRETPROBE(KtretprobesecurityVZVXsocketVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103996;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/security_socket_listen")
int BPF_KPROBE(KtprobesecurityVZVXsocketVZVXlisten)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103997;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/security_socket_listen")
int BPF_KRETPROBE(KtretprobesecurityVZVXsocketVZVXlisten)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103997;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/security_socket_post_create")
int BPF_KPROBE(KtprobesecurityVZVXsocketVZVXpostVZVXcreate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103998;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/security_socket_post_create")
int BPF_KRETPROBE(KtretprobesecurityVZVXsocketVZVXpostVZVXcreate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=103998;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/security_socket_setsockopt")
int BPF_KPROBE(KtprobesecurityVZVXsocketVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104001;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/security_socket_setsockopt")
int BPF_KRETPROBE(KtretprobesecurityVZVXsocketVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104001;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/security_socket_shutdown")
int BPF_KPROBE(KtprobesecurityVZVXsocketVZVXshutdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104002;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/security_socket_shutdown")
int BPF_KRETPROBE(KtretprobesecurityVZVXsocketVZVXshutdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104002;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/security_socket_socketpair")
int BPF_KPROBE(KtprobesecurityVZVXsocketVZVXsocketpair)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104003;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/security_socket_socketpair")
int BPF_KRETPROBE(KtretprobesecurityVZVXsocketVZVXsocketpair)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104003;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/security_unix_may_send")
int BPF_KPROBE(KtprobesecurityVZVXunixVZVXmayVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104034;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/security_unix_may_send")
int BPF_KRETPROBE(KtretprobesecurityVZVXunixVZVXmayVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104034;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/seg6_icmp_srh")
int BPF_KPROBE(Ktprobeseg6VZVXicmpVZVXsrh)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104083;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/seg6_icmp_srh")
int BPF_KRETPROBE(Ktretprobeseg6VZVXicmpVZVXsrh)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104083;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_xfrm_skb_sid")
int BPF_KPROBE(KtprobeselinuxVZVXxfrmVZVXskbVZVXsid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104115;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_xfrm_skb_sid")
int BPF_KRETPROBE(KtretprobeselinuxVZVXxfrmVZVXskbVZVXsid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104115;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_xfrm_skb_sid_ingress")
int BPF_KPROBE(KtprobeselinuxVZVXxfrmVZVXskbVZVXsidVZVXingress)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104116;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_xfrm_skb_sid_ingress")
int BPF_KRETPROBE(KtretprobeselinuxVZVXxfrmVZVXskbVZVXsidVZVXingress)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104116;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_xfrm_sock_rcv_skb")
int BPF_KPROBE(KtprobeselinuxVZVXxfrmVZVXsockVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104118;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_xfrm_sock_rcv_skb")
int BPF_KRETPROBE(KtretprobeselinuxVZVXxfrmVZVXsockVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104118;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sfp_unregister_socket")
int BPF_KPROBE(KtprobesfpVZVXunregisterVZVXsocket)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104431;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sfp_unregister_socket")
int BPF_KRETPROBE(KtretprobesfpVZVXunregisterVZVXsocket)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104431;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_stream_error")
int BPF_KPROBE(KtprobeskVZVXstreamVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104823;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_stream_error")
int BPF_KRETPROBE(KtretprobeskVZVXstreamVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104823;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_stream_kill_queues")
int BPF_KPROBE(KtprobeskVZVXstreamVZVXkillVZVXqueues)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104824;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_stream_kill_queues")
int BPF_KRETPROBE(KtretprobeskVZVXstreamVZVXkillVZVXqueues)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104824;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_stream_wait_close")
int BPF_KPROBE(KtprobeskVZVXstreamVZVXwaitVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104825;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_stream_wait_close")
int BPF_KRETPROBE(KtretprobeskVZVXstreamVZVXwaitVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104825;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_stream_wait_connect")
int BPF_KPROBE(KtprobeskVZVXstreamVZVXwaitVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104827;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_stream_wait_connect")
int BPF_KRETPROBE(KtretprobeskVZVXstreamVZVXwaitVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104827;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_stream_wait_memory")
int BPF_KPROBE(KtprobeskVZVXstreamVZVXwaitVZVXmemory)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104828;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_stream_wait_memory")
int BPF_KRETPROBE(KtretprobeskVZVXstreamVZVXwaitVZVXmemory)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104828;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_stream_write_space")
int BPF_KPROBE(KtprobeskVZVXstreamVZVXwriteVZVXspace)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104829;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_stream_write_space")
int BPF_KRETPROBE(KtretprobeskVZVXstreamVZVXwriteVZVXspace)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104829;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_copy_to_linear_data")
int BPF_KPROBE(KtprobeskbVZVXcopyVZVXtoVZVXlinearVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104831;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_copy_to_linear_data")
int BPF_KRETPROBE(KtretprobeskbVZVXcopyVZVXtoVZVXlinearVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104831;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_get_tx_timestamp")
int BPF_KPROBE(KtprobeskbVZVXgetVZVXtxVZVXtimestamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104833;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_get_tx_timestamp")
int BPF_KRETPROBE(KtretprobeskbVZVXgetVZVXtxVZVXtimestamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104833;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_has_tx_timestamp")
int BPF_KPROBE(KtprobeskbVZVXhasVZVXtxVZVXtimestamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104835;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_has_tx_timestamp")
int BPF_KRETPROBE(KtretprobeskbVZVXhasVZVXtxVZVXtimestamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104835;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_put_data")
int BPF_KPROBE(KtprobeskbVZVXputVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104837;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_put_data")
int BPF_KRETPROBE(KtretprobeskbVZVXputVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104837;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_alloc_file")
int BPF_KPROBE(KtprobesockVZVXallocVZVXfile)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104973;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_alloc_file")
int BPF_KRETPROBE(KtretprobesockVZVXallocVZVXfile)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104973;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_create")
int BPF_KPROBE(KtprobesockVZVXcreate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104977;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_create")
int BPF_KRETPROBE(KtretprobesockVZVXcreate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104977;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_create_kern")
int BPF_KPROBE(KtprobesockVZVXcreateVZVXkern)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104979;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_create_kern")
int BPF_KRETPROBE(KtretprobesockVZVXcreateVZVXkern)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104979;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_create_lite")
int BPF_KPROBE(KtprobesockVZVXcreateVZVXlite)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104980;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_create_lite")
int BPF_KRETPROBE(KtretprobesockVZVXcreateVZVXlite)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104980;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_release")
int BPF_KPROBE(KtprobesockVZVXrelease)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104998;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_release")
int BPF_KRETPROBE(KtretprobesockVZVXrelease)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=104998;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_sendmsg")
int BPF_KPROBE(KtprobesockVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105000;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_sendmsg")
int BPF_KRETPROBE(KtretprobesockVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105000;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sockfs_init_fs_context")
int BPF_KPROBE(KtprobesockfsVZVXinitVZVXfsVZVXcontext)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105014;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sockfs_init_fs_context")
int BPF_KRETPROBE(KtretprobesockfsVZVXinitVZVXfsVZVXcontext)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105014;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sockopt_alloc_buf")
int BPF_KPROBE(KtprobesockoptVZVXallocVZVXbuf)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105022;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sockopt_alloc_buf")
int BPF_KRETPROBE(KtretprobesockoptVZVXallocVZVXbuf)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105022;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp4_gro_complete")
int BPF_KPROBE(Ktprobetcp4VZVXgroVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105601;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp4_gro_complete")
int BPF_KRETPROBE(Ktretprobetcp4VZVXgroVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105601;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp4_gro_receive")
int BPF_KPROBE(Ktprobetcp4VZVXgroVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105602;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp4_gro_receive")
int BPF_KRETPROBE(Ktretprobetcp4VZVXgroVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105602;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp4_gso_segment")
int BPF_KPROBE(Ktprobetcp4VZVXgsoVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105603;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp4_gso_segment")
int BPF_KRETPROBE(Ktretprobetcp4VZVXgsoVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105603;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_add_cmd")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXaddVZVXcmd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105605;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_add_cmd")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXaddVZVXcmd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105605;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_cache_traffic_keys")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXcacheVZVXtrafficVZVXkeys)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105609;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_cache_traffic_keys")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXcacheVZVXtrafficVZVXkeys)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105609;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_calc_key_skb")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXcalcVZVXkeyVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105611;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_calc_key_skb")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXcalcVZVXkeyVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105611;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_connect_init")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXconnectVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105616;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_connect_init")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXconnectVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105616;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_copy_all_matching")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXcopyVZVXallVZVXmatching)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105618;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_copy_all_matching")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXcopyVZVXallVZVXmatching)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105618;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_copy_mkts_to_user")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXcopyVZVXmktsVZVXtoVZVXuser)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105620;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_copy_mkts_to_user")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXcopyVZVXmktsVZVXtoVZVXuser)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105620;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_del_cmd")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXdelVZVXcmd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105621;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_del_cmd")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXdelVZVXcmd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105621;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_destroy_sock")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXdestroyVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105623;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_destroy_sock")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXdestroyVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105623;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_do_lookup")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXdoVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105625;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_do_lookup")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXdoVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105625;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_established")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXestablished)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105626;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_established")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXestablished)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105626;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_established_key")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXestablishedVZVXkey)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105628;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_established_key")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXestablishedVZVXkey)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105628;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_finish_connect")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXfinishVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105629;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_finish_connect")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXfinishVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105629;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_get_mkts")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXgetVZVXmkts)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105631;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_get_mkts")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXgetVZVXmkts)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105631;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_get_repair")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXgetVZVXrepair)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105632;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_get_repair")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXgetVZVXrepair)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105632;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_get_sock_info")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXgetVZVXsockVZVXinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105633;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_get_sock_info")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXgetVZVXsockVZVXinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105633;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_hash_skb")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXhashVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105639;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_hash_skb")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXhashVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105639;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_ignore_icmp")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXignoreVZVXicmp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105641;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_ignore_icmp")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXignoreVZVXicmp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105641;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_inbound_lookup")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXinboundVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105643;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_inbound_lookup")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXinboundVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105643;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_info_cmd")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXinfoVZVXcmd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105644;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_info_cmd")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXinfoVZVXcmd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105644;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_prepare_reset")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXprepareVZVXreset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105651;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_prepare_reset")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXprepareVZVXreset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105651;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_set_repair")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXsetVZVXrepair)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105652;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_set_repair")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXsetVZVXrepair)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105652;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_syncookie")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXsyncookie)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105654;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_syncookie")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXsyncookie)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105654;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_time_wait")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXtimeVZVXwait)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105656;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_time_wait")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXtimeVZVXwait)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105656;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_transmit_skb")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXtransmitVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105658;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_transmit_skb")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXtransmitVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105658;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_verify_hash")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXverifyVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105660;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_verify_hash")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXverifyVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105660;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ao_verify_ipv6")
int BPF_KPROBE(KtprobetcpVZVXaoVZVXverifyVZVXipv6)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105662;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ao_verify_ipv6")
int BPF_KRETPROBE(KtretprobetcpVZVXaoVZVXverifyVZVXipv6)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105662;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_assign_congestion_control")
int BPF_KPROBE(KtprobetcpVZVXassignVZVXcongestionVZVXcontrol)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105663;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_assign_congestion_control")
int BPF_KRETPROBE(KtretprobetcpVZVXassignVZVXcongestionVZVXcontrol)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105663;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_clamp_probe0_to_user_timeout")
int BPF_KPROBE(KtprobetcpVZVXclampVZVXprobe0VZVXtoVZVXuserVZVXtimeout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105674;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_clamp_probe0_to_user_timeout")
int BPF_KRETPROBE(KtretprobetcpVZVXclampVZVXprobe0VZVXtoVZVXuserVZVXtimeout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105674;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_cleanup_congestion_control")
int BPF_KPROBE(KtprobetcpVZVXcleanupVZVXcongestionVZVXcontrol)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105675;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_cleanup_congestion_control")
int BPF_KRETPROBE(KtretprobetcpVZVXcleanupVZVXcongestionVZVXcontrol)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105675;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_cong_avoid_ai")
int BPF_KPROBE(KtprobetcpVZVXcongVZVXavoidVZVXai)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105678;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_cong_avoid_ai")
int BPF_KRETPROBE(KtretprobetcpVZVXcongVZVXavoidVZVXai)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105678;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_delack_timer_handler")
int BPF_KPROBE(KtprobetcpVZVXdelackVZVXtimerVZVXhandler)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105681;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_delack_timer_handler")
int BPF_KRETPROBE(KtretprobetcpVZVXdelackVZVXtimerVZVXhandler)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105681;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_fastopen_active_detect_blackhole")
int BPF_KPROBE(KtprobetcpVZVXfastopenVZVXactiveVZVXdetectVZVXblackhole)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105683;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_fastopen_active_detect_blackhole")
int BPF_KRETPROBE(KtretprobetcpVZVXfastopenVZVXactiveVZVXdetectVZVXblackhole)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105683;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_fastopen_active_disable")
int BPF_KPROBE(KtprobetcpVZVXfastopenVZVXactiveVZVXdisable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105684;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_fastopen_active_disable")
int BPF_KRETPROBE(KtretprobetcpVZVXfastopenVZVXactiveVZVXdisable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105684;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_fastopen_active_disable_ofo_check")
int BPF_KPROBE(KtprobetcpVZVXfastopenVZVXactiveVZVXdisableVZVXofoVZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105685;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_fastopen_active_disable_ofo_check")
int BPF_KRETPROBE(KtretprobetcpVZVXfastopenVZVXactiveVZVXdisableVZVXofoVZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105685;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_fastopen_active_should_disable")
int BPF_KPROBE(KtprobetcpVZVXfastopenVZVXactiveVZVXshouldVZVXdisable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105686;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_fastopen_active_should_disable")
int BPF_KRETPROBE(KtretprobetcpVZVXfastopenVZVXactiveVZVXshouldVZVXdisable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105686;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_fastopen_add_skb")
int BPF_KPROBE(KtprobetcpVZVXfastopenVZVXaddVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105687;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_fastopen_add_skb")
int BPF_KRETPROBE(KtretprobetcpVZVXfastopenVZVXaddVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105687;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_fastopen_cookie_check")
int BPF_KPROBE(KtprobetcpVZVXfastopenVZVXcookieVZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105689;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_fastopen_cookie_check")
int BPF_KRETPROBE(KtretprobetcpVZVXfastopenVZVXcookieVZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105689;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_fastopen_cookie_gen_check")
int BPF_KPROBE(KtprobetcpVZVXfastopenVZVXcookieVZVXgenVZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105691;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_fastopen_cookie_gen_check")
int BPF_KRETPROBE(KtretprobetcpVZVXfastopenVZVXcookieVZVXgenVZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105691;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_fastopen_create_child")
int BPF_KPROBE(KtprobetcpVZVXfastopenVZVXcreateVZVXchild)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105693;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_fastopen_create_child")
int BPF_KRETPROBE(KtretprobetcpVZVXfastopenVZVXcreateVZVXchild)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105693;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_fastopen_ctx_destroy")
int BPF_KPROBE(KtprobetcpVZVXfastopenVZVXctxVZVXdestroy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105694;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_fastopen_ctx_destroy")
int BPF_KRETPROBE(KtretprobetcpVZVXfastopenVZVXctxVZVXdestroy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105694;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_fastopen_defer_connect")
int BPF_KPROBE(KtprobetcpVZVXfastopenVZVXdeferVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105697;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_fastopen_defer_connect")
int BPF_KRETPROBE(KtretprobetcpVZVXfastopenVZVXdeferVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105697;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_fastopen_destroy_cipher")
int BPF_KPROBE(KtprobetcpVZVXfastopenVZVXdestroyVZVXcipher)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105698;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_fastopen_destroy_cipher")
int BPF_KRETPROBE(KtretprobetcpVZVXfastopenVZVXdestroyVZVXcipher)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105698;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_fastopen_get_cipher")
int BPF_KPROBE(KtprobetcpVZVXfastopenVZVXgetVZVXcipher)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105700;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_fastopen_get_cipher")
int BPF_KRETPROBE(KtretprobetcpVZVXfastopenVZVXgetVZVXcipher)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105700;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_fastopen_init_key_once")
int BPF_KPROBE(KtprobetcpVZVXfastopenVZVXinitVZVXkeyVZVXonce)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105701;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_fastopen_init_key_once")
int BPF_KRETPROBE(KtretprobetcpVZVXfastopenVZVXinitVZVXkeyVZVXonce)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105701;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_fastopen_reset_cipher")
int BPF_KPROBE(KtprobetcpVZVXfastopenVZVXresetVZVXcipher)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105703;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_fastopen_reset_cipher")
int BPF_KRETPROBE(KtretprobetcpVZVXfastopenVZVXresetVZVXcipher)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105703;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_fastopen_synack_timer")
int BPF_KPROBE(KtprobetcpVZVXfastopenVZVXsynackVZVXtimer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105704;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_fastopen_synack_timer")
int BPF_KRETPROBE(KtretprobetcpVZVXfastopenVZVXsynackVZVXtimer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105704;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_get_default_congestion_control")
int BPF_KPROBE(KtprobetcpVZVXgetVZVXdefaultVZVXcongestionVZVXcontrol)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105709;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_get_default_congestion_control")
int BPF_KRETPROBE(KtretprobetcpVZVXgetVZVXdefaultVZVXcongestionVZVXcontrol)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105709;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_gro_complete")
int BPF_KPROBE(KtprobetcpVZVXgroVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105710;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_gro_complete")
int BPF_KRETPROBE(KtretprobetcpVZVXgroVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105710;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_gro_pull_header")
int BPF_KPROBE(KtprobetcpVZVXgroVZVXpullVZVXheader)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105714;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_gro_pull_header")
int BPF_KRETPROBE(KtretprobetcpVZVXgroVZVXpullVZVXheader)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105714;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_gro_receive")
int BPF_KPROBE(KtprobetcpVZVXgroVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105716;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_gro_receive")
int BPF_KRETPROBE(KtretprobetcpVZVXgroVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105716;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_gso_segment")
int BPF_KPROBE(KtprobetcpVZVXgsoVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105717;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_gso_segment")
int BPF_KRETPROBE(KtretprobetcpVZVXgsoVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105717;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_inbound_ao_hash")
int BPF_KPROBE(KtprobetcpVZVXinboundVZVXaoVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105719;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_inbound_ao_hash")
int BPF_KRETPROBE(KtretprobetcpVZVXinboundVZVXaoVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105719;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_init_congestion_control")
int BPF_KPROBE(KtprobetcpVZVXinitVZVXcongestionVZVXcontrol)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105720;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_init_congestion_control")
int BPF_KRETPROBE(KtretprobetcpVZVXinitVZVXcongestionVZVXcontrol)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105720;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_init_xmit_timers")
int BPF_KPROBE(KtprobetcpVZVXinitVZVXxmitVZVXtimers)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105721;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_init_xmit_timers")
int BPF_KRETPROBE(KtretprobetcpVZVXinitVZVXxmitVZVXtimers)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105721;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_out_of_resources")
int BPF_KPROBE(KtprobetcpVZVXoutVZVXofVZVXresources)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105724;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_out_of_resources")
int BPF_KRETPROBE(KtretprobetcpVZVXoutVZVXofVZVXresources)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105724;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_parse_ao")
int BPF_KPROBE(KtprobetcpVZVXparseVZVXao)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105726;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_parse_ao")
int BPF_KRETPROBE(KtretprobetcpVZVXparseVZVXao)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105726;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_probe_timer")
int BPF_KPROBE(KtprobetcpVZVXprobeVZVXtimer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105727;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_probe_timer")
int BPF_KRETPROBE(KtretprobetcpVZVXprobeVZVXtimer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105727;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_reno_cong_avoid")
int BPF_KPROBE(KtprobetcpVZVXrenoVZVXcongVZVXavoid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105730;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_reno_cong_avoid")
int BPF_KRETPROBE(KtretprobetcpVZVXrenoVZVXcongVZVXavoid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105730;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_reno_ssthresh")
int BPF_KPROBE(KtprobetcpVZVXrenoVZVXssthresh)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105731;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_reno_ssthresh")
int BPF_KRETPROBE(KtretprobetcpVZVXrenoVZVXssthresh)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105731;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_reno_undo_cwnd")
int BPF_KPROBE(KtprobetcpVZVXrenoVZVXundoVZVXcwnd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105732;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_reno_undo_cwnd")
int BPF_KRETPROBE(KtretprobetcpVZVXrenoVZVXundoVZVXcwnd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105732;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_reset_keepalive_timer")
int BPF_KPROBE(KtprobetcpVZVXresetVZVXkeepaliveVZVXtimer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105734;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_reset_keepalive_timer")
int BPF_KRETPROBE(KtretprobetcpVZVXresetVZVXkeepaliveVZVXtimer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105734;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_retransmit_timer")
int BPF_KPROBE(KtprobetcpVZVXretransmitVZVXtimer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105735;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_retransmit_timer")
int BPF_KRETPROBE(KtretprobetcpVZVXretransmitVZVXtimer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105735;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_set_ca_state")
int BPF_KPROBE(KtprobetcpVZVXsetVZVXcaVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105738;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_set_ca_state")
int BPF_KRETPROBE(KtretprobetcpVZVXsetVZVXcaVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105738;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_set_congestion_control")
int BPF_KPROBE(KtprobetcpVZVXsetVZVXcongestionVZVXcontrol)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105740;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_set_congestion_control")
int BPF_KRETPROBE(KtretprobetcpVZVXsetVZVXcongestionVZVXcontrol)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105740;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_set_default_congestion_control")
int BPF_KPROBE(KtprobetcpVZVXsetVZVXdefaultVZVXcongestionVZVXcontrol)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105742;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_set_default_congestion_control")
int BPF_KRETPROBE(KtretprobetcpVZVXsetVZVXdefaultVZVXcongestionVZVXcontrol)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105742;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_set_keepalive")
int BPF_KPROBE(KtprobetcpVZVXsetVZVXkeepalive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105743;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_set_keepalive")
int BPF_KRETPROBE(KtretprobetcpVZVXsetVZVXkeepalive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105743;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_sigpool_hash_skb_data")
int BPF_KPROBE(KtprobetcpVZVXsigpoolVZVXhashVZVXskbVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105753;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_sigpool_hash_skb_data")
int BPF_KRETPROBE(KtretprobetcpVZVXsigpoolVZVXhashVZVXskbVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105753;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_slow_start")
int BPF_KPROBE(KtprobetcpVZVXslowVZVXstart)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105758;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_slow_start")
int BPF_KRETPROBE(KtretprobetcpVZVXslowVZVXstart)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105758;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_syn_ack_timeout")
int BPF_KPROBE(KtprobetcpVZVXsynVZVXackVZVXtimeout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105760;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_syn_ack_timeout")
int BPF_KRETPROBE(KtretprobetcpVZVXsynVZVXackVZVXtimeout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105760;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_try_fastopen")
int BPF_KPROBE(KtprobetcpVZVXtryVZVXfastopen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105762;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_try_fastopen")
int BPF_KRETPROBE(KtretprobetcpVZVXtryVZVXfastopen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105762;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_ao_calc_key_rsk")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXaoVZVXcalcVZVXkeyVZVXrsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105769;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_ao_calc_key_rsk")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXaoVZVXcalcVZVXkeyVZVXrsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105769;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_ao_calc_key_sk")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXaoVZVXcalcVZVXkeyVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105770;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_ao_calc_key_sk")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXaoVZVXcalcVZVXkeyVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105770;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_ao_hash_skb")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXaoVZVXhashVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105773;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_ao_hash_skb")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXaoVZVXhashVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105773;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_ao_lookup")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXaoVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105774;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_ao_lookup")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXaoVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105774;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_ao_lookup_rsk")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXaoVZVXlookupVZVXrsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105775;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_ao_lookup_rsk")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXaoVZVXlookupVZVXrsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105775;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_ao_synack_hash")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXaoVZVXsynackVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105776;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_ao_synack_hash")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXaoVZVXsynackVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105776;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_parse_ao")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXparseVZVXao)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105777;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_parse_ao")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXparseVZVXao)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105777;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_write_timeout")
int BPF_KPROBE(KtprobetcpVZVXwriteVZVXtimeout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105779;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_write_timeout")
int BPF_KRETPROBE(KtretprobetcpVZVXwriteVZVXtimeout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105779;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_write_timer_handler")
int BPF_KPROBE(KtprobetcpVZVXwriteVZVXtimerVZVXhandler)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105781;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_write_timer_handler")
int BPF_KRETPROBE(KtretprobetcpVZVXwriteVZVXtimerVZVXhandler)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105781;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/thermal_genl_cmd_tz_get_trip")
int BPF_KPROBE(KtprobethermalVZVXgenlVZVXcmdVZVXtzVZVXgetVZVXtrip)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105843;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/thermal_genl_cmd_tz_get_trip")
int BPF_KRETPROBE(KtretprobethermalVZVXgenlVZVXcmdVZVXtzVZVXgetVZVXtrip)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105843;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/thermal_genl_event_tz_trip_change")
int BPF_KPROBE(KtprobethermalVZVXgenlVZVXeventVZVXtzVZVXtripVZVXchange)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105856;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/thermal_genl_event_tz_trip_change")
int BPF_KRETPROBE(KtretprobethermalVZVXgenlVZVXeventVZVXtzVZVXtripVZVXchange)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105856;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/thermal_genl_event_tz_trip_up")
int BPF_KPROBE(KtprobethermalVZVXgenlVZVXeventVZVXtzVZVXtripVZVXup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105857;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/thermal_genl_event_tz_trip_up")
int BPF_KRETPROBE(KtretprobethermalVZVXgenlVZVXeventVZVXtzVZVXtripVZVXup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105857;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/thermal_genl_send_event")
int BPF_KPROBE(KtprobethermalVZVXgenlVZVXsendVZVXevent)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105862;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/thermal_genl_send_event")
int BPF_KRETPROBE(KtretprobethermalVZVXgenlVZVXsendVZVXevent)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=105862;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tpacket_destruct_skb")
int BPF_KPROBE(KtprobetpacketVZVXdestructVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=106124;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tpacket_destruct_skb")
int BPF_KRETPROBE(KtretprobetpacketVZVXdestructVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=106124;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tpacket_fill_skb")
int BPF_KPROBE(KtprobetpacketVZVXfillVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=106126;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tpacket_fill_skb")
int BPF_KRETPROBE(KtretprobetpacketVZVXfillVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=106126;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udplite6_proc_exit_net")
int BPF_KPROBE(Ktprobeudplite6VZVXprocVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=106753;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udplite6_proc_exit_net")
int BPF_KRETPROBE(Ktretprobeudplite6VZVXprocVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=106753;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udplite6_proc_init_net")
int BPF_KPROBE(Ktprobeudplite6VZVXprocVZVXinitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=106755;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udplite6_proc_init_net")
int BPF_KRETPROBE(Ktretprobeudplite6VZVXprocVZVXinitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=106755;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udplitev6_err")
int BPF_KPROBE(Ktprobeudplitev6VZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=106756;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udplitev6_err")
int BPF_KRETPROBE(Ktretprobeudplitev6VZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=106756;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udplitev6_rcv")
int BPF_KPROBE(Ktprobeudplitev6VZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=106759;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udplitev6_rcv")
int BPF_KRETPROBE(Ktretprobeudplitev6VZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=106759;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udplitev6_sk_init")
int BPF_KPROBE(Ktprobeudplitev6VZVXskVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=106760;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udplitev6_sk_init")
int BPF_KRETPROBE(Ktretprobeudplitev6VZVXskVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=106760;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/virtio_net_hdr_from_skb")
int BPF_KPROBE(KtprobevirtioVZVXnetVZVXhdrVZVXfromVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=107382;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/virtio_net_hdr_from_skb")
int BPF_KRETPROBE(KtretprobevirtioVZVXnetVZVXhdrVZVXfromVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=107382;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xdp_build_skb_from_buff")
int BPF_KPROBE(KtprobexdpVZVXbuildVZVXskbVZVXfromVZVXbuff)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=107887;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xdp_build_skb_from_buff")
int BPF_KRETPROBE(KtretprobexdpVZVXbuildVZVXskbVZVXfromVZVXbuff)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=107887;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xdp_build_skb_from_frame")
int BPF_KPROBE(KtprobexdpVZVXbuildVZVXskbVZVXfromVZVXframe)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=107889;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xdp_build_skb_from_frame")
int BPF_KRETPROBE(KtretprobexdpVZVXbuildVZVXskbVZVXfromVZVXframe)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=107889;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xdp_build_skb_from_zc")
int BPF_KPROBE(KtprobexdpVZVXbuildVZVXskbVZVXfromVZVXzc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=107891;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xdp_build_skb_from_zc")
int BPF_KRETPROBE(KtretprobexdpVZVXbuildVZVXskbVZVXfromVZVXzc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=107891;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xp_add_xsk")
int BPF_KPROBE(KtprobexpVZVXaddVZVXxsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108409;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xp_add_xsk")
int BPF_KRETPROBE(KtretprobexpVZVXaddVZVXxsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108409;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xp_del_xsk")
int BPF_KPROBE(KtprobexpVZVXdelVZVXxsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108428;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xp_del_xsk")
int BPF_KRETPROBE(KtretprobexpVZVXdelVZVXxsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108428;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xp_get_xskb")
int BPF_KPROBE(KtprobexpVZVXgetVZVXxskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108441;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xp_get_xskb")
int BPF_KRETPROBE(KtretprobexpVZVXgetVZVXxskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108441;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_bind")
int BPF_KPROBE(KtprobexskVZVXbind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108458;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_bind")
int BPF_KRETPROBE(KtretprobexskVZVXbind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108458;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_build_skb")
int BPF_KPROBE(KtprobexskVZVXbuildVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108460;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_build_skb")
int BPF_KRETPROBE(KtretprobexskVZVXbuildVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108460;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_build_skb_zerocopy")
int BPF_KPROBE(KtprobexskVZVXbuildVZVXskbVZVXzerocopy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108461;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_build_skb_zerocopy")
int BPF_KRETPROBE(KtretprobexskVZVXbuildVZVXskbVZVXzerocopy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108461;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_clear_pool_at_qid")
int BPF_KPROBE(KtprobexskVZVXclearVZVXpoolVZVXatVZVXqid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108463;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_clear_pool_at_qid")
int BPF_KRETPROBE(KtretprobexskVZVXclearVZVXpoolVZVXatVZVXqid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108463;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_clear_rx_need_wakeup")
int BPF_KPROBE(KtprobexskVZVXclearVZVXrxVZVXneedVZVXwakeup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108464;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_clear_rx_need_wakeup")
int BPF_KRETPROBE(KtretprobexskVZVXclearVZVXrxVZVXneedVZVXwakeup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108464;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_clear_tx_need_wakeup")
int BPF_KPROBE(KtprobexskVZVXclearVZVXtxVZVXneedVZVXwakeup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108465;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_clear_tx_need_wakeup")
int BPF_KRETPROBE(KtretprobexskVZVXclearVZVXtxVZVXneedVZVXwakeup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108465;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_consume_skb")
int BPF_KPROBE(KtprobexskVZVXconsumeVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108466;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_consume_skb")
int BPF_KRETPROBE(KtretprobexskVZVXconsumeVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108466;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_create")
int BPF_KPROBE(KtprobexskVZVXcreate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108469;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_create")
int BPF_KRETPROBE(KtretprobexskVZVXcreate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108469;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_destruct")
int BPF_KPROBE(KtprobexskVZVXdestruct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108470;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_destruct")
int BPF_KRETPROBE(KtretprobexskVZVXdestruct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108470;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_destruct_skb")
int BPF_KPROBE(KtprobexskVZVXdestructVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108471;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_destruct_skb")
int BPF_KRETPROBE(KtretprobexskVZVXdestructVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108471;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_generic_rcv")
int BPF_KPROBE(KtprobexskVZVXgenericVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108472;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_generic_rcv")
int BPF_KRETPROBE(KtretprobexskVZVXgenericVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108472;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_get_pool_from_qid")
int BPF_KPROBE(KtprobexskVZVXgetVZVXpoolVZVXfromVZVXqid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108474;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_get_pool_from_qid")
int BPF_KRETPROBE(KtretprobexskVZVXgetVZVXpoolVZVXfromVZVXqid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108474;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_getsockopt")
int BPF_KPROBE(KtprobexskVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108475;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_getsockopt")
int BPF_KRETPROBE(KtretprobexskVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108475;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_mmap")
int BPF_KPROBE(KtprobexskVZVXmmap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108477;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_mmap")
int BPF_KRETPROBE(KtretprobexskVZVXmmap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108477;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_net_exit")
int BPF_KPROBE(KtprobexskVZVXnetVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108478;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_net_exit")
int BPF_KRETPROBE(KtretprobexskVZVXnetVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108478;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_net_init")
int BPF_KPROBE(KtprobexskVZVXnetVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108479;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_net_init")
int BPF_KRETPROBE(KtretprobexskVZVXnetVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108479;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_poll")
int BPF_KPROBE(KtprobexskVZVXpoll)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108482;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_poll")
int BPF_KRETPROBE(KtretprobexskVZVXpoll)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108482;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_recvmsg")
int BPF_KPROBE(KtprobexskVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108483;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_recvmsg")
int BPF_KRETPROBE(KtretprobexskVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108483;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_reg_pool_at_qid")
int BPF_KPROBE(KtprobexskVZVXregVZVXpoolVZVXatVZVXqid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108485;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_reg_pool_at_qid")
int BPF_KRETPROBE(KtretprobexskVZVXregVZVXpoolVZVXatVZVXqid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108485;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_release")
int BPF_KPROBE(KtprobexskVZVXrelease)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108486;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_release")
int BPF_KRETPROBE(KtretprobexskVZVXrelease)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108486;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_sendmsg")
int BPF_KPROBE(KtprobexskVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108487;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_sendmsg")
int BPF_KRETPROBE(KtretprobexskVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108487;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_set_rx_need_wakeup")
int BPF_KPROBE(KtprobexskVZVXsetVZVXrxVZVXneedVZVXwakeup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108488;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_set_rx_need_wakeup")
int BPF_KRETPROBE(KtretprobexskVZVXsetVZVXrxVZVXneedVZVXwakeup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108488;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_set_tx_need_wakeup")
int BPF_KPROBE(KtprobexskVZVXsetVZVXtxVZVXneedVZVXwakeup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108489;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_set_tx_need_wakeup")
int BPF_KRETPROBE(KtretprobexskVZVXsetVZVXtxVZVXneedVZVXwakeup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108489;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_setsockopt")
int BPF_KPROBE(KtprobexskVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108490;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_setsockopt")
int BPF_KRETPROBE(KtretprobexskVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108490;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_tx_completed")
int BPF_KPROBE(KtprobexskVZVXtxVZVXcompleted)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108492;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_tx_completed")
int BPF_KRETPROBE(KtretprobexskVZVXtxVZVXcompleted)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108492;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_tx_peek_desc")
int BPF_KPROBE(KtprobexskVZVXtxVZVXpeekVZVXdesc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108494;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_tx_peek_desc")
int BPF_KRETPROBE(KtretprobexskVZVXtxVZVXpeekVZVXdesc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108494;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_tx_peek_release_desc_batch")
int BPF_KPROBE(KtprobexskVZVXtxVZVXpeekVZVXreleaseVZVXdescVZVXbatch)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108496;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_tx_peek_release_desc_batch")
int BPF_KRETPROBE(KtretprobexskVZVXtxVZVXpeekVZVXreleaseVZVXdescVZVXbatch)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108496;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_tx_release")
int BPF_KPROBE(KtprobexskVZVXtxVZVXrelease)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108497;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_tx_release")
int BPF_KRETPROBE(KtretprobexskVZVXtxVZVXrelease)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108497;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_uses_need_wakeup")
int BPF_KPROBE(KtprobexskVZVXusesVZVXneedVZVXwakeup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108498;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_uses_need_wakeup")
int BPF_KRETPROBE(KtretprobexskVZVXusesVZVXneedVZVXwakeup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=108498;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/___pskb_trim")
int BPF_KPROBE(KtprobeVZVXVZVXVZVXpskbVZVXtrim)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=118138;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/___pskb_trim")
int BPF_KRETPROBE(KtretprobeVZVXVZVXVZVXpskbVZVXtrim)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=118138;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__consume_stateless_skb")
int BPF_KPROBE(KtprobeVZVXVZVXconsumeVZVXstatelessVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=118629;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__consume_stateless_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXconsumeVZVXstatelessVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=118629;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__copy_skb_header")
int BPF_KPROBE(KtprobeVZVXVZVXcopyVZVXskbVZVXheader)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=118639;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__copy_skb_header")
int BPF_KRETPROBE(KtretprobeVZVXVZVXcopyVZVXskbVZVXheader)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=118639;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__fl6_sock_lookup")
int BPF_KPROBE(KtprobeVZVXVZVXfl6VZVXsockVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=118831;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__fl6_sock_lookup")
int BPF_KRETPROBE(KtretprobeVZVXVZVXfl6VZVXsockVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=118831;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__inet6_lookup_skb")
int BPF_KPROBE(KtprobeVZVXVZVXinet6VZVXlookupVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119034;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__inet6_lookup_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXinet6VZVXlookupVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119034;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__inet_listen_sk")
int BPF_KPROBE(KtprobeVZVXVZVXinetVZVXlistenVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119039;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__inet_listen_sk")
int BPF_KRETPROBE(KtretprobeVZVXVZVXinetVZVXlistenVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119039;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip4_datagram_connect")
int BPF_KPROBE(KtprobeVZVXVZVXip4VZVXdatagramVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119082;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip4_datagram_connect")
int BPF_KRETPROBE(KtretprobeVZVXVZVXip4VZVXdatagramVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119082;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip_sock_set_tos")
int BPF_KPROBE(KtprobeVZVXVZVXipVZVXsockVZVXsetVZVXtos)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119083;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip_sock_set_tos")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipVZVXsockVZVXsetVZVXtos)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119083;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ipv6_chk_addr_and_flags")
int BPF_KPROBE(KtprobeVZVXVZVXipv6VZVXchkVZVXaddrVZVXandVZVXflags)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119085;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ipv6_chk_addr_and_flags")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipv6VZVXchkVZVXaddrVZVXandVZVXflags)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119085;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ipv6_dev_get_saddr")
int BPF_KPROBE(KtprobeVZVXVZVXipv6VZVXdevVZVXgetVZVXsaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119087;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ipv6_dev_get_saddr")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipv6VZVXdevVZVXgetVZVXsaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119087;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ipv6_ifa_notify")
int BPF_KPROBE(KtprobeVZVXVZVXipv6VZVXifaVZVXnotify)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119089;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ipv6_ifa_notify")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipv6VZVXifaVZVXnotify)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119089;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__kfree_skb")
int BPF_KPROBE(KtprobeVZVXVZVXkfreeVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119113;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__kfree_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXkfreeVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119113;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_subflow_connect")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXsubflowVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119211;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_subflow_connect")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXsubflowVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119211;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_subflow_fully_established")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXsubflowVZVXfullyVZVXestablished)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119213;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_subflow_fully_established")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXsubflowVZVXfullyVZVXestablished)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119213;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_sync_state")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXsyncVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119215;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_sync_state")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXsyncVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119215;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__napi_kfree_skb")
int BPF_KPROBE(KtprobeVZVXVZVXnapiVZVXkfreeVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119224;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__napi_kfree_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXnapiVZVXkfreeVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119224;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__netdev_alloc_skb")
int BPF_KPROBE(KtprobeVZVXVZVXnetdevVZVXallocVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119233;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__netdev_alloc_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXnetdevVZVXallocVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119233;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__pskb_copy_fclone")
int BPF_KPROBE(KtprobeVZVXVZVXpskbVZVXcopyVZVXfclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119522;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__pskb_copy_fclone")
int BPF_KRETPROBE(KtretprobeVZVXVZVXpskbVZVXcopyVZVXfclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119522;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__pskb_pull_tail")
int BPF_KPROBE(KtprobeVZVXVZVXpskbVZVXpullVZVXtail)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119524;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__pskb_pull_tail")
int BPF_KRETPROBE(KtretprobeVZVXVZVXpskbVZVXpullVZVXtail)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119524;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_checksum_complete")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXchecksumVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119666;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_checksum_complete")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXchecksumVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119666;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_checksum_complete_head")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXchecksumVZVXcompleteVZVXhead)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119668;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_checksum_complete_head")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXchecksumVZVXcompleteVZVXhead)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119668;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_complete_tx_timestamp")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXcompleteVZVXtxVZVXtimestamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119670;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_complete_tx_timestamp")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXcompleteVZVXtxVZVXtimestamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119670;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_ext_del")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXextVZVXdel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119673;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_ext_del")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXextVZVXdel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119673;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_ext_set")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXextVZVXset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119677;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_ext_set")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXextVZVXset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119677;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_flow_dissect")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXflowVZVXdissect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119681;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_flow_dissect")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXflowVZVXdissect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119681;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_flow_dissect_arp")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXflowVZVXdissectVZVXarp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119683;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_flow_dissect_arp")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXflowVZVXdissectVZVXarp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119683;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_flow_dissect_gre")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXflowVZVXdissectVZVXgre)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119685;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_flow_dissect_gre")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXflowVZVXdissectVZVXgre)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119685;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_get_hash_net")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXgetVZVXhashVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119687;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_get_hash_net")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXgetVZVXhashVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119687;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_get_hash_symmetric_net")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXgetVZVXhashVZVXsymmetricVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119689;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_get_hash_symmetric_net")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXgetVZVXhashVZVXsymmetricVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119689;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_get_poff")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXgetVZVXpoff)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119691;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_get_poff")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXgetVZVXpoff)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119691;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_gro_checksum_complete")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXgroVZVXchecksumVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119692;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_gro_checksum_complete")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXgroVZVXchecksumVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119692;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_pad")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXpad)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119694;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_pad")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXpad)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119694;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_send_sock")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXsendVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119696;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_send_sock")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXsendVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119696;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_splice_bits")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXspliceVZVXbits)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119698;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_splice_bits")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXspliceVZVXbits)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119698;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_to_sgvec")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXtoVZVXsgvec)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119700;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_to_sgvec")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXtoVZVXsgvec)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119700;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_tstamp_tx")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXtstampVZVXtx)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119702;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_tstamp_tx")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXtstampVZVXtx)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119702;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_udp_tunnel_segment")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXudpVZVXtunnelVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119704;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_udp_tunnel_segment")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXudpVZVXtunnelVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119704;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_unclone_keeptruesize")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXuncloneVZVXkeeptruesize)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119706;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_unclone_keeptruesize")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXuncloneVZVXkeeptruesize)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119706;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_vlan_pop")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXvlanVZVXpop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119708;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_vlan_pop")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXvlanVZVXpop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119708;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_warn_lro_forwarding")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXwarnVZVXlroVZVXforwarding)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119710;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_warn_lro_forwarding")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXwarnVZVXlroVZVXforwarding)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119710;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_zcopy_downgrade_managed")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXzcopyVZVXdowngradeVZVXmanaged)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119711;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_zcopy_downgrade_managed")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXzcopyVZVXdowngradeVZVXmanaged)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119711;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__tcp_cleanup_rbuf")
int BPF_KPROBE(KtprobeVZVXVZVXtcpVZVXcleanupVZVXrbuf)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119786;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__tcp_cleanup_rbuf")
int BPF_KRETPROBE(KtretprobeVZVXVZVXtcpVZVXcleanupVZVXrbuf)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119786;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__tcp_close")
int BPF_KPROBE(KtprobeVZVXVZVXtcpVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119787;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__tcp_close")
int BPF_KRETPROBE(KtretprobeVZVXVZVXtcpVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119787;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__tcp_md5_do_add")
int BPF_KPROBE(KtprobeVZVXVZVXtcpVZVXmd5VZVXdoVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119789;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__tcp_md5_do_add")
int BPF_KRETPROBE(KtretprobeVZVXVZVXtcpVZVXmd5VZVXdoVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119789;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__tcp_md5_do_lookup")
int BPF_KPROBE(KtprobeVZVXVZVXtcpVZVXmd5VZVXdoVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119791;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__tcp_md5_do_lookup")
int BPF_KRETPROBE(KtretprobeVZVXVZVXtcpVZVXmd5VZVXdoVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119791;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__tcp_read_sock")
int BPF_KPROBE(KtprobeVZVXVZVXtcpVZVXreadVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119793;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__tcp_read_sock")
int BPF_KRETPROBE(KtretprobeVZVXVZVXtcpVZVXreadVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119793;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__tcp_sock_set_cork")
int BPF_KPROBE(KtprobeVZVXVZVXtcpVZVXsockVZVXsetVZVXcork)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119795;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__tcp_sock_set_cork")
int BPF_KRETPROBE(KtretprobeVZVXVZVXtcpVZVXsockVZVXsetVZVXcork)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119795;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__tcp_sock_set_nodelay")
int BPF_KPROBE(KtprobeVZVXVZVXtcpVZVXsockVZVXsetVZVXnodelay)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119796;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__tcp_sock_set_nodelay")
int BPF_KRETPROBE(KtretprobeVZVXVZVXtcpVZVXsockVZVXsetVZVXnodelay)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119796;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__tcp_sock_set_quickack")
int BPF_KPROBE(KtprobeVZVXVZVXtcpVZVXsockVZVXsetVZVXquickack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119797;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__tcp_sock_set_quickack")
int BPF_KRETPROBE(KtretprobeVZVXVZVXtcpVZVXsockVZVXsetVZVXquickack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119797;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__tcp_v4_send_check")
int BPF_KPROBE(KtprobeVZVXVZVXtcpVZVXv4VZVXsendVZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119799;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__tcp_v4_send_check")
int BPF_KRETPROBE(KtretprobeVZVXVZVXtcpVZVXv4VZVXsendVZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=119799;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__udp_gso_segment")
int BPF_KPROBE(KtprobeVZVXVZVXudpVZVXgsoVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=120178;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__udp_gso_segment")
int BPF_KRETPROBE(KtretprobeVZVXVZVXudpVZVXgsoVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=120178;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__udp_gso_segment_list")
int BPF_KPROBE(KtprobeVZVXVZVXudpVZVXgsoVZVXsegmentVZVXlist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=120180;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__udp_gso_segment_list")
int BPF_KRETPROBE(KtretprobeVZVXVZVXudpVZVXgsoVZVXsegmentVZVXlist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=120180;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__udpv4_gso_segment_csum")
int BPF_KPROBE(KtprobeVZVXVZVXudpv4VZVXgsoVZVXsegmentVZVXcsum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=120182;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__udpv4_gso_segment_csum")
int BPF_KRETPROBE(KtretprobeVZVXVZVXudpv4VZVXgsoVZVXsegmentVZVXcsum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=120182;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__udpv6_gso_segment_csum")
int BPF_KPROBE(KtprobeVZVXVZVXudpv6VZVXgsoVZVXsegmentVZVXcsum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=120183;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__udpv6_gso_segment_csum")
int BPF_KRETPROBE(KtretprobeVZVXVZVXudpv6VZVXgsoVZVXsegmentVZVXcsum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=120183;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__xfrm4_udp_encap_rcv")
int BPF_KPROBE(KtprobeVZVXVZVXxfrm4VZVXudpVZVXencapVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=120413;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__xfrm4_udp_encap_rcv")
int BPF_KRETPROBE(KtretprobeVZVXVZVXxfrm4VZVXudpVZVXencapVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=120413;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__xfrm6_udp_encap_rcv")
int BPF_KPROBE(KtprobeVZVXVZVXxfrm6VZVXudpVZVXencapVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=120416;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__xfrm6_udp_encap_rcv")
int BPF_KRETPROBE(KtretprobeVZVXVZVXxfrm6VZVXudpVZVXencapVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=120416;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__xfrm_sk_clone_policy")
int BPF_KPROBE(KtprobeVZVXVZVXxfrmVZVXskVZVXcloneVZVXpolicy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=120434;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__xfrm_sk_clone_policy")
int BPF_KRETPROBE(KtretprobeVZVXVZVXxfrmVZVXskVZVXcloneVZVXpolicy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=120434;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/_ip6mr_fill_mroute")
int BPF_KPROBE(KtprobeVZVXip6mrVZVXfillVZVXmroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=120516;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/_ip6mr_fill_mroute")
int BPF_KRETPROBE(KtretprobeVZVXip6mrVZVXfillVZVXmroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=120516;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/aa_audit_perm_mask")
int BPF_KPROBE(KtprobeaaVZVXauditVZVXpermVZVXmask)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=120579;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/aa_audit_perm_mask")
int BPF_KRETPROBE(KtretprobeaaVZVXauditVZVXpermVZVXmask)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=120579;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/aa_label_sk_perm")
int BPF_KPROBE(KtprobeaaVZVXlabelVZVXskVZVXperm)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=120603;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/aa_label_sk_perm")
int BPF_KRETPROBE(KtretprobeaaVZVXlabelVZVXskVZVXperm)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=120603;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/aa_sk_perm")
int BPF_KPROBE(KtprobeaaVZVXskVZVXperm)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=120629;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/aa_sk_perm")
int BPF_KRETPROBE(KtretprobeaaVZVXskVZVXperm)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=120629;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/addrconf_ifid_ip6tnl")
int BPF_KPROBE(KtprobeaddrconfVZVXifidVZVXip6tnl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=121568;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/addrconf_ifid_ip6tnl")
int BPF_KRETPROBE(KtretprobeaddrconfVZVXifidVZVXip6tnl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=121568;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/alloc_skb_for_msg")
int BPF_KPROBE(KtprobeallocVZVXskbVZVXforVZVXmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=121813;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/alloc_skb_for_msg")
int BPF_KRETPROBE(KtretprobeallocVZVXskbVZVXforVZVXmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=121813;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/audit_list_rules_send")
int BPF_KPROBE(KtprobeauditVZVXlistVZVXrulesVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=122195;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/audit_list_rules_send")
int BPF_KRETPROBE(KtretprobeauditVZVXlistVZVXrulesVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=122195;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/build_skb_around")
int BPF_KPROBE(KtprobebuildVZVXskbVZVXaround)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123274;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/build_skb_around")
int BPF_KRETPROBE(KtretprobebuildVZVXskbVZVXaround)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123274;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/calipso_opt_update")
int BPF_KPROBE(KtprobecalipsoVZVXoptVZVXupdate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123364;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/calipso_opt_update")
int BPF_KRETPROBE(KtretprobecalipsoVZVXoptVZVXupdate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123364;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/calipso_optptr")
int BPF_KPROBE(KtprobecalipsoVZVXoptptr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123365;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/calipso_optptr")
int BPF_KRETPROBE(KtretprobecalipsoVZVXoptptr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123365;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/calipso_req_delattr")
int BPF_KPROBE(KtprobecalipsoVZVXreqVZVXdelattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123368;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/calipso_req_delattr")
int BPF_KRETPROBE(KtretprobecalipsoVZVXreqVZVXdelattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123368;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/calipso_req_setattr")
int BPF_KPROBE(KtprobecalipsoVZVXreqVZVXsetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123370;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/calipso_req_setattr")
int BPF_KRETPROBE(KtretprobecalipsoVZVXreqVZVXsetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123370;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/calipso_skbuff_delattr")
int BPF_KPROBE(KtprobecalipsoVZVXskbuffVZVXdelattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123371;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/calipso_skbuff_delattr")
int BPF_KRETPROBE(KtretprobecalipsoVZVXskbuffVZVXdelattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123371;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/calipso_skbuff_optptr")
int BPF_KPROBE(KtprobecalipsoVZVXskbuffVZVXoptptr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123372;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/calipso_skbuff_optptr")
int BPF_KRETPROBE(KtretprobecalipsoVZVXskbuffVZVXoptptr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123372;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/calipso_skbuff_setattr")
int BPF_KPROBE(KtprobecalipsoVZVXskbuffVZVXsetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123374;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/calipso_skbuff_setattr")
int BPF_KRETPROBE(KtretprobecalipsoVZVXskbuffVZVXsetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123374;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/calipso_sock_delattr")
int BPF_KPROBE(KtprobecalipsoVZVXsockVZVXdelattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123375;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/calipso_sock_delattr")
int BPF_KRETPROBE(KtretprobecalipsoVZVXsockVZVXdelattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123375;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/calipso_sock_getattr")
int BPF_KPROBE(KtprobecalipsoVZVXsockVZVXgetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123376;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/calipso_sock_getattr")
int BPF_KRETPROBE(KtretprobecalipsoVZVXsockVZVXgetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123376;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/calipso_sock_setattr")
int BPF_KPROBE(KtprobecalipsoVZVXsockVZVXsetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123378;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/calipso_sock_setattr")
int BPF_KRETPROBE(KtretprobecalipsoVZVXsockVZVXsetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123378;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/calipso_validate")
int BPF_KPROBE(KtprobecalipsoVZVXvalidate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123382;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/calipso_validate")
int BPF_KRETPROBE(KtretprobecalipsoVZVXvalidate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123382;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/compat_ip_get_mcast_msfilter")
int BPF_KPROBE(KtprobecompatVZVXipVZVXgetVZVXmcastVZVXmsfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123901;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/compat_ip_get_mcast_msfilter")
int BPF_KRETPROBE(KtretprobecompatVZVXipVZVXgetVZVXmcastVZVXmsfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123901;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/compat_ip_mcast_join_leave")
int BPF_KPROBE(KtprobecompatVZVXipVZVXmcastVZVXjoinVZVXleave)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123902;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/compat_ip_mcast_join_leave")
int BPF_KRETPROBE(KtretprobecompatVZVXipVZVXmcastVZVXjoinVZVXleave)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123902;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/compat_ip_set_mcast_msfilter")
int BPF_KPROBE(KtprobecompatVZVXipVZVXsetVZVXmcastVZVXmsfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123903;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/compat_ip_set_mcast_msfilter")
int BPF_KRETPROBE(KtretprobecompatVZVXipVZVXsetVZVXmcastVZVXmsfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123903;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/compat_ipv6_get_msfilter")
int BPF_KPROBE(KtprobecompatVZVXipv6VZVXgetVZVXmsfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/compat_ipv6_get_msfilter")
int BPF_KRETPROBE(KtretprobecompatVZVXipv6VZVXgetVZVXmsfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/compat_ipv6_mcast_join_leave")
int BPF_KPROBE(KtprobecompatVZVXipv6VZVXmcastVZVXjoinVZVXleave)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123905;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/compat_ipv6_mcast_join_leave")
int BPF_KRETPROBE(KtretprobecompatVZVXipv6VZVXmcastVZVXjoinVZVXleave)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123905;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/compat_ipv6_set_mcast_msfilter")
int BPF_KPROBE(KtprobecompatVZVXipv6VZVXsetVZVXmcastVZVXmsfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123906;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/compat_ipv6_set_mcast_msfilter")
int BPF_KRETPROBE(KtretprobecompatVZVXipv6VZVXsetVZVXmcastVZVXmsfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=123906;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/convert___skb_to_skb")
int BPF_KPROBE(KtprobeconvertVZVXVZVXVZVXskbVZVXtoVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=124023;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/convert___skb_to_skb")
int BPF_KRETPROBE(KtretprobeconvertVZVXVZVXVZVXskbVZVXtoVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=124023;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/convert_skb_to___skb")
int BPF_KPROBE(KtprobeconvertVZVXskbVZVXtoVZVXVZVXVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=124025;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/convert_skb_to___skb")
int BPF_KRETPROBE(KtretprobeconvertVZVXskbVZVXtoVZVXVZVXVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=124025;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/cookie_tcp_reqsk_alloc")
int BPF_KPROBE(KtprobecookieVZVXtcpVZVXreqskVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=124031;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/cookie_tcp_reqsk_alloc")
int BPF_KRETPROBE(KtretprobecookieVZVXtcpVZVXreqskVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=124031;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/cookie_tcp_reqsk_init")
int BPF_KPROBE(KtprobecookieVZVXtcpVZVXreqskVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=124033;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/cookie_tcp_reqsk_init")
int BPF_KRETPROBE(KtretprobecookieVZVXtcpVZVXreqskVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=124033;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/dev_mc_sync_multiple")
int BPF_KPROBE(KtprobedevVZVXmcVZVXsyncVZVXmultiple)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=124783;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/dev_mc_sync_multiple")
int BPF_KRETPROBE(KtretprobedevVZVXmcVZVXsyncVZVXmultiple)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=124783;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/dev_uc_sync_multiple")
int BPF_KPROBE(KtprobedevVZVXucVZVXsyncVZVXmultiple)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=124868;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/dev_uc_sync_multiple")
int BPF_KRETPROBE(KtretprobedevVZVXucVZVXsyncVZVXmultiple)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=124868;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/do_ip_getsockopt")
int BPF_KPROBE(KtprobedoVZVXipVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=125604;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/do_ip_getsockopt")
int BPF_KRETPROBE(KtretprobedoVZVXipVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=125604;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/do_ip_setsockopt")
int BPF_KPROBE(KtprobedoVZVXipVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=125605;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/do_ip_setsockopt")
int BPF_KRETPROBE(KtretprobedoVZVXipVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=125605;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/do_ipv6_getsockopt")
int BPF_KPROBE(KtprobedoVZVXipv6VZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=125606;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/do_ipv6_getsockopt")
int BPF_KRETPROBE(KtretprobedoVZVXipv6VZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=125606;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/do_ipv6_mcast_group_source")
int BPF_KPROBE(KtprobedoVZVXipv6VZVXmcastVZVXgroupVZVXsource)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=125607;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/do_ipv6_mcast_group_source")
int BPF_KRETPROBE(KtretprobedoVZVXipv6VZVXmcastVZVXgroupVZVXsource)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=125607;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/do_ipv6_setsockopt")
int BPF_KPROBE(KtprobedoVZVXipv6VZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=125608;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/do_ipv6_setsockopt")
int BPF_KRETPROBE(KtretprobedoVZVXipv6VZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=125608;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/do_tcp_getsockopt")
int BPF_KPROBE(KtprobedoVZVXtcpVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=125746;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/do_tcp_getsockopt")
int BPF_KRETPROBE(KtretprobedoVZVXtcpVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=125746;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/do_tcp_setsockopt")
int BPF_KPROBE(KtprobedoVZVXtcpVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=125747;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/do_tcp_setsockopt")
int BPF_KRETPROBE(KtretprobedoVZVXtcpVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=125747;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ethnl_module_fw_flash_sock_destroy")
int BPF_KPROBE(KtprobeethnlVZVXmoduleVZVXfwVZVXflashVZVXsockVZVXdestroy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=126659;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ethnl_module_fw_flash_sock_destroy")
int BPF_KRETPROBE(KtretprobeethnlVZVXmoduleVZVXfwVZVXflashVZVXsockVZVXdestroy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=126659;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ethnl_sock_priv_set")
int BPF_KPROBE(KtprobeethnlVZVXsockVZVXprivVZVXset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=126691;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ethnl_sock_priv_set")
int BPF_KRETPROBE(KtretprobeethnlVZVXsockVZVXprivVZVXset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=126691;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/fib_select_multipath")
int BPF_KPROBE(KtprobefibVZVXselectVZVXmultipath)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=127301;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/fib_select_multipath")
int BPF_KRETPROBE(KtretprobefibVZVXselectVZVXmultipath)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=127301;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/fl6_free_socklist")
int BPF_KPROBE(Ktprobefl6VZVXfreeVZVXsocklist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=127485;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/fl6_free_socklist")
int BPF_KRETPROBE(Ktretprobefl6VZVXfreeVZVXsocklist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=127485;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/get_tcp4_sock")
int BPF_KPROBE(KtprobegetVZVXtcp4VZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=128087;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/get_tcp4_sock")
int BPF_KRETPROBE(KtretprobegetVZVXtcp4VZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=128087;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/get_tcp6_sock")
int BPF_KPROBE(KtprobegetVZVXtcp6VZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=128089;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/get_tcp6_sock")
int BPF_KRETPROBE(KtretprobegetVZVXtcp6VZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=128089;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/gro_receive_skb")
int BPF_KPROBE(KtprobegroVZVXreceiveVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=128420;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/gro_receive_skb")
int BPF_KRETPROBE(KtretprobegroVZVXreceiveVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=128420;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/icmpv6_ndo_send")
int BPF_KPROBE(Ktprobeicmpv6VZVXndoVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=128910;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/icmpv6_ndo_send")
int BPF_KRETPROBE(Ktretprobeicmpv6VZVXndoVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=128910;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet6_bind_sk")
int BPF_KPROBE(Ktprobeinet6VZVXbindVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129056;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet6_bind_sk")
int BPF_KRETPROBE(Ktretprobeinet6VZVXbindVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129056;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet6_cleanup_sock")
int BPF_KPROBE(Ktprobeinet6VZVXcleanupVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129057;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet6_cleanup_sock")
int BPF_KRETPROBE(Ktretprobeinet6VZVXcleanupVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129057;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet6_recvmsg")
int BPF_KPROBE(Ktprobeinet6VZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129095;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet6_recvmsg")
int BPF_KRETPROBE(Ktretprobeinet6VZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129095;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet6_sendmsg")
int BPF_KPROBE(Ktprobeinet6VZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129103;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet6_sendmsg")
int BPF_KRETPROBE(Ktretprobeinet6VZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129103;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet6_sk_rebuild_header")
int BPF_KPROBE(Ktprobeinet6VZVXskVZVXrebuildVZVXheader)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129108;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet6_sk_rebuild_header")
int BPF_KRETPROBE(Ktretprobeinet6VZVXskVZVXrebuildVZVXheader)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129108;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet6_sk_rx_dst_set")
int BPF_KPROBE(Ktprobeinet6VZVXskVZVXrxVZVXdstVZVXset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129109;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet6_sk_rx_dst_set")
int BPF_KRETPROBE(Ktretprobeinet6VZVXskVZVXrxVZVXdstVZVXset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129109;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet6_sock_destruct")
int BPF_KPROBE(Ktprobeinet6VZVXsockVZVXdestruct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129110;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet6_sock_destruct")
int BPF_KRETPROBE(Ktretprobeinet6VZVXsockVZVXdestruct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129110;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet6_steal_sock")
int BPF_KPROBE(Ktprobeinet6VZVXstealVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129112;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet6_steal_sock")
int BPF_KRETPROBE(Ktretprobeinet6VZVXstealVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129112;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_bind_sk")
int BPF_KPROBE(KtprobeinetVZVXbindVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129122;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_bind_sk")
int BPF_KRETPROBE(KtretprobeinetVZVXbindVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129122;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_ctl_sock_create")
int BPF_KPROBE(KtprobeinetVZVXctlVZVXsockVZVXcreate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129128;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_ctl_sock_create")
int BPF_KRETPROBE(KtretprobeinetVZVXctlVZVXsockVZVXcreate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129128;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_recv_error")
int BPF_KPROBE(KtprobeinetVZVXrecvVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129149;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_recv_error")
int BPF_KRETPROBE(KtretprobeinetVZVXrecvVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129149;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_recvmsg")
int BPF_KPROBE(KtprobeinetVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129150;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_recvmsg")
int BPF_KRETPROBE(KtretprobeinetVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129150;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_send_prepare")
int BPF_KPROBE(KtprobeinetVZVXsendVZVXprepare)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129153;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_send_prepare")
int BPF_KRETPROBE(KtretprobeinetVZVXsendVZVXprepare)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129153;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_sendmsg")
int BPF_KPROBE(KtprobeinetVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129154;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_sendmsg")
int BPF_KRETPROBE(KtretprobeinetVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129154;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_sk_rebuild_header")
int BPF_KPROBE(KtprobeinetVZVXskVZVXrebuildVZVXheader)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129156;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_sk_rebuild_header")
int BPF_KRETPROBE(KtretprobeinetVZVXskVZVXrebuildVZVXheader)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129156;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_sk_reselect_saddr")
int BPF_KPROBE(KtprobeinetVZVXskVZVXreselectVZVXsaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129157;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_sk_reselect_saddr")
int BPF_KRETPROBE(KtretprobeinetVZVXskVZVXreselectVZVXsaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129157;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_sk_rx_dst_set")
int BPF_KPROBE(KtprobeinetVZVXskVZVXrxVZVXdstVZVXset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129158;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_sk_rx_dst_set")
int BPF_KRETPROBE(KtretprobeinetVZVXskVZVXrxVZVXdstVZVXset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129158;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_sk_set_state")
int BPF_KPROBE(KtprobeinetVZVXskVZVXsetVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129159;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_sk_set_state")
int BPF_KRETPROBE(KtretprobeinetVZVXskVZVXsetVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129159;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_sk_state_store")
int BPF_KPROBE(KtprobeinetVZVXskVZVXstateVZVXstore)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129161;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_sk_state_store")
int BPF_KRETPROBE(KtretprobeinetVZVXskVZVXstateVZVXstore)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129161;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_sock_destruct")
int BPF_KPROBE(KtprobeinetVZVXsockVZVXdestruct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129162;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_sock_destruct")
int BPF_KRETPROBE(KtretprobeinetVZVXsockVZVXdestruct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129162;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip4_datagram_connect")
int BPF_KPROBE(Ktprobeip4VZVXdatagramVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129686;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip4_datagram_connect")
int BPF_KRETPROBE(Ktretprobeip4VZVXdatagramVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129686;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip4_datagram_release_cb")
int BPF_KPROBE(Ktprobeip4VZVXdatagramVZVXreleaseVZVXcb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129687;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip4_datagram_release_cb")
int BPF_KRETPROBE(Ktretprobeip4VZVXdatagramVZVXreleaseVZVXcb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129687;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip4_frag_free")
int BPF_KPROBE(Ktprobeip4VZVXfragVZVXfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129688;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip4_frag_free")
int BPF_KRETPROBE(Ktretprobeip4VZVXfragVZVXfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129688;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip4_frag_init")
int BPF_KPROBE(Ktprobeip4VZVXfragVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129690;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip4_frag_init")
int BPF_KRETPROBE(Ktretprobeip4VZVXfragVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129690;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip4ip6_gro_complete")
int BPF_KPROBE(Ktprobeip4ip6VZVXgroVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129694;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip4ip6_gro_complete")
int BPF_KRETPROBE(Ktretprobeip4ip6VZVXgroVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129694;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip4ip6_gro_receive")
int BPF_KPROBE(Ktprobeip4ip6VZVXgroVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129695;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip4ip6_gro_receive")
int BPF_KRETPROBE(Ktretprobeip4ip6VZVXgroVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129695;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip4ip6_gso_segment")
int BPF_KPROBE(Ktprobeip4ip6VZVXgsoVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129696;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip4ip6_gso_segment")
int BPF_KRETPROBE(Ktretprobeip4ip6VZVXgsoVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129696;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_flowlabel_net_exit")
int BPF_KPROBE(Ktprobeip6VZVXflowlabelVZVXnetVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129700;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_flowlabel_net_exit")
int BPF_KRETPROBE(Ktretprobeip6VZVXflowlabelVZVXnetVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129700;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_flowlabel_proc_init")
int BPF_KPROBE(Ktprobeip6VZVXflowlabelVZVXprocVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129701;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_flowlabel_proc_init")
int BPF_KRETPROBE(Ktretprobeip6VZVXflowlabelVZVXprocVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129701;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_mr_forward")
int BPF_KPROBE(Ktprobeip6VZVXmrVZVXforward)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129704;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_mr_forward")
int BPF_KRETPROBE(Ktretprobeip6VZVXmrVZVXforward)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129704;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_mr_input")
int BPF_KPROBE(Ktprobeip6VZVXmrVZVXinput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129706;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_mr_input")
int BPF_KRETPROBE(Ktretprobeip6VZVXmrVZVXinput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129706;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_mr_output")
int BPF_KPROBE(Ktprobeip6VZVXmrVZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129707;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_mr_output")
int BPF_KRETPROBE(Ktretprobeip6VZVXmrVZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129707;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_mr_output_finish")
int BPF_KPROBE(Ktprobeip6VZVXmrVZVXoutputVZVXfinish)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129708;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_mr_output_finish")
int BPF_KRETPROBE(Ktretprobeip6VZVXmrVZVXoutputVZVXfinish)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129708;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_mroute_getsockopt")
int BPF_KPROBE(Ktprobeip6VZVXmrouteVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129709;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_mroute_getsockopt")
int BPF_KRETPROBE(Ktretprobeip6VZVXmrouteVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129709;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_mroute_setsockopt")
int BPF_KPROBE(Ktprobeip6VZVXmrouteVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129710;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_mroute_setsockopt")
int BPF_KRETPROBE(Ktretprobeip6VZVXmrouteVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129710;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_ra_control")
int BPF_KPROBE(Ktprobeip6VZVXraVZVXcontrol)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129712;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_ra_control")
int BPF_KRETPROBE(Ktretprobeip6VZVXraVZVXcontrol)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129712;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6ip6_gro_complete")
int BPF_KPROBE(Ktprobeip6ip6VZVXgroVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129717;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6ip6_gro_complete")
int BPF_KRETPROBE(Ktretprobeip6ip6VZVXgroVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129717;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6ip6_gso_segment")
int BPF_KPROBE(Ktprobeip6ip6VZVXgsoVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129718;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6ip6_gso_segment")
int BPF_KRETPROBE(Ktretprobeip6ip6VZVXgsoVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129718;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_cache_find_any")
int BPF_KPROBE(Ktprobeip6mrVZVXcacheVZVXfindVZVXany)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129720;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_cache_find_any")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXcacheVZVXfindVZVXany)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129720;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_cache_report")
int BPF_KPROBE(Ktprobeip6mrVZVXcacheVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129723;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_cache_report")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXcacheVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129723;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_cache_unresolved")
int BPF_KPROBE(Ktprobeip6mrVZVXcacheVZVXunresolved)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129725;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_cache_unresolved")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXcacheVZVXunresolved)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129725;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_compat_ioctl")
int BPF_KPROBE(Ktprobeip6mrVZVXcompatVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129726;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_compat_ioctl")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXcompatVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129726;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_destroy_unres")
int BPF_KPROBE(Ktprobeip6mrVZVXdestroyVZVXunres)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129728;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_destroy_unres")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXdestroyVZVXunres)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129728;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_dump")
int BPF_KPROBE(Ktprobeip6mrVZVXdump)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129730;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_dump")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXdump)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129730;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_fib_lookup")
int BPF_KPROBE(Ktprobeip6mrVZVXfibVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129732;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_fib_lookup")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXfibVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129732;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_fill_mroute")
int BPF_KPROBE(Ktprobeip6mrVZVXfillVZVXmroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129734;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_fill_mroute")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXfillVZVXmroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129734;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_forward2")
int BPF_KPROBE(Ktprobeip6mrVZVXforward2)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129736;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_forward2")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXforward2)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129736;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_forward2_finish")
int BPF_KPROBE(Ktprobeip6mrVZVXforward2VZVXfinish)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129737;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_forward2_finish")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXforward2VZVXfinish)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129737;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_free_table")
int BPF_KPROBE(Ktprobeip6mrVZVXfreeVZVXtable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129738;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_free_table")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXfreeVZVXtable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129738;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_get_route")
int BPF_KPROBE(Ktprobeip6mrVZVXgetVZVXroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129740;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_get_route")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXgetVZVXroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129740;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_ioctl")
int BPF_KPROBE(Ktprobeip6mrVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129742;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_ioctl")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129742;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_mfc_add")
int BPF_KPROBE(Ktprobeip6mrVZVXmfcVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129744;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_mfc_add")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXmfcVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129744;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_mr_table_iter")
int BPF_KPROBE(Ktprobeip6mrVZVXmrVZVXtableVZVXiter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129745;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_mr_table_iter")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXmrVZVXtableVZVXiter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129745;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_net_exit")
int BPF_KPROBE(Ktprobeip6mrVZVXnetVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129746;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_net_exit")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXnetVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129746;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_net_init")
int BPF_KPROBE(Ktprobeip6mrVZVXnetVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129748;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_net_init")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXnetVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129748;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_new_table_set")
int BPF_KPROBE(Ktprobeip6mrVZVXnewVZVXtableVZVXset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129749;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_new_table_set")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXnewVZVXtableVZVXset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129749;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_prepare_xmit")
int BPF_KPROBE(Ktprobeip6mrVZVXprepareVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129750;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_prepare_xmit")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXprepareVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129750;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_rtm_dumproute")
int BPF_KPROBE(Ktprobeip6mrVZVXrtmVZVXdumproute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129751;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_rtm_dumproute")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXrtmVZVXdumproute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129751;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_rtm_getroute")
int BPF_KPROBE(Ktprobeip6mrVZVXrtmVZVXgetroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129752;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_rtm_getroute")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXrtmVZVXgetroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129752;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_rule_action")
int BPF_KPROBE(Ktprobeip6mrVZVXruleVZVXaction)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129753;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_rule_action")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXruleVZVXaction)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129753;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_rule_compare")
int BPF_KPROBE(Ktprobeip6mrVZVXruleVZVXcompare)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129754;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_rule_compare")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXruleVZVXcompare)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129754;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_rule_configure")
int BPF_KPROBE(Ktprobeip6mrVZVXruleVZVXconfigure)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129755;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_rule_configure")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXruleVZVXconfigure)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129755;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_rule_default")
int BPF_KPROBE(Ktprobeip6mrVZVXruleVZVXdefault)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129756;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_rule_default")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXruleVZVXdefault)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129756;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_rule_fill")
int BPF_KPROBE(Ktprobeip6mrVZVXruleVZVXfill)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129757;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_rule_fill")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXruleVZVXfill)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129757;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_rule_match")
int BPF_KPROBE(Ktprobeip6mrVZVXruleVZVXmatch)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129759;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_rule_match")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXruleVZVXmatch)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129759;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_rules_dump")
int BPF_KPROBE(Ktprobeip6mrVZVXrulesVZVXdump)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129760;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_rules_dump")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXrulesVZVXdump)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129760;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_rules_exit")
int BPF_KPROBE(Ktprobeip6mrVZVXrulesVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129761;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_rules_exit")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXrulesVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129761;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_seq_read")
int BPF_KPROBE(Ktprobeip6mrVZVXseqVZVXread)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129762;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_seq_read")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXseqVZVXread)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129762;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_sk_done")
int BPF_KPROBE(Ktprobeip6mrVZVXskVZVXdone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129763;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_sk_done")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXskVZVXdone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129763;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6mr_update_thresholds")
int BPF_KPROBE(Ktprobeip6mrVZVXupdateVZVXthresholds)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129764;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6mr_update_thresholds")
int BPF_KRETPROBE(Ktretprobeip6mrVZVXupdateVZVXthresholds)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129764;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_check_defrag")
int BPF_KPROBE(KtprobeipVZVXcheckVZVXdefrag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129769;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_check_defrag")
int BPF_KRETPROBE(KtretprobeipVZVXcheckVZVXdefrag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129769;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_cmsg_recv_offset")
int BPF_KPROBE(KtprobeipVZVXcmsgVZVXrecvVZVXoffset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129771;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_cmsg_recv_offset")
int BPF_KRETPROBE(KtretprobeipVZVXcmsgVZVXrecvVZVXoffset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129771;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_cmsg_send")
int BPF_KPROBE(KtprobeipVZVXcmsgVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129773;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_cmsg_send")
int BPF_KRETPROBE(KtretprobeipVZVXcmsgVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129773;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_defrag")
int BPF_KPROBE(KtprobeipVZVXdefrag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129775;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_defrag")
int BPF_KRETPROBE(KtretprobeipVZVXdefrag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129775;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_fib_check_default")
int BPF_KPROBE(KtprobeipVZVXfibVZVXcheckVZVXdefault)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129778;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_fib_check_default")
int BPF_KRETPROBE(KtretprobeipVZVXfibVZVXcheckVZVXdefault)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129778;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_frag_queue")
int BPF_KPROBE(KtprobeipVZVXfragVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129780;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_frag_queue")
int BPF_KRETPROBE(KtretprobeipVZVXfragVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129780;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_frag_reasm")
int BPF_KPROBE(KtprobeipVZVXfragVZVXreasm)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129782;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_frag_reasm")
int BPF_KRETPROBE(KtretprobeipVZVXfragVZVXreasm)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129782;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_get_mcast_msfilter")
int BPF_KPROBE(KtprobeipVZVXgetVZVXmcastVZVXmsfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129783;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_get_mcast_msfilter")
int BPF_KRETPROBE(KtretprobeipVZVXgetVZVXmcastVZVXmsfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129783;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_getsockopt")
int BPF_KPROBE(KtprobeipVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129784;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_getsockopt")
int BPF_KRETPROBE(KtretprobeipVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129784;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_icmp_error")
int BPF_KPROBE(KtprobeipVZVXicmpVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129785;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_icmp_error")
int BPF_KRETPROBE(KtretprobeipVZVXicmpVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129785;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_local_error")
int BPF_KPROBE(KtprobeipVZVXlocalVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129787;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_local_error")
int BPF_KRETPROBE(KtretprobeipVZVXlocalVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129787;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mcast_join_leave")
int BPF_KPROBE(KtprobeipVZVXmcastVZVXjoinVZVXleave)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129788;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mcast_join_leave")
int BPF_KRETPROBE(KtretprobeipVZVXmcastVZVXjoinVZVXleave)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129788;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_ra_control")
int BPF_KPROBE(KtprobeipVZVXraVZVXcontrol)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129790;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_ra_control")
int BPF_KRETPROBE(KtretprobeipVZVXraVZVXcontrol)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129790;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_recv_error")
int BPF_KPROBE(KtprobeipVZVXrecvVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129792;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_recv_error")
int BPF_KRETPROBE(KtretprobeipVZVXrecvVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129792;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_setsockopt")
int BPF_KPROBE(KtprobeipVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129793;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_setsockopt")
int BPF_KRETPROBE(KtretprobeipVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129793;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_sock_set_freebind")
int BPF_KPROBE(KtprobeipVZVXsockVZVXsetVZVXfreebind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129794;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_sock_set_freebind")
int BPF_KRETPROBE(KtretprobeipVZVXsockVZVXsetVZVXfreebind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129794;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_sock_set_mtu_discover")
int BPF_KPROBE(KtprobeipVZVXsockVZVXsetVZVXmtuVZVXdiscover)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129795;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_sock_set_mtu_discover")
int BPF_KRETPROBE(KtretprobeipVZVXsockVZVXsetVZVXmtuVZVXdiscover)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129795;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_sock_set_pktinfo")
int BPF_KPROBE(KtprobeipVZVXsockVZVXsetVZVXpktinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129796;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_sock_set_pktinfo")
int BPF_KRETPROBE(KtretprobeipVZVXsockVZVXsetVZVXpktinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129796;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_sock_set_recverr")
int BPF_KPROBE(KtprobeipVZVXsockVZVXsetVZVXrecverr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129797;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_sock_set_recverr")
int BPF_KRETPROBE(KtretprobeipVZVXsockVZVXsetVZVXrecverr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129797;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_sock_set_tos")
int BPF_KPROBE(KtprobeipVZVXsockVZVXsetVZVXtos)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129798;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_sock_set_tos")
int BPF_KRETPROBE(KtretprobeipVZVXsockVZVXsetVZVXtos)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129798;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipcns_install")
int BPF_KPROBE(KtprobeipcnsVZVXinstall)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129800;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipcns_install")
int BPF_KRETPROBE(KtretprobeipcnsVZVXinstall)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129800;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipip_gro_complete")
int BPF_KPROBE(KtprobeipipVZVXgroVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129816;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipip_gro_complete")
int BPF_KRETPROBE(KtretprobeipipVZVXgroVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129816;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipip_gro_receive")
int BPF_KPROBE(KtprobeipipVZVXgroVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129817;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipip_gro_receive")
int BPF_KRETPROBE(KtretprobeipipVZVXgroVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129817;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipip_gso_segment")
int BPF_KPROBE(KtprobeipipVZVXgsoVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129818;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipip_gso_segment")
int BPF_KRETPROBE(KtretprobeipipVZVXgsoVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129818;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipmr_do_expire_process")
int BPF_KPROBE(KtprobeipmrVZVXdoVZVXexpireVZVXprocess)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129823;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipmr_do_expire_process")
int BPF_KRETPROBE(KtretprobeipmrVZVXdoVZVXexpireVZVXprocess)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129823;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv4_frags_exit_net")
int BPF_KPROBE(Ktprobeipv4VZVXfragsVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129824;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv4_frags_exit_net")
int BPF_KRETPROBE(Ktretprobeipv4VZVXfragsVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129824;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv4_frags_init_net")
int BPF_KPROBE(Ktprobeipv4VZVXfragsVZVXinitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129825;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv4_frags_init_net")
int BPF_KRETPROBE(Ktretprobeipv4VZVXfragsVZVXinitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129825;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv4_frags_pre_exit_net")
int BPF_KPROBE(Ktprobeipv4VZVXfragsVZVXpreVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129826;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv4_frags_pre_exit_net")
int BPF_KRETPROBE(Ktretprobeipv4VZVXfragsVZVXpreVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129826;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv4_mib_exit_net")
int BPF_KPROBE(Ktprobeipv4VZVXmibVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129829;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv4_mib_exit_net")
int BPF_KRETPROBE(Ktretprobeipv4VZVXmibVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129829;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv4_mib_init_net")
int BPF_KPROBE(Ktprobeipv4VZVXmibVZVXinitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129830;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv4_mib_init_net")
int BPF_KRETPROBE(Ktretprobeipv4VZVXmibVZVXinitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129830;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv4_pktinfo_prepare")
int BPF_KPROBE(Ktprobeipv4VZVXpktinfoVZVXprepare)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129834;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv4_pktinfo_prepare")
int BPF_KRETPROBE(Ktretprobeipv4VZVXpktinfoVZVXprepare)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129834;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv4_sysctl_exit_net")
int BPF_KPROBE(Ktprobeipv4VZVXsysctlVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129836;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv4_sysctl_exit_net")
int BPF_KRETPROBE(Ktretprobeipv4VZVXsysctlVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129836;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv4_sysctl_init_net")
int BPF_KPROBE(Ktprobeipv4VZVXsysctlVZVXinitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129837;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv4_sysctl_init_net")
int BPF_KRETPROBE(Ktretprobeipv4VZVXsysctlVZVXinitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129837;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_add_addr")
int BPF_KPROBE(Ktprobeipv6VZVXaddVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129839;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_add_addr")
int BPF_KRETPROBE(Ktretprobeipv6VZVXaddVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129839;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_add_dev")
int BPF_KPROBE(Ktprobeipv6VZVXaddVZVXdev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129840;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_add_dev")
int BPF_KRETPROBE(Ktretprobeipv6VZVXaddVZVXdev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129840;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_chk_addr")
int BPF_KPROBE(Ktprobeipv6VZVXchkVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129841;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_chk_addr")
int BPF_KRETPROBE(Ktretprobeipv6VZVXchkVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129841;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_chk_addr_and_flags")
int BPF_KPROBE(Ktprobeipv6VZVXchkVZVXaddrVZVXandVZVXflags)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129843;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_chk_addr_and_flags")
int BPF_KRETPROBE(Ktretprobeipv6VZVXchkVZVXaddrVZVXandVZVXflags)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129843;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_chk_custom_prefix")
int BPF_KPROBE(Ktprobeipv6VZVXchkVZVXcustomVZVXprefix)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129845;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_chk_custom_prefix")
int BPF_KRETPROBE(Ktretprobeipv6VZVXchkVZVXcustomVZVXprefix)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129845;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_chk_home_addr")
int BPF_KPROBE(Ktprobeipv6VZVXchkVZVXhomeVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129847;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_chk_home_addr")
int BPF_KRETPROBE(Ktretprobeipv6VZVXchkVZVXhomeVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129847;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_chk_prefix")
int BPF_KPROBE(Ktprobeipv6VZVXchkVZVXprefix)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129849;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_chk_prefix")
int BPF_KRETPROBE(Ktretprobeipv6VZVXchkVZVXprefix)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129849;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_chk_rpl_srh_loop")
int BPF_KPROBE(Ktprobeipv6VZVXchkVZVXrplVZVXsrhVZVXloop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129851;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_chk_rpl_srh_loop")
int BPF_KRETPROBE(Ktretprobeipv6VZVXchkVZVXrplVZVXsrhVZVXloop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129851;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_create_tempaddr")
int BPF_KPROBE(Ktprobeipv6VZVXcreateVZVXtempaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129853;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_create_tempaddr")
int BPF_KRETPROBE(Ktretprobeipv6VZVXcreateVZVXtempaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129853;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_del_addr")
int BPF_KPROBE(Ktprobeipv6VZVXdelVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129854;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_del_addr")
int BPF_KRETPROBE(Ktretprobeipv6VZVXdelVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129854;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_dev_find")
int BPF_KPROBE(Ktprobeipv6VZVXdevVZVXfind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129855;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_dev_find")
int BPF_KRETPROBE(Ktretprobeipv6VZVXdevVZVXfind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129855;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_dev_get_saddr")
int BPF_KPROBE(Ktprobeipv6VZVXdevVZVXgetVZVXsaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129857;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_dev_get_saddr")
int BPF_KRETPROBE(Ktretprobeipv6VZVXdevVZVXgetVZVXsaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129857;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_find_idev")
int BPF_KPROBE(Ktprobeipv6VZVXfindVZVXidev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129858;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_find_idev")
int BPF_KRETPROBE(Ktretprobeipv6VZVXfindVZVXidev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129858;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_flowlabel_get")
int BPF_KPROBE(Ktprobeipv6VZVXflowlabelVZVXget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129860;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_flowlabel_get")
int BPF_KRETPROBE(Ktretprobeipv6VZVXflowlabelVZVXget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129860;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_flowlabel_opt")
int BPF_KPROBE(Ktprobeipv6VZVXflowlabelVZVXopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129861;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_flowlabel_opt")
int BPF_KRETPROBE(Ktretprobeipv6VZVXflowlabelVZVXopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129861;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_flowlabel_opt_get")
int BPF_KPROBE(Ktprobeipv6VZVXflowlabelVZVXoptVZVXget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129863;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_flowlabel_opt_get")
int BPF_KRETPROBE(Ktretprobeipv6VZVXflowlabelVZVXoptVZVXget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129863;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_generate_eui64")
int BPF_KPROBE(Ktprobeipv6VZVXgenerateVZVXeui64)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129864;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_generate_eui64")
int BPF_KRETPROBE(Ktretprobeipv6VZVXgenerateVZVXeui64)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129864;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_generate_stable_address")
int BPF_KPROBE(Ktprobeipv6VZVXgenerateVZVXstableVZVXaddress)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129866;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_generate_stable_address")
int BPF_KRETPROBE(Ktretprobeipv6VZVXgenerateVZVXstableVZVXaddress)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129866;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_get_ifaddr")
int BPF_KPROBE(Ktprobeipv6VZVXgetVZVXifaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129868;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_get_ifaddr")
int BPF_KRETPROBE(Ktretprobeipv6VZVXgetVZVXifaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129868;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_get_lladdr")
int BPF_KPROBE(Ktprobeipv6VZVXgetVZVXlladdr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129870;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_get_lladdr")
int BPF_KRETPROBE(Ktretprobeipv6VZVXgetVZVXlladdr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129870;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_get_msfilter")
int BPF_KPROBE(Ktprobeipv6VZVXgetVZVXmsfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129871;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_get_msfilter")
int BPF_KRETPROBE(Ktretprobeipv6VZVXgetVZVXmsfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129871;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_get_saddr_eval")
int BPF_KPROBE(Ktprobeipv6VZVXgetVZVXsaddrVZVXeval)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129873;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_get_saddr_eval")
int BPF_KRETPROBE(Ktretprobeipv6VZVXgetVZVXsaddrVZVXeval)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129873;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_getsockopt")
int BPF_KPROBE(Ktprobeipv6VZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129874;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_getsockopt")
int BPF_KRETPROBE(Ktretprobeipv6VZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129874;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_gro_complete")
int BPF_KPROBE(Ktprobeipv6VZVXgroVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129875;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_gro_complete")
int BPF_KRETPROBE(Ktretprobeipv6VZVXgroVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129875;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_gro_receive")
int BPF_KPROBE(Ktprobeipv6VZVXgroVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129876;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_gro_receive")
int BPF_KRETPROBE(Ktretprobeipv6VZVXgroVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129876;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_gso_segment")
int BPF_KPROBE(Ktprobeipv6VZVXgsoVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129877;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_gso_segment")
int BPF_KRETPROBE(Ktretprobeipv6VZVXgsoVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129877;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_hopopt_jumbo_remove")
int BPF_KPROBE(Ktprobeipv6VZVXhopoptVZVXjumboVZVXremove)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129878;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_hopopt_jumbo_remove")
int BPF_KRETPROBE(Ktretprobeipv6VZVXhopoptVZVXjumboVZVXremove)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129878;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_inherit_eui64")
int BPF_KPROBE(Ktprobeipv6VZVXinheritVZVXeui64)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129880;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_inherit_eui64")
int BPF_KRETPROBE(Ktretprobeipv6VZVXinheritVZVXeui64)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129880;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_init_mibs")
int BPF_KPROBE(Ktprobeipv6VZVXinitVZVXmibs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129881;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_init_mibs")
int BPF_KRETPROBE(Ktretprobeipv6VZVXinitVZVXmibs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129881;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_mc_check_mld")
int BPF_KPROBE(Ktprobeipv6VZVXmcVZVXcheckVZVXmld)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129882;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_mc_check_mld")
int BPF_KRETPROBE(Ktretprobeipv6VZVXmcVZVXcheckVZVXmld)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129882;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_mc_check_mld_msg")
int BPF_KPROBE(Ktprobeipv6VZVXmcVZVXcheckVZVXmldVZVXmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129883;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_mc_check_mld_msg")
int BPF_KRETPROBE(Ktretprobeipv6VZVXmcVZVXcheckVZVXmldVZVXmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129883;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_mc_config")
int BPF_KPROBE(Ktprobeipv6VZVXmcVZVXconfig)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129885;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_mc_config")
int BPF_KRETPROBE(Ktretprobeipv6VZVXmcVZVXconfig)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129885;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_mc_validate_checksum")
int BPF_KPROBE(Ktprobeipv6VZVXmcVZVXvalidateVZVXchecksum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129886;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_mc_validate_checksum")
int BPF_KRETPROBE(Ktretprobeipv6VZVXmcVZVXvalidateVZVXchecksum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129886;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_mcast_join_leave")
int BPF_KPROBE(Ktprobeipv6VZVXmcastVZVXjoinVZVXleave)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129887;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_mcast_join_leave")
int BPF_KRETPROBE(Ktretprobeipv6VZVXmcastVZVXjoinVZVXleave)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129887;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_opt_accepted")
int BPF_KPROBE(Ktprobeipv6VZVXoptVZVXaccepted)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129891;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_opt_accepted")
int BPF_KRETPROBE(Ktretprobeipv6VZVXoptVZVXaccepted)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129891;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_raw_deliver")
int BPF_KPROBE(Ktprobeipv6VZVXrawVZVXdeliver)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129893;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_raw_deliver")
int BPF_KRETPROBE(Ktretprobeipv6VZVXrawVZVXdeliver)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129893;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_route_input")
int BPF_KPROBE(Ktprobeipv6VZVXrouteVZVXinput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129894;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_route_input")
int BPF_KRETPROBE(Ktretprobeipv6VZVXrouteVZVXinput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129894;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_set_mcast_msfilter")
int BPF_KPROBE(Ktprobeipv6VZVXsetVZVXmcastVZVXmsfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129895;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_set_mcast_msfilter")
int BPF_KRETPROBE(Ktretprobeipv6VZVXsetVZVXmcastVZVXmsfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129895;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_set_opt_hdr")
int BPF_KPROBE(Ktprobeipv6VZVXsetVZVXoptVZVXhdr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129896;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_set_opt_hdr")
int BPF_KRETPROBE(Ktretprobeipv6VZVXsetVZVXoptVZVXhdr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129896;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_setsockopt")
int BPF_KPROBE(Ktprobeipv6VZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129897;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_setsockopt")
int BPF_KRETPROBE(Ktretprobeipv6VZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129897;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_update_options")
int BPF_KPROBE(Ktprobeipv6VZVXupdateVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129901;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_update_options")
int BPF_KRETPROBE(Ktretprobeipv6VZVXupdateVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=129901;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/kfree_skb_list_reason")
int BPF_KPROBE(KtprobekfreeVZVXskbVZVXlistVZVXreason)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=130316;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/kfree_skb_list_reason")
int BPF_KRETPROBE(KtretprobekfreeVZVXskbVZVXlistVZVXreason)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=130316;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/kfree_skb_partial")
int BPF_KPROBE(KtprobekfreeVZVXskbVZVXpartial)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=130318;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/kfree_skb_partial")
int BPF_KRETPROBE(KtretprobekfreeVZVXskbVZVXpartial)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=130318;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mem_cgroup_sk_alloc")
int BPF_KPROBE(KtprobememVZVXcgroupVZVXskVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131136;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mem_cgroup_sk_alloc")
int BPF_KRETPROBE(KtretprobememVZVXcgroupVZVXskVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131136;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mem_cgroup_sk_free")
int BPF_KPROBE(KtprobememVZVXcgroupVZVXskVZVXfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131137;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mem_cgroup_sk_free")
int BPF_KRETPROBE(KtretprobememVZVXcgroupVZVXskVZVXfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131137;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mem_cgroup_sk_inherit")
int BPF_KPROBE(KtprobememVZVXcgroupVZVXskVZVXinherit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131138;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mem_cgroup_sk_inherit")
int BPF_KRETPROBE(KtretprobememVZVXcgroupVZVXskVZVXinherit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131138;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_active_detect_blackhole")
int BPF_KPROBE(KtprobemptcpVZVXactiveVZVXdetectVZVXblackhole)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131808;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_active_detect_blackhole")
int BPF_KRETPROBE(KtretprobemptcpVZVXactiveVZVXdetectVZVXblackhole)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131808;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_active_disable")
int BPF_KPROBE(KtprobemptcpVZVXactiveVZVXdisable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131809;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_active_disable")
int BPF_KRETPROBE(KtretprobemptcpVZVXactiveVZVXdisable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131809;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_active_enable")
int BPF_KPROBE(KtprobemptcpVZVXactiveVZVXenable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131810;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_active_enable")
int BPF_KRETPROBE(KtretprobemptcpVZVXactiveVZVXenable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131810;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_active_should_disable")
int BPF_KPROBE(KtprobemptcpVZVXactiveVZVXshouldVZVXdisable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131812;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_active_should_disable")
int BPF_KRETPROBE(KtretprobemptcpVZVXactiveVZVXshouldVZVXdisable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131812;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_allow_join_id0")
int BPF_KPROBE(KtprobemptcpVZVXallowVZVXjoinVZVXid0)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131814;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_allow_join_id0")
int BPF_KRETPROBE(KtretprobemptcpVZVXallowVZVXjoinVZVXid0)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131814;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_can_accept_new_subflow")
int BPF_KPROBE(KtprobemptcpVZVXcanVZVXacceptVZVXnewVZVXsubflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131816;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_can_accept_new_subflow")
int BPF_KRETPROBE(KtretprobemptcpVZVXcanVZVXacceptVZVXnewVZVXsubflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131816;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_close_timeout")
int BPF_KPROBE(KtprobemptcpVZVXcloseVZVXtimeout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131818;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_close_timeout")
int BPF_KRETPROBE(KtretprobemptcpVZVXcloseVZVXtimeout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131818;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_event")
int BPF_KPROBE(KtprobemptcpVZVXevent)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131824;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_event")
int BPF_KRETPROBE(KtretprobemptcpVZVXevent)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131824;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_event_add_subflow")
int BPF_KPROBE(KtprobemptcpVZVXeventVZVXaddVZVXsubflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131826;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_event_add_subflow")
int BPF_KRETPROBE(KtretprobemptcpVZVXeventVZVXaddVZVXsubflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131826;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_event_addr_announced")
int BPF_KPROBE(KtprobemptcpVZVXeventVZVXaddrVZVXannounced)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131828;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_event_addr_announced")
int BPF_KRETPROBE(KtretprobemptcpVZVXeventVZVXaddrVZVXannounced)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131828;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_event_addr_removed")
int BPF_KPROBE(KtprobemptcpVZVXeventVZVXaddrVZVXremoved)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131830;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_event_addr_removed")
int BPF_KRETPROBE(KtretprobemptcpVZVXeventVZVXaddrVZVXremoved)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131830;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_event_pm_listener")
int BPF_KPROBE(KtprobemptcpVZVXeventVZVXpmVZVXlistener)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131832;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_event_pm_listener")
int BPF_KRETPROBE(KtretprobemptcpVZVXeventVZVXpmVZVXlistener)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131832;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_event_put_token_and_ssk")
int BPF_KPROBE(KtprobemptcpVZVXeventVZVXputVZVXtokenVZVXandVZVXssk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131834;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_event_put_token_and_ssk")
int BPF_KRETPROBE(KtretprobemptcpVZVXeventVZVXputVZVXtokenVZVXandVZVXssk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131834;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_get_add_addr_timeout")
int BPF_KPROBE(KtprobemptcpVZVXgetVZVXaddVZVXaddrVZVXtimeout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131835;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_get_add_addr_timeout")
int BPF_KRETPROBE(KtretprobemptcpVZVXgetVZVXaddVZVXaddrVZVXtimeout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131835;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_get_path_manager")
int BPF_KPROBE(KtprobemptcpVZVXgetVZVXpathVZVXmanager)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131838;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_get_path_manager")
int BPF_KRETPROBE(KtretprobemptcpVZVXgetVZVXpathVZVXmanager)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131838;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_get_pm_type")
int BPF_KPROBE(KtprobemptcpVZVXgetVZVXpmVZVXtype)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131839;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_get_pm_type")
int BPF_KRETPROBE(KtretprobemptcpVZVXgetVZVXpmVZVXtype)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131839;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_get_scheduler")
int BPF_KPROBE(KtprobemptcpVZVXgetVZVXscheduler)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131840;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_get_scheduler")
int BPF_KRETPROBE(KtretprobemptcpVZVXgetVZVXscheduler)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131840;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_init_sched")
int BPF_KPROBE(KtprobemptcpVZVXinitVZVXsched)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131845;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_init_sched")
int BPF_KRETPROBE(KtretprobemptcpVZVXinitVZVXsched)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131845;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_is_checksum_enabled")
int BPF_KPROBE(KtprobemptcpVZVXisVZVXchecksumVZVXenabled)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131846;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_is_checksum_enabled")
int BPF_KRETPROBE(KtretprobemptcpVZVXisVZVXchecksumVZVXenabled)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131846;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_is_enabled")
int BPF_KPROBE(KtprobemptcpVZVXisVZVXenabled)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131847;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_is_enabled")
int BPF_KRETPROBE(KtretprobemptcpVZVXisVZVXenabled)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131847;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_join_entry_hash")
int BPF_KPROBE(KtprobemptcpVZVXjoinVZVXentryVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131850;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_join_entry_hash")
int BPF_KRETPROBE(KtretprobemptcpVZVXjoinVZVXentryVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131850;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_net_exit")
int BPF_KPROBE(KtprobemptcpVZVXnetVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131851;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_net_exit")
int BPF_KRETPROBE(KtretprobemptcpVZVXnetVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131851;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_net_init")
int BPF_KPROBE(KtprobemptcpVZVXnetVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131852;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_net_init")
int BPF_KRETPROBE(KtretprobemptcpVZVXnetVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131852;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_nl_fill_addr")
int BPF_KPROBE(KtprobemptcpVZVXnlVZVXfillVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131854;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_nl_fill_addr")
int BPF_KRETPROBE(KtretprobemptcpVZVXnlVZVXfillVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131854;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_nl_mcast_send")
int BPF_KPROBE(KtprobemptcpVZVXnlVZVXmcastVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131856;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_nl_mcast_send")
int BPF_KRETPROBE(KtretprobemptcpVZVXnlVZVXmcastVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131856;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_genl_fill_addr")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXgenlVZVXfillVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131858;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_genl_fill_addr")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXgenlVZVXfillVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131858;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_nl_get_addr_doit")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXnlVZVXgetVZVXaddrVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131859;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_nl_get_addr_doit")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXnlVZVXgetVZVXaddrVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131859;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_nl_get_addr_dumpit")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXnlVZVXgetVZVXaddrVZVXdumpit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131860;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_nl_get_addr_dumpit")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXnlVZVXgetVZVXaddrVZVXdumpit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131860;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_nl_set_flags_doit")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXnlVZVXsetVZVXflagsVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131862;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_nl_set_flags_doit")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXnlVZVXsetVZVXflagsVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131862;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_parse_entry")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXparseVZVXentry)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131866;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_parse_entry")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXparseVZVXentry)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131866;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_propagate_state")
int BPF_KPROBE(KtprobemptcpVZVXpropagateVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131870;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_propagate_state")
int BPF_KRETPROBE(KtretprobemptcpVZVXpropagateVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131870;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_release_sched")
int BPF_KPROBE(KtprobemptcpVZVXreleaseVZVXsched)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131873;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_release_sched")
int BPF_KRETPROBE(KtretprobemptcpVZVXreleaseVZVXsched)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131873;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_sched_default_get_retrans")
int BPF_KPROBE(KtprobemptcpVZVXschedVZVXdefaultVZVXgetVZVXretrans)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131875;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_sched_default_get_retrans")
int BPF_KRETPROBE(KtretprobemptcpVZVXschedVZVXdefaultVZVXgetVZVXretrans)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131875;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_sched_default_get_send")
int BPF_KPROBE(KtprobemptcpVZVXschedVZVXdefaultVZVXgetVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131876;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_sched_default_get_send")
int BPF_KRETPROBE(KtretprobemptcpVZVXschedVZVXdefaultVZVXgetVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131876;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_sched_get_retrans")
int BPF_KPROBE(KtprobemptcpVZVXschedVZVXgetVZVXretrans)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131879;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_sched_get_retrans")
int BPF_KRETPROBE(KtretprobemptcpVZVXschedVZVXgetVZVXretrans)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131879;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_sched_get_send")
int BPF_KPROBE(KtprobemptcpVZVXschedVZVXgetVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131880;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_sched_get_send")
int BPF_KRETPROBE(KtretprobemptcpVZVXschedVZVXgetVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131880;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_space")
int BPF_KPROBE(KtprobemptcpVZVXspace)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131887;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_space")
int BPF_KRETPROBE(KtretprobemptcpVZVXspace)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131887;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_stale_loss_cnt")
int BPF_KPROBE(KtprobemptcpVZVXstaleVZVXlossVZVXcnt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131888;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_stale_loss_cnt")
int BPF_KRETPROBE(KtretprobemptcpVZVXstaleVZVXlossVZVXcnt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131888;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_stream_memory_free")
int BPF_KPROBE(KtprobemptcpVZVXstreamVZVXmemoryVZVXfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131890;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_stream_memory_free")
int BPF_KRETPROBE(KtretprobemptcpVZVXstreamVZVXmemoryVZVXfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131890;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_subflow_create_socket")
int BPF_KPROBE(KtprobemptcpVZVXsubflowVZVXcreateVZVXsocket)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131892;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_subflow_create_socket")
int BPF_KRETPROBE(KtretprobemptcpVZVXsubflowVZVXcreateVZVXsocket)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131892;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_subflow_data_available")
int BPF_KPROBE(KtprobemptcpVZVXsubflowVZVXdataVZVXavailable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131893;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_subflow_data_available")
int BPF_KRETPROBE(KtretprobemptcpVZVXsubflowVZVXdataVZVXavailable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131893;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_subflow_drop_ctx")
int BPF_KPROBE(KtprobemptcpVZVXsubflowVZVXdropVZVXctx)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131895;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_subflow_drop_ctx")
int BPF_KRETPROBE(KtretprobemptcpVZVXsubflowVZVXdropVZVXctx)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131895;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_subflow_init_cookie_req")
int BPF_KPROBE(KtprobemptcpVZVXsubflowVZVXinitVZVXcookieVZVXreq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131898;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_subflow_init_cookie_req")
int BPF_KRETPROBE(KtretprobemptcpVZVXsubflowVZVXinitVZVXcookieVZVXreq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131898;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_subflow_queue_clean")
int BPF_KPROBE(KtprobemptcpVZVXsubflowVZVXqueueVZVXclean)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131900;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_subflow_queue_clean")
int BPF_KRETPROBE(KtretprobemptcpVZVXsubflowVZVXqueueVZVXclean)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131900;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_subflow_reqsk_alloc")
int BPF_KPROBE(KtprobemptcpVZVXsubflowVZVXreqskVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131901;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_subflow_reqsk_alloc")
int BPF_KRETPROBE(KtretprobemptcpVZVXsubflowVZVXreqskVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131901;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_subflow_reset")
int BPF_KPROBE(KtprobemptcpVZVXsubflowVZVXreset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131902;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_subflow_reset")
int BPF_KRETPROBE(KtretprobemptcpVZVXsubflowVZVXreset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131902;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_subflow_set_scheduled")
int BPF_KPROBE(KtprobemptcpVZVXsubflowVZVXsetVZVXscheduled)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_subflow_set_scheduled")
int BPF_KRETPROBE(KtretprobemptcpVZVXsubflowVZVXsetVZVXscheduled)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_token_join_cookie_init_state")
int BPF_KPROBE(KtprobemptcpVZVXtokenVZVXjoinVZVXcookieVZVXinitVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131906;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_token_join_cookie_init_state")
int BPF_KRETPROBE(KtretprobemptcpVZVXtokenVZVXjoinVZVXcookieVZVXinitVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131906;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_userspace_pm_active")
int BPF_KPROBE(KtprobemptcpVZVXuserspaceVZVXpmVZVXactive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131909;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_userspace_pm_active")
int BPF_KRETPROBE(KtretprobemptcpVZVXuserspaceVZVXpmVZVXactive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131909;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcpv6_handle_mapped")
int BPF_KPROBE(Ktprobemptcpv6VZVXhandleVZVXmapped)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131912;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcpv6_handle_mapped")
int BPF_KRETPROBE(Ktretprobemptcpv6VZVXhandleVZVXmapped)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131912;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mroute6_is_socket")
int BPF_KPROBE(Ktprobemroute6VZVXisVZVXsocket)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131942;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mroute6_is_socket")
int BPF_KRETPROBE(Ktretprobemroute6VZVXisVZVXsocket)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=131942;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/napi_alloc_skb")
int BPF_KPROBE(KtprobenapiVZVXallocVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132049;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/napi_alloc_skb")
int BPF_KRETPROBE(KtretprobenapiVZVXallocVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132049;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/napi_consume_skb")
int BPF_KPROBE(KtprobenapiVZVXconsumeVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132052;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/napi_consume_skb")
int BPF_KRETPROBE(KtretprobenapiVZVXconsumeVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132052;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/napi_frags_skb")
int BPF_KPROBE(KtprobenapiVZVXfragsVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132056;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/napi_frags_skb")
int BPF_KRETPROBE(KtretprobenapiVZVXfragsVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132056;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/napi_reuse_skb")
int BPF_KPROBE(KtprobenapiVZVXreuseVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132063;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/napi_reuse_skb")
int BPF_KRETPROBE(KtretprobenapiVZVXreuseVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132063;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/napi_skb_cache_put")
int BPF_KPROBE(KtprobenapiVZVXskbVZVXcacheVZVXput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132068;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/napi_skb_cache_put")
int BPF_KRETPROBE(KtretprobenapiVZVXskbVZVXcacheVZVXput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132068;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/napi_skb_free_stolen_head")
int BPF_KPROBE(KtprobenapiVZVXskbVZVXfreeVZVXstolenVZVXhead)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132069;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/napi_skb_free_stolen_head")
int BPF_KRETPROBE(KtretprobenapiVZVXskbVZVXfreeVZVXstolenVZVXhead)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132069;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/nat_keepalive_send")
int BPF_KPROBE(KtprobenatVZVXkeepaliveVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132071;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/nat_keepalive_send")
int BPF_KRETPROBE(KtretprobenatVZVXkeepaliveVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132071;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/nat_keepalive_sk_fini")
int BPF_KPROBE(KtprobenatVZVXkeepaliveVZVXskVZVXfini)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132073;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/nat_keepalive_sk_fini")
int BPF_KRETPROBE(KtretprobenatVZVXkeepaliveVZVXskVZVXfini)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132073;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/nat_keepalive_sk_init")
int BPF_KPROBE(KtprobenatVZVXkeepaliveVZVXskVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132075;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/nat_keepalive_sk_init")
int BPF_KRETPROBE(KtretprobenatVZVXkeepaliveVZVXskVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132075;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/net_test_get_skb")
int BPF_KPROBE(KtprobenetVZVXtestVZVXgetVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132253;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/net_test_get_skb")
int BPF_KRETPROBE(KtretprobenetVZVXtestVZVXgetVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132253;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/net_test_phy_loopback_tcp")
int BPF_KPROBE(KtprobenetVZVXtestVZVXphyVZVXloopbackVZVXtcp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132260;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/net_test_phy_loopback_tcp")
int BPF_KRETPROBE(KtretprobenetVZVXtestVZVXphyVZVXloopbackVZVXtcp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132260;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/net_test_phy_loopback_tcp_bad_csum")
int BPF_KPROBE(KtprobenetVZVXtestVZVXphyVZVXloopbackVZVXtcpVZVXbadVZVXcsum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132261;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/net_test_phy_loopback_tcp_bad_csum")
int BPF_KRETPROBE(KtretprobenetVZVXtestVZVXphyVZVXloopbackVZVXtcpVZVXbadVZVXcsum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132261;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/net_test_phy_loopback_udp")
int BPF_KPROBE(KtprobenetVZVXtestVZVXphyVZVXloopbackVZVXudp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132262;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/net_test_phy_loopback_udp")
int BPF_KRETPROBE(KtretprobenetVZVXtestVZVXphyVZVXloopbackVZVXudp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132262;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/net_test_phy_loopback_udp_mtu")
int BPF_KPROBE(KtprobenetVZVXtestVZVXphyVZVXloopbackVZVXudpVZVXmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132263;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/net_test_phy_loopback_udp_mtu")
int BPF_KRETPROBE(KtretprobenetVZVXtestVZVXphyVZVXloopbackVZVXudpVZVXmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132263;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netdev_rx_queue_set_rps_mask")
int BPF_KPROBE(KtprobenetdevVZVXrxVZVXqueueVZVXsetVZVXrpsVZVXmask)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132294;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netdev_rx_queue_set_rps_mask")
int BPF_KRETPROBE(KtretprobenetdevVZVXrxVZVXqueueVZVXsetVZVXrpsVZVXmask)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132294;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlbl_calipso_add")
int BPF_KPROBE(KtprobenetlblVZVXcalipsoVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132318;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlbl_calipso_add")
int BPF_KRETPROBE(KtretprobenetlblVZVXcalipsoVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132318;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlbl_calipso_list")
int BPF_KPROBE(KtprobenetlblVZVXcalipsoVZVXlist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132320;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlbl_calipso_list")
int BPF_KRETPROBE(KtretprobenetlblVZVXcalipsoVZVXlist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132320;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlbl_calipso_listall")
int BPF_KPROBE(KtprobenetlblVZVXcalipsoVZVXlistall)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132321;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlbl_calipso_listall")
int BPF_KRETPROBE(KtretprobenetlblVZVXcalipsoVZVXlistall)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132321;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlbl_calipso_remove")
int BPF_KPROBE(KtprobenetlblVZVXcalipsoVZVXremove)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132326;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlbl_calipso_remove")
int BPF_KRETPROBE(KtretprobenetlblVZVXcalipsoVZVXremove)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132326;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlbl_sk_lock_check")
int BPF_KPROBE(KtprobenetlblVZVXskVZVXlockVZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132362;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlbl_sk_lock_check")
int BPF_KRETPROBE(KtretprobenetlblVZVXskVZVXlockVZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132362;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlbl_skbuff_err")
int BPF_KPROBE(KtprobenetlblVZVXskbuffVZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132363;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlbl_skbuff_err")
int BPF_KRETPROBE(KtretprobenetlblVZVXskbuffVZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132363;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlbl_skbuff_getattr")
int BPF_KPROBE(KtprobenetlblVZVXskbuffVZVXgetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132365;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlbl_skbuff_getattr")
int BPF_KRETPROBE(KtretprobenetlblVZVXskbuffVZVXgetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132365;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlbl_skbuff_setattr")
int BPF_KPROBE(KtprobenetlblVZVXskbuffVZVXsetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132367;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlbl_skbuff_setattr")
int BPF_KRETPROBE(KtretprobenetlblVZVXskbuffVZVXsetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132367;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlbl_sock_delattr")
int BPF_KPROBE(KtprobenetlblVZVXsockVZVXdelattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132368;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlbl_sock_delattr")
int BPF_KRETPROBE(KtretprobenetlblVZVXsockVZVXdelattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132368;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlbl_sock_getattr")
int BPF_KPROBE(KtprobenetlblVZVXsockVZVXgetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132369;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlbl_sock_getattr")
int BPF_KRETPROBE(KtretprobenetlblVZVXsockVZVXgetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132369;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlbl_sock_setattr")
int BPF_KPROBE(KtprobenetlblVZVXsockVZVXsetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132371;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlbl_sock_setattr")
int BPF_KRETPROBE(KtretprobenetlblVZVXsockVZVXsetattr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132371;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/nf_ct_get_tuple_skb")
int BPF_KPROBE(KtprobenfVZVXctVZVXgetVZVXtupleVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132403;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/nf_ct_get_tuple_skb")
int BPF_KRETPROBE(KtretprobenfVZVXctVZVXgetVZVXtupleVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=132403;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/pskb_carve_inside_header")
int BPF_KPROBE(KtprobepskbVZVXcarveVZVXinsideVZVXheader)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134275;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/pskb_carve_inside_header")
int BPF_KRETPROBE(KtretprobepskbVZVXcarveVZVXinsideVZVXheader)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134275;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/pskb_carve_inside_nonlinear")
int BPF_KPROBE(KtprobepskbVZVXcarveVZVXinsideVZVXnonlinear)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134277;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/pskb_carve_inside_nonlinear")
int BPF_KRETPROBE(KtretprobepskbVZVXcarveVZVXinsideVZVXnonlinear)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134277;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/pskb_expand_head")
int BPF_KPROBE(KtprobepskbVZVXexpandVZVXhead)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134279;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/pskb_expand_head")
int BPF_KRETPROBE(KtretprobepskbVZVXexpandVZVXhead)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134279;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/pskb_extract")
int BPF_KPROBE(KtprobepskbVZVXextract)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134281;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/pskb_extract")
int BPF_KRETPROBE(KtretprobepskbVZVXextract)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134281;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/pskb_put")
int BPF_KPROBE(KtprobepskbVZVXput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134283;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/pskb_put")
int BPF_KRETPROBE(KtretprobepskbVZVXput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134283;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/pskb_trim_rcsum_slow")
int BPF_KPROBE(KtprobepskbVZVXtrimVZVXrcsumVZVXslow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134284;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/pskb_trim_rcsum_slow")
int BPF_KRETPROBE(KtretprobepskbVZVXtrimVZVXrcsumVZVXslow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134284;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/raw6_icmp_error")
int BPF_KPROBE(Ktproberaw6VZVXicmpVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134745;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/raw6_icmp_error")
int BPF_KRETPROBE(Ktretproberaw6VZVXicmpVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134745;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/rawv6_getsockopt")
int BPF_KPROBE(Ktproberawv6VZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134761;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/rawv6_getsockopt")
int BPF_KRETPROBE(Ktretproberawv6VZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134761;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/rawv6_init_sk")
int BPF_KPROBE(Ktproberawv6VZVXinitVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134763;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/rawv6_init_sk")
int BPF_KRETPROBE(Ktretproberawv6VZVXinitVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134763;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/rawv6_rcv_skb")
int BPF_KPROBE(Ktproberawv6VZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134771;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/rawv6_rcv_skb")
int BPF_KRETPROBE(Ktretproberawv6VZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134771;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/rawv6_recvmsg")
int BPF_KPROBE(Ktproberawv6VZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134772;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/rawv6_recvmsg")
int BPF_KRETPROBE(Ktretproberawv6VZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134772;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/rawv6_send_hdrinc")
int BPF_KPROBE(Ktproberawv6VZVXsendVZVXhdrinc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134774;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/rawv6_send_hdrinc")
int BPF_KRETPROBE(Ktretproberawv6VZVXsendVZVXhdrinc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134774;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/rawv6_setsockopt")
int BPF_KPROBE(Ktproberawv6VZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134776;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/rawv6_setsockopt")
int BPF_KRETPROBE(Ktretproberawv6VZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=134776;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/rtmsg_ifinfo_build_skb")
int BPF_KPROBE(KtprobertmsgVZVXifinfoVZVXbuildVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=135591;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/rtmsg_ifinfo_build_skb")
int BPF_KRETPROBE(KtretprobertmsgVZVXifinfoVZVXbuildVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=135591;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/rtmsg_ifinfo_send")
int BPF_KPROBE(KtprobertmsgVZVXifinfoVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=135595;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/rtmsg_ifinfo_send")
int BPF_KRETPROBE(KtretprobertmsgVZVXifinfoVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=135595;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/rtnetlink_send")
int BPF_KPROBE(KtprobertnetlinkVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=135606;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/rtnetlink_send")
int BPF_KRETPROBE(KtretprobertnetlinkVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=135606;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/rtnl_kfree_skbs")
int BPF_KPROBE(KtprobertnlVZVXkfreeVZVXskbs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=135654;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/rtnl_kfree_skbs")
int BPF_KRETPROBE(KtretprobertnlVZVXkfreeVZVXskbs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=135654;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/rtnl_set_sk_err")
int BPF_KPROBE(KtprobertnlVZVXsetVZVXskVZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=135693;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/rtnl_set_sk_err")
int BPF_KRETPROBE(KtretprobertnlVZVXsetVZVXskVZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=135693;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_inet_csk_clone")
int BPF_KPROBE(KtprobeselinuxVZVXinetVZVXcskVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136052;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_inet_csk_clone")
int BPF_KRETPROBE(KtretprobeselinuxVZVXinetVZVXcskVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136052;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_inet_sys_rcv_skb")
int BPF_KPROBE(KtprobeselinuxVZVXinetVZVXsysVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136054;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_inet_sys_rcv_skb")
int BPF_KRETPROBE(KtretprobeselinuxVZVXinetVZVXsysVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136054;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_ip_forward")
int BPF_KPROBE(KtprobeselinuxVZVXipVZVXforward)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136097;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_ip_forward")
int BPF_KRETPROBE(KtretprobeselinuxVZVXipVZVXforward)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136097;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_ip_output")
int BPF_KPROBE(KtprobeselinuxVZVXipVZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136098;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_ip_output")
int BPF_KRETPROBE(KtretprobeselinuxVZVXipVZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136098;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_ip_postroute")
int BPF_KPROBE(KtprobeselinuxVZVXipVZVXpostroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136099;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_ip_postroute")
int BPF_KRETPROBE(KtretprobeselinuxVZVXipVZVXpostroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136099;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_ip_postroute_compat")
int BPF_KPROBE(KtprobeselinuxVZVXipVZVXpostrouteVZVXcompat)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136101;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_ip_postroute_compat")
int BPF_KRETPROBE(KtretprobeselinuxVZVXipVZVXpostrouteVZVXcompat)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136101;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_mptcp_add_subflow")
int BPF_KPROBE(KtprobeselinuxVZVXmptcpVZVXaddVZVXsubflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136128;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_mptcp_add_subflow")
int BPF_KRETPROBE(KtretprobeselinuxVZVXmptcpVZVXaddVZVXsubflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136128;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_netlink_send")
int BPF_KPROBE(KtprobeselinuxVZVXnetlinkVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136136;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_netlink_send")
int BPF_KRETPROBE(KtretprobeselinuxVZVXnetlinkVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136136;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_parse_skb")
int BPF_KPROBE(KtprobeselinuxVZVXparseVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136141;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_parse_skb")
int BPF_KRETPROBE(KtretprobeselinuxVZVXparseVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136141;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_parse_skb_ipv4")
int BPF_KPROBE(KtprobeselinuxVZVXparseVZVXskbVZVXipv4)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136142;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_parse_skb_ipv4")
int BPF_KRETPROBE(KtretprobeselinuxVZVXparseVZVXskbVZVXipv4)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136142;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_parse_skb_ipv6")
int BPF_KPROBE(KtprobeselinuxVZVXparseVZVXskbVZVXipv6)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136143;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_parse_skb_ipv6")
int BPF_KRETPROBE(KtretprobeselinuxVZVXparseVZVXskbVZVXipv6)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136143;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_sctp_sk_clone")
int BPF_KPROBE(KtprobeselinuxVZVXsctpVZVXskVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136167;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_sctp_sk_clone")
int BPF_KRETPROBE(KtretprobeselinuxVZVXsctpVZVXskVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136167;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_sk_alloc_security")
int BPF_KPROBE(KtprobeselinuxVZVXskVZVXallocVZVXsecurity)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136186;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_sk_alloc_security")
int BPF_KRETPROBE(KtretprobeselinuxVZVXskVZVXallocVZVXsecurity)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136186;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_sk_clone_security")
int BPF_KPROBE(KtprobeselinuxVZVXskVZVXcloneVZVXsecurity)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136187;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_sk_clone_security")
int BPF_KRETPROBE(KtretprobeselinuxVZVXskVZVXcloneVZVXsecurity)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136187;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_sk_free_security")
int BPF_KPROBE(KtprobeselinuxVZVXskVZVXfreeVZVXsecurity)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136188;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_sk_free_security")
int BPF_KRETPROBE(KtretprobeselinuxVZVXskVZVXfreeVZVXsecurity)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136188;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_sk_getsecid")
int BPF_KPROBE(KtprobeselinuxVZVXskVZVXgetsecid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136189;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_sk_getsecid")
int BPF_KRETPROBE(KtretprobeselinuxVZVXskVZVXgetsecid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136189;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_skb_peerlbl_sid")
int BPF_KPROBE(KtprobeselinuxVZVXskbVZVXpeerlblVZVXsid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136191;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_skb_peerlbl_sid")
int BPF_KRETPROBE(KtretprobeselinuxVZVXskbVZVXpeerlblVZVXsid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136191;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_sock_graft")
int BPF_KPROBE(KtprobeselinuxVZVXsockVZVXgraft)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136192;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_sock_graft")
int BPF_KRETPROBE(KtretprobeselinuxVZVXsockVZVXgraft)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136192;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_sock_rcv_skb_compat")
int BPF_KPROBE(KtprobeselinuxVZVXsockVZVXrcvVZVXskbVZVXcompat)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136194;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_sock_rcv_skb_compat")
int BPF_KRETPROBE(KtretprobeselinuxVZVXsockVZVXrcvVZVXskbVZVXcompat)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136194;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_socket_accept")
int BPF_KPROBE(KtprobeselinuxVZVXsocketVZVXaccept)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136195;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_socket_accept")
int BPF_KRETPROBE(KtretprobeselinuxVZVXsocketVZVXaccept)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136195;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_socket_bind")
int BPF_KPROBE(KtprobeselinuxVZVXsocketVZVXbind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136196;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_socket_bind")
int BPF_KRETPROBE(KtretprobeselinuxVZVXsocketVZVXbind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136196;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_socket_connect")
int BPF_KPROBE(KtprobeselinuxVZVXsocketVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136197;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_socket_connect")
int BPF_KRETPROBE(KtretprobeselinuxVZVXsocketVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136197;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_socket_getpeername")
int BPF_KPROBE(KtprobeselinuxVZVXsocketVZVXgetpeername)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136199;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_socket_getpeername")
int BPF_KRETPROBE(KtretprobeselinuxVZVXsocketVZVXgetpeername)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136199;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_socket_getpeersec_dgram")
int BPF_KPROBE(KtprobeselinuxVZVXsocketVZVXgetpeersecVZVXdgram)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136200;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_socket_getpeersec_dgram")
int BPF_KRETPROBE(KtretprobeselinuxVZVXsocketVZVXgetpeersecVZVXdgram)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136200;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_socket_getpeersec_stream")
int BPF_KPROBE(KtprobeselinuxVZVXsocketVZVXgetpeersecVZVXstream)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136201;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_socket_getpeersec_stream")
int BPF_KRETPROBE(KtretprobeselinuxVZVXsocketVZVXgetpeersecVZVXstream)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136201;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_socket_getsockname")
int BPF_KPROBE(KtprobeselinuxVZVXsocketVZVXgetsockname)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136202;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_socket_getsockname")
int BPF_KRETPROBE(KtretprobeselinuxVZVXsocketVZVXgetsockname)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136202;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_socket_getsockopt")
int BPF_KPROBE(KtprobeselinuxVZVXsocketVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136203;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_socket_getsockopt")
int BPF_KRETPROBE(KtretprobeselinuxVZVXsocketVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136203;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_socket_listen")
int BPF_KPROBE(KtprobeselinuxVZVXsocketVZVXlisten)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136204;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_socket_listen")
int BPF_KRETPROBE(KtretprobeselinuxVZVXsocketVZVXlisten)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136204;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_socket_post_create")
int BPF_KPROBE(KtprobeselinuxVZVXsocketVZVXpostVZVXcreate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136205;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_socket_post_create")
int BPF_KRETPROBE(KtretprobeselinuxVZVXsocketVZVXpostVZVXcreate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136205;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_socket_recvmsg")
int BPF_KPROBE(KtprobeselinuxVZVXsocketVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136206;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_socket_recvmsg")
int BPF_KRETPROBE(KtretprobeselinuxVZVXsocketVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136206;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_socket_sendmsg")
int BPF_KPROBE(KtprobeselinuxVZVXsocketVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136207;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_socket_sendmsg")
int BPF_KRETPROBE(KtretprobeselinuxVZVXsocketVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136207;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_socket_setsockopt")
int BPF_KPROBE(KtprobeselinuxVZVXsocketVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136208;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_socket_setsockopt")
int BPF_KRETPROBE(KtretprobeselinuxVZVXsocketVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136208;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_socket_shutdown")
int BPF_KPROBE(KtprobeselinuxVZVXsocketVZVXshutdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136209;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_socket_shutdown")
int BPF_KRETPROBE(KtretprobeselinuxVZVXsocketVZVXshutdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136209;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_socket_sock_rcv_skb")
int BPF_KPROBE(KtprobeselinuxVZVXsocketVZVXsockVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136210;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_socket_sock_rcv_skb")
int BPF_KRETPROBE(KtretprobeselinuxVZVXsocketVZVXsockVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136210;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_socket_socketpair")
int BPF_KPROBE(KtprobeselinuxVZVXsocketVZVXsocketpair)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136211;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_socket_socketpair")
int BPF_KRETPROBE(KtretprobeselinuxVZVXsocketVZVXsocketpair)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136211;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_socket_unix_may_send")
int BPF_KPROBE(KtprobeselinuxVZVXsocketVZVXunixVZVXmayVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136212;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_socket_unix_may_send")
int BPF_KRETPROBE(KtretprobeselinuxVZVXsocketVZVXunixVZVXmayVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136212;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/selinux_socket_unix_stream_connect")
int BPF_KPROBE(KtprobeselinuxVZVXsocketVZVXunixVZVXstreamVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136213;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/selinux_socket_unix_stream_connect")
int BPF_KRETPROBE(KtretprobeselinuxVZVXsocketVZVXunixVZVXstreamVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136213;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sendmsg_locked")
int BPF_KPROBE(KtprobesendmsgVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136244;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sendmsg_locked")
int BPF_KRETPROBE(KtretprobesendmsgVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136244;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sendmsg_unlocked")
int BPF_KPROBE(KtprobesendmsgVZVXunlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136245;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sendmsg_unlocked")
int BPF_KRETPROBE(KtretprobesendmsgVZVXunlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136245;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sit_ip6ip6_gro_receive")
int BPF_KPROBE(KtprobesitVZVXip6ip6VZVXgroVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136628;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sit_ip6ip6_gro_receive")
int BPF_KRETPROBE(KtretprobesitVZVXip6ip6VZVXgroVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136628;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_skb_reason_drop")
int BPF_KPROBE(KtprobeskVZVXskbVZVXreasonVZVXdrop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136631;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_skb_reason_drop")
int BPF_KRETPROBE(KtretprobeskVZVXskbVZVXreasonVZVXdrop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136631;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_udp_recvmsg")
int BPF_KPROBE(KtprobeskVZVXudpVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136632;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_udp_recvmsg")
int BPF_KRETPROBE(KtretprobeskVZVXudpVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136632;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_abort_seq_read")
int BPF_KPROBE(KtprobeskbVZVXabortVZVXseqVZVXread)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136634;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_abort_seq_read")
int BPF_KRETPROBE(KtretprobeskbVZVXabortVZVXseqVZVXread)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136634;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_add_rx_frag_netmem")
int BPF_KPROBE(KtprobeskbVZVXaddVZVXrxVZVXfragVZVXnetmem)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136636;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_add_rx_frag_netmem")
int BPF_KRETPROBE(KtretprobeskbVZVXaddVZVXrxVZVXfragVZVXnetmem)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136636;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_advance_to_frag")
int BPF_KPROBE(KtprobeskbVZVXadvanceVZVXtoVZVXfrag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136638;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_advance_to_frag")
int BPF_KRETPROBE(KtretprobeskbVZVXadvanceVZVXtoVZVXfrag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136638;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_append")
int BPF_KPROBE(KtprobeskbVZVXappend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136640;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_append")
int BPF_KRETPROBE(KtretprobeskbVZVXappend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136640;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_append_pagefrags")
int BPF_KPROBE(KtprobeskbVZVXappendVZVXpagefrags)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136642;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_append_pagefrags")
int BPF_KRETPROBE(KtretprobeskbVZVXappendVZVXpagefrags)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136642;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_attempt_defer_free")
int BPF_KPROBE(KtprobeskbVZVXattemptVZVXdeferVZVXfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136643;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_attempt_defer_free")
int BPF_KRETPROBE(KtretprobeskbVZVXattemptVZVXdeferVZVXfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136643;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_checksum")
int BPF_KPROBE(KtprobeskbVZVXchecksum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136645;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_checksum")
int BPF_KRETPROBE(KtretprobeskbVZVXchecksum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136645;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_checksum_setup")
int BPF_KPROBE(KtprobeskbVZVXchecksumVZVXsetup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136647;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_checksum_setup")
int BPF_KRETPROBE(KtretprobeskbVZVXchecksumVZVXsetup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136647;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_checksum_setup_ip")
int BPF_KPROBE(KtprobeskbVZVXchecksumVZVXsetupVZVXip)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136649;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_checksum_setup_ip")
int BPF_KRETPROBE(KtretprobeskbVZVXchecksumVZVXsetupVZVXip)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136649;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_checksum_setup_ipv6")
int BPF_KPROBE(KtprobeskbVZVXchecksumVZVXsetupVZVXipv6)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136650;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_checksum_setup_ipv6")
int BPF_KRETPROBE(KtretprobeskbVZVXchecksumVZVXsetupVZVXipv6)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136650;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_checksum_trimmed")
int BPF_KPROBE(KtprobeskbVZVXchecksumVZVXtrimmed)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136652;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_checksum_trimmed")
int BPF_KRETPROBE(KtretprobeskbVZVXchecksumVZVXtrimmed)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136652;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_clone")
int BPF_KPROBE(KtprobeskbVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136654;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_clone")
int BPF_KRETPROBE(KtretprobeskbVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136654;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_clone_fraglist")
int BPF_KPROBE(KtprobeskbVZVXcloneVZVXfraglist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136655;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_clone_fraglist")
int BPF_KRETPROBE(KtretprobeskbVZVXcloneVZVXfraglist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136655;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_clone_sk")
int BPF_KPROBE(KtprobeskbVZVXcloneVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136656;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_clone_sk")
int BPF_KRETPROBE(KtretprobeskbVZVXcloneVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136656;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_coalesce_rx_frag")
int BPF_KPROBE(KtprobeskbVZVXcoalesceVZVXrxVZVXfrag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136658;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_coalesce_rx_frag")
int BPF_KRETPROBE(KtretprobeskbVZVXcoalesceVZVXrxVZVXfrag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136658;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_complete_tx_timestamp")
int BPF_KPROBE(KtprobeskbVZVXcompleteVZVXtxVZVXtimestamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136660;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_complete_tx_timestamp")
int BPF_KRETPROBE(KtretprobeskbVZVXcompleteVZVXtxVZVXtimestamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136660;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_complete_wifi_ack")
int BPF_KPROBE(KtprobeskbVZVXcompleteVZVXwifiVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136662;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_complete_wifi_ack")
int BPF_KRETPROBE(KtretprobeskbVZVXcompleteVZVXwifiVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136662;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_condense")
int BPF_KPROBE(KtprobeskbVZVXcondense)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136663;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_condense")
int BPF_KRETPROBE(KtretprobeskbVZVXcondense)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136663;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_copy")
int BPF_KPROBE(KtprobeskbVZVXcopy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136665;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_copy")
int BPF_KRETPROBE(KtretprobeskbVZVXcopy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136665;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_copy_and_csum_bits")
int BPF_KPROBE(KtprobeskbVZVXcopyVZVXandVZVXcsumVZVXbits)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136667;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_copy_and_csum_bits")
int BPF_KRETPROBE(KtretprobeskbVZVXcopyVZVXandVZVXcsumVZVXbits)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136667;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_copy_and_csum_dev")
int BPF_KPROBE(KtprobeskbVZVXcopyVZVXandVZVXcsumVZVXdev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136669;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_copy_and_csum_dev")
int BPF_KRETPROBE(KtretprobeskbVZVXcopyVZVXandVZVXcsumVZVXdev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136669;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_copy_expand")
int BPF_KPROBE(KtprobeskbVZVXcopyVZVXexpand)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136671;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_copy_expand")
int BPF_KRETPROBE(KtretprobeskbVZVXcopyVZVXexpand)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136671;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_copy_header")
int BPF_KPROBE(KtprobeskbVZVXcopyVZVXheader)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136672;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_copy_header")
int BPF_KRETPROBE(KtretprobeskbVZVXcopyVZVXheader)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136672;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_copy_seq_read")
int BPF_KPROBE(KtprobeskbVZVXcopyVZVXseqVZVXread)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136674;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_copy_seq_read")
int BPF_KRETPROBE(KtretprobeskbVZVXcopyVZVXseqVZVXread)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136674;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_copy_ubufs")
int BPF_KPROBE(KtprobeskbVZVXcopyVZVXubufs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136676;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_copy_ubufs")
int BPF_KRETPROBE(KtretprobeskbVZVXcopyVZVXubufs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136676;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_cow_data")
int BPF_KPROBE(KtprobeskbVZVXcowVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136678;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_cow_data")
int BPF_KRETPROBE(KtretprobeskbVZVXcowVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136678;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_cow_data_for_xdp")
int BPF_KPROBE(KtprobeskbVZVXcowVZVXdataVZVXforVZVXxdp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136680;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_cow_data_for_xdp")
int BPF_KRETPROBE(KtretprobeskbVZVXcowVZVXdataVZVXforVZVXxdp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136680;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_crc32c")
int BPF_KPROBE(KtprobeskbVZVXcrc32c)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136682;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_crc32c")
int BPF_KRETPROBE(KtretprobeskbVZVXcrc32c)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136682;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_do_copy_data_nocache")
int BPF_KPROBE(KtprobeskbVZVXdoVZVXcopyVZVXdataVZVXnocache)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136687;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_do_copy_data_nocache")
int BPF_KRETPROBE(KtretprobeskbVZVXdoVZVXcopyVZVXdataVZVXnocache)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136687;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_dump")
int BPF_KPROBE(KtprobeskbVZVXdump)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136689;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_dump")
int BPF_KRETPROBE(KtretprobeskbVZVXdump)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136689;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_ensure_writable")
int BPF_KPROBE(KtprobeskbVZVXensureVZVXwritable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136691;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_ensure_writable")
int BPF_KRETPROBE(KtretprobeskbVZVXensureVZVXwritable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136691;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_ensure_writable_head_tail")
int BPF_KPROBE(KtprobeskbVZVXensureVZVXwritableVZVXheadVZVXtail)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136692;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_ensure_writable_head_tail")
int BPF_KRETPROBE(KtretprobeskbVZVXensureVZVXwritableVZVXheadVZVXtail)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136692;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_eth_pop")
int BPF_KPROBE(KtprobeskbVZVXethVZVXpop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136695;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_eth_pop")
int BPF_KRETPROBE(KtretprobeskbVZVXethVZVXpop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136695;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_eth_push")
int BPF_KPROBE(KtprobeskbVZVXethVZVXpush)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136697;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_eth_push")
int BPF_KRETPROBE(KtretprobeskbVZVXethVZVXpush)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136697;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_expand_head")
int BPF_KPROBE(KtprobeskbVZVXexpandVZVXhead)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136699;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_expand_head")
int BPF_KRETPROBE(KtretprobeskbVZVXexpandVZVXhead)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136699;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_ext_add")
int BPF_KPROBE(KtprobeskbVZVXextVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136701;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_ext_add")
int BPF_KRETPROBE(KtretprobeskbVZVXextVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136701;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_find_text")
int BPF_KPROBE(KtprobeskbVZVXfindVZVXtext)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136705;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_find_text")
int BPF_KRETPROBE(KtretprobeskbVZVXfindVZVXtext)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136705;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_flow_dissect_ct")
int BPF_KPROBE(KtprobeskbVZVXflowVZVXdissectVZVXct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136707;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_flow_dissect_ct")
int BPF_KRETPROBE(KtretprobeskbVZVXflowVZVXdissectVZVXct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136707;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_flow_dissect_hash")
int BPF_KPROBE(KtprobeskbVZVXflowVZVXdissectVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136709;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_flow_dissect_hash")
int BPF_KRETPROBE(KtretprobeskbVZVXflowVZVXdissectVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136709;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_flow_dissect_meta")
int BPF_KPROBE(KtprobeskbVZVXflowVZVXdissectVZVXmeta)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136710;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_flow_dissect_meta")
int BPF_KRETPROBE(KtretprobeskbVZVXflowVZVXdissectVZVXmeta)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136710;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_flow_dissect_tunnel_info")
int BPF_KPROBE(KtprobeskbVZVXflowVZVXdissectVZVXtunnelVZVXinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136711;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_flow_dissect_tunnel_info")
int BPF_KRETPROBE(KtretprobeskbVZVXflowVZVXdissectVZVXtunnelVZVXinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136711;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_flow_get_icmp_tci")
int BPF_KPROBE(KtprobeskbVZVXflowVZVXgetVZVXicmpVZVXtci)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136715;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_flow_get_icmp_tci")
int BPF_KRETPROBE(KtretprobeskbVZVXflowVZVXgetVZVXicmpVZVXtci)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136715;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_flow_get_ports")
int BPF_KPROBE(KtprobeskbVZVXflowVZVXgetVZVXports)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136717;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_flow_get_ports")
int BPF_KRETPROBE(KtretprobeskbVZVXflowVZVXgetVZVXports)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136717;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_get_hash_perturb")
int BPF_KPROBE(KtprobeskbVZVXgetVZVXhashVZVXperturb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136720;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_get_hash_perturb")
int BPF_KRETPROBE(KtretprobeskbVZVXgetVZVXhashVZVXperturb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136720;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_get_poff")
int BPF_KPROBE(KtprobeskbVZVXgetVZVXpoff)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136722;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_get_poff")
int BPF_KRETPROBE(KtretprobeskbVZVXgetVZVXpoff)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136722;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_gro_receive")
int BPF_KPROBE(KtprobeskbVZVXgroVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136724;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_gro_receive")
int BPF_KRETPROBE(KtretprobeskbVZVXgroVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136724;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_gro_receive_list")
int BPF_KPROBE(KtprobeskbVZVXgroVZVXreceiveVZVXlist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136725;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_gro_receive_list")
int BPF_KRETPROBE(KtretprobeskbVZVXgroVZVXreceiveVZVXlist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136725;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_headers_offset_update")
int BPF_KPROBE(KtprobeskbVZVXheadersVZVXoffsetVZVXupdate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136727;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_headers_offset_update")
int BPF_KRETPROBE(KtretprobeskbVZVXheadersVZVXoffsetVZVXupdate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136727;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_metadata_dst_cmp")
int BPF_KPROBE(KtprobeskbVZVXmetadataVZVXdstVZVXcmp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136732;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_metadata_dst_cmp")
int BPF_KRETPROBE(KtretprobeskbVZVXmetadataVZVXdstVZVXcmp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136732;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_morph")
int BPF_KPROBE(KtprobeskbVZVXmorph)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136734;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_morph")
int BPF_KRETPROBE(KtretprobeskbVZVXmorph)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136734;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_mpls_dec_ttl")
int BPF_KPROBE(KtprobeskbVZVXmplsVZVXdecVZVXttl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136735;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_mpls_dec_ttl")
int BPF_KRETPROBE(KtretprobeskbVZVXmplsVZVXdecVZVXttl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136735;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_mpls_pop")
int BPF_KPROBE(KtprobeskbVZVXmplsVZVXpop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136737;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_mpls_pop")
int BPF_KRETPROBE(KtretprobeskbVZVXmplsVZVXpop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136737;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_mpls_push")
int BPF_KPROBE(KtprobeskbVZVXmplsVZVXpush)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136739;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_mpls_push")
int BPF_KRETPROBE(KtretprobeskbVZVXmplsVZVXpush)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136739;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_mpls_update_lse")
int BPF_KPROBE(KtprobeskbVZVXmplsVZVXupdateVZVXlse)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136741;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_mpls_update_lse")
int BPF_KRETPROBE(KtretprobeskbVZVXmplsVZVXupdateVZVXlse)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136741;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_panic")
int BPF_KPROBE(KtprobeskbVZVXpanic)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136743;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_panic")
int BPF_KRETPROBE(KtretprobeskbVZVXpanic)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136743;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_partial_csum_set")
int BPF_KPROBE(KtprobeskbVZVXpartialVZVXcsumVZVXset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136745;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_partial_csum_set")
int BPF_KRETPROBE(KtretprobeskbVZVXpartialVZVXcsumVZVXset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136745;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_pp_cow_data")
int BPF_KPROBE(KtprobeskbVZVXppVZVXcowVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136747;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_pp_cow_data")
int BPF_KRETPROBE(KtretprobeskbVZVXppVZVXcowVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136747;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_prepare_seq_read")
int BPF_KPROBE(KtprobeskbVZVXprepareVZVXseqVZVXread)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136749;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_prepare_seq_read")
int BPF_KRETPROBE(KtretprobeskbVZVXprepareVZVXseqVZVXread)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136749;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_pull")
int BPF_KPROBE(KtprobeskbVZVXpull)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136751;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_pull")
int BPF_KRETPROBE(KtretprobeskbVZVXpull)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136751;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_pull_data")
int BPF_KPROBE(KtprobeskbVZVXpullVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136753;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_pull_data")
int BPF_KRETPROBE(KtretprobeskbVZVXpullVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136753;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_pull_rcsum")
int BPF_KPROBE(KtprobeskbVZVXpullVZVXrcsum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136754;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_pull_rcsum")
int BPF_KRETPROBE(KtretprobeskbVZVXpullVZVXrcsum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136754;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_push")
int BPF_KPROBE(KtprobeskbVZVXpush)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136755;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_push")
int BPF_KRETPROBE(KtretprobeskbVZVXpush)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136755;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_queue_head")
int BPF_KPROBE(KtprobeskbVZVXqueueVZVXhead)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136758;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_queue_head")
int BPF_KRETPROBE(KtretprobeskbVZVXqueueVZVXhead)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136758;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_realloc_headroom")
int BPF_KPROBE(KtprobeskbVZVXreallocVZVXheadroom)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136764;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_realloc_headroom")
int BPF_KRETPROBE(KtretprobeskbVZVXreallocVZVXheadroom)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136764;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_reorder_vlan_header")
int BPF_KPROBE(KtprobeskbVZVXreorderVZVXvlanVZVXheader)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136767;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_reorder_vlan_header")
int BPF_KRETPROBE(KtretprobeskbVZVXreorderVZVXvlanVZVXheader)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136767;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_scrub_packet")
int BPF_KPROBE(KtprobeskbVZVXscrubVZVXpacket)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136769;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_scrub_packet")
int BPF_KRETPROBE(KtretprobeskbVZVXscrubVZVXpacket)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136769;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_segment")
int BPF_KPROBE(KtprobeskbVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136771;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_segment")
int BPF_KRETPROBE(KtretprobeskbVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136771;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_segment_list")
int BPF_KPROBE(KtprobeskbVZVXsegmentVZVXlist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136773;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_segment_list")
int BPF_KRETPROBE(KtretprobeskbVZVXsegmentVZVXlist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136773;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_send_sock")
int BPF_KPROBE(KtprobeskbVZVXsendVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136775;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_send_sock")
int BPF_KRETPROBE(KtretprobeskbVZVXsendVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136775;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_send_sock_locked")
int BPF_KPROBE(KtprobeskbVZVXsendVZVXsockVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136776;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_send_sock_locked")
int BPF_KRETPROBE(KtretprobeskbVZVXsendVZVXsockVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136776;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_send_sock_locked_with_flags")
int BPF_KPROBE(KtprobeskbVZVXsendVZVXsockVZVXlockedVZVXwithVZVXflags)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136778;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_send_sock_locked_with_flags")
int BPF_KRETPROBE(KtretprobeskbVZVXsendVZVXsockVZVXlockedVZVXwithVZVXflags)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136778;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_seq_read")
int BPF_KPROBE(KtprobeskbVZVXseqVZVXread)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136780;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_seq_read")
int BPF_KRETPROBE(KtretprobeskbVZVXseqVZVXread)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136780;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_shift")
int BPF_KPROBE(KtprobeskbVZVXshift)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136782;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_shift")
int BPF_KRETPROBE(KtretprobeskbVZVXshift)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136782;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_splice_bits")
int BPF_KPROBE(KtprobeskbVZVXspliceVZVXbits)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136784;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_splice_bits")
int BPF_KRETPROBE(KtretprobeskbVZVXspliceVZVXbits)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136784;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_splice_from_iter")
int BPF_KPROBE(KtprobeskbVZVXspliceVZVXfromVZVXiter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136786;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_splice_from_iter")
int BPF_KRETPROBE(KtretprobeskbVZVXspliceVZVXfromVZVXiter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136786;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_split")
int BPF_KPROBE(KtprobeskbVZVXsplit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136788;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_split")
int BPF_KRETPROBE(KtretprobeskbVZVXsplit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136788;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_store_bits")
int BPF_KPROBE(KtprobeskbVZVXstoreVZVXbits)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136790;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_store_bits")
int BPF_KRETPROBE(KtretprobeskbVZVXstoreVZVXbits)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136790;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_to_sgvec")
int BPF_KPROBE(KtprobeskbVZVXtoVZVXsgvec)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136792;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_to_sgvec")
int BPF_KRETPROBE(KtretprobeskbVZVXtoVZVXsgvec)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136792;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_to_sgvec_nomark")
int BPF_KPROBE(KtprobeskbVZVXtoVZVXsgvecVZVXnomark)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136793;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_to_sgvec_nomark")
int BPF_KRETPROBE(KtretprobeskbVZVXtoVZVXsgvecVZVXnomark)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136793;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_trim")
int BPF_KPROBE(KtprobeskbVZVXtrim)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136795;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_trim")
int BPF_KRETPROBE(KtretprobeskbVZVXtrim)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136795;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_try_coalesce")
int BPF_KPROBE(KtprobeskbVZVXtryVZVXcoalesce)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136797;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_try_coalesce")
int BPF_KRETPROBE(KtretprobeskbVZVXtryVZVXcoalesce)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136797;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_tstamp_tx")
int BPF_KPROBE(KtprobeskbVZVXtstampVZVXtx)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136803;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_tstamp_tx")
int BPF_KRETPROBE(KtretprobeskbVZVXtstampVZVXtx)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136803;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_tx_error")
int BPF_KPROBE(KtprobeskbVZVXtxVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136804;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_tx_error")
int BPF_KRETPROBE(KtretprobeskbVZVXtxVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136804;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_udp_tunnel_segment")
int BPF_KPROBE(KtprobeskbVZVXudpVZVXtunnelVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136805;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_udp_tunnel_segment")
int BPF_KRETPROBE(KtretprobeskbVZVXudpVZVXtunnelVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136805;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_vlan_pop")
int BPF_KPROBE(KtprobeskbVZVXvlanVZVXpop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136808;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_vlan_pop")
int BPF_KRETPROBE(KtretprobeskbVZVXvlanVZVXpop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136808;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_vlan_push")
int BPF_KPROBE(KtprobeskbVZVXvlanVZVXpush)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136810;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_vlan_push")
int BPF_KRETPROBE(KtretprobeskbVZVXvlanVZVXpush)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136810;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_vlan_untag")
int BPF_KPROBE(KtprobeskbVZVXvlanVZVXuntag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136811;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_vlan_untag")
int BPF_KRETPROBE(KtretprobeskbVZVXvlanVZVXuntag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136811;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_zerocopy")
int BPF_KPROBE(KtprobeskbVZVXzerocopy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136813;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_zerocopy")
int BPF_KRETPROBE(KtretprobeskbVZVXzerocopy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136813;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_zerocopy_clone")
int BPF_KPROBE(KtprobeskbVZVXzerocopyVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136815;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_zerocopy_clone")
int BPF_KRETPROBE(KtretprobeskbVZVXzerocopyVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136815;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_zerocopy_headlen")
int BPF_KPROBE(KtprobeskbVZVXzerocopyVZVXheadlen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136817;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_zerocopy_headlen")
int BPF_KRETPROBE(KtretprobeskbVZVXzerocopyVZVXheadlen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136817;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_zerocopy_iter_stream")
int BPF_KPROBE(KtprobeskbVZVXzerocopyVZVXiterVZVXstream)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136819;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_zerocopy_iter_stream")
int BPF_KRETPROBE(KtretprobeskbVZVXzerocopyVZVXiterVZVXstream)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136819;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/smack_ip_output")
int BPF_KPROBE(KtprobesmackVZVXipVZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136875;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/smack_ip_output")
int BPF_KRETPROBE(KtretprobesmackVZVXipVZVXoutput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136875;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_dequeue_err_skb")
int BPF_KPROBE(KtprobesockVZVXdequeueVZVXerrVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136945;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_dequeue_err_skb")
int BPF_KRETPROBE(KtretprobesockVZVXdequeueVZVXerrVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136945;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_has_perm")
int BPF_KPROBE(KtprobesockVZVXhasVZVXperm)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136947;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_has_perm")
int BPF_KRETPROBE(KtretprobesockVZVXhasVZVXperm)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136947;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_hash_alloc_elem")
int BPF_KPROBE(KtprobesockVZVXhashVZVXallocVZVXelem)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136950;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_hash_alloc_elem")
int BPF_KRETPROBE(KtretprobesockVZVXhashVZVXallocVZVXelem)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136950;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_hash_seq_find_next")
int BPF_KPROBE(KtprobesockVZVXhashVZVXseqVZVXfindVZVXnext)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136964;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_hash_seq_find_next")
int BPF_KRETPROBE(KtretprobesockVZVXhashVZVXseqVZVXfindVZVXnext)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136964;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_hash_update_common")
int BPF_KPROBE(KtprobesockVZVXhashVZVXupdateVZVXcommon)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136970;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_hash_update_common")
int BPF_KRETPROBE(KtretprobesockVZVXhashVZVXupdateVZVXcommon)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136970;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_map_close")
int BPF_KPROBE(KtprobesockVZVXmapVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136973;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_map_close")
int BPF_KRETPROBE(KtretprobesockVZVXmapVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136973;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_map_destroy")
int BPF_KPROBE(KtprobesockVZVXmapVZVXdestroy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136975;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_map_destroy")
int BPF_KRETPROBE(KtretprobesockVZVXmapVZVXdestroy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136975;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_map_link")
int BPF_KPROBE(KtprobesockVZVXmapVZVXlink)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136985;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_map_link")
int BPF_KRETPROBE(KtretprobesockVZVXmapVZVXlink)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=136985;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_map_remove_links")
int BPF_KPROBE(KtprobesockVZVXmapVZVXremoveVZVXlinks)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137004;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_map_remove_links")
int BPF_KRETPROBE(KtretprobesockVZVXmapVZVXremoveVZVXlinks)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137004;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_map_sk_state_allowed")
int BPF_KPROBE(KtprobesockVZVXmapVZVXskVZVXstateVZVXallowed)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137009;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_map_sk_state_allowed")
int BPF_KRETPROBE(KtretprobesockVZVXmapVZVXskVZVXstateVZVXallowed)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137009;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_map_unhash")
int BPF_KPROBE(KtprobesockVZVXmapVZVXunhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137010;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_map_unhash")
int BPF_KRETPROBE(KtretprobesockVZVXmapVZVXunhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137010;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_map_unref")
int BPF_KPROBE(KtprobesockVZVXmapVZVXunref)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137012;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_map_unref")
int BPF_KRETPROBE(KtretprobesockVZVXmapVZVXunref)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137012;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_map_update_common")
int BPF_KPROBE(KtprobesockVZVXmapVZVXupdateVZVXcommon)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137014;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_map_update_common")
int BPF_KRETPROBE(KtretprobesockVZVXmapVZVXupdateVZVXcommon)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137014;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_queue_err_skb")
int BPF_KPROBE(KtprobesockVZVXqueueVZVXerrVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137017;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_queue_err_skb")
int BPF_KRETPROBE(KtretprobesockVZVXqueueVZVXerrVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137017;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_rmem_free")
int BPF_KPROBE(KtprobesockVZVXrmemVZVXfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137018;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_rmem_free")
int BPF_KRETPROBE(KtretprobesockVZVXrmemVZVXfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137018;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/subflow_syn_recv_sock")
int BPF_KPROBE(KtprobesubflowVZVXsynVZVXrecvVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137436;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/subflow_syn_recv_sock")
int BPF_KRETPROBE(KtretprobesubflowVZVXsynVZVXrecvVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137436;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/subflow_v4_send_synack")
int BPF_KPROBE(KtprobesubflowVZVXv4VZVXsendVZVXsynack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137446;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/subflow_v4_send_synack")
int BPF_KRETPROBE(KtretprobesubflowVZVXv4VZVXsendVZVXsynack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137446;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/subflow_v6_send_synack")
int BPF_KPROBE(KtprobesubflowVZVXv6VZVXsendVZVXsynack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137451;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/subflow_v6_send_synack")
int BPF_KRETPROBE(KtretprobesubflowVZVXv6VZVXsendVZVXsynack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137451;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/taskstats_user_cmd")
int BPF_KPROBE(KtprobetaskstatsVZVXuserVZVXcmd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137729;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/taskstats_user_cmd")
int BPF_KRETPROBE(KtretprobetaskstatsVZVXuserVZVXcmd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137729;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp4_proc_exit_net")
int BPF_KPROBE(Ktprobetcp4VZVXprocVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137760;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp4_proc_exit_net")
int BPF_KRETPROBE(Ktretprobetcp4VZVXprocVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137760;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp4_proc_init_net")
int BPF_KPROBE(Ktprobetcp4VZVXprocVZVXinitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137762;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp4_proc_init_net")
int BPF_KRETPROBE(Ktretprobetcp4VZVXprocVZVXinitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137762;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp6_proc_exit")
int BPF_KPROBE(Ktprobetcp6VZVXprocVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137764;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp6_proc_exit")
int BPF_KRETPROBE(Ktretprobetcp6VZVXprocVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137764;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp6_proc_init")
int BPF_KPROBE(Ktprobetcp6VZVXprocVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137765;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp6_proc_init")
int BPF_KRETPROBE(Ktretprobetcp6VZVXprocVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137765;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_abort")
int BPF_KPROBE(KtprobetcpVZVXabort)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137767;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_abort")
int BPF_KRETPROBE(KtretprobetcpVZVXabort)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137767;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_abort_override")
int BPF_KPROBE(KtprobetcpVZVXabortVZVXoverride)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137769;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_abort_override")
int BPF_KRETPROBE(KtretprobetcpVZVXabortVZVXoverride)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137769;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_add_backlog")
int BPF_KPROBE(KtprobetcpVZVXaddVZVXbacklog)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137771;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_add_backlog")
int BPF_KRETPROBE(KtretprobetcpVZVXaddVZVXbacklog)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137771;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_can_repair_sock")
int BPF_KPROBE(KtprobetcpVZVXcanVZVXrepairVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137774;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_can_repair_sock")
int BPF_KRETPROBE(KtretprobetcpVZVXcanVZVXrepairVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137774;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_check_oom")
int BPF_KPROBE(KtprobetcpVZVXcheckVZVXoom)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137776;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_check_oom")
int BPF_KRETPROBE(KtretprobetcpVZVXcheckVZVXoom)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137776;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_cleanup_rbuf")
int BPF_KPROBE(KtprobetcpVZVXcleanupVZVXrbuf)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137777;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_cleanup_rbuf")
int BPF_KRETPROBE(KtretprobetcpVZVXcleanupVZVXrbuf)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137777;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_cleanup_ulp")
int BPF_KPROBE(KtprobetcpVZVXcleanupVZVXulp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137778;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_cleanup_ulp")
int BPF_KRETPROBE(KtretprobetcpVZVXcleanupVZVXulp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137778;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_clear_md5_list")
int BPF_KPROBE(KtprobetcpVZVXclearVZVXmd5VZVXlist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137779;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_clear_md5_list")
int BPF_KRETPROBE(KtretprobetcpVZVXclearVZVXmd5VZVXlist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137779;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_close")
int BPF_KPROBE(KtprobetcpVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137782;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_close")
int BPF_KRETPROBE(KtretprobetcpVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137782;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_disconnect")
int BPF_KPROBE(KtprobetcpVZVXdisconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137783;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_disconnect")
int BPF_KRETPROBE(KtretprobetcpVZVXdisconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137783;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_done")
int BPF_KPROBE(KtprobetcpVZVXdone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137784;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_done")
int BPF_KRETPROBE(KtretprobetcpVZVXdone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137784;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_downgrade_zcopy_pure")
int BPF_KPROBE(KtprobetcpVZVXdowngradeVZVXzcopyVZVXpure)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137785;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_downgrade_zcopy_pure")
int BPF_KRETPROBE(KtretprobetcpVZVXdowngradeVZVXzcopyVZVXpure)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137785;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_enter_memory_pressure")
int BPF_KPROBE(KtprobetcpVZVXenterVZVXmemoryVZVXpressure)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137786;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_enter_memory_pressure")
int BPF_KRETPROBE(KtretprobetcpVZVXenterVZVXmemoryVZVXpressure)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137786;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_filter")
int BPF_KPROBE(KtprobetcpVZVXfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137787;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_filter")
int BPF_KRETPROBE(KtretprobetcpVZVXfilter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137787;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_free_fastopen_req")
int BPF_KPROBE(KtprobetcpVZVXfreeVZVXfastopenVZVXreq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137788;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_free_fastopen_req")
int BPF_KRETPROBE(KtretprobetcpVZVXfreeVZVXfastopenVZVXreq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137788;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_get_cookie_sock")
int BPF_KPROBE(KtprobetcpVZVXgetVZVXcookieVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137791;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_get_cookie_sock")
int BPF_KRETPROBE(KtretprobetcpVZVXgetVZVXcookieVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137791;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_get_info")
int BPF_KPROBE(KtprobetcpVZVXgetVZVXinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137795;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_get_info")
int BPF_KRETPROBE(KtretprobetcpVZVXgetVZVXinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137795;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_get_info_chrono_stats")
int BPF_KPROBE(KtprobetcpVZVXgetVZVXinfoVZVXchronoVZVXstats)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137797;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_get_info_chrono_stats")
int BPF_KRETPROBE(KtretprobetcpVZVXgetVZVXinfoVZVXchronoVZVXstats)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137797;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_get_timestamping_opt_stats")
int BPF_KPROBE(KtprobetcpVZVXgetVZVXtimestampingVZVXoptVZVXstats)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137799;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_get_timestamping_opt_stats")
int BPF_KRETPROBE(KtretprobetcpVZVXgetVZVXtimestampingVZVXoptVZVXstats)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137799;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_getsockopt")
int BPF_KPROBE(KtprobetcpVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137800;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_getsockopt")
int BPF_KRETPROBE(KtretprobetcpVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137800;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_inbound_hash")
int BPF_KPROBE(KtprobetcpVZVXinboundVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137802;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_inbound_hash")
int BPF_KRETPROBE(KtretprobetcpVZVXinboundVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137802;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_inbound_md5_hash")
int BPF_KPROBE(KtprobetcpVZVXinboundVZVXmd5VZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137804;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_inbound_md5_hash")
int BPF_KRETPROBE(KtretprobetcpVZVXinboundVZVXmd5VZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137804;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_init_sock")
int BPF_KPROBE(KtprobetcpVZVXinitVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137806;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_init_sock")
int BPF_KRETPROBE(KtretprobetcpVZVXinitVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137806;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_inq_hint")
int BPF_KPROBE(KtprobetcpVZVXinqVZVXhint)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137807;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_inq_hint")
int BPF_KRETPROBE(KtretprobetcpVZVXinqVZVXhint)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137807;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ioctl")
int BPF_KPROBE(KtprobetcpVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137808;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ioctl")
int BPF_KRETPROBE(KtretprobetcpVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137808;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ld_RTO_revert")
int BPF_KPROBE(KtprobetcpVZVXldVZVXRTOVZVXrevert)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137810;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ld_RTO_revert")
int BPF_KRETPROBE(KtretprobetcpVZVXldVZVXRTOVZVXrevert)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137810;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_leave_memory_pressure")
int BPF_KPROBE(KtprobetcpVZVXleaveVZVXmemoryVZVXpressure)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137811;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_leave_memory_pressure")
int BPF_KRETPROBE(KtretprobetcpVZVXleaveVZVXmemoryVZVXpressure)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137811;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_mark_push")
int BPF_KPROBE(KtprobetcpVZVXmarkVZVXpush)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137813;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_mark_push")
int BPF_KRETPROBE(KtretprobetcpVZVXmarkVZVXpush)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137813;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_md5_do_add")
int BPF_KPROBE(KtprobetcpVZVXmd5VZVXdoVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137817;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_md5_do_add")
int BPF_KRETPROBE(KtretprobetcpVZVXmd5VZVXdoVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137817;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_md5_do_del")
int BPF_KPROBE(KtprobetcpVZVXmd5VZVXdoVZVXdel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137819;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_md5_do_del")
int BPF_KRETPROBE(KtretprobetcpVZVXmd5VZVXdoVZVXdel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137819;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_md5_do_lookup_exact")
int BPF_KPROBE(KtprobetcpVZVXmd5VZVXdoVZVXlookupVZVXexact)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137821;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_md5_do_lookup_exact")
int BPF_KRETPROBE(KtretprobetcpVZVXmd5VZVXdoVZVXlookupVZVXexact)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137821;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_md5_key_copy")
int BPF_KPROBE(KtprobetcpVZVXmd5VZVXkeyVZVXcopy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137825;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_md5_key_copy")
int BPF_KRETPROBE(KtretprobetcpVZVXmd5VZVXkeyVZVXcopy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137825;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_md5sig_info_add")
int BPF_KPROBE(KtprobetcpVZVXmd5sigVZVXinfoVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137828;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_md5sig_info_add")
int BPF_KRETPROBE(KtretprobetcpVZVXmd5sigVZVXinfoVZVXadd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137828;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_mmap")
int BPF_KPROBE(KtprobetcpVZVXmmap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137830;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_mmap")
int BPF_KRETPROBE(KtretprobetcpVZVXmmap)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137830;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_peek_len")
int BPF_KPROBE(KtprobetcpVZVXpeekVZVXlen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137833;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_peek_len")
int BPF_KRETPROBE(KtretprobetcpVZVXpeekVZVXlen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137833;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_push")
int BPF_KPROBE(KtprobetcpVZVXpush)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137836;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_push")
int BPF_KRETPROBE(KtretprobetcpVZVXpush)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137836;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_rate_check_app_limited")
int BPF_KPROBE(KtprobetcpVZVXrateVZVXcheckVZVXappVZVXlimited)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137837;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_rate_check_app_limited")
int BPF_KRETPROBE(KtretprobetcpVZVXrateVZVXcheckVZVXappVZVXlimited)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137837;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_rate_gen")
int BPF_KPROBE(KtprobetcpVZVXrateVZVXgen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137839;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_rate_gen")
int BPF_KRETPROBE(KtretprobetcpVZVXrateVZVXgen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137839;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_rate_skb_delivered")
int BPF_KPROBE(KtprobetcpVZVXrateVZVXskbVZVXdelivered)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137841;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_rate_skb_delivered")
int BPF_KRETPROBE(KtretprobetcpVZVXrateVZVXskbVZVXdelivered)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137841;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_rate_skb_sent")
int BPF_KPROBE(KtprobetcpVZVXrateVZVXskbVZVXsent)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137842;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_rate_skb_sent")
int BPF_KRETPROBE(KtretprobetcpVZVXrateVZVXskbVZVXsent)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137842;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_read_done")
int BPF_KPROBE(KtprobetcpVZVXreadVZVXdone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137844;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_read_done")
int BPF_KRETPROBE(KtretprobetcpVZVXreadVZVXdone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137844;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_read_skb")
int BPF_KPROBE(KtprobetcpVZVXreadVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137845;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_read_skb")
int BPF_KRETPROBE(KtretprobetcpVZVXreadVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137845;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_read_sock")
int BPF_KPROBE(KtprobetcpVZVXreadVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137847;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_read_sock")
int BPF_KRETPROBE(KtretprobetcpVZVXreadVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137847;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_read_sock_noack")
int BPF_KPROBE(KtprobetcpVZVXreadVZVXsockVZVXnoack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137848;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_read_sock_noack")
int BPF_KRETPROBE(KtretprobetcpVZVXreadVZVXsockVZVXnoack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137848;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_recv_skb")
int BPF_KPROBE(KtprobetcpVZVXrecvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137850;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_recv_skb")
int BPF_KRETPROBE(KtretprobetcpVZVXrecvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137850;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_recv_timestamp")
int BPF_KPROBE(KtprobetcpVZVXrecvVZVXtimestamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137852;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_recv_timestamp")
int BPF_KRETPROBE(KtretprobetcpVZVXrecvVZVXtimestamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137852;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_recvmsg")
int BPF_KPROBE(KtprobetcpVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137853;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_recvmsg")
int BPF_KRETPROBE(KtretprobetcpVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137853;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_recvmsg_dmabuf")
int BPF_KPROBE(KtprobetcpVZVXrecvmsgVZVXdmabuf)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137855;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_recvmsg_dmabuf")
int BPF_KRETPROBE(KtretprobetcpVZVXrecvmsgVZVXdmabuf)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137855;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_recvmsg_locked")
int BPF_KPROBE(KtprobetcpVZVXrecvmsgVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137857;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_recvmsg_locked")
int BPF_KRETPROBE(KtretprobetcpVZVXrecvmsgVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137857;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_release_cb_override")
int BPF_KPROBE(KtprobetcpVZVXreleaseVZVXcbVZVXoverride)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137860;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_release_cb_override")
int BPF_KRETPROBE(KtretprobetcpVZVXreleaseVZVXcbVZVXoverride)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137860;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_remove_empty_skb")
int BPF_KPROBE(KtprobetcpVZVXremoveVZVXemptyVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137861;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_remove_empty_skb")
int BPF_KRETPROBE(KtretprobetcpVZVXremoveVZVXemptyVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137861;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_repair_options_est")
int BPF_KPROBE(KtprobetcpVZVXrepairVZVXoptionsVZVXest)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137863;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_repair_options_est")
int BPF_KRETPROBE(KtretprobetcpVZVXrepairVZVXoptionsVZVXest)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137863;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_req_err")
int BPF_KPROBE(KtprobetcpVZVXreqVZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137865;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_req_err")
int BPF_KRETPROBE(KtretprobetcpVZVXreqVZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137865;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_send_mss")
int BPF_KPROBE(KtprobetcpVZVXsendVZVXmss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137867;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_send_mss")
int BPF_KRETPROBE(KtretprobetcpVZVXsendVZVXmss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137867;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_sendmsg_fastopen")
int BPF_KPROBE(KtprobetcpVZVXsendmsgVZVXfastopen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137870;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_sendmsg_fastopen")
int BPF_KRETPROBE(KtretprobetcpVZVXsendmsgVZVXfastopen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137870;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_sendmsg_locked")
int BPF_KPROBE(KtprobetcpVZVXsendmsgVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137871;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_sendmsg_locked")
int BPF_KRETPROBE(KtretprobetcpVZVXsendmsgVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137871;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_set_rcvlowat")
int BPF_KPROBE(KtprobetcpVZVXsetVZVXrcvlowat)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137875;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_set_rcvlowat")
int BPF_KRETPROBE(KtretprobetcpVZVXsetVZVXrcvlowat)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137875;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_set_state")
int BPF_KPROBE(KtprobetcpVZVXsetVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137876;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_set_state")
int BPF_KRETPROBE(KtretprobetcpVZVXsetVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137876;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_set_ulp")
int BPF_KPROBE(KtprobetcpVZVXsetVZVXulp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137878;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_set_ulp")
int BPF_KRETPROBE(KtretprobetcpVZVXsetVZVXulp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137878;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_set_window_clamp")
int BPF_KPROBE(KtprobetcpVZVXsetVZVXwindowVZVXclamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137879;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_set_window_clamp")
int BPF_KRETPROBE(KtretprobetcpVZVXsetVZVXwindowVZVXclamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137879;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_setsockopt")
int BPF_KPROBE(KtprobetcpVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137880;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_setsockopt")
int BPF_KRETPROBE(KtretprobetcpVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137880;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_shutdown")
int BPF_KPROBE(KtprobetcpVZVXshutdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137882;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_shutdown")
int BPF_KRETPROBE(KtretprobetcpVZVXshutdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137882;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_sk_exit")
int BPF_KPROBE(KtprobetcpVZVXskVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137883;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_sk_exit")
int BPF_KRETPROBE(KtretprobetcpVZVXskVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137883;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_sk_init")
int BPF_KPROBE(KtprobetcpVZVXskVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137886;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_sk_init")
int BPF_KRETPROBE(KtretprobetcpVZVXskVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137886;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_skb_entail")
int BPF_KPROBE(KtprobetcpVZVXskbVZVXentail)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137887;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_skb_entail")
int BPF_KRETPROBE(KtretprobetcpVZVXskbVZVXentail)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137887;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_sock_set_cork")
int BPF_KPROBE(KtprobetcpVZVXsockVZVXsetVZVXcork)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137888;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_sock_set_cork")
int BPF_KRETPROBE(KtretprobetcpVZVXsockVZVXsetVZVXcork)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137888;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_sock_set_keepcnt")
int BPF_KPROBE(KtprobetcpVZVXsockVZVXsetVZVXkeepcnt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137889;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_sock_set_keepcnt")
int BPF_KRETPROBE(KtretprobetcpVZVXsockVZVXsetVZVXkeepcnt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137889;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_sock_set_keepidle")
int BPF_KPROBE(KtprobetcpVZVXsockVZVXsetVZVXkeepidle)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137890;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_sock_set_keepidle")
int BPF_KRETPROBE(KtretprobetcpVZVXsockVZVXsetVZVXkeepidle)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137890;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_sock_set_keepidle_locked")
int BPF_KPROBE(KtprobetcpVZVXsockVZVXsetVZVXkeepidleVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137891;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_sock_set_keepidle_locked")
int BPF_KRETPROBE(KtretprobetcpVZVXsockVZVXsetVZVXkeepidleVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137891;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_sock_set_keepintvl")
int BPF_KPROBE(KtprobetcpVZVXsockVZVXsetVZVXkeepintvl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137892;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_sock_set_keepintvl")
int BPF_KRETPROBE(KtretprobetcpVZVXsockVZVXsetVZVXkeepintvl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137892;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_sock_set_maxseg")
int BPF_KPROBE(KtprobetcpVZVXsockVZVXsetVZVXmaxseg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137893;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_sock_set_maxseg")
int BPF_KRETPROBE(KtretprobetcpVZVXsockVZVXsetVZVXmaxseg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137893;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_sock_set_nodelay")
int BPF_KPROBE(KtprobetcpVZVXsockVZVXsetVZVXnodelay)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137894;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_sock_set_nodelay")
int BPF_KRETPROBE(KtretprobetcpVZVXsockVZVXsetVZVXnodelay)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137894;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_sock_set_quickack")
int BPF_KPROBE(KtprobetcpVZVXsockVZVXsetVZVXquickack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137895;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_sock_set_quickack")
int BPF_KRETPROBE(KtretprobetcpVZVXsockVZVXsetVZVXquickack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137895;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_sock_set_syncnt")
int BPF_KPROBE(KtprobetcpVZVXsockVZVXsetVZVXsyncnt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137896;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_sock_set_syncnt")
int BPF_KRETPROBE(KtretprobetcpVZVXsockVZVXsetVZVXsyncnt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137896;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_sock_set_user_timeout")
int BPF_KPROBE(KtprobetcpVZVXsockVZVXsetVZVXuserVZVXtimeout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137897;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_sock_set_user_timeout")
int BPF_KRETPROBE(KtretprobetcpVZVXsockVZVXsetVZVXuserVZVXtimeout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137897;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_splice_data_recv")
int BPF_KPROBE(KtprobetcpVZVXspliceVZVXdataVZVXrecv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137899;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_splice_data_recv")
int BPF_KRETPROBE(KtretprobetcpVZVXspliceVZVXdataVZVXrecv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137899;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_splice_eof")
int BPF_KPROBE(KtprobetcpVZVXspliceVZVXeof)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137900;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_splice_eof")
int BPF_KRETPROBE(KtretprobetcpVZVXspliceVZVXeof)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137900;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_splice_read")
int BPF_KPROBE(KtprobetcpVZVXspliceVZVXread)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137902;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_splice_read")
int BPF_KRETPROBE(KtretprobetcpVZVXspliceVZVXread)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137902;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_stream_alloc_skb")
int BPF_KPROBE(KtprobetcpVZVXstreamVZVXallocVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_stream_alloc_skb")
int BPF_KRETPROBE(KtretprobetcpVZVXstreamVZVXallocVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_twsk_unique")
int BPF_KPROBE(KtprobetcpVZVXtwskVZVXunique)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137907;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_twsk_unique")
int BPF_KRETPROBE(KtretprobetcpVZVXtwskVZVXunique)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137907;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_update_recv_tstamps")
int BPF_KPROBE(KtprobetcpVZVXupdateVZVXrecvVZVXtstamps)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137911;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_update_recv_tstamps")
int BPF_KRETPROBE(KtretprobetcpVZVXupdateVZVXrecvVZVXtstamps)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137911;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_update_ulp")
int BPF_KPROBE(KtprobetcpVZVXupdateVZVXulp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137913;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_update_ulp")
int BPF_KRETPROBE(KtretprobetcpVZVXupdateVZVXulp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137913;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_ao_sign_reset")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXaoVZVXsignVZVXreset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137915;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_ao_sign_reset")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXaoVZVXsignVZVXreset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137915;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_conn_request")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXconnVZVXrequest)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137916;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_conn_request")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXconnVZVXrequest)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137916;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_connect")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137917;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_connect")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137917;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_destroy_sock")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXdestroyVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137918;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_destroy_sock")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXdestroyVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137918;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_do_rcv")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXdoVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137919;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_do_rcv")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXdoVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137919;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_early_demux")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXearlyVZVXdemux)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137920;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_early_demux")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXearlyVZVXdemux)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137920;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_err")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137921;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_err")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137921;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_fill_cb")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXfillVZVXcb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137923;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_fill_cb")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXfillVZVXcb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137923;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_get_syncookie")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXgetVZVXsyncookie)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137925;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_get_syncookie")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXgetVZVXsyncookie)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137925;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_init_seq")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXinitVZVXseq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137927;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_init_seq")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXinitVZVXseq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137927;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_init_sock")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXinitVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137928;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_init_sock")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXinitVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137928;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_init_ts_off")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXinitVZVXtsVZVXoff)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137929;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_init_ts_off")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXinitVZVXtsVZVXoff)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137929;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_md5_hash_skb")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXmd5VZVXhashVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137935;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_md5_hash_skb")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXmd5VZVXhashVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137935;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_md5_lookup")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXmd5VZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137937;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_md5_lookup")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXmd5VZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137937;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_mtu_reduced")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXmtuVZVXreduced)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137938;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_mtu_reduced")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXmtuVZVXreduced)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137938;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_parse_md5_keys")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXparseVZVXmd5VZVXkeys)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137939;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_parse_md5_keys")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXparseVZVXmd5VZVXkeys)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137939;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_pre_connect")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXpreVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137940;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_pre_connect")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXpreVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137940;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_rcv")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137941;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_rcv")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137941;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_reqsk_destructor")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXreqskVZVXdestructor)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137942;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_reqsk_destructor")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXreqskVZVXdestructor)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137942;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_reqsk_send_ack")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXreqskVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137944;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_reqsk_send_ack")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXreqskVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137944;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_restore_cb")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXrestoreVZVXcb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137945;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_restore_cb")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXrestoreVZVXcb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137945;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_route_req")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXrouteVZVXreq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137946;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_route_req")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXrouteVZVXreq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137946;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_send_ack")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137948;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_send_ack")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137948;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_send_check")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXsendVZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137949;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_send_check")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXsendVZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137949;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_send_reset")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXsendVZVXreset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137951;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_send_reset")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXsendVZVXreset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137951;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_send_synack")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXsendVZVXsynack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137952;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_send_synack")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXsendVZVXsynack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137952;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_syn_recv_sock")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXsynVZVXrecvVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137953;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_syn_recv_sock")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXsynVZVXrecvVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137953;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v4_timewait_ack")
int BPF_KPROBE(KtprobetcpVZVXv4VZVXtimewaitVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137955;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v4_timewait_ack")
int BPF_KRETPROBE(KtretprobetcpVZVXv4VZVXtimewaitVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137955;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_conn_request")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXconnVZVXrequest)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137956;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_conn_request")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXconnVZVXrequest)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137956;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_connect")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137957;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_connect")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137957;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_do_rcv")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXdoVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137958;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_do_rcv")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXdoVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137958;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_early_demux")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXearlyVZVXdemux)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137959;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_early_demux")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXearlyVZVXdemux)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137959;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_err")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137960;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_err")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137960;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_fill_cb")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXfillVZVXcb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137962;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_fill_cb")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXfillVZVXcb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137962;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_get_syncookie")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXgetVZVXsyncookie)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137964;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_get_syncookie")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXgetVZVXsyncookie)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137964;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_init_seq")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXinitVZVXseq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137965;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_init_seq")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXinitVZVXseq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137965;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_init_sock")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXinitVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137966;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_init_sock")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXinitVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137966;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_init_ts_off")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXinitVZVXtsVZVXoff)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137967;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_init_ts_off")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXinitVZVXtsVZVXoff)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137967;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_md5_hash_skb")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXmd5VZVXhashVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137970;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_md5_hash_skb")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXmd5VZVXhashVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137970;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_md5_lookup")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXmd5VZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137971;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_md5_lookup")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXmd5VZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137971;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_mtu_reduced")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXmtuVZVXreduced)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137972;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_mtu_reduced")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXmtuVZVXreduced)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137972;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_parse_md5_keys")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXparseVZVXmd5VZVXkeys)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137973;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_parse_md5_keys")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXparseVZVXmd5VZVXkeys)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137973;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_pre_connect")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXpreVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137974;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_pre_connect")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXpreVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137974;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_rcv")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137975;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_rcv")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137975;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_reqsk_destructor")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXreqskVZVXdestructor)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137976;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_reqsk_destructor")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXreqskVZVXdestructor)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137976;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_reqsk_send_ack")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXreqskVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137977;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_reqsk_send_ack")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXreqskVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137977;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_restore_cb")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXrestoreVZVXcb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137978;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_restore_cb")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXrestoreVZVXcb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137978;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_route_req")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXrouteVZVXreq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137979;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_route_req")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXrouteVZVXreq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137979;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_send_check")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXsendVZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137980;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_send_check")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXsendVZVXcheck)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137980;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_send_reset")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXsendVZVXreset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137981;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_send_reset")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXsendVZVXreset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137981;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_send_response")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXsendVZVXresponse)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137983;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_send_response")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXsendVZVXresponse)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137983;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_send_synack")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXsendVZVXsynack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137984;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_send_synack")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXsendVZVXsynack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137984;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_syn_recv_sock")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXsynVZVXrecvVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137985;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_syn_recv_sock")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXsynVZVXrecvVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137985;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_v6_timewait_ack")
int BPF_KPROBE(KtprobetcpVZVXv6VZVXtimewaitVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137986;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_v6_timewait_ack")
int BPF_KRETPROBE(KtretprobetcpVZVXv6VZVXtimewaitVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137986;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_wmem_schedule")
int BPF_KPROBE(KtprobetcpVZVXwmemVZVXschedule)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137988;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_wmem_schedule")
int BPF_KRETPROBE(KtretprobetcpVZVXwmemVZVXschedule)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137988;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_write_queue_purge")
int BPF_KPROBE(KtprobetcpVZVXwriteVZVXqueueVZVXpurge)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137989;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_write_queue_purge")
int BPF_KRETPROBE(KtretprobetcpVZVXwriteVZVXqueueVZVXpurge)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137989;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_xa_pool_commit_locked")
int BPF_KPROBE(KtprobetcpVZVXxaVZVXpoolVZVXcommitVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137991;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_xa_pool_commit_locked")
int BPF_KRETPROBE(KtretprobetcpVZVXxaVZVXpoolVZVXcommitVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137991;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_zerocopy_receive")
int BPF_KPROBE(KtprobetcpVZVXzerocopyVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137993;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_zerocopy_receive")
int BPF_KRETPROBE(KtretprobetcpVZVXzerocopyVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137993;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcpv6_net_exit")
int BPF_KPROBE(Ktprobetcpv6VZVXnetVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137998;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcpv6_net_exit")
int BPF_KRETPROBE(Ktretprobetcpv6VZVXnetVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137998;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcpv6_net_init")
int BPF_KPROBE(Ktprobetcpv6VZVXnetVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137999;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcpv6_net_init")
int BPF_KRETPROBE(Ktretprobetcpv6VZVXnetVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=137999;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tomoyo_socket_bind")
int BPF_KPROBE(KtprobetomoyoVZVXsocketVZVXbind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=138280;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tomoyo_socket_bind")
int BPF_KRETPROBE(KtretprobetomoyoVZVXsocketVZVXbind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=138280;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tomoyo_socket_connect")
int BPF_KPROBE(KtprobetomoyoVZVXsocketVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=138281;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tomoyo_socket_connect")
int BPF_KRETPROBE(KtretprobetomoyoVZVXsocketVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=138281;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tomoyo_socket_listen")
int BPF_KPROBE(KtprobetomoyoVZVXsocketVZVXlisten)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=138282;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tomoyo_socket_listen")
int BPF_KRETPROBE(KtretprobetomoyoVZVXsocketVZVXlisten)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=138282;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tomoyo_socket_sendmsg")
int BPF_KPROBE(KtprobetomoyoVZVXsocketVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=138283;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tomoyo_socket_sendmsg")
int BPF_KRETPROBE(KtretprobetomoyoVZVXsocketVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=138283;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tun_build_skb")
int BPF_KPROBE(KtprobetunVZVXbuildVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=138912;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tun_build_skb")
int BPF_KRETPROBE(KtretprobetunVZVXbuildVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=138912;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tun_net_xmit")
int BPF_KPROBE(KtprobetunVZVXnetVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=138966;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tun_net_xmit")
int BPF_KRETPROBE(KtretprobetunVZVXnetVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=138966;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tun_recvmsg")
int BPF_KPROBE(KtprobetunVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=138975;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tun_recvmsg")
int BPF_KRETPROBE(KtretprobetunVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=138975;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tun_ring_recv")
int BPF_KPROBE(KtprobetunVZVXringVZVXrecv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=138977;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tun_ring_recv")
int BPF_KRETPROBE(KtretprobetunVZVXringVZVXrecv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=138977;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tun_sendmsg")
int BPF_KPROBE(KtprobetunVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=138980;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tun_sendmsg")
int BPF_KRETPROBE(KtretprobetunVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=138980;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tun_sock_write_space")
int BPF_KPROBE(KtprobetunVZVXsockVZVXwriteVZVXspace)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=138993;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tun_sock_write_space")
int BPF_KRETPROBE(KtretprobetunVZVXsockVZVXwriteVZVXspace)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=138993;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tun_vnet_hdr_tnl_from_skb")
int BPF_KPROBE(KtprobetunVZVXvnetVZVXhdrVZVXtnlVZVXfromVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=138996;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tun_vnet_hdr_tnl_from_skb")
int BPF_KRETPROBE(KtretprobetunVZVXvnetVZVXhdrVZVXtnlVZVXfromVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=138996;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tun_xdp_xmit")
int BPF_KPROBE(KtprobetunVZVXxdpVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139004;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tun_xdp_xmit")
int BPF_KRETPROBE(KtretprobetunVZVXxdpVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139004;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp4_gro_complete")
int BPF_KPROBE(Ktprobeudp4VZVXgroVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139170;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp4_gro_complete")
int BPF_KRETPROBE(Ktretprobeudp4VZVXgroVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139170;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp4_gro_receive")
int BPF_KPROBE(Ktprobeudp4VZVXgroVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139171;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp4_gro_receive")
int BPF_KRETPROBE(Ktretprobeudp4VZVXgroVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139171;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp4_ufo_fragment")
int BPF_KPROBE(Ktprobeudp4VZVXufoVZVXfragment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139172;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp4_ufo_fragment")
int BPF_KRETPROBE(Ktretprobeudp4VZVXufoVZVXfragment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139172;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_gro_complete")
int BPF_KPROBE(KtprobeudpVZVXgroVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139177;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_gro_complete")
int BPF_KRETPROBE(KtretprobeudpVZVXgroVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139177;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_gro_receive")
int BPF_KPROBE(KtprobeudpVZVXgroVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139179;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_gro_receive")
int BPF_KRETPROBE(KtretprobeudpVZVXgroVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139179;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_gro_receive_segment")
int BPF_KPROBE(KtprobeudpVZVXgroVZVXreceiveVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139180;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_gro_receive_segment")
int BPF_KRETPROBE(KtretprobeudpVZVXgroVZVXreceiveVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139180;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_set_peek_off")
int BPF_KPROBE(KtprobeudpVZVXsetVZVXpeekVZVXoff)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139181;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_set_peek_off")
int BPF_KRETPROBE(KtretprobeudpVZVXsetVZVXpeekVZVXoff)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139181;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_tunnel_update_gro_lookup")
int BPF_KPROBE(KtprobeudpVZVXtunnelVZVXupdateVZVXgroVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139183;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_tunnel_update_gro_lookup")
int BPF_KRETPROBE(KtretprobeudpVZVXtunnelVZVXupdateVZVXgroVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139183;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_tunnel_update_gro_rcv")
int BPF_KPROBE(KtprobeudpVZVXtunnelVZVXupdateVZVXgroVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139185;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_tunnel_update_gro_rcv")
int BPF_KRETPROBE(KtretprobeudpVZVXtunnelVZVXupdateVZVXgroVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139185;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/uevent_net_rcv_skb")
int BPF_KPROBE(KtprobeueventVZVXnetVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139192;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/uevent_net_rcv_skb")
int BPF_KRETPROBE(KtretprobeueventVZVXnetVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139192;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/virtio_net_hdr_tnl_to_skb")
int BPF_KPROBE(KtprobevirtioVZVXnetVZVXhdrVZVXtnlVZVXtoVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139935;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/virtio_net_hdr_tnl_to_skb")
int BPF_KRETPROBE(KtretprobevirtioVZVXnetVZVXhdrVZVXtnlVZVXtoVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=139935;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xennet_start_xmit")
int BPF_KPROBE(KtprobexennetVZVXstartVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140712;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xennet_start_xmit")
int BPF_KRETPROBE(KtretprobexennetVZVXstartVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140712;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xennet_xdp_xmit")
int BPF_KPROBE(KtprobexennetVZVXxdpVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140721;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xennet_xdp_xmit")
int BPF_KRETPROBE(KtretprobexennetVZVXxdpVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140721;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xfrm4_gro_udp_encap_rcv")
int BPF_KPROBE(Ktprobexfrm4VZVXgroVZVXudpVZVXencapVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140729;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xfrm4_gro_udp_encap_rcv")
int BPF_KRETPROBE(Ktretprobexfrm4VZVXgroVZVXudpVZVXencapVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140729;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xfrm4_udp_encap_rcv")
int BPF_KPROBE(Ktprobexfrm4VZVXudpVZVXencapVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140735;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xfrm4_udp_encap_rcv")
int BPF_KRETPROBE(Ktretprobexfrm4VZVXudpVZVXencapVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140735;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xfrm6_gro_udp_encap_rcv")
int BPF_KPROBE(Ktprobexfrm6VZVXgroVZVXudpVZVXencapVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140736;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xfrm6_gro_udp_encap_rcv")
int BPF_KRETPROBE(Ktretprobexfrm6VZVXgroVZVXudpVZVXencapVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140736;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xfrm6_udp_encap_rcv")
int BPF_KPROBE(Ktprobexfrm6VZVXudpVZVXencapVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140746;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xfrm6_udp_encap_rcv")
int BPF_KRETPROBE(Ktretprobexfrm6VZVXudpVZVXencapVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140746;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xfrm_icmp_flow_decode")
int BPF_KPROBE(KtprobexfrmVZVXicmpVZVXflowVZVXdecode)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140773;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xfrm_icmp_flow_decode")
int BPF_KRETPROBE(KtretprobexfrmVZVXicmpVZVXflowVZVXdecode)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140773;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xfrm_in_fwd_icmp")
int BPF_KPROBE(KtprobexfrmVZVXinVZVXfwdVZVXicmp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140778;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xfrm_in_fwd_icmp")
int BPF_KRETPROBE(KtretprobexfrmVZVXinVZVXfwdVZVXicmp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140778;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xfrm_out_fwd_icmp")
int BPF_KPROBE(KtprobexfrmVZVXoutVZVXfwdVZVXicmp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140799;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xfrm_out_fwd_icmp")
int BPF_KRETPROBE(KtretprobexfrmVZVXoutVZVXfwdVZVXicmp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140799;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xfrm_selector_inner_icmp_match")
int BPF_KPROBE(KtprobexfrmVZVXselectorVZVXinnerVZVXicmpVZVXmatch)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140870;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xfrm_selector_inner_icmp_match")
int BPF_KRETPROBE(KtretprobexfrmVZVXselectorVZVXinnerVZVXicmpVZVXmatch)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140870;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xfrm_sk_policy_insert")
int BPF_KPROBE(KtprobexfrmVZVXskVZVXpolicyVZVXinsert)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140874;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xfrm_sk_policy_insert")
int BPF_KRETPROBE(KtretprobexfrmVZVXskVZVXpolicyVZVXinsert)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140874;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xfrm_sk_policy_lookup")
int BPF_KPROBE(KtprobexfrmVZVXskVZVXpolicyVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140876;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xfrm_sk_policy_lookup")
int BPF_KRETPROBE(KtretprobexfrmVZVXskVZVXpolicyVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=140876;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_map_sock_delete")
int BPF_KPROBE(KtprobexskVZVXmapVZVXsockVZVXdelete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=141041;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_map_sock_delete")
int BPF_KRETPROBE(KtretprobexskVZVXmapVZVXsockVZVXdelete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=141041;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_map_try_sock_delete")
int BPF_KPROBE(KtprobexskVZVXmapVZVXtryVZVXsockVZVXdelete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=141043;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_map_try_sock_delete")
int BPF_KRETPROBE(KtretprobexskVZVXmapVZVXtryVZVXsockVZVXdelete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=141043;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__dev_direct_xmit")
int BPF_KPROBE(KtprobeVZVXVZVXdevVZVXdirectVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142304;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__dev_direct_xmit")
int BPF_KRETPROBE(KtretprobeVZVXVZVXdevVZVXdirectVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142304;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__dev_forward_skb")
int BPF_KPROBE(KtprobeVZVXVZVXdevVZVXforwardVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142308;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__dev_forward_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXdevVZVXforwardVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142308;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__dev_forward_skb2")
int BPF_KPROBE(KtprobeVZVXVZVXdevVZVXforwardVZVXskb2)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142310;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__dev_forward_skb2")
int BPF_KRETPROBE(KtretprobeVZVXVZVXdevVZVXforwardVZVXskb2)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142310;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__dev_queue_xmit")
int BPF_KPROBE(KtprobeVZVXVZVXdevVZVXqueueVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142337;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__dev_queue_xmit")
int BPF_KRETPROBE(KtretprobeVZVXVZVXdevVZVXqueueVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142337;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__dev_xmit_skb")
int BPF_KPROBE(KtprobeVZVXVZVXdevVZVXxmitVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142344;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__dev_xmit_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXdevVZVXxmitVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142344;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__free_old_xmit")
int BPF_KPROBE(KtprobeVZVXVZVXfreeVZVXoldVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142593;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__free_old_xmit")
int BPF_KRETPROBE(KtretprobeVZVXVZVXfreeVZVXoldVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142593;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip_dev_find")
int BPF_KPROBE(KtprobeVZVXVZVXipVZVXdevVZVXfind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142894;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip_dev_find")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipVZVXdevVZVXfind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142894;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip_do_redirect")
int BPF_KPROBE(KtprobeVZVXVZVXipVZVXdoVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142896;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip_do_redirect")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipVZVXdoVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142896;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip_options_compile")
int BPF_KPROBE(KtprobeVZVXVZVXipVZVXoptionsVZVXcompile)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142898;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip_options_compile")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipVZVXoptionsVZVXcompile)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142898;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip_options_echo")
int BPF_KPROBE(KtprobeVZVXVZVXipVZVXoptionsVZVXecho)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142900;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip_options_echo")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipVZVXoptionsVZVXecho)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142900;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ip_select_ident")
int BPF_KPROBE(KtprobeVZVXVZVXipVZVXselectVZVXident)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ip_select_ident")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipVZVXselectVZVXident)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__iptunnel_pull_header")
int BPF_KPROBE(KtprobeVZVXVZVXiptunnelVZVXpullVZVXheader)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142906;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__iptunnel_pull_header")
int BPF_KRETPROBE(KtretprobeVZVXVZVXiptunnelVZVXpullVZVXheader)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142906;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__ipv4_sk_update_pmtu")
int BPF_KPROBE(KtprobeVZVXVZVXipv4VZVXskVZVXupdateVZVXpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142908;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__ipv4_sk_update_pmtu")
int BPF_KRETPROBE(KtretprobeVZVXVZVXipv4VZVXskVZVXupdateVZVXpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=142908;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_check_push")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXcheckVZVXpush)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143064;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_check_push")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXcheckVZVXpush)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143064;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_clean_una")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXcleanVZVXuna)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143065;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_clean_una")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXcleanVZVXuna)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143065;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_clear_xmit")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXclearVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143066;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_clear_xmit")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXclearVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143066;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_close")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143068;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_close")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143068;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_close_ssk")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXcloseVZVXssk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143070;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_close_ssk")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXcloseVZVXssk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143070;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_data_acked")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXdataVZVXacked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143071;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_data_acked")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXdataVZVXacked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143071;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_destroy_sock")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXdestroyVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143072;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_destroy_sock")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXdestroyVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143072;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_error_report")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXerrorVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143073;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_error_report")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXerrorVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143073;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_finish_join")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXfinishVZVXjoin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143075;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_finish_join")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXfinishVZVXjoin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143075;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_init_sock")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXinitVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143076;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_init_sock")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXinitVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143076;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_move_skb")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXmoveVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143078;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_move_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXmoveVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143078;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_move_skbs")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXmoveVZVXskbs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143079;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_move_skbs")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXmoveVZVXskbs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143079;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_move_skbs_from_subflow")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXmoveVZVXskbsVZVXfromVZVXsubflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143080;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_move_skbs_from_subflow")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXmoveVZVXskbsVZVXfromVZVXsubflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143080;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_nmpc_sk")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXnmpcVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143082;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_nmpc_sk")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXnmpcVZVXsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143082;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_ofo_queue")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXofoVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143084;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_ofo_queue")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXofoVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143084;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_pm_kernel_worker")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXpmVZVXkernelVZVXworker)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143085;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_pm_kernel_worker")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXpmVZVXkernelVZVXworker)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143085;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_pm_send_ack")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXpmVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143087;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_pm_send_ack")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXpmVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143087;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_push_pending")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXpushVZVXpending)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143089;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_push_pending")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXpushVZVXpending)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143089;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_recvmsg_mskq")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXrecvmsgVZVXmskq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143091;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_recvmsg_mskq")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXrecvmsgVZVXmskq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143091;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_retrans")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXretrans)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143092;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_retrans")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXretrans)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143092;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_retransmit_pending_data")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXretransmitVZVXpendingVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143093;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_retransmit_pending_data")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXretransmitVZVXpendingVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143093;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_setsockopt_set_val")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXsetsockoptVZVXsetVZVXval)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143095;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_setsockopt_set_val")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXsetsockoptVZVXsetVZVXval)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143095;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_subflow_push_pending")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXsubflowVZVXpushVZVXpending)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143097;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_subflow_push_pending")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXsubflowVZVXpushVZVXpending)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143097;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_subflow_send_ack")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXsubflowVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143098;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_subflow_send_ack")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXsubflowVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143098;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_try_fallback")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXtryVZVXfallback)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143100;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_try_fallback")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXtryVZVXfallback)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143100;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_unaccepted_force_close")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXunacceptedVZVXforceVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143101;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_unaccepted_force_close")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXunacceptedVZVXforceVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143101;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__mptcp_wr_shutdown")
int BPF_KPROBE(KtprobeVZVXVZVXmptcpVZVXwrVZVXshutdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143102;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__mptcp_wr_shutdown")
int BPF_KRETPROBE(KtretprobeVZVXVZVXmptcpVZVXwrVZVXshutdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143102;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__netif_receive_skb")
int BPF_KPROBE(KtprobeVZVXVZVXnetifVZVXreceiveVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143182;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__netif_receive_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXnetifVZVXreceiveVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143182;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__netif_receive_skb_core")
int BPF_KPROBE(KtprobeVZVXVZVXnetifVZVXreceiveVZVXskbVZVXcore)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143184;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__netif_receive_skb_core")
int BPF_KRETPROBE(KtretprobeVZVXVZVXnetifVZVXreceiveVZVXskbVZVXcore)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143184;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__netif_receive_skb_one_core")
int BPF_KPROBE(KtprobeVZVXVZVXnetifVZVXreceiveVZVXskbVZVXoneVZVXcore)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143188;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__netif_receive_skb_one_core")
int BPF_KRETPROBE(KtretprobeVZVXVZVXnetifVZVXreceiveVZVXskbVZVXoneVZVXcore)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143188;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__netlink_sendskb")
int BPF_KPROBE(KtprobeVZVXVZVXnetlinkVZVXsendskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143210;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__netlink_sendskb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXnetlinkVZVXsendskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143210;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__nf_ip6_route")
int BPF_KPROBE(KtprobeVZVXVZVXnfVZVXip6VZVXroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143215;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__nf_ip6_route")
int BPF_KRETPROBE(KtretprobeVZVXVZVXnfVZVXip6VZVXroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143215;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_consume_skb")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXconsumeVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143321;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_consume_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXconsumeVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143321;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_inet_sk_error_report")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXinetVZVXskVZVXerrorVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143350;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_inet_sk_error_report")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXinetVZVXskVZVXerrorVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143350;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_inet_sock_set_state")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXinetVZVXsockVZVXsetVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143351;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_inet_sock_set_state")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXinetVZVXsockVZVXsetVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143351;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_kfree_skb")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXkfreeVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143359;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_kfree_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXkfreeVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143359;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_mptcp_subflow_get_send")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXmptcpVZVXsubflowVZVXgetVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143397;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_mptcp_subflow_get_send")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXmptcpVZVXsubflowVZVXgetVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143397;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_neigh_event_send_dead")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXneighVZVXeventVZVXsendVZVXdead)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143407;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_neigh_event_send_dead")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXneighVZVXeventVZVXsendVZVXdead)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143407;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_neigh_event_send_done")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXneighVZVXeventVZVXsendVZVXdone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143408;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_neigh_event_send_done")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXneighVZVXeventVZVXsendVZVXdone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143408;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_net_dev_start_xmit")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXnetVZVXdevVZVXstartVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143413;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_net_dev_start_xmit")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXnetVZVXdevVZVXstartVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143413;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_net_dev_xmit")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXnetVZVXdevVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143414;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_net_dev_xmit")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXnetVZVXdevVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143414;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_net_dev_xmit_timeout")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXnetVZVXdevVZVXxmitVZVXtimeout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143415;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_net_dev_xmit_timeout")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXnetVZVXdevVZVXxmitVZVXtimeout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143415;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_netif_receive_skb")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXnetifVZVXreceiveVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143416;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_netif_receive_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXnetifVZVXreceiveVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143416;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_netif_receive_skb_entry")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXnetifVZVXreceiveVZVXskbVZVXentry)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143417;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_netif_receive_skb_entry")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXnetifVZVXreceiveVZVXskbVZVXentry)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143417;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_netif_receive_skb_list_entry")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXnetifVZVXreceiveVZVXskbVZVXlistVZVXentry)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143419;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_netif_receive_skb_list_entry")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXnetifVZVXreceiveVZVXskbVZVXlistVZVXentry)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143419;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_sk_data_ready")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXskVZVXdataVZVXready)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143461;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_sk_data_ready")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXskVZVXdataVZVXready)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143461;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_skb_copy_datagram_iovec")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXskbVZVXcopyVZVXdatagramVZVXiovec)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143462;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_skb_copy_datagram_iovec")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXskbVZVXcopyVZVXdatagramVZVXiovec)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143462;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_sock_exceed_buf_limit")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXsockVZVXexceedVZVXbufVZVXlimit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143463;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_sock_exceed_buf_limit")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXsockVZVXexceedVZVXbufVZVXlimit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143463;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_sock_rcvqueue_full")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXsockVZVXrcvqueueVZVXfull)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143464;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_sock_rcvqueue_full")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXsockVZVXrcvqueueVZVXfull)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143464;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_sock_recv_length")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXsockVZVXrecvVZVXlength)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143465;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_sock_recv_length")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXsockVZVXrecvVZVXlength)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143465;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_sock_send_length")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXsockVZVXsendVZVXlength)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143466;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_sock_send_length")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXsockVZVXsendVZVXlength)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143466;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_ao_handshake_failure")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXaoVZVXhandshakeVZVXfailure)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143472;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_ao_handshake_failure")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXaoVZVXhandshakeVZVXfailure)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143472;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_ao_key_not_found")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXaoVZVXkeyVZVXnotVZVXfound)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143473;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_ao_key_not_found")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXaoVZVXkeyVZVXnotVZVXfound)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143473;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_ao_mismatch")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXaoVZVXmismatch)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143474;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_ao_mismatch")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXaoVZVXmismatch)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143474;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_ao_rcv_sne_update")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXaoVZVXrcvVZVXsneVZVXupdate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143475;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_ao_rcv_sne_update")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXaoVZVXrcvVZVXsneVZVXupdate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143475;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_ao_rnext_request")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXaoVZVXrnextVZVXrequest)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143476;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_ao_rnext_request")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXaoVZVXrnextVZVXrequest)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143476;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_ao_snd_sne_update")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXaoVZVXsndVZVXsneVZVXupdate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143477;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_ao_snd_sne_update")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXaoVZVXsndVZVXsneVZVXupdate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143477;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_ao_synack_no_key")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXaoVZVXsynackVZVXnoVZVXkey)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143478;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_ao_synack_no_key")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXaoVZVXsynackVZVXnoVZVXkey)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143478;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_ao_wrong_maclen")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXaoVZVXwrongVZVXmaclen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143479;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_ao_wrong_maclen")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXaoVZVXwrongVZVXmaclen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143479;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_bad_csum")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXbadVZVXcsum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143480;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_bad_csum")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXbadVZVXcsum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143480;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_cong_state_set")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXcongVZVXstateVZVXset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143481;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_cong_state_set")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXcongVZVXstateVZVXset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143481;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_cwnd_reduction_tp")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXcwndVZVXreductionVZVXtp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143482;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_cwnd_reduction_tp")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXcwndVZVXreductionVZVXtp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143482;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_destroy_sock")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXdestroyVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143483;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_destroy_sock")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXdestroyVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143483;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_hash_ao_required")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXhashVZVXaoVZVXrequired)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143484;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_hash_ao_required")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXhashVZVXaoVZVXrequired)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143484;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_hash_bad_header")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXhashVZVXbadVZVXheader)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143485;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_hash_bad_header")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXhashVZVXbadVZVXheader)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143485;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_hash_md5_mismatch")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXhashVZVXmd5VZVXmismatch)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143486;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_hash_md5_mismatch")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXhashVZVXmd5VZVXmismatch)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143486;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_hash_md5_required")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXhashVZVXmd5VZVXrequired)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143487;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_hash_md5_required")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXhashVZVXmd5VZVXrequired)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143487;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_hash_md5_unexpected")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXhashVZVXmd5VZVXunexpected)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143488;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_hash_md5_unexpected")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXhashVZVXmd5VZVXunexpected)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143488;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_probe")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXprobe)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143489;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_probe")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXprobe)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143489;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_rcv_space_adjust")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXrcvVZVXspaceVZVXadjust)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143490;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_rcv_space_adjust")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXrcvVZVXspaceVZVXadjust)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143490;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_rcvbuf_grow")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXrcvbufVZVXgrow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143491;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_rcvbuf_grow")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXrcvbufVZVXgrow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143491;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_receive_reset")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXreceiveVZVXreset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143492;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_receive_reset")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXreceiveVZVXreset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143492;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_retransmit_skb")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXretransmitVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143493;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_retransmit_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXretransmitVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143493;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_retransmit_synack")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXretransmitVZVXsynack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143494;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_retransmit_synack")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXretransmitVZVXsynack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143494;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_send_reset")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXsendVZVXreset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143495;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_send_reset")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXsendVZVXreset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143495;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_tcp_sendmsg_locked")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXtcpVZVXsendmsgVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143496;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_tcp_sendmsg_locked")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXtcpVZVXsendmsgVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143496;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__probestub_udp_fail_queue_rcv_skb")
int BPF_KPROBE(KtprobeVZVXVZVXprobestubVZVXudpVZVXfailVZVXqueueVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143518;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__probestub_udp_fail_queue_rcv_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXprobestubVZVXudpVZVXfailVZVXqueueVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143518;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__pskb_trim_head")
int BPF_KPROBE(KtprobeVZVXVZVXpskbVZVXtrimVZVXhead)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143541;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__pskb_trim_head")
int BPF_KRETPROBE(KtretprobeVZVXVZVXpskbVZVXtrimVZVXhead)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143541;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__reuseport_detach_closed_sock")
int BPF_KPROBE(KtprobeVZVXVZVXreuseportVZVXdetachVZVXclosedVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143632;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__reuseport_detach_closed_sock")
int BPF_KRETPROBE(KtretprobeVZVXVZVXreuseportVZVXdetachVZVXclosedVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143632;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__reuseport_detach_sock")
int BPF_KPROBE(KtprobeVZVXVZVXreuseportVZVXdetachVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143633;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__reuseport_detach_sock")
int BPF_KRETPROBE(KtretprobeVZVXVZVXreuseportVZVXdetachVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143633;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__scm_recv_common")
int BPF_KPROBE(KtprobeVZVXVZVXscmVZVXrecvVZVXcommon)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143648;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__scm_recv_common")
int BPF_KRETPROBE(KtretprobeVZVXVZVXscmVZVXrecvVZVXcommon)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143648;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__scm_send")
int BPF_KPROBE(KtprobeVZVXVZVXscmVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143650;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__scm_send")
int BPF_KRETPROBE(KtretprobeVZVXVZVXscmVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143650;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sk_msg_free")
int BPF_KPROBE(KtprobeVZVXVZVXskVZVXmsgVZVXfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143711;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sk_msg_free")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskVZVXmsgVZVXfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143711;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sk_msg_free_partial")
int BPF_KPROBE(KtprobeVZVXVZVXskVZVXmsgVZVXfreeVZVXpartial)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143713;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sk_msg_free_partial")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskVZVXmsgVZVXfreeVZVXpartial)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143713;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sk_queue_drop_skb")
int BPF_KPROBE(KtprobeVZVXVZVXskVZVXqueueVZVXdropVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143715;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sk_queue_drop_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskVZVXqueueVZVXdropVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143715;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_gso_segment")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXgsoVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143719;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_gso_segment")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXgsoVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143719;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_recv_datagram")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXrecvVZVXdatagram)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143721;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_recv_datagram")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXrecvVZVXdatagram)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143721;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_try_recv_datagram")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXtryVZVXrecvVZVXdatagram)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143723;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_try_recv_datagram")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXtryVZVXrecvVZVXdatagram)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143723;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_try_recv_from_queue")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXtryVZVXrecvVZVXfromVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143725;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_try_recv_from_queue")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXtryVZVXrecvVZVXfromVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143725;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__skb_wait_for_more_packets")
int BPF_KPROBE(KtprobeVZVXVZVXskbVZVXwaitVZVXforVZVXmoreVZVXpackets)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143727;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__skb_wait_for_more_packets")
int BPF_KRETPROBE(KtretprobeVZVXVZVXskbVZVXwaitVZVXforVZVXmoreVZVXpackets)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143727;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__sock_gen_cookie")
int BPF_KPROBE(KtprobeVZVXVZVXsockVZVXgenVZVXcookie)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143737;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__sock_gen_cookie")
int BPF_KRETPROBE(KtretprobeVZVXVZVXsockVZVXgenVZVXcookie)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143737;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__strp_recv")
int BPF_KPROBE(KtprobeVZVXVZVXstrpVZVXrecv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143747;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__strp_recv")
int BPF_KRETPROBE(KtretprobeVZVXVZVXstrpVZVXrecv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143747;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__tcp_get_metrics")
int BPF_KPROBE(KtprobeVZVXVZVXtcpVZVXgetVZVXmetrics)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143776;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__tcp_get_metrics")
int BPF_KRETPROBE(KtretprobeVZVXVZVXtcpVZVXgetVZVXmetrics)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143776;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__tcp_push_pending_frames")
int BPF_KPROBE(KtprobeVZVXVZVXtcpVZVXpushVZVXpendingVZVXframes)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143778;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__tcp_push_pending_frames")
int BPF_KRETPROBE(KtretprobeVZVXVZVXtcpVZVXpushVZVXpendingVZVXframes)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143778;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__tcp_retransmit_skb")
int BPF_KPROBE(KtprobeVZVXVZVXtcpVZVXretransmitVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143780;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__tcp_retransmit_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXtcpVZVXretransmitVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143780;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__tcp_select_window")
int BPF_KPROBE(KtprobeVZVXVZVXtcpVZVXselectVZVXwindow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143781;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__tcp_select_window")
int BPF_KRETPROBE(KtretprobeVZVXVZVXtcpVZVXselectVZVXwindow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143781;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__tcp_send_ack")
int BPF_KPROBE(KtprobeVZVXVZVXtcpVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143783;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__tcp_send_ack")
int BPF_KRETPROBE(KtretprobeVZVXVZVXtcpVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143783;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__tcp_transmit_skb")
int BPF_KPROBE(KtprobeVZVXVZVXtcpVZVXtransmitVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143785;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__tcp_transmit_skb")
int BPF_KRETPROBE(KtretprobeVZVXVZVXtcpVZVXtransmitVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=143785;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__udp6_lib_err")
int BPF_KPROBE(KtprobeVZVXVZVXudp6VZVXlibVZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=144258;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__udp6_lib_err")
int BPF_KRETPROBE(KtretprobeVZVXVZVXudp6VZVXlibVZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=144258;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__udp6_lib_lookup")
int BPF_KPROBE(KtprobeVZVXVZVXudp6VZVXlibVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=144260;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__udp6_lib_lookup")
int BPF_KRETPROBE(KtretprobeVZVXVZVXudp6VZVXlibVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=144260;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__udp6_lib_mcast_deliver")
int BPF_KPROBE(KtprobeVZVXVZVXudp6VZVXlibVZVXmcastVZVXdeliver)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=144262;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__udp6_lib_mcast_deliver")
int BPF_KRETPROBE(KtretprobeVZVXVZVXudp6VZVXlibVZVXmcastVZVXdeliver)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=144262;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__udp6_lib_rcv")
int BPF_KPROBE(KtprobeVZVXVZVXudp6VZVXlibVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=144263;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__udp6_lib_rcv")
int BPF_KRETPROBE(KtretprobeVZVXVZVXudp6VZVXlibVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=144263;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__unix_dgram_recvmsg")
int BPF_KPROBE(KtprobeVZVXVZVXunixVZVXdgramVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=144270;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__unix_dgram_recvmsg")
int BPF_KRETPROBE(KtretprobeVZVXVZVXunixVZVXdgramVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=144270;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__unix_stream_recvmsg")
int BPF_KPROBE(KtprobeVZVXVZVXunixVZVXstreamVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=144273;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__unix_stream_recvmsg")
int BPF_KRETPROBE(KtretprobeVZVXVZVXunixVZVXstreamVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=144273;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/__virtnet_xdp_xmit_one")
int BPF_KPROBE(KtprobeVZVXVZVXvirtnetVZVXxdpVZVXxmitVZVXone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=144296;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/__virtnet_xdp_xmit_one")
int BPF_KRETPROBE(KtretprobeVZVXVZVXvirtnetVZVXxdpVZVXxmitVZVXone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=144296;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/add_recvbuf_big")
int BPF_KPROBE(KtprobeaddVZVXrecvbufVZVXbig)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=145160;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/add_recvbuf_big")
int BPF_KRETPROBE(KtretprobeaddVZVXrecvbufVZVXbig)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=145160;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/audit_log_common_recv_msg")
int BPF_KPROBE(KtprobeauditVZVXlogVZVXcommonVZVXrecvVZVXmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=145951;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/audit_log_common_recv_msg")
int BPF_KRETPROBE(KtretprobeauditVZVXlogVZVXcommonVZVXrecvVZVXmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=145951;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/audit_log_task_context")
int BPF_KPROBE(KtprobeauditVZVXlogVZVXtaskVZVXcontext)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=145985;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/audit_log_task_context")
int BPF_KRETPROBE(KtretprobeauditVZVXlogVZVXtaskVZVXcontext)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=145985;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/audit_log_task_info")
int BPF_KPROBE(KtprobeauditVZVXlogVZVXtaskVZVXinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=145986;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/audit_log_task_info")
int BPF_KRETPROBE(KtretprobeauditVZVXlogVZVXtaskVZVXinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=145986;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/audit_send_reply")
int BPF_KPROBE(KtprobeauditVZVXsendVZVXreply)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=146020;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/audit_send_reply")
int BPF_KRETPROBE(KtretprobeauditVZVXsendVZVXreply)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=146020;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/blackhole_netdev_xmit")
int BPF_KPROBE(KtprobeblackholeVZVXnetdevVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=146167;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/blackhole_netdev_xmit")
int BPF_KRETPROBE(KtretprobeblackholeVZVXnetdevVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=146167;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/br_ip6_fragment")
int BPF_KPROBE(KtprobebrVZVXip6VZVXfragment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=146788;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/br_ip6_fragment")
int BPF_KRETPROBE(KtretprobebrVZVXip6VZVXfragment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=146788;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/call_fib6_multipath_entry_notifiers")
int BPF_KPROBE(KtprobecallVZVXfib6VZVXmultipathVZVXentryVZVXnotifiers)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=146938;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/call_fib6_multipath_entry_notifiers")
int BPF_KRETPROBE(KtretprobecallVZVXfib6VZVXmultipathVZVXentryVZVXnotifiers)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=146938;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/crypto_lskcipher_report")
int BPF_KPROBE(KtprobecryptoVZVXlskcipherVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=147996;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/crypto_lskcipher_report")
int BPF_KRETPROBE(KtretprobecryptoVZVXlskcipherVZVXreport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=147996;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/default_read_sock_done")
int BPF_KPROBE(KtprobedefaultVZVXreadVZVXsockVZVXdone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148300;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/default_read_sock_done")
int BPF_KRETPROBE(KtretprobedefaultVZVXreadVZVXsockVZVXdone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148300;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/dequeue_skb")
int BPF_KPROBE(KtprobedequeueVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148344;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/dequeue_skb")
int BPF_KRETPROBE(KtretprobedequeueVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148344;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/dev_forward_skb")
int BPF_KPROBE(KtprobedevVZVXforwardVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148410;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/dev_forward_skb")
int BPF_KRETPROBE(KtretprobedevVZVXforwardVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148410;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/dev_forward_skb_nomtu")
int BPF_KPROBE(KtprobedevVZVXforwardVZVXskbVZVXnomtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148411;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/dev_forward_skb_nomtu")
int BPF_KRETPROBE(KtretprobedevVZVXforwardVZVXskbVZVXnomtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148411;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/dev_hard_start_xmit")
int BPF_KPROBE(KtprobedevVZVXhardVZVXstartVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148436;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/dev_hard_start_xmit")
int BPF_KRETPROBE(KtretprobedevVZVXhardVZVXstartVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148436;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/dev_kfree_skb_any_reason")
int BPF_KPROBE(KtprobedevVZVXkfreeVZVXskbVZVXanyVZVXreason)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148448;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/dev_kfree_skb_any_reason")
int BPF_KRETPROBE(KtretprobedevVZVXkfreeVZVXskbVZVXanyVZVXreason)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148448;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/dev_kfree_skb_irq_reason")
int BPF_KPROBE(KtprobedevVZVXkfreeVZVXskbVZVXirqVZVXreason)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148449;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/dev_kfree_skb_irq_reason")
int BPF_KRETPROBE(KtretprobedevVZVXkfreeVZVXskbVZVXirqVZVXreason)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148449;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/dev_loopback_xmit")
int BPF_KPROBE(KtprobedevVZVXloopbackVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148450;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/dev_loopback_xmit")
int BPF_KRETPROBE(KtretprobedevVZVXloopbackVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148450;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/dev_queue_xmit_nit")
int BPF_KPROBE(KtprobedevVZVXqueueVZVXxmitVZVXnit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148504;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/dev_queue_xmit_nit")
int BPF_KRETPROBE(KtretprobedevVZVXqueueVZVXxmitVZVXnit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148504;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/devlink_dpipe_action_put")
int BPF_KPROBE(KtprobedevlinkVZVXdpipeVZVXactionVZVXput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148769;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/devlink_dpipe_action_put")
int BPF_KRETPROBE(KtretprobedevlinkVZVXdpipeVZVXactionVZVXput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148769;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/devlink_dpipe_entry_ctx_append")
int BPF_KPROBE(KtprobedevlinkVZVXdpipeVZVXentryVZVXctxVZVXappend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148773;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/devlink_dpipe_entry_ctx_append")
int BPF_KRETPROBE(KtretprobedevlinkVZVXdpipeVZVXentryVZVXctxVZVXappend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148773;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/devlink_dpipe_entry_ctx_close")
int BPF_KPROBE(KtprobedevlinkVZVXdpipeVZVXentryVZVXctxVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148775;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/devlink_dpipe_entry_ctx_close")
int BPF_KRETPROBE(KtretprobedevlinkVZVXdpipeVZVXentryVZVXctxVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148775;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/devlink_dpipe_entry_ctx_prepare")
int BPF_KPROBE(KtprobedevlinkVZVXdpipeVZVXentryVZVXctxVZVXprepare)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148776;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/devlink_dpipe_entry_ctx_prepare")
int BPF_KRETPROBE(KtretprobedevlinkVZVXdpipeVZVXentryVZVXctxVZVXprepare)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148776;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/devlink_dpipe_entry_put")
int BPF_KPROBE(KtprobedevlinkVZVXdpipeVZVXentryVZVXput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148778;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/devlink_dpipe_entry_put")
int BPF_KRETPROBE(KtretprobedevlinkVZVXdpipeVZVXentryVZVXput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148778;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/devlink_dpipe_match_put")
int BPF_KPROBE(KtprobedevlinkVZVXdpipeVZVXmatchVZVXput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148782;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/devlink_dpipe_match_put")
int BPF_KRETPROBE(KtretprobedevlinkVZVXdpipeVZVXmatchVZVXput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148782;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/devlink_dpipe_send_and_alloc_skb")
int BPF_KPROBE(KtprobedevlinkVZVXdpipeVZVXsendVZVXandVZVXallocVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148784;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/devlink_dpipe_send_and_alloc_skb")
int BPF_KRETPROBE(KtretprobedevlinkVZVXdpipeVZVXsendVZVXandVZVXallocVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148784;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/devlink_dpipe_table_put")
int BPF_KPROBE(KtprobedevlinkVZVXdpipeVZVXtableVZVXput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148788;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/devlink_dpipe_table_put")
int BPF_KRETPROBE(KtretprobedevlinkVZVXdpipeVZVXtableVZVXput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148788;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/devlink_dpipe_value_put")
int BPF_KPROBE(KtprobedevlinkVZVXdpipeVZVXvalueVZVXput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148792;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/devlink_dpipe_value_put")
int BPF_KRETPROBE(KtretprobedevlinkVZVXdpipeVZVXvalueVZVXput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148792;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/devlink_nl_dpipe_entries_get_doit")
int BPF_KPROBE(KtprobedevlinkVZVXnlVZVXdpipeVZVXentriesVZVXgetVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148797;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/devlink_nl_dpipe_entries_get_doit")
int BPF_KRETPROBE(KtretprobedevlinkVZVXnlVZVXdpipeVZVXentriesVZVXgetVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148797;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/devlink_nl_dpipe_headers_get_doit")
int BPF_KPROBE(KtprobedevlinkVZVXnlVZVXdpipeVZVXheadersVZVXgetVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148798;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/devlink_nl_dpipe_headers_get_doit")
int BPF_KRETPROBE(KtretprobedevlinkVZVXnlVZVXdpipeVZVXheadersVZVXgetVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148798;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/devlink_nl_dpipe_table_counters_set_doit")
int BPF_KPROBE(KtprobedevlinkVZVXnlVZVXdpipeVZVXtableVZVXcountersVZVXsetVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148799;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/devlink_nl_dpipe_table_counters_set_doit")
int BPF_KRETPROBE(KtretprobedevlinkVZVXnlVZVXdpipeVZVXtableVZVXcountersVZVXsetVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148799;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/devlink_nl_dpipe_table_get_doit")
int BPF_KPROBE(KtprobedevlinkVZVXnlVZVXdpipeVZVXtableVZVXgetVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148800;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/devlink_nl_dpipe_table_get_doit")
int BPF_KRETPROBE(KtretprobedevlinkVZVXnlVZVXdpipeVZVXtableVZVXgetVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=148800;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ethnl_pse_send_ntf")
int BPF_KPROBE(KtprobeethnlVZVXpseVZVXsendVZVXntf)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=151016;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ethnl_pse_send_ntf")
int BPF_KRETPROBE(KtretprobeethnlVZVXpseVZVXsendVZVXntf)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=151016;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/features_send_reply")
int BPF_KPROBE(KtprobefeaturesVZVXsendVZVXreply)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=151719;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/features_send_reply")
int BPF_KRETPROBE(KtretprobefeaturesVZVXsendVZVXreply)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=151719;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/fib6_clean_all_skip_notify")
int BPF_KPROBE(Ktprobefib6VZVXcleanVZVXallVZVXskipVZVXnotify)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=151744;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/fib6_clean_all_skip_notify")
int BPF_KRETPROBE(Ktretprobefib6VZVXcleanVZVXallVZVXskipVZVXnotify)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=151744;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/fib_multipath_hash")
int BPF_KPROBE(KtprobefibVZVXmultipathVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=151809;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/fib_multipath_hash")
int BPF_KRETPROBE(KtretprobefibVZVXmultipathVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=151809;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/free_old_xmit")
int BPF_KPROBE(KtprobefreeVZVXoldVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=152196;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/free_old_xmit")
int BPF_KRETPROBE(KtretprobefreeVZVXoldVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=152196;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/hook_unix_may_send")
int BPF_KPROBE(KtprobehookVZVXunixVZVXmayVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=153035;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/hook_unix_may_send")
int BPF_KRETPROBE(KtretprobehookVZVXunixVZVXmayVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=153035;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet6_lookup_run_sk_lookup")
int BPF_KPROBE(Ktprobeinet6VZVXlookupVZVXrunVZVXskVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=153428;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet6_lookup_run_sk_lookup")
int BPF_KRETPROBE(Ktretprobeinet6VZVXlookupVZVXrunVZVXskVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=153428;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_twsk_alloc")
int BPF_KPROBE(KtprobeinetVZVXtwskVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=153470;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_twsk_alloc")
int BPF_KRETPROBE(KtretprobeinetVZVXtwskVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=153470;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/inet_twsk_hashdance_schedule")
int BPF_KPROBE(KtprobeinetVZVXtwskVZVXhashdanceVZVXschedule)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=153477;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/inet_twsk_hashdance_schedule")
int BPF_KRETPROBE(KtretprobeinetVZVXtwskVZVXhashdanceVZVXschedule)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=153477;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/io_recvmsg_multishot")
int BPF_KPROBE(KtprobeioVZVXrecvmsgVZVXmultishot)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=153955;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/io_recvmsg_multishot")
int BPF_KRETPROBE(KtretprobeioVZVXrecvmsgVZVXmultishot)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=153955;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_frag_queue")
int BPF_KPROBE(Ktprobeip6VZVXfragVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154407;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_frag_queue")
int BPF_KRETPROBE(Ktretprobeip6VZVXfragVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154407;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_frag_reasm")
int BPF_KPROBE(Ktprobeip6VZVXfragVZVXreasm)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154409;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_frag_reasm")
int BPF_KRETPROBE(Ktretprobeip6VZVXfragVZVXreasm)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154409;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_input")
int BPF_KPROBE(Ktprobeip6VZVXinput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154410;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_input")
int BPF_KRETPROBE(Ktretprobeip6VZVXinput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154410;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_input_finish")
int BPF_KPROBE(Ktprobeip6VZVXinputVZVXfinish)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154411;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_input_finish")
int BPF_KRETPROBE(Ktretprobeip6VZVXinputVZVXfinish)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154411;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_list_rcv_finish")
int BPF_KPROBE(Ktprobeip6VZVXlistVZVXrcvVZVXfinish)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154413;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_list_rcv_finish")
int BPF_KRETPROBE(Ktretprobeip6VZVXlistVZVXrcvVZVXfinish)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154413;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_mc_input")
int BPF_KPROBE(Ktprobeip6VZVXmcVZVXinput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154414;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_mc_input")
int BPF_KRETPROBE(Ktretprobeip6VZVXmcVZVXinput)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154414;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_parse_tlv")
int BPF_KPROBE(Ktprobeip6VZVXparseVZVXtlv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154416;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_parse_tlv")
int BPF_KRETPROBE(Ktretprobeip6VZVXparseVZVXtlv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154416;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_protocol_deliver_rcu")
int BPF_KPROBE(Ktprobeip6VZVXprotocolVZVXdeliverVZVXrcu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154418;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_protocol_deliver_rcu")
int BPF_KRETPROBE(Ktretprobeip6VZVXprotocolVZVXdeliverVZVXrcu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154418;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_rcv_finish")
int BPF_KPROBE(Ktprobeip6VZVXrcvVZVXfinish)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154421;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_rcv_finish")
int BPF_KRETPROBE(Ktretprobeip6VZVXrcvVZVXfinish)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154421;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_rcv_finish_core")
int BPF_KPROBE(Ktprobeip6VZVXrcvVZVXfinishVZVXcore)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154423;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_rcv_finish_core")
int BPF_KRETPROBE(Ktretprobeip6VZVXrcvVZVXfinishVZVXcore)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154423;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_route_me_harder")
int BPF_KPROBE(Ktprobeip6VZVXrouteVZVXmeVZVXharder)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154425;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_route_me_harder")
int BPF_KRETPROBE(Ktretprobeip6VZVXrouteVZVXmeVZVXharder)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154425;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_sublist_rcv")
int BPF_KPROBE(Ktprobeip6VZVXsublistVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154426;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_sublist_rcv")
int BPF_KRETPROBE(Ktretprobeip6VZVXsublistVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154426;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_tlvopt_unknown")
int BPF_KPROBE(Ktprobeip6VZVXtlvoptVZVXunknown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154429;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_tlvopt_unknown")
int BPF_KRETPROBE(Ktretprobeip6VZVXtlvoptVZVXunknown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154429;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_tun_build_state")
int BPF_KPROBE(Ktprobeip6VZVXtunVZVXbuildVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154431;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_tun_build_state")
int BPF_KRETPROBE(Ktretprobeip6VZVXtunVZVXbuildVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154431;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6_tun_fill_encap_info")
int BPF_KPROBE(Ktprobeip6VZVXtunVZVXfillVZVXencapVZVXinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154433;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6_tun_fill_encap_info")
int BPF_KRETPROBE(Ktretprobeip6VZVXtunVZVXfillVZVXencapVZVXinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154433;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6frag_expire_frag_queue")
int BPF_KPROBE(Ktprobeip6fragVZVXexpireVZVXfragVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154435;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6frag_expire_frag_queue")
int BPF_KRETPROBE(Ktretprobeip6fragVZVXexpireVZVXfragVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154435;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip6frag_init")
int BPF_KPROBE(Ktprobeip6fragVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154436;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip6frag_init")
int BPF_KRETPROBE(Ktretprobeip6fragVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154436;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_do_redirect")
int BPF_KPROBE(KtprobeipVZVXdoVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154440;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_do_redirect")
int BPF_KRETPROBE(KtretprobeipVZVXdoVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154440;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_error")
int BPF_KPROBE(KtprobeipVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154441;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_error")
int BPF_KRETPROBE(KtretprobeipVZVXerror)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154441;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_forward_options")
int BPF_KPROBE(KtprobeipVZVXforwardVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154442;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_forward_options")
int BPF_KRETPROBE(KtretprobeipVZVXforwardVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154442;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_handle_martian_source")
int BPF_KPROBE(KtprobeipVZVXhandleVZVXmartianVZVXsource)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154444;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_handle_martian_source")
int BPF_KRETPROBE(KtretprobeipVZVXhandleVZVXmartianVZVXsource)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154444;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mc_validate_source")
int BPF_KPROBE(KtprobeipVZVXmcVZVXvalidateVZVXsource)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154446;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mc_validate_source")
int BPF_KRETPROBE(KtretprobeipVZVXmcVZVXvalidateVZVXsource)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154446;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_mtu_from_fib_result")
int BPF_KPROBE(KtprobeipVZVXmtuVZVXfromVZVXfibVZVXresult)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154449;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_mtu_from_fib_result")
int BPF_KRETPROBE(KtretprobeipVZVXmtuVZVXfromVZVXfibVZVXresult)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154449;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_multipath_l3_keys")
int BPF_KPROBE(KtprobeipVZVXmultipathVZVXl3VZVXkeys)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154451;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_multipath_l3_keys")
int BPF_KRETPROBE(KtretprobeipVZVXmultipathVZVXl3VZVXkeys)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154451;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_options_build")
int BPF_KPROBE(KtprobeipVZVXoptionsVZVXbuild)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154453;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_options_build")
int BPF_KRETPROBE(KtretprobeipVZVXoptionsVZVXbuild)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154453;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_options_compile")
int BPF_KPROBE(KtprobeipVZVXoptionsVZVXcompile)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154455;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_options_compile")
int BPF_KRETPROBE(KtretprobeipVZVXoptionsVZVXcompile)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154455;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_options_fragment")
int BPF_KPROBE(KtprobeipVZVXoptionsVZVXfragment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154456;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_options_fragment")
int BPF_KRETPROBE(KtretprobeipVZVXoptionsVZVXfragment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154456;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_options_get")
int BPF_KPROBE(KtprobeipVZVXoptionsVZVXget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154458;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_options_get")
int BPF_KRETPROBE(KtretprobeipVZVXoptionsVZVXget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154458;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_options_rcv_srr")
int BPF_KPROBE(KtprobeipVZVXoptionsVZVXrcvVZVXsrr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154459;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_options_rcv_srr")
int BPF_KRETPROBE(KtretprobeipVZVXoptionsVZVXrcvVZVXsrr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154459;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_proc_exit_net")
int BPF_KPROBE(KtprobeipVZVXprocVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154462;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_proc_exit_net")
int BPF_KRETPROBE(KtretprobeipVZVXprocVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154462;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_proc_init_net")
int BPF_KPROBE(KtprobeipVZVXprocVZVXinitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154463;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_proc_init_net")
int BPF_KRETPROBE(KtretprobeipVZVXprocVZVXinitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154463;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_route_input_noref")
int BPF_KPROBE(KtprobeipVZVXrouteVZVXinputVZVXnoref)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154465;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_route_input_noref")
int BPF_KRETPROBE(KtretprobeipVZVXrouteVZVXinputVZVXnoref)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154465;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_route_input_rcu")
int BPF_KPROBE(KtprobeipVZVXrouteVZVXinputVZVXrcu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154467;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_route_input_rcu")
int BPF_KRETPROBE(KtretprobeipVZVXrouteVZVXinputVZVXrcu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154467;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_route_input_slow")
int BPF_KPROBE(KtprobeipVZVXrouteVZVXinputVZVXslow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154468;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_route_input_slow")
int BPF_KRETPROBE(KtretprobeipVZVXrouteVZVXinputVZVXslow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154468;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_route_me_harder")
int BPF_KPROBE(KtprobeipVZVXrouteVZVXmeVZVXharder)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154470;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_route_me_harder")
int BPF_KRETPROBE(KtretprobeipVZVXrouteVZVXmeVZVXharder)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154470;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_route_output_flow")
int BPF_KPROBE(KtprobeipVZVXrouteVZVXoutputVZVXflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154472;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_route_output_flow")
int BPF_KRETPROBE(KtretprobeipVZVXrouteVZVXoutputVZVXflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154472;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_route_output_key_hash")
int BPF_KPROBE(KtprobeipVZVXrouteVZVXoutputVZVXkeyVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154474;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_route_output_key_hash")
int BPF_KRETPROBE(KtretprobeipVZVXrouteVZVXoutputVZVXkeyVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154474;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_route_output_key_hash_rcu")
int BPF_KPROBE(KtprobeipVZVXrouteVZVXoutputVZVXkeyVZVXhashVZVXrcu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154476;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_route_output_key_hash_rcu")
int BPF_KRETPROBE(KtretprobeipVZVXrouteVZVXoutputVZVXkeyVZVXhashVZVXrcu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154476;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_route_use_hint")
int BPF_KPROBE(KtprobeipVZVXrouteVZVXuseVZVXhint)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154478;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_route_use_hint")
int BPF_KRETPROBE(KtretprobeipVZVXrouteVZVXuseVZVXhint)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154478;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_rt_bug")
int BPF_KPROBE(KtprobeipVZVXrtVZVXbug)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154479;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_rt_bug")
int BPF_KRETPROBE(KtretprobeipVZVXrtVZVXbug)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154479;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_rt_do_proc_exit")
int BPF_KPROBE(KtprobeipVZVXrtVZVXdoVZVXprocVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154480;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_rt_do_proc_exit")
int BPF_KRETPROBE(KtretprobeipVZVXrtVZVXdoVZVXprocVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154480;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_rt_do_proc_init")
int BPF_KPROBE(KtprobeipVZVXrtVZVXdoVZVXprocVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154481;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_rt_do_proc_init")
int BPF_KRETPROBE(KtretprobeipVZVXrtVZVXdoVZVXprocVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154481;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_rt_get_source")
int BPF_KPROBE(KtprobeipVZVXrtVZVXgetVZVXsource)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154483;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_rt_get_source")
int BPF_KRETPROBE(KtretprobeipVZVXrtVZVXgetVZVXsource)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154483;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_rt_multicast_event")
int BPF_KPROBE(KtprobeipVZVXrtVZVXmulticastVZVXevent)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154485;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_rt_multicast_event")
int BPF_KRETPROBE(KtretprobeipVZVXrtVZVXmulticastVZVXevent)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154485;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_rt_send_redirect")
int BPF_KPROBE(KtprobeipVZVXrtVZVXsendVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154486;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_rt_send_redirect")
int BPF_KRETPROBE(KtretprobeipVZVXrtVZVXsendVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154486;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_rt_update_pmtu")
int BPF_KPROBE(KtprobeipVZVXrtVZVXupdateVZVXpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154487;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_rt_update_pmtu")
int BPF_KRETPROBE(KtretprobeipVZVXrtVZVXupdateVZVXpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154487;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_tun_build_state")
int BPF_KPROBE(KtprobeipVZVXtunVZVXbuildVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154489;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_tun_build_state")
int BPF_KRETPROBE(KtretprobeipVZVXtunVZVXbuildVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154489;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_tun_fill_encap_info")
int BPF_KPROBE(KtprobeipVZVXtunVZVXfillVZVXencapVZVXinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154494;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_tun_fill_encap_info")
int BPF_KRETPROBE(KtretprobeipVZVXtunVZVXfillVZVXencapVZVXinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154494;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_tun_fill_encap_opts")
int BPF_KPROBE(KtprobeipVZVXtunVZVXfillVZVXencapVZVXopts)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154496;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_tun_fill_encap_opts")
int BPF_KRETPROBE(KtretprobeipVZVXtunVZVXfillVZVXencapVZVXopts)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154496;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ip_tunnel_parse_protocol")
int BPF_KPROBE(KtprobeipVZVXtunnelVZVXparseVZVXprotocol)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154509;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ip_tunnel_parse_protocol")
int BPF_KRETPROBE(KtretprobeipVZVXtunnelVZVXparseVZVXprotocol)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154509;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/iptunnel_handle_offloads")
int BPF_KPROBE(KtprobeiptunnelVZVXhandleVZVXoffloads)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154516;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/iptunnel_handle_offloads")
int BPF_KRETPROBE(KtretprobeiptunnelVZVXhandleVZVXoffloads)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154516;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/iptunnel_pmtud_build_icmp")
int BPF_KPROBE(KtprobeiptunnelVZVXpmtudVZVXbuildVZVXicmp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154520;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/iptunnel_pmtud_build_icmp")
int BPF_KRETPROBE(KtretprobeiptunnelVZVXpmtudVZVXbuildVZVXicmp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154520;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/iptunnel_pmtud_build_icmpv6")
int BPF_KPROBE(KtprobeiptunnelVZVXpmtudVZVXbuildVZVXicmpv6)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154521;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/iptunnel_pmtud_build_icmpv6")
int BPF_KRETPROBE(KtretprobeiptunnelVZVXpmtudVZVXbuildVZVXicmpv6)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154521;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/iptunnel_xmit")
int BPF_KPROBE(KtprobeiptunnelVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154523;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/iptunnel_xmit")
int BPF_KRETPROBE(KtretprobeiptunnelVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154523;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv4_blackhole_route")
int BPF_KPROBE(Ktprobeipv4VZVXblackholeVZVXroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154524;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv4_blackhole_route")
int BPF_KRETPROBE(Ktretprobeipv4VZVXblackholeVZVXroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154524;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv4_inetpeer_exit")
int BPF_KPROBE(Ktprobeipv4VZVXinetpeerVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154531;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv4_inetpeer_exit")
int BPF_KRETPROBE(Ktretprobeipv4VZVXinetpeerVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154531;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv4_inetpeer_init")
int BPF_KPROBE(Ktprobeipv4VZVXinetpeerVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154532;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv4_inetpeer_init")
int BPF_KRETPROBE(Ktretprobeipv4VZVXinetpeerVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154532;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv4_link_failure")
int BPF_KPROBE(Ktprobeipv4VZVXlinkVZVXfailure)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154533;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv4_link_failure")
int BPF_KRETPROBE(Ktretprobeipv4VZVXlinkVZVXfailure)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154533;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv4_negative_advice")
int BPF_KPROBE(Ktprobeipv4VZVXnegativeVZVXadvice)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154535;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv4_negative_advice")
int BPF_KRETPROBE(Ktretprobeipv4VZVXnegativeVZVXadvice)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154535;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv4_neigh_lookup")
int BPF_KPROBE(Ktprobeipv4VZVXneighVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154536;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv4_neigh_lookup")
int BPF_KRETPROBE(Ktretprobeipv4VZVXneighVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154536;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv4_redirect")
int BPF_KPROBE(Ktprobeipv4VZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154538;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv4_redirect")
int BPF_KRETPROBE(Ktretprobeipv4VZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154538;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv4_send_dest_unreach")
int BPF_KPROBE(Ktprobeipv4VZVXsendVZVXdestVZVXunreach)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154539;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv4_send_dest_unreach")
int BPF_KRETPROBE(Ktretprobeipv4VZVXsendVZVXdestVZVXunreach)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154539;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv4_sk_redirect")
int BPF_KPROBE(Ktprobeipv4VZVXskVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154540;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv4_sk_redirect")
int BPF_KRETPROBE(Ktretprobeipv4VZVXskVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154540;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv4_sk_update_pmtu")
int BPF_KPROBE(Ktprobeipv4VZVXskVZVXupdateVZVXpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154541;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv4_sk_update_pmtu")
int BPF_KRETPROBE(Ktretprobeipv4VZVXskVZVXupdateVZVXpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154541;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv4_update_pmtu")
int BPF_KPROBE(Ktprobeipv4VZVXupdateVZVXpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154545;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv4_update_pmtu")
int BPF_KRETPROBE(Ktretprobeipv4VZVXupdateVZVXpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154545;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_dest_hao")
int BPF_KPROBE(Ktprobeipv6VZVXdestVZVXhao)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154547;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_dest_hao")
int BPF_KRETPROBE(Ktretprobeipv6VZVXdestVZVXhao)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154547;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_destopt_rcv")
int BPF_KPROBE(Ktprobeipv6VZVXdestoptVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154548;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_destopt_rcv")
int BPF_KRETPROBE(Ktretprobeipv6VZVXdestoptVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154548;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_dup_options")
int BPF_KPROBE(Ktprobeipv6VZVXdupVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154549;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_dup_options")
int BPF_KRETPROBE(Ktretprobeipv6VZVXdupVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154549;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_find_hdr")
int BPF_KPROBE(Ktprobeipv6VZVXfindVZVXhdr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154555;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_find_hdr")
int BPF_KRETPROBE(Ktretprobeipv6VZVXfindVZVXhdr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154555;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_find_tlv")
int BPF_KPROBE(Ktprobeipv6VZVXfindVZVXtlv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154557;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_find_tlv")
int BPF_KRETPROBE(Ktretprobeipv6VZVXfindVZVXtlv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154557;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_frag_rcv")
int BPF_KPROBE(Ktprobeipv6VZVXfragVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154560;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_frag_rcv")
int BPF_KRETPROBE(Ktretprobeipv6VZVXfragVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154560;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_frags_exit_net")
int BPF_KPROBE(Ktprobeipv6VZVXfragsVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154561;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_frags_exit_net")
int BPF_KRETPROBE(Ktretprobeipv6VZVXfragsVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154561;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_frags_init_net")
int BPF_KPROBE(Ktprobeipv6VZVXfragsVZVXinitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154562;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_frags_init_net")
int BPF_KRETPROBE(Ktretprobeipv6VZVXfragsVZVXinitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154562;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_frags_pre_exit_net")
int BPF_KPROBE(Ktprobeipv6VZVXfragsVZVXpreVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154563;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_frags_pre_exit_net")
int BPF_KRETPROBE(Ktretprobeipv6VZVXfragsVZVXpreVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154563;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_hop_ioam")
int BPF_KPROBE(Ktprobeipv6VZVXhopVZVXioam)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154564;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_hop_ioam")
int BPF_KRETPROBE(Ktretprobeipv6VZVXhopVZVXioam)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154564;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_hop_ra")
int BPF_KPROBE(Ktprobeipv6VZVXhopVZVXra)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154565;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_hop_ra")
int BPF_KRETPROBE(Ktretprobeipv6VZVXhopVZVXra)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154565;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_is_mld")
int BPF_KPROBE(Ktprobeipv6VZVXisVZVXmld)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154567;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_is_mld")
int BPF_KRETPROBE(Ktretprobeipv6VZVXisVZVXmld)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154567;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_parse_hopopts")
int BPF_KPROBE(Ktprobeipv6VZVXparseVZVXhopopts)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154571;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_parse_hopopts")
int BPF_KRETPROBE(Ktretprobeipv6VZVXparseVZVXhopopts)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154571;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_push_exthdr")
int BPF_KPROBE(Ktprobeipv6VZVXpushVZVXexthdr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154573;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_push_exthdr")
int BPF_KRETPROBE(Ktretprobeipv6VZVXpushVZVXexthdr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154573;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_push_frag_opts")
int BPF_KPROBE(Ktprobeipv6VZVXpushVZVXfragVZVXopts)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154575;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_push_frag_opts")
int BPF_KRETPROBE(Ktretprobeipv6VZVXpushVZVXfragVZVXopts)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154575;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_push_nfrag_opts")
int BPF_KPROBE(Ktprobeipv6VZVXpushVZVXnfragVZVXopts)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154577;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_push_nfrag_opts")
int BPF_KRETPROBE(Ktretprobeipv6VZVXpushVZVXnfragVZVXopts)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154577;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_push_rthdr0")
int BPF_KPROBE(Ktprobeipv6VZVXpushVZVXrthdr0)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154579;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_push_rthdr0")
int BPF_KRETPROBE(Ktretprobeipv6VZVXpushVZVXrthdr0)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154579;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_push_rthdr4")
int BPF_KPROBE(Ktprobeipv6VZVXpushVZVXrthdr4)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154580;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_push_rthdr4")
int BPF_KRETPROBE(Ktretprobeipv6VZVXpushVZVXrthdr4)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154580;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_renew_options")
int BPF_KPROBE(Ktprobeipv6VZVXrenewVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154585;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_renew_options")
int BPF_KRETPROBE(Ktretprobeipv6VZVXrenewVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154585;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_route_seq_setup_walk")
int BPF_KPROBE(Ktprobeipv6VZVXrouteVZVXseqVZVXsetupVZVXwalk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154588;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_route_seq_setup_walk")
int BPF_KRETPROBE(Ktretprobeipv6VZVXrouteVZVXseqVZVXsetupVZVXwalk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154588;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_route_yield")
int BPF_KPROBE(Ktprobeipv6VZVXrouteVZVXyield)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154592;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_route_yield")
int BPF_KRETPROBE(Ktretprobeipv6VZVXrouteVZVXyield)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154592;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_rpl_srh_rcv")
int BPF_KPROBE(Ktprobeipv6VZVXrplVZVXsrhVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154600;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_rpl_srh_rcv")
int BPF_KRETPROBE(Ktretprobeipv6VZVXrplVZVXsrhVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154600;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_rthdr_rcv")
int BPF_KPROBE(Ktprobeipv6VZVXrthdrVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154601;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_rthdr_rcv")
int BPF_KRETPROBE(Ktretprobeipv6VZVXrthdrVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154601;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_skip_exthdr")
int BPF_KPROBE(Ktprobeipv6VZVXskipVZVXexthdr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154603;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_skip_exthdr")
int BPF_KRETPROBE(Ktretprobeipv6VZVXskipVZVXexthdr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154603;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/ipv6_srh_rcv")
int BPF_KPROBE(Ktprobeipv6VZVXsrhVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154604;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/ipv6_srh_rcv")
int BPF_KRETPROBE(Ktretprobeipv6VZVXsrhVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154604;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/is_skb_forwardable")
int BPF_KPROBE(KtprobeisVZVXskbVZVXforwardable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154734;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/is_skb_forwardable")
int BPF_KRETPROBE(KtretprobeisVZVXskbVZVXforwardable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154734;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/kauditd_hold_skb")
int BPF_KPROBE(KtprobekauditdVZVXholdVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154822;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/kauditd_hold_skb")
int BPF_KRETPROBE(KtretprobekauditdVZVXholdVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154822;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/kauditd_rehold_skb")
int BPF_KPROBE(KtprobekauditdVZVXreholdVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154823;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/kauditd_rehold_skb")
int BPF_KRETPROBE(KtretprobekauditdVZVXreholdVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154823;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/kauditd_retry_skb")
int BPF_KPROBE(KtprobekauditdVZVXretryVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154824;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/kauditd_retry_skb")
int BPF_KRETPROBE(KtretprobekauditdVZVXretryVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154824;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/kauditd_send_multicast_skb")
int BPF_KPROBE(KtprobekauditdVZVXsendVZVXmulticastVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154825;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/kauditd_send_multicast_skb")
int BPF_KRETPROBE(KtretprobekauditdVZVXsendVZVXmulticastVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154825;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/kauditd_send_queue")
int BPF_KPROBE(KtprobekauditdVZVXsendVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154827;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/kauditd_send_queue")
int BPF_KRETPROBE(KtretprobekauditdVZVXsendVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=154827;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/loopback_xmit")
int BPF_KPROBE(KtprobeloopbackVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=155439;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/loopback_xmit")
int BPF_KRETPROBE(KtretprobeloopbackVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=155439;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_bind")
int BPF_KPROBE(KtprobemptcpVZVXbind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156508;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_bind")
int BPF_KRETPROBE(KtretprobemptcpVZVXbind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156508;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_ca_reset")
int BPF_KPROBE(KtprobemptcpVZVXcaVZVXreset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156509;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_ca_reset")
int BPF_KRETPROBE(KtretprobemptcpVZVXcaVZVXreset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156509;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_cancel_work")
int BPF_KPROBE(KtprobemptcpVZVXcancelVZVXwork)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156510;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_cancel_work")
int BPF_KRETPROBE(KtretprobemptcpVZVXcancelVZVXwork)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156510;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_check_and_set_pending")
int BPF_KPROBE(KtprobemptcpVZVXcheckVZVXandVZVXsetVZVXpending)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156511;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_check_and_set_pending")
int BPF_KRETPROBE(KtretprobemptcpVZVXcheckVZVXandVZVXsetVZVXpending)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156511;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_check_data_fin")
int BPF_KPROBE(KtprobemptcpVZVXcheckVZVXdataVZVXfin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156512;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_check_data_fin")
int BPF_KRETPROBE(KtretprobemptcpVZVXcheckVZVXdataVZVXfin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156512;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_check_listen_stop")
int BPF_KPROBE(KtprobemptcpVZVXcheckVZVXlistenVZVXstop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156513;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_check_listen_stop")
int BPF_KRETPROBE(KtretprobemptcpVZVXcheckVZVXlistenVZVXstop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156513;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_check_send_data_fin")
int BPF_KPROBE(KtprobemptcpVZVXcheckVZVXsendVZVXdataVZVXfin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156514;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_check_send_data_fin")
int BPF_KRETPROBE(KtretprobemptcpVZVXcheckVZVXsendVZVXdataVZVXfin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156514;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_cleanup_rbuf")
int BPF_KPROBE(KtprobemptcpVZVXcleanupVZVXrbuf)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156516;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_cleanup_rbuf")
int BPF_KRETPROBE(KtretprobemptcpVZVXcleanupVZVXrbuf)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156516;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_close")
int BPF_KPROBE(KtprobemptcpVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156517;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_close")
int BPF_KRETPROBE(KtretprobemptcpVZVXclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156517;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_close_ssk")
int BPF_KPROBE(KtprobemptcpVZVXcloseVZVXssk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156519;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_close_ssk")
int BPF_KRETPROBE(KtretprobemptcpVZVXcloseVZVXssk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156519;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_close_wake_up")
int BPF_KPROBE(KtprobemptcpVZVXcloseVZVXwakeVZVXup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156520;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_close_wake_up")
int BPF_KRETPROBE(KtretprobemptcpVZVXcloseVZVXwakeVZVXup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156520;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_connect")
int BPF_KPROBE(KtprobemptcpVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156521;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_connect")
int BPF_KRETPROBE(KtretprobemptcpVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156521;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_copy_inaddrs")
int BPF_KPROBE(KtprobemptcpVZVXcopyVZVXinaddrs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156523;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_copy_inaddrs")
int BPF_KRETPROBE(KtretprobemptcpVZVXcopyVZVXinaddrs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156523;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_data_queue_ofo")
int BPF_KPROBE(KtprobemptcpVZVXdataVZVXqueueVZVXofo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156525;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_data_queue_ofo")
int BPF_KRETPROBE(KtretprobemptcpVZVXdataVZVXqueueVZVXofo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156525;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_data_ready")
int BPF_KPROBE(KtprobemptcpVZVXdataVZVXready)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156526;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_data_ready")
int BPF_KRETPROBE(KtretprobemptcpVZVXdataVZVXready)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156526;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_destroy")
int BPF_KPROBE(KtprobemptcpVZVXdestroy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156527;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_destroy")
int BPF_KRETPROBE(KtretprobemptcpVZVXdestroy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156527;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_destroy_common")
int BPF_KPROBE(KtprobemptcpVZVXdestroyVZVXcommon)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156528;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_destroy_common")
int BPF_KRETPROBE(KtretprobemptcpVZVXdestroyVZVXcommon)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156528;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_diag_fill_info")
int BPF_KPROBE(KtprobemptcpVZVXdiagVZVXfillVZVXinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156530;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_diag_fill_info")
int BPF_KRETPROBE(KtretprobemptcpVZVXdiagVZVXfillVZVXinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156530;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_disconnect")
int BPF_KPROBE(KtprobemptcpVZVXdisconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156531;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_disconnect")
int BPF_KRETPROBE(KtretprobemptcpVZVXdisconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156531;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_do_fastclose")
int BPF_KPROBE(KtprobemptcpVZVXdoVZVXfastclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156532;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_do_fastclose")
int BPF_KRETPROBE(KtretprobemptcpVZVXdoVZVXfastclose)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156532;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_enter_memory_pressure")
int BPF_KPROBE(KtprobemptcpVZVXenterVZVXmemoryVZVXpressure)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156533;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_enter_memory_pressure")
int BPF_KRETPROBE(KtretprobemptcpVZVXenterVZVXmemoryVZVXpressure)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156533;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_finish_connect")
int BPF_KPROBE(KtprobemptcpVZVXfinishVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156534;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_finish_connect")
int BPF_KRETPROBE(KtretprobemptcpVZVXfinishVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156534;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_finish_join")
int BPF_KPROBE(KtprobemptcpVZVXfinishVZVXjoin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156535;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_finish_join")
int BPF_KRETPROBE(KtretprobemptcpVZVXfinishVZVXjoin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156535;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_get_port")
int BPF_KPROBE(KtprobemptcpVZVXgetVZVXport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156536;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_get_port")
int BPF_KRETPROBE(KtretprobemptcpVZVXgetVZVXport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156536;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_get_sub_addrs")
int BPF_KPROBE(KtprobemptcpVZVXgetVZVXsubVZVXaddrs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156538;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_get_sub_addrs")
int BPF_KRETPROBE(KtretprobemptcpVZVXgetVZVXsubVZVXaddrs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156538;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_getsockopt")
int BPF_KPROBE(KtprobemptcpVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156542;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_getsockopt")
int BPF_KRETPROBE(KtretprobemptcpVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156542;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_getsockopt_full_info")
int BPF_KPROBE(KtprobemptcpVZVXgetsockoptVZVXfullVZVXinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156544;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_getsockopt_full_info")
int BPF_KRETPROBE(KtretprobemptcpVZVXgetsockoptVZVXfullVZVXinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156544;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_getsockopt_sol_tcp")
int BPF_KPROBE(KtprobemptcpVZVXgetsockoptVZVXsolVZVXtcp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156546;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_getsockopt_sol_tcp")
int BPF_KRETPROBE(KtretprobemptcpVZVXgetsockoptVZVXsolVZVXtcp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156546;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_getsockopt_subflow_addrs")
int BPF_KPROBE(KtprobemptcpVZVXgetsockoptVZVXsubflowVZVXaddrs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156547;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_getsockopt_subflow_addrs")
int BPF_KRETPROBE(KtretprobemptcpVZVXgetsockoptVZVXsubflowVZVXaddrs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156547;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_getsockopt_tcpinfo")
int BPF_KPROBE(KtprobemptcpVZVXgetsockoptVZVXtcpinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156548;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_getsockopt_tcpinfo")
int BPF_KRETPROBE(KtretprobemptcpVZVXgetsockoptVZVXtcpinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156548;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_hash")
int BPF_KPROBE(KtprobemptcpVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156549;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_hash")
int BPF_KRETPROBE(KtretprobemptcpVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156549;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_init_sock")
int BPF_KPROBE(KtprobemptcpVZVXinitVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156550;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_init_sock")
int BPF_KRETPROBE(KtretprobemptcpVZVXinitVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156550;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_ioctl")
int BPF_KPROBE(KtprobemptcpVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156551;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_ioctl")
int BPF_KRETPROBE(KtretprobemptcpVZVXioctl)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156551;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_ioctl_outq")
int BPF_KPROBE(KtprobemptcpVZVXioctlVZVXoutq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156553;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_ioctl_outq")
int BPF_KRETPROBE(KtretprobemptcpVZVXioctlVZVXoutq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156553;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_listen")
int BPF_KPROBE(KtprobemptcpVZVXlisten)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156554;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_listen")
int BPF_KRETPROBE(KtretprobemptcpVZVXlisten)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156554;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_napi_poll")
int BPF_KPROBE(KtprobemptcpVZVXnapiVZVXpoll)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156559;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_napi_poll")
int BPF_KRETPROBE(KtretprobemptcpVZVXnapiVZVXpoll)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156559;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_nl_remove_id_zero_address")
int BPF_KPROBE(KtprobemptcpVZVXnlVZVXremoveVZVXidVZVXzeroVZVXaddress)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156561;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_nl_remove_id_zero_address")
int BPF_KRETPROBE(KtretprobemptcpVZVXnlVZVXremoveVZVXidVZVXzeroVZVXaddress)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156561;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_nl_remove_subflow_and_signal_addr")
int BPF_KPROBE(KtprobemptcpVZVXnlVZVXremoveVZVXsubflowVZVXandVZVXsignalVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156563;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_nl_remove_subflow_and_signal_addr")
int BPF_KRETPROBE(KtretprobemptcpVZVXnlVZVXremoveVZVXsubflowVZVXandVZVXsignalVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156563;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pending_data_fin")
int BPF_KPROBE(KtprobemptcpVZVXpendingVZVXdataVZVXfin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156565;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pending_data_fin")
int BPF_KRETPROBE(KtretprobemptcpVZVXpendingVZVXdataVZVXfin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156565;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_add_addr_echoed")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXaddVZVXaddrVZVXechoed)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156567;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_add_addr_echoed")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXaddVZVXaddrVZVXechoed)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156567;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_add_addr_received")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXaddVZVXaddrVZVXreceived)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156569;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_add_addr_received")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXaddVZVXaddrVZVXreceived)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156569;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_add_addr_send_ack")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXaddVZVXaddrVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156570;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_add_addr_send_ack")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXaddVZVXaddrVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156570;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_add_addr_signal")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXaddVZVXaddrVZVXsignal)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156572;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_add_addr_signal")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXaddVZVXaddrVZVXsignal)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156572;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_addr_families_match")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXaddrVZVXfamiliesVZVXmatch)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156575;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_addr_families_match")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXaddrVZVXfamiliesVZVXmatch)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156575;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_addr_send_ack")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXaddrVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156576;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_addr_send_ack")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXaddrVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156576;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_alloc_anno_list")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXallocVZVXannoVZVXlist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156578;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_alloc_anno_list")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXallocVZVXannoVZVXlist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156578;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_allow_new_subflow")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXallowVZVXnewVZVXsubflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156579;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_allow_new_subflow")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXallowVZVXnewVZVXsubflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156579;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_announce_addr")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXannounceVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156581;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_announce_addr")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXannounceVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156581;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_connection_closed")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXconnectionVZVXclosed)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156582;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_connection_closed")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXconnectionVZVXclosed)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156582;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_create_subflow_or_signal_addr")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXcreateVZVXsubflowVZVXorVZVXsignalVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156583;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_create_subflow_or_signal_addr")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXcreateVZVXsubflowVZVXorVZVXsignalVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156583;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_data_init")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXdataVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156584;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_data_init")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXdataVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156584;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_data_reset")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXdataVZVXreset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156585;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_data_reset")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXdataVZVXreset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156585;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_del_add_timer")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXdelVZVXaddVZVXtimer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156587;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_del_add_timer")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXdelVZVXaddVZVXtimer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156587;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_destroy")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXdestroy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156588;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_destroy")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXdestroy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156588;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_flush_addrs_and_subflows")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXflushVZVXaddrsVZVXandVZVXsubflows)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156592;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_flush_addrs_and_subflows")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXflushVZVXaddrsVZVXandVZVXsubflows)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156592;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_fully_established")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXfullyVZVXestablished)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156594;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_fully_established")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXfullyVZVXestablished)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156594;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_get_add_addr_accept_max")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXgetVZVXaddVZVXaddrVZVXacceptVZVXmax)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156596;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_get_add_addr_accept_max")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXgetVZVXaddVZVXaddrVZVXacceptVZVXmax)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156596;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_get_add_addr_signal_max")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXgetVZVXaddVZVXaddrVZVXsignalVZVXmax)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156597;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_get_add_addr_signal_max")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXgetVZVXaddVZVXaddrVZVXsignalVZVXmax)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156597;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_get_local_addr_max")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXgetVZVXlocalVZVXaddrVZVXmax)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156599;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_get_local_addr_max")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXgetVZVXlocalVZVXaddrVZVXmax)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156599;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_get_local_id")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXgetVZVXlocalVZVXid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156601;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_get_local_id")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXgetVZVXlocalVZVXid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156601;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_get_subflows_max")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXgetVZVXsubflowsVZVXmax)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156602;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_get_subflows_max")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXgetVZVXsubflowsVZVXmax)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156602;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_is_backup")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXisVZVXbackup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156607;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_is_backup")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXisVZVXbackup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156607;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_mp_fail_received")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXmpVZVXfailVZVXreceived)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156610;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_mp_fail_received")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXmpVZVXfailVZVXreceived)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156610;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_mp_prio_received")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXmpVZVXprioVZVXreceived)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156612;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_mp_prio_received")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXmpVZVXprioVZVXreceived)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156612;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_mp_prio_send_ack")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXmpVZVXprioVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156614;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_mp_prio_send_ack")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXmpVZVXprioVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156614;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_new_connection")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXnewVZVXconnection)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156616;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_new_connection")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXnewVZVXconnection)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156616;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_nl_add_addr_doit")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXnlVZVXaddVZVXaddrVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156617;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_nl_add_addr_doit")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXnlVZVXaddVZVXaddrVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156617;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_nl_add_addr_received")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXnlVZVXaddVZVXaddrVZVXreceived)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156618;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_nl_add_addr_received")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXnlVZVXaddVZVXaddrVZVXreceived)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156618;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_nl_append_new_local_addr")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXnlVZVXappendVZVXnewVZVXlocalVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156620;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_nl_append_new_local_addr")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXnlVZVXappendVZVXnewVZVXlocalVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156620;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_nl_check_work_pending")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXnlVZVXcheckVZVXworkVZVXpending)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156621;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_nl_check_work_pending")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXnlVZVXcheckVZVXworkVZVXpending)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156621;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_nl_create_listen_socket")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXnlVZVXcreateVZVXlistenVZVXsocket)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156623;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_nl_create_listen_socket")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXnlVZVXcreateVZVXlistenVZVXsocket)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156623;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_nl_del_addr_doit")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXnlVZVXdelVZVXaddrVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156624;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_nl_del_addr_doit")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXnlVZVXdelVZVXaddrVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156624;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_nl_dump_addr")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXnlVZVXdumpVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156625;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_nl_dump_addr")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXnlVZVXdumpVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156625;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_nl_flush_addrs_doit")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXnlVZVXflushVZVXaddrsVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156626;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_nl_flush_addrs_doit")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXnlVZVXflushVZVXaddrsVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156626;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_nl_get_addr")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXnlVZVXgetVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156627;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_nl_get_addr")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXnlVZVXgetVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156627;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_nl_get_limits_doit")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXnlVZVXgetVZVXlimitsVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156628;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_nl_get_limits_doit")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXnlVZVXgetVZVXlimitsVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156628;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_nl_get_local_id")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXnlVZVXgetVZVXlocalVZVXid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156629;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_nl_get_local_id")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXnlVZVXgetVZVXlocalVZVXid)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156629;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_nl_is_backup")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXnlVZVXisVZVXbackup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156630;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_nl_is_backup")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXnlVZVXisVZVXbackup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156630;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_nl_rm_addr")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXnlVZVXrmVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156632;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_nl_rm_addr")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXnlVZVXrmVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156632;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_nl_set_flags")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXnlVZVXsetVZVXflags)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156633;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_nl_set_flags")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXnlVZVXsetVZVXflags)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156633;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_nl_set_limits_doit")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXnlVZVXsetVZVXlimitsVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156634;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_nl_set_limits_doit")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXnlVZVXsetVZVXlimitsVZVXdoit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156634;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_remove_addr")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXremoveVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156638;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_remove_addr")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXremoveVZVXaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156638;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_rm_addr_or_subflow")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXrmVZVXaddrVZVXorVZVXsubflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156640;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_rm_addr_or_subflow")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXrmVZVXaddrVZVXorVZVXsubflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156640;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_rm_addr_received")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXrmVZVXaddrVZVXreceived)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156642;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_rm_addr_received")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXrmVZVXaddrVZVXreceived)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156642;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_rm_addr_signal")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXrmVZVXaddrVZVXsignal)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156644;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_rm_addr_signal")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXrmVZVXaddrVZVXsignal)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156644;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_rm_subflow")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXrmVZVXsubflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156645;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_rm_subflow")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXrmVZVXsubflow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156645;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_schedule_work")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXscheduleVZVXwork)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156647;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_schedule_work")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXscheduleVZVXwork)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156647;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_send_ack")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156648;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_send_ack")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156648;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_sport_in_anno_list")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXsportVZVXinVZVXannoVZVXlist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156650;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_sport_in_anno_list")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXsportVZVXinVZVXannoVZVXlist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156650;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_subflow_check_next")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXsubflowVZVXcheckVZVXnext)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156652;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_subflow_check_next")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXsubflowVZVXcheckVZVXnext)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156652;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_subflow_chk_stale")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXsubflowVZVXchkVZVXstale)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156654;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_subflow_chk_stale")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXsubflowVZVXchkVZVXstale)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156654;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_subflow_established")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXsubflowVZVXestablished)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156655;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_subflow_established")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXsubflowVZVXestablished)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156655;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_pm_worker")
int BPF_KPROBE(KtprobemptcpVZVXpmVZVXworker)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156659;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_pm_worker")
int BPF_KRETPROBE(KtretprobemptcpVZVXpmVZVXworker)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156659;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_poll")
int BPF_KPROBE(KtprobemptcpVZVXpoll)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156660;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_poll")
int BPF_KRETPROBE(KtretprobemptcpVZVXpoll)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156660;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_put_int_option")
int BPF_KPROBE(KtprobemptcpVZVXputVZVXintVZVXoption)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156664;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_put_int_option")
int BPF_KRETPROBE(KtretprobemptcpVZVXputVZVXintVZVXoption)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156664;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_rcv_space_adjust")
int BPF_KPROBE(KtprobemptcpVZVXrcvVZVXspaceVZVXadjust)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156667;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_rcv_space_adjust")
int BPF_KRETPROBE(KtretprobemptcpVZVXrcvVZVXspaceVZVXadjust)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156667;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_rcv_space_init")
int BPF_KPROBE(KtprobemptcpVZVXrcvVZVXspaceVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156668;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_rcv_space_init")
int BPF_KRETPROBE(KtretprobemptcpVZVXrcvVZVXspaceVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156668;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_recvmsg")
int BPF_KPROBE(KtprobemptcpVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156669;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_recvmsg")
int BPF_KRETPROBE(KtretprobemptcpVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156669;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_release_cb")
int BPF_KPROBE(KtprobemptcpVZVXreleaseVZVXcb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156670;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_release_cb")
int BPF_KRETPROBE(KtretprobemptcpVZVXreleaseVZVXcb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156670;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_remove_anno_list_by_saddr")
int BPF_KPROBE(KtprobemptcpVZVXremoveVZVXannoVZVXlistVZVXbyVZVXsaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156672;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_remove_anno_list_by_saddr")
int BPF_KRETPROBE(KtretprobemptcpVZVXremoveVZVXannoVZVXlistVZVXbyVZVXsaddr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156672;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_reset_tout_timer")
int BPF_KPROBE(KtprobemptcpVZVXresetVZVXtoutVZVXtimer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156674;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_reset_tout_timer")
int BPF_KRETPROBE(KtretprobemptcpVZVXresetVZVXtoutVZVXtimer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156674;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_schedule_work")
int BPF_KPROBE(KtprobemptcpVZVXscheduleVZVXwork)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156676;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_schedule_work")
int BPF_KRETPROBE(KtretprobemptcpVZVXscheduleVZVXwork)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156676;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_send_ack")
int BPF_KPROBE(KtprobemptcpVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156677;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_send_ack")
int BPF_KRETPROBE(KtretprobemptcpVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156677;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_sendmsg")
int BPF_KPROBE(KtprobemptcpVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156678;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_sendmsg")
int BPF_KRETPROBE(KtretprobemptcpVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156678;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_sendmsg_frag")
int BPF_KPROBE(KtprobemptcpVZVXsendmsgVZVXfrag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156680;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_sendmsg_frag")
int BPF_KRETPROBE(KtretprobemptcpVZVXsendmsgVZVXfrag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156680;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_set_rcvlowat")
int BPF_KPROBE(KtprobemptcpVZVXsetVZVXrcvlowat)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156681;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_set_rcvlowat")
int BPF_KRETPROBE(KtretprobemptcpVZVXsetVZVXrcvlowat)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156681;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_set_state")
int BPF_KPROBE(KtprobemptcpVZVXsetVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156682;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_set_state")
int BPF_KRETPROBE(KtretprobemptcpVZVXsetVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156682;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_setsockopt")
int BPF_KPROBE(KtprobemptcpVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156683;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_setsockopt")
int BPF_KRETPROBE(KtretprobemptcpVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156683;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_setsockopt_sol_socket")
int BPF_KPROBE(KtprobemptcpVZVXsetsockoptVZVXsolVZVXsocket)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156685;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_setsockopt_sol_socket")
int BPF_KRETPROBE(KtretprobemptcpVZVXsetsockoptVZVXsolVZVXsocket)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156685;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_setsockopt_sol_socket_int")
int BPF_KPROBE(KtprobemptcpVZVXsetsockoptVZVXsolVZVXsocketVZVXint)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156686;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_setsockopt_sol_socket_int")
int BPF_KRETPROBE(KtretprobemptcpVZVXsetsockoptVZVXsolVZVXsocketVZVXint)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156686;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_setsockopt_sol_socket_linger")
int BPF_KPROBE(KtprobemptcpVZVXsetsockoptVZVXsolVZVXsocketVZVXlinger)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156688;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_setsockopt_sol_socket_linger")
int BPF_KRETPROBE(KtretprobemptcpVZVXsetsockoptVZVXsolVZVXsocketVZVXlinger)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156688;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_setsockopt_sol_tcp")
int BPF_KPROBE(KtprobemptcpVZVXsetsockoptVZVXsolVZVXtcp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156689;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_setsockopt_sol_tcp")
int BPF_KRETPROBE(KtretprobemptcpVZVXsetsockoptVZVXsolVZVXtcp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156689;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_setsockopt_sol_tcp_congestion")
int BPF_KPROBE(KtprobemptcpVZVXsetsockoptVZVXsolVZVXtcpVZVXcongestion)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156690;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_setsockopt_sol_tcp_congestion")
int BPF_KRETPROBE(KtretprobemptcpVZVXsetsockoptVZVXsolVZVXtcpVZVXcongestion)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156690;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_setsockopt_v4")
int BPF_KPROBE(KtprobemptcpVZVXsetsockoptVZVXv4)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156691;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_setsockopt_v4")
int BPF_KRETPROBE(KtretprobemptcpVZVXsetsockoptVZVXv4)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156691;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_shutdown")
int BPF_KPROBE(KtprobemptcpVZVXshutdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156692;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_shutdown")
int BPF_KRETPROBE(KtretprobemptcpVZVXshutdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156692;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_shutdown_subflows")
int BPF_KPROBE(KtprobemptcpVZVXshutdownVZVXsubflows)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156693;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_shutdown_subflows")
int BPF_KRETPROBE(KtretprobemptcpVZVXshutdownVZVXsubflows)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156693;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_sk_clone_init")
int BPF_KPROBE(KtprobemptcpVZVXskVZVXcloneVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156695;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_sk_clone_init")
int BPF_KRETPROBE(KtretprobemptcpVZVXskVZVXcloneVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156695;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_sock_graft")
int BPF_KPROBE(KtprobemptcpVZVXsockVZVXgraft)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156696;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_sock_graft")
int BPF_KRETPROBE(KtretprobemptcpVZVXsockVZVXgraft)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156696;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_sockopt_sync_locked")
int BPF_KPROBE(KtprobemptcpVZVXsockoptVZVXsyncVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156697;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_sockopt_sync_locked")
int BPF_KRETPROBE(KtretprobemptcpVZVXsockoptVZVXsyncVZVXlocked)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156697;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_sol_socket_sync_intval")
int BPF_KPROBE(KtprobemptcpVZVXsolVZVXsocketVZVXsyncVZVXintval)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156699;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_sol_socket_sync_intval")
int BPF_KRETPROBE(KtretprobemptcpVZVXsolVZVXsocketVZVXsyncVZVXintval)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156699;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_stream_accept")
int BPF_KPROBE(KtprobemptcpVZVXstreamVZVXaccept)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156700;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_stream_accept")
int BPF_KRETPROBE(KtretprobemptcpVZVXstreamVZVXaccept)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156700;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_subflow_active")
int BPF_KPROBE(KtprobemptcpVZVXsubflowVZVXactive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156702;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_subflow_active")
int BPF_KRETPROBE(KtretprobemptcpVZVXsubflowVZVXactive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156702;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_subflow_get_retrans")
int BPF_KPROBE(KtprobemptcpVZVXsubflowVZVXgetVZVXretrans)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156703;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_subflow_get_retrans")
int BPF_KRETPROBE(KtretprobemptcpVZVXsubflowVZVXgetVZVXretrans)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156703;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_subflow_get_send")
int BPF_KPROBE(KtprobemptcpVZVXsubflowVZVXgetVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156704;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_subflow_get_send")
int BPF_KRETPROBE(KtretprobemptcpVZVXsubflowVZVXgetVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156704;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_subflow_process_delegated")
int BPF_KPROBE(KtprobemptcpVZVXsubflowVZVXprocessVZVXdelegated)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156706;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_subflow_process_delegated")
int BPF_KRETPROBE(KtretprobemptcpVZVXsubflowVZVXprocessVZVXdelegated)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156706;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_subflow_set_active")
int BPF_KPROBE(KtprobemptcpVZVXsubflowVZVXsetVZVXactive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156708;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_subflow_set_active")
int BPF_KRETPROBE(KtretprobemptcpVZVXsubflowVZVXsetVZVXactive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156708;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_subflow_shutdown")
int BPF_KPROBE(KtprobemptcpVZVXsubflowVZVXshutdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156710;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_subflow_shutdown")
int BPF_KRETPROBE(KtretprobemptcpVZVXsubflowVZVXshutdown)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156710;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_sync_mss")
int BPF_KPROBE(KtprobemptcpVZVXsyncVZVXmss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156713;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_sync_mss")
int BPF_KRETPROBE(KtretprobemptcpVZVXsyncVZVXmss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156713;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_try_coalesce")
int BPF_KPROBE(KtprobemptcpVZVXtryVZVXcoalesce)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156716;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_try_coalesce")
int BPF_KRETPROBE(KtretprobemptcpVZVXtryVZVXcoalesce)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156716;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_unhash")
int BPF_KPROBE(KtprobemptcpVZVXunhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156717;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_unhash")
int BPF_KRETPROBE(KtretprobemptcpVZVXunhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156717;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/mptcp_update_data_checksum")
int BPF_KPROBE(KtprobemptcpVZVXupdateVZVXdataVZVXchecksum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156719;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/mptcp_update_data_checksum")
int BPF_KRETPROBE(KtretprobemptcpVZVXupdateVZVXdataVZVXchecksum)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=156719;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/net_failover_start_xmit")
int BPF_KPROBE(KtprobenetVZVXfailoverVZVXstartVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157005;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/net_failover_start_xmit")
int BPF_KRETPROBE(KtretprobenetVZVXfailoverVZVXstartVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157005;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/net_failover_xmit_ready")
int BPF_KPROBE(KtprobenetVZVXfailoverVZVXxmitVZVXready)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157008;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/net_failover_xmit_ready")
int BPF_KRETPROBE(KtretprobenetVZVXfailoverVZVXxmitVZVXready)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157008;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/net_rps_send_ipi")
int BPF_KPROBE(KtprobenetVZVXrpsVZVXsendVZVXipi)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157018;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/net_rps_send_ipi")
int BPF_KRETPROBE(KtretprobenetVZVXrpsVZVXsendVZVXipi)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157018;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netdev_get_xmit_slave")
int BPF_KPROBE(KtprobenetdevVZVXgetVZVXxmitVZVXslave)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157069;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netdev_get_xmit_slave")
int BPF_KRETPROBE(KtretprobenetdevVZVXgetVZVXxmitVZVXslave)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157069;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netdev_sk_get_lowest_dev")
int BPF_KPROBE(KtprobenetdevVZVXskVZVXgetVZVXlowestVZVXdev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157148;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netdev_sk_get_lowest_dev")
int BPF_KRETPROBE(KtretprobenetdevVZVXskVZVXgetVZVXlowestVZVXdev)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157148;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netif_receive_skb")
int BPF_KPROBE(KtprobenetifVZVXreceiveVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157214;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netif_receive_skb")
int BPF_KRETPROBE(KtretprobenetifVZVXreceiveVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157214;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netif_receive_skb_core")
int BPF_KPROBE(KtprobenetifVZVXreceiveVZVXskbVZVXcore)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157215;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netif_receive_skb_core")
int BPF_KRETPROBE(KtretprobenetifVZVXreceiveVZVXskbVZVXcore)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157215;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netif_skb_features")
int BPF_KPROBE(KtprobenetifVZVXskbVZVXfeatures)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157245;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netif_skb_features")
int BPF_KRETPROBE(KtretprobenetifVZVXskbVZVXfeatures)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157245;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlink_attachskb")
int BPF_KPROBE(KtprobenetlinkVZVXattachskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157291;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlink_attachskb")
int BPF_KRETPROBE(KtretprobenetlinkVZVXattachskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157291;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlink_detachskb")
int BPF_KPROBE(KtprobenetlinkVZVXdetachskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157308;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlink_detachskb")
int BPF_KRETPROBE(KtretprobenetlinkVZVXdetachskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157308;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlink_getsockopt")
int BPF_KPROBE(KtprobenetlinkVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157317;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlink_getsockopt")
int BPF_KRETPROBE(KtretprobenetlinkVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157317;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlink_rcv_skb")
int BPF_KPROBE(KtprobenetlinkVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157332;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlink_rcv_skb")
int BPF_KRETPROBE(KtretprobenetlinkVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157332;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlink_recvmsg")
int BPF_KPROBE(KtprobenetlinkVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157334;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlink_recvmsg")
int BPF_KRETPROBE(KtretprobenetlinkVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157334;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlink_sendmsg")
int BPF_KPROBE(KtprobenetlinkVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157338;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlink_sendmsg")
int BPF_KRETPROBE(KtretprobenetlinkVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157338;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlink_sendskb")
int BPF_KPROBE(KtprobenetlinkVZVXsendskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157339;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlink_sendskb")
int BPF_KRETPROBE(KtretprobenetlinkVZVXsendskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157339;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlink_setsockopt")
int BPF_KPROBE(KtprobenetlinkVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157347;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlink_setsockopt")
int BPF_KRETPROBE(KtretprobenetlinkVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157347;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlink_skb_destructor")
int BPF_KPROBE(KtprobenetlinkVZVXskbVZVXdestructor)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157348;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlink_skb_destructor")
int BPF_KRETPROBE(KtretprobenetlinkVZVXskbVZVXdestructor)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157348;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlink_sock_destruct")
int BPF_KPROBE(KtprobenetlinkVZVXsockVZVXdestruct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157349;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlink_sock_destruct")
int BPF_KRETPROBE(KtretprobenetlinkVZVXsockVZVXdestruct)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157349;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlink_to_full_skb")
int BPF_KPROBE(KtprobenetlinkVZVXtoVZVXfullVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157354;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlink_to_full_skb")
int BPF_KRETPROBE(KtretprobenetlinkVZVXtoVZVXfullVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157354;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlink_update_socket_mc")
int BPF_KPROBE(KtprobenetlinkVZVXupdateVZVXsocketVZVXmc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157364;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlink_update_socket_mc")
int BPF_KRETPROBE(KtretprobenetlinkVZVXupdateVZVXsocketVZVXmc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157364;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netlink_update_subscriptions")
int BPF_KPROBE(KtprobenetlinkVZVXupdateVZVXsubscriptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157366;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netlink_update_subscriptions")
int BPF_KRETPROBE(KtretprobenetlinkVZVXupdateVZVXsubscriptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157366;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/netns_ip_rt_init")
int BPF_KPROBE(KtprobenetnsVZVXipVZVXrtVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157367;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/netns_ip_rt_init")
int BPF_KRETPROBE(KtretprobenetnsVZVXipVZVXrtVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157367;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/nf_getsockopt")
int BPF_KPROBE(KtprobenfVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157384;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/nf_getsockopt")
int BPF_KRETPROBE(KtretprobenfVZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157384;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/nf_ip6_reroute")
int BPF_KPROBE(KtprobenfVZVXip6VZVXreroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157386;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/nf_ip6_reroute")
int BPF_KRETPROBE(KtretprobenfVZVXip6VZVXreroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157386;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/nf_ip_route")
int BPF_KPROBE(KtprobenfVZVXipVZVXroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157387;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/nf_ip_route")
int BPF_KRETPROBE(KtretprobenfVZVXipVZVXroute)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157387;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/nf_setsockopt")
int BPF_KPROBE(KtprobenfVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157391;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/nf_setsockopt")
int BPF_KRETPROBE(KtretprobenfVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157391;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/nf_sockopt_find")
int BPF_KPROBE(KtprobenfVZVXsockoptVZVXfind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157393;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/nf_sockopt_find")
int BPF_KRETPROBE(KtretprobenfVZVXsockoptVZVXfind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157393;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/page_to_skb")
int BPF_KPROBE(KtprobepageVZVXtoVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157656;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/page_to_skb")
int BPF_KRETPROBE(KtretprobepageVZVXtoVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=157656;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/reqsk_fastopen_remove")
int BPF_KPROBE(KtprobereqskVZVXfastopenVZVXremove)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=159925;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/reqsk_fastopen_remove")
int BPF_KRETPROBE(KtretprobereqskVZVXfastopenVZVXremove)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=159925;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/reqsk_queue_alloc")
int BPF_KPROBE(KtprobereqskVZVXqueueVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=159927;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/reqsk_queue_alloc")
int BPF_KRETPROBE(KtretprobereqskVZVXqueueVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=159927;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/reuseport_add_sock")
int BPF_KPROBE(KtprobereuseportVZVXaddVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=160032;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/reuseport_add_sock")
int BPF_KRETPROBE(KtretprobereuseportVZVXaddVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=160032;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/reuseport_detach_sock")
int BPF_KPROBE(KtprobereuseportVZVXdetachVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=160038;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/reuseport_detach_sock")
int BPF_KRETPROBE(KtretprobereuseportVZVXdetachVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=160038;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/reuseport_migrate_sock")
int BPF_KPROBE(KtprobereuseportVZVXmigrateVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=160044;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/reuseport_migrate_sock")
int BPF_KRETPROBE(KtretprobereuseportVZVXmigrateVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=160044;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/reuseport_select_sock")
int BPF_KPROBE(KtprobereuseportVZVXselectVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=160048;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/reuseport_select_sock")
int BPF_KRETPROBE(KtretprobereuseportVZVXselectVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=160048;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/reuseport_select_sock_by_hash")
int BPF_KPROBE(KtprobereuseportVZVXselectVZVXsockVZVXbyVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=160050;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/reuseport_select_sock_by_hash")
int BPF_KRETPROBE(KtretprobereuseportVZVXselectVZVXsockVZVXbyVZVXhash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=160050;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/reuseport_stop_listen_sock")
int BPF_KPROBE(KtprobereuseportVZVXstopVZVXlistenVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=160051;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/reuseport_stop_listen_sock")
int BPF_KRETPROBE(KtretprobereuseportVZVXstopVZVXlistenVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=160051;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sch_direct_xmit")
int BPF_KPROBE(KtprobeschVZVXdirectVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=160281;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sch_direct_xmit")
int BPF_KRETPROBE(KtretprobeschVZVXdirectVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=160281;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/scm_recv")
int BPF_KPROBE(KtprobescmVZVXrecv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=160338;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/scm_recv")
int BPF_KRETPROBE(KtretprobescmVZVXrecv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=160338;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/scm_recv_unix")
int BPF_KPROBE(KtprobescmVZVXrecvVZVXunix)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=160339;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/scm_recv_unix")
int BPF_KRETPROBE(KtretprobescmVZVXrecvVZVXunix)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=160339;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/secure_tcp_ts_off")
int BPF_KPROBE(KtprobesecureVZVXtcpVZVXtsVZVXoff)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=160592;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/secure_tcp_ts_off")
int BPF_KRETPROBE(KtretprobesecureVZVXtcpVZVXtsVZVXoff)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=160592;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/secure_tcpv6_ts_off")
int BPF_KPROBE(KtprobesecureVZVXtcpv6VZVXtsVZVXoff)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=160596;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/secure_tcpv6_ts_off")
int BPF_KRETPROBE(KtretprobesecureVZVXtcpv6VZVXtsVZVXoff)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=160596;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_forced_mem_schedule")
int BPF_KPROBE(KtprobeskVZVXforcedVZVXmemVZVXschedule)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161330;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_forced_mem_schedule")
int BPF_KRETPROBE(KtretprobeskVZVXforcedVZVXmemVZVXschedule)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161330;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_msg_alloc")
int BPF_KPROBE(KtprobeskVZVXmsgVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161332;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_msg_alloc")
int BPF_KRETPROBE(KtretprobeskVZVXmsgVZVXalloc)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161332;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_msg_clone")
int BPF_KPROBE(KtprobeskVZVXmsgVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161334;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_msg_clone")
int BPF_KRETPROBE(KtretprobeskVZVXmsgVZVXclone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161334;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_msg_free")
int BPF_KPROBE(KtprobeskVZVXmsgVZVXfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161336;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_msg_free")
int BPF_KRETPROBE(KtretprobeskVZVXmsgVZVXfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161336;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_msg_free_elem")
int BPF_KPROBE(KtprobeskVZVXmsgVZVXfreeVZVXelem)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161337;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_msg_free_elem")
int BPF_KRETPROBE(KtretprobeskVZVXmsgVZVXfreeVZVXelem)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161337;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_msg_free_nocharge")
int BPF_KPROBE(KtprobeskVZVXmsgVZVXfreeVZVXnocharge)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161338;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_msg_free_nocharge")
int BPF_KRETPROBE(KtretprobeskVZVXmsgVZVXfreeVZVXnocharge)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161338;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_msg_free_partial")
int BPF_KPROBE(KtprobeskVZVXmsgVZVXfreeVZVXpartial)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161340;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_msg_free_partial")
int BPF_KRETPROBE(KtretprobeskVZVXmsgVZVXfreeVZVXpartial)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161340;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_msg_free_partial_nocharge")
int BPF_KPROBE(KtprobeskVZVXmsgVZVXfreeVZVXpartialVZVXnocharge)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161341;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_msg_free_partial_nocharge")
int BPF_KRETPROBE(KtretprobeskVZVXmsgVZVXfreeVZVXpartialVZVXnocharge)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161341;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_msg_is_readable")
int BPF_KPROBE(KtprobeskVZVXmsgVZVXisVZVXreadable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161342;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_msg_is_readable")
int BPF_KRETPROBE(KtretprobeskVZVXmsgVZVXisVZVXreadable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161342;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_msg_memcopy_from_iter")
int BPF_KPROBE(KtprobeskVZVXmsgVZVXmemcopyVZVXfromVZVXiter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161344;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_msg_memcopy_from_iter")
int BPF_KRETPROBE(KtretprobeskVZVXmsgVZVXmemcopyVZVXfromVZVXiter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161344;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_msg_recvmsg")
int BPF_KPROBE(KtprobeskVZVXmsgVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161346;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_msg_recvmsg")
int BPF_KRETPROBE(KtretprobeskVZVXmsgVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161346;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_msg_return")
int BPF_KPROBE(KtprobeskVZVXmsgVZVXreturn)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161348;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_msg_return")
int BPF_KRETPROBE(KtretprobeskVZVXmsgVZVXreturn)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161348;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_msg_return_zero")
int BPF_KPROBE(KtprobeskVZVXmsgVZVXreturnVZVXzero)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161349;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_msg_return_zero")
int BPF_KRETPROBE(KtretprobeskVZVXmsgVZVXreturnVZVXzero)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161349;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_msg_trim")
int BPF_KPROBE(KtprobeskVZVXmsgVZVXtrim)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161351;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_msg_trim")
int BPF_KRETPROBE(KtretprobeskVZVXmsgVZVXtrim)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161351;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_msg_zerocopy_from_iter")
int BPF_KPROBE(KtprobeskVZVXmsgVZVXzerocopyVZVXfromVZVXiter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161352;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_msg_zerocopy_from_iter")
int BPF_KRETPROBE(KtretprobeskVZVXmsgVZVXzerocopyVZVXfromVZVXiter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161352;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_psock_drop")
int BPF_KPROBE(KtprobeskVZVXpsockVZVXdrop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161355;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_psock_drop")
int BPF_KRETPROBE(KtretprobeskVZVXpsockVZVXdrop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161355;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_psock_init")
int BPF_KPROBE(KtprobeskVZVXpsockVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161357;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_psock_init")
int BPF_KRETPROBE(KtretprobeskVZVXpsockVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161357;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_psock_init_strp")
int BPF_KPROBE(KtprobeskVZVXpsockVZVXinitVZVXstrp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161359;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_psock_init_strp")
int BPF_KRETPROBE(KtretprobeskVZVXpsockVZVXinitVZVXstrp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161359;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_psock_link_pop")
int BPF_KPROBE(KtprobeskVZVXpsockVZVXlinkVZVXpop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161361;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_psock_link_pop")
int BPF_KRETPROBE(KtretprobeskVZVXpsockVZVXlinkVZVXpop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161361;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_psock_msg_verdict")
int BPF_KPROBE(KtprobeskVZVXpsockVZVXmsgVZVXverdict)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161363;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_psock_msg_verdict")
int BPF_KRETPROBE(KtretprobeskVZVXpsockVZVXmsgVZVXverdict)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161363;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_psock_skb_ingress_enqueue")
int BPF_KPROBE(KtprobeskVZVXpsockVZVXskbVZVXingressVZVXenqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161365;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_psock_skb_ingress_enqueue")
int BPF_KRETPROBE(KtretprobeskVZVXpsockVZVXskbVZVXingressVZVXenqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161365;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_psock_skb_ingress_self")
int BPF_KPROBE(KtprobeskVZVXpsockVZVXskbVZVXingressVZVXself)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161367;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_psock_skb_ingress_self")
int BPF_KRETPROBE(KtretprobeskVZVXpsockVZVXskbVZVXingressVZVXself)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161367;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_psock_skb_redirect")
int BPF_KPROBE(KtprobeskVZVXpsockVZVXskbVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161369;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_psock_skb_redirect")
int BPF_KRETPROBE(KtretprobeskVZVXpsockVZVXskbVZVXredirect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161369;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_psock_start_strp")
int BPF_KPROBE(KtprobeskVZVXpsockVZVXstartVZVXstrp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161370;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_psock_start_strp")
int BPF_KRETPROBE(KtretprobeskVZVXpsockVZVXstartVZVXstrp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161370;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_psock_start_verdict")
int BPF_KPROBE(KtprobeskVZVXpsockVZVXstartVZVXverdict)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161371;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_psock_start_verdict")
int BPF_KRETPROBE(KtretprobeskVZVXpsockVZVXstartVZVXverdict)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161371;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_psock_stop")
int BPF_KPROBE(KtprobeskVZVXpsockVZVXstop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161373;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_psock_stop")
int BPF_KRETPROBE(KtretprobeskVZVXpsockVZVXstop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161373;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_psock_stop_strp")
int BPF_KPROBE(KtprobeskVZVXpsockVZVXstopVZVXstrp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161374;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_psock_stop_strp")
int BPF_KRETPROBE(KtretprobeskVZVXpsockVZVXstopVZVXstrp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161374;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_psock_stop_verdict")
int BPF_KPROBE(KtprobeskVZVXpsockVZVXstopVZVXverdict)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161375;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_psock_stop_verdict")
int BPF_KRETPROBE(KtretprobeskVZVXpsockVZVXstopVZVXverdict)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161375;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_psock_strp_data_ready")
int BPF_KPROBE(KtprobeskVZVXpsockVZVXstrpVZVXdataVZVXready)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161376;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_psock_strp_data_ready")
int BPF_KRETPROBE(KtretprobeskVZVXpsockVZVXstrpVZVXdataVZVXready)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161376;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_psock_strp_parse")
int BPF_KPROBE(KtprobeskVZVXpsockVZVXstrpVZVXparse)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161377;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_psock_strp_parse")
int BPF_KRETPROBE(KtretprobeskVZVXpsockVZVXstrpVZVXparse)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161377;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_psock_strp_read")
int BPF_KPROBE(KtprobeskVZVXpsockVZVXstrpVZVXread)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161378;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_psock_strp_read")
int BPF_KRETPROBE(KtretprobeskVZVXpsockVZVXstrpVZVXread)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161378;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_psock_strp_read_done")
int BPF_KPROBE(KtprobeskVZVXpsockVZVXstrpVZVXreadVZVXdone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161379;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_psock_strp_read_done")
int BPF_KRETPROBE(KtretprobeskVZVXpsockVZVXstrpVZVXreadVZVXdone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161379;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_psock_tls_strp_read")
int BPF_KPROBE(KtprobeskVZVXpsockVZVXtlsVZVXstrpVZVXread)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161381;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_psock_tls_strp_read")
int BPF_KRETPROBE(KtretprobeskVZVXpsockVZVXtlsVZVXstrpVZVXread)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161381;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_psock_verdict_apply")
int BPF_KPROBE(KtprobeskVZVXpsockVZVXverdictVZVXapply)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161383;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_psock_verdict_apply")
int BPF_KRETPROBE(KtretprobeskVZVXpsockVZVXverdictVZVXapply)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161383;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_psock_verdict_data_ready")
int BPF_KPROBE(KtprobeskVZVXpsockVZVXverdictVZVXdataVZVXready)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161384;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_psock_verdict_data_ready")
int BPF_KRETPROBE(KtretprobeskVZVXpsockVZVXverdictVZVXdataVZVXready)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161384;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_psock_verdict_recv")
int BPF_KPROBE(KtprobeskVZVXpsockVZVXverdictVZVXrecv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161385;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_psock_verdict_recv")
int BPF_KRETPROBE(KtretprobeskVZVXpsockVZVXverdictVZVXrecv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161385;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sk_psock_write_space")
int BPF_KPROBE(KtprobeskVZVXpsockVZVXwriteVZVXspace)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161386;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sk_psock_write_space")
int BPF_KRETPROBE(KtretprobeskVZVXpsockVZVXwriteVZVXspace)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161386;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_checksum_help")
int BPF_KPROBE(KtprobeskbVZVXchecksumVZVXhelp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161387;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_checksum_help")
int BPF_KRETPROBE(KtretprobeskbVZVXchecksumVZVXhelp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161387;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_copy_and_crc32c_datagram_iter")
int BPF_KPROBE(KtprobeskbVZVXcopyVZVXandVZVXcrc32cVZVXdatagramVZVXiter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161389;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_copy_and_crc32c_datagram_iter")
int BPF_KRETPROBE(KtretprobeskbVZVXcopyVZVXandVZVXcrc32cVZVXdatagramVZVXiter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161389;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_copy_and_csum_datagram_msg")
int BPF_KPROBE(KtprobeskbVZVXcopyVZVXandVZVXcsumVZVXdatagramVZVXmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161391;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_copy_and_csum_datagram_msg")
int BPF_KRETPROBE(KtretprobeskbVZVXcopyVZVXandVZVXcsumVZVXdatagramVZVXmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161391;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_copy_datagram_from_iter_full")
int BPF_KPROBE(KtprobeskbVZVXcopyVZVXdatagramVZVXfromVZVXiterVZVXfull)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161394;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_copy_datagram_from_iter_full")
int BPF_KRETPROBE(KtretprobeskbVZVXcopyVZVXdatagramVZVXfromVZVXiterVZVXfull)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161394;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_crc32c_csum_help")
int BPF_KPROBE(KtprobeskbVZVXcrc32cVZVXcsumVZVXhelp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161397;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_crc32c_csum_help")
int BPF_KRETPROBE(KtretprobeskbVZVXcrc32cVZVXcsumVZVXhelp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161397;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_csum_hwoffload_help")
int BPF_KPROBE(KtprobeskbVZVXcsumVZVXhwoffloadVZVXhelp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161399;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_csum_hwoffload_help")
int BPF_KRETPROBE(KtretprobeskbVZVXcsumVZVXhwoffloadVZVXhelp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161399;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_dst_pop")
int BPF_KPROBE(KtprobeskbVZVXdstVZVXpop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161401;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_dst_pop")
int BPF_KRETPROBE(KtretprobeskbVZVXdstVZVXpop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161401;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_eth_gso_segment")
int BPF_KPROBE(KtprobeskbVZVXethVZVXgsoVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161403;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_eth_gso_segment")
int BPF_KRETPROBE(KtretprobeskbVZVXethVZVXgsoVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161403;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_free_datagram")
int BPF_KPROBE(KtprobeskbVZVXfreeVZVXdatagram)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161404;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_free_datagram")
int BPF_KRETPROBE(KtretprobeskbVZVXfreeVZVXdatagram)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161404;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_gso_transport_seglen")
int BPF_KPROBE(KtprobeskbVZVXgsoVZVXtransportVZVXseglen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161405;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_gso_transport_seglen")
int BPF_KRETPROBE(KtretprobeskbVZVXgsoVZVXtransportVZVXseglen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161405;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_gso_validate_mac_len")
int BPF_KPROBE(KtprobeskbVZVXgsoVZVXvalidateVZVXmacVZVXlen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161407;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_gso_validate_mac_len")
int BPF_KRETPROBE(KtretprobeskbVZVXgsoVZVXvalidateVZVXmacVZVXlen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161407;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_gso_validate_network_len")
int BPF_KPROBE(KtprobeskbVZVXgsoVZVXvalidateVZVXnetworkVZVXlen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161409;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_gso_validate_network_len")
int BPF_KRETPROBE(KtretprobeskbVZVXgsoVZVXvalidateVZVXnetworkVZVXlen)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161409;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_kill_datagram")
int BPF_KPROBE(KtprobeskbVZVXkillVZVXdatagram)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161411;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_kill_datagram")
int BPF_KRETPROBE(KtretprobeskbVZVXkillVZVXdatagram)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161411;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_mac_gso_segment")
int BPF_KPROBE(KtprobeskbVZVXmacVZVXgsoVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161412;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_mac_gso_segment")
int BPF_KRETPROBE(KtretprobeskbVZVXmacVZVXgsoVZVXsegment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161412;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_network_protocol")
int BPF_KPROBE(KtprobeskbVZVXnetworkVZVXprotocol)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161414;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_network_protocol")
int BPF_KRETPROBE(KtretprobeskbVZVXnetworkVZVXprotocol)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161414;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_recv_datagram")
int BPF_KPROBE(KtprobeskbVZVXrecvVZVXdatagram)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161416;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_recv_datagram")
int BPF_KRETPROBE(KtretprobeskbVZVXrecvVZVXdatagram)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161416;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_still_in_host_queue")
int BPF_KPROBE(KtprobeskbVZVXstillVZVXinVZVXhostVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161420;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_still_in_host_queue")
int BPF_KRETPROBE(KtretprobeskbVZVXstillVZVXinVZVXhostVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161420;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_tunnel_check_pmtu")
int BPF_KPROBE(KtprobeskbVZVXtunnelVZVXcheckVZVXpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161422;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_tunnel_check_pmtu")
int BPF_KRETPROBE(KtretprobeskbVZVXtunnelVZVXcheckVZVXpmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161422;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/skb_warn_bad_offload")
int BPF_KPROBE(KtprobeskbVZVXwarnVZVXbadVZVXoffload)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161423;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/skb_warn_bad_offload")
int BPF_KRETPROBE(KtretprobeskbVZVXwarnVZVXbadVZVXoffload)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161423;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_diag_bind")
int BPF_KPROBE(KtprobesockVZVXdiagVZVXbind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161579;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_diag_bind")
int BPF_KRETPROBE(KtretprobesockVZVXdiagVZVXbind)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161579;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_diag_broadcast_destroy")
int BPF_KPROBE(KtprobesockVZVXdiagVZVXbroadcastVZVXdestroy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161580;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_diag_broadcast_destroy")
int BPF_KRETPROBE(KtretprobesockVZVXdiagVZVXbroadcastVZVXdestroy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161580;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_diag_check_cookie")
int BPF_KPROBE(KtprobesockVZVXdiagVZVXcheckVZVXcookie)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161583;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_diag_check_cookie")
int BPF_KRETPROBE(KtretprobesockVZVXdiagVZVXcheckVZVXcookie)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161583;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_diag_destroy")
int BPF_KPROBE(KtprobesockVZVXdiagVZVXdestroy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161584;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_diag_destroy")
int BPF_KRETPROBE(KtretprobesockVZVXdiagVZVXdestroy)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161584;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_diag_put_filterinfo")
int BPF_KPROBE(KtprobesockVZVXdiagVZVXputVZVXfilterinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161587;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_diag_put_filterinfo")
int BPF_KRETPROBE(KtretprobesockVZVXdiagVZVXputVZVXfilterinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161587;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_diag_put_meminfo")
int BPF_KPROBE(KtprobesockVZVXdiagVZVXputVZVXmeminfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161589;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_diag_put_meminfo")
int BPF_KRETPROBE(KtretprobesockVZVXdiagVZVXputVZVXmeminfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161589;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_diag_rcv")
int BPF_KPROBE(KtprobesockVZVXdiagVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161590;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_diag_rcv")
int BPF_KRETPROBE(KtretprobesockVZVXdiagVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161590;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_diag_rcv_msg")
int BPF_KPROBE(KtprobesockVZVXdiagVZVXrcvVZVXmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161591;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_diag_rcv_msg")
int BPF_KRETPROBE(KtretprobesockVZVXdiagVZVXrcvVZVXmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161591;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sock_diag_save_cookie")
int BPF_KPROBE(KtprobesockVZVXdiagVZVXsaveVZVXcookie)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161597;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sock_diag_save_cookie")
int BPF_KRETPROBE(KtretprobesockVZVXdiagVZVXsaveVZVXcookie)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161597;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/start_xmit")
int BPF_KPROBE(KtprobestartVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161766;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/start_xmit")
int BPF_KRETPROBE(KtretprobestartVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161766;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/strp_read_sock")
int BPF_KPROBE(KtprobestrpVZVXreadVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161844;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/strp_read_sock")
int BPF_KRETPROBE(KtretprobestrpVZVXreadVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161844;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/strp_recv")
int BPF_KPROBE(KtprobestrpVZVXrecv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161846;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/strp_recv")
int BPF_KRETPROBE(KtretprobestrpVZVXrecv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161846;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/strp_sock_lock")
int BPF_KPROBE(KtprobestrpVZVXsockVZVXlock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161847;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/strp_sock_lock")
int BPF_KRETPROBE(KtretprobestrpVZVXsockVZVXlock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161847;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/strp_sock_unlock")
int BPF_KPROBE(KtprobestrpVZVXsockVZVXunlock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161848;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/strp_sock_unlock")
int BPF_KRETPROBE(KtretprobestrpVZVXsockVZVXunlock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161848;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/sync_socket_options")
int BPF_KPROBE(KtprobesyncVZVXsocketVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161999;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/sync_socket_options")
int BPF_KRETPROBE(KtretprobesyncVZVXsocketVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=161999;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcf_dev_queue_xmit")
int BPF_KPROBE(KtprobetcfVZVXdevVZVXqueueVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162164;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcf_dev_queue_xmit")
int BPF_KRETPROBE(KtretprobetcfVZVXdevVZVXqueueVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162164;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_adjust_pcount")
int BPF_KPROBE(KtprobetcpVZVXadjustVZVXpcount)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162195;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_adjust_pcount")
int BPF_KRETPROBE(KtretprobetcpVZVXadjustVZVXpcount)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162195;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_ca_openreq_child")
int BPF_KPROBE(KtprobetcpVZVXcaVZVXopenreqVZVXchild)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162211;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_ca_openreq_child")
int BPF_KRETPROBE(KtretprobetcpVZVXcaVZVXopenreqVZVXchild)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162211;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_check_req")
int BPF_KPROBE(KtprobetcpVZVXcheckVZVXreq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162213;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_check_req")
int BPF_KRETPROBE(KtretprobetcpVZVXcheckVZVXreq)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162213;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_child_process")
int BPF_KPROBE(KtprobetcpVZVXchildVZVXprocess)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162215;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_child_process")
int BPF_KRETPROBE(KtretprobetcpVZVXchildVZVXprocess)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162215;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_chrono_start")
int BPF_KPROBE(KtprobetcpVZVXchronoVZVXstart)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162217;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_chrono_start")
int BPF_KRETPROBE(KtretprobetcpVZVXchronoVZVXstart)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162217;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_chrono_stop")
int BPF_KPROBE(KtprobetcpVZVXchronoVZVXstop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162218;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_chrono_stop")
int BPF_KRETPROBE(KtretprobetcpVZVXchronoVZVXstop)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162218;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_clone_payload")
int BPF_KPROBE(KtprobetcpVZVXcloneVZVXpayload)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162220;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_clone_payload")
int BPF_KRETPROBE(KtretprobetcpVZVXcloneVZVXpayload)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162220;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_connect")
int BPF_KPROBE(KtprobetcpVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162221;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_connect")
int BPF_KRETPROBE(KtretprobetcpVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162221;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_connect_init")
int BPF_KPROBE(KtprobetcpVZVXconnectVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162222;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_connect_init")
int BPF_KRETPROBE(KtretprobetcpVZVXconnectVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162222;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_connect_queue_skb")
int BPF_KPROBE(KtprobetcpVZVXconnectVZVXqueueVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162223;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_connect_queue_skb")
int BPF_KRETPROBE(KtretprobetcpVZVXconnectVZVXqueueVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162223;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_create_openreq_child")
int BPF_KPROBE(KtprobetcpVZVXcreateVZVXopenreqVZVXchild)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162225;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_create_openreq_child")
int BPF_KRETPROBE(KtretprobetcpVZVXcreateVZVXopenreqVZVXchild)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162225;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_current_mss")
int BPF_KPROBE(KtprobetcpVZVXcurrentVZVXmss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162227;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_current_mss")
int BPF_KRETPROBE(KtretprobetcpVZVXcurrentVZVXmss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162227;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_cwnd_restart")
int BPF_KPROBE(KtprobetcpVZVXcwndVZVXrestart)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162229;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_cwnd_restart")
int BPF_KRETPROBE(KtretprobetcpVZVXcwndVZVXrestart)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162229;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_cwnd_validate")
int BPF_KPROBE(KtprobetcpVZVXcwndVZVXvalidate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162231;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_cwnd_validate")
int BPF_KRETPROBE(KtretprobetcpVZVXcwndVZVXvalidate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162231;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_delack_max")
int BPF_KPROBE(KtprobetcpVZVXdelackVZVXmax)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162233;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_delack_max")
int BPF_KRETPROBE(KtretprobetcpVZVXdelackVZVXmax)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162233;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_eat_skb")
int BPF_KPROBE(KtprobetcpVZVXeatVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162234;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_eat_skb")
int BPF_KRETPROBE(KtretprobetcpVZVXeatVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162234;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_established_options")
int BPF_KPROBE(KtprobetcpVZVXestablishedVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162236;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_established_options")
int BPF_KRETPROBE(KtretprobetcpVZVXestablishedVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162236;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_event_new_data_sent")
int BPF_KPROBE(KtprobetcpVZVXeventVZVXnewVZVXdataVZVXsent)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162237;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_event_new_data_sent")
int BPF_KRETPROBE(KtretprobetcpVZVXeventVZVXnewVZVXdataVZVXsent)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162237;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_fastopen_cache_get")
int BPF_KPROBE(KtprobetcpVZVXfastopenVZVXcacheVZVXget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162239;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_fastopen_cache_get")
int BPF_KRETPROBE(KtretprobetcpVZVXfastopenVZVXcacheVZVXget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162239;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_fastopen_cache_set")
int BPF_KPROBE(KtprobetcpVZVXfastopenVZVXcacheVZVXset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162241;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_fastopen_cache_set")
int BPF_KRETPROBE(KtretprobetcpVZVXfastopenVZVXcacheVZVXset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162241;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_fragment")
int BPF_KPROBE(KtprobetcpVZVXfragment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162243;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_fragment")
int BPF_KRETPROBE(KtretprobetcpVZVXfragment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162243;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_fragment_tstamp")
int BPF_KPROBE(KtprobetcpVZVXfragmentVZVXtstamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162245;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_fragment_tstamp")
int BPF_KRETPROBE(KtretprobetcpVZVXfragmentVZVXtstamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162245;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_get_metrics")
int BPF_KPROBE(KtprobetcpVZVXgetVZVXmetrics)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162247;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_get_metrics")
int BPF_KRETPROBE(KtretprobetcpVZVXgetVZVXmetrics)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162247;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_init_metrics")
int BPF_KPROBE(KtprobetcpVZVXinitVZVXmetrics)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162248;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_init_metrics")
int BPF_KRETPROBE(KtretprobetcpVZVXinitVZVXmetrics)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162248;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_init_tso_segs")
int BPF_KPROBE(KtprobetcpVZVXinitVZVXtsoVZVXsegs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162250;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_init_tso_segs")
int BPF_KRETPROBE(KtretprobetcpVZVXinitVZVXtsoVZVXsegs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162250;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_make_synack")
int BPF_KPROBE(KtprobetcpVZVXmakeVZVXsynack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162252;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_make_synack")
int BPF_KRETPROBE(KtretprobetcpVZVXmakeVZVXsynack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162252;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_metrics_fill_info")
int BPF_KPROBE(KtprobetcpVZVXmetricsVZVXfillVZVXinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162255;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_metrics_fill_info")
int BPF_KRETPROBE(KtretprobetcpVZVXmetricsVZVXfillVZVXinfo)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162255;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_metrics_flush_all")
int BPF_KPROBE(KtprobetcpVZVXmetricsVZVXflushVZVXall)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162256;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_metrics_flush_all")
int BPF_KRETPROBE(KtretprobetcpVZVXmetricsVZVXflushVZVXall)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162256;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_metrics_nl_cmd_del")
int BPF_KPROBE(KtprobetcpVZVXmetricsVZVXnlVZVXcmdVZVXdel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162258;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_metrics_nl_cmd_del")
int BPF_KRETPROBE(KtretprobetcpVZVXmetricsVZVXnlVZVXcmdVZVXdel)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162258;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_metrics_nl_cmd_get")
int BPF_KPROBE(KtprobetcpVZVXmetricsVZVXnlVZVXcmdVZVXget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162259;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_metrics_nl_cmd_get")
int BPF_KRETPROBE(KtretprobetcpVZVXmetricsVZVXnlVZVXcmdVZVXget)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162259;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_metrics_nl_dump")
int BPF_KPROBE(KtprobetcpVZVXmetricsVZVXnlVZVXdump)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162260;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_metrics_nl_dump")
int BPF_KRETPROBE(KtretprobetcpVZVXmetricsVZVXnlVZVXdump)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162260;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_msg_wait_data")
int BPF_KPROBE(KtprobetcpVZVXmsgVZVXwaitVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162262;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_msg_wait_data")
int BPF_KRETPROBE(KtretprobetcpVZVXmsgVZVXwaitVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162262;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_mss_to_mtu")
int BPF_KPROBE(KtprobetcpVZVXmssVZVXtoVZVXmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162264;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_mss_to_mtu")
int BPF_KRETPROBE(KtretprobetcpVZVXmssVZVXtoVZVXmtu)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162264;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_mstamp_refresh")
int BPF_KPROBE(KtprobetcpVZVXmstampVZVXrefresh)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162265;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_mstamp_refresh")
int BPF_KRETPROBE(KtretprobetcpVZVXmstampVZVXrefresh)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162265;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_mtu_probe")
int BPF_KPROBE(KtprobetcpVZVXmtuVZVXprobe)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162266;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_mtu_probe")
int BPF_KRETPROBE(KtretprobetcpVZVXmtuVZVXprobe)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162266;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_mtu_to_mss")
int BPF_KPROBE(KtprobetcpVZVXmtuVZVXtoVZVXmss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162268;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_mtu_to_mss")
int BPF_KRETPROBE(KtretprobetcpVZVXmtuVZVXtoVZVXmss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162268;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_mtup_init")
int BPF_KPROBE(KtprobetcpVZVXmtupVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162269;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_mtup_init")
int BPF_KRETPROBE(KtretprobetcpVZVXmtupVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162269;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_newreno_mark_lost")
int BPF_KPROBE(KtprobetcpVZVXnewrenoVZVXmarkVZVXlost)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162272;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_newreno_mark_lost")
int BPF_KRETPROBE(KtretprobetcpVZVXnewrenoVZVXmarkVZVXlost)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162272;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_openreq_init_rwin")
int BPF_KPROBE(KtprobetcpVZVXopenreqVZVXinitVZVXrwin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162274;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_openreq_init_rwin")
int BPF_KRETPROBE(KtretprobetcpVZVXopenreqVZVXinitVZVXrwin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162274;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_options_write")
int BPF_KPROBE(KtprobetcpVZVXoptionsVZVXwrite)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162276;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_options_write")
int BPF_KRETPROBE(KtretprobetcpVZVXoptionsVZVXwrite)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162276;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_peer_is_proven")
int BPF_KPROBE(KtprobetcpVZVXpeerVZVXisVZVXproven)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162279;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_peer_is_proven")
int BPF_KRETPROBE(KtretprobetcpVZVXpeerVZVXisVZVXproven)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162279;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_plb_check_rehash")
int BPF_KPROBE(KtprobetcpVZVXplbVZVXcheckVZVXrehash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162281;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_plb_check_rehash")
int BPF_KRETPROBE(KtretprobetcpVZVXplbVZVXcheckVZVXrehash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162281;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_plb_update_state")
int BPF_KPROBE(KtprobetcpVZVXplbVZVXupdateVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162283;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_plb_update_state")
int BPF_KRETPROBE(KtretprobetcpVZVXplbVZVXupdateVZVXstate)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162283;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_plb_update_state_upon_rto")
int BPF_KPROBE(KtprobetcpVZVXplbVZVXupdateVZVXstateVZVXuponVZVXrto)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162284;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_plb_update_state_upon_rto")
int BPF_KRETPROBE(KtretprobetcpVZVXplbVZVXupdateVZVXstateVZVXuponVZVXrto)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162284;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_push_one")
int BPF_KPROBE(KtprobetcpVZVXpushVZVXone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162286;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_push_one")
int BPF_KRETPROBE(KtretprobetcpVZVXpushVZVXone)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162286;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_rack_advance")
int BPF_KPROBE(KtprobetcpVZVXrackVZVXadvance)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162288;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_rack_advance")
int BPF_KRETPROBE(KtretprobetcpVZVXrackVZVXadvance)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162288;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_rack_detect_loss")
int BPF_KPROBE(KtprobetcpVZVXrackVZVXdetectVZVXloss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162290;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_rack_detect_loss")
int BPF_KRETPROBE(KtretprobetcpVZVXrackVZVXdetectVZVXloss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162290;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_rack_mark_lost")
int BPF_KPROBE(KtprobetcpVZVXrackVZVXmarkVZVXlost)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162291;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_rack_mark_lost")
int BPF_KRETPROBE(KtretprobetcpVZVXrackVZVXmarkVZVXlost)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162291;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_rack_reo_timeout")
int BPF_KPROBE(KtprobetcpVZVXrackVZVXreoVZVXtimeout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162292;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_rack_reo_timeout")
int BPF_KRETPROBE(KtretprobetcpVZVXrackVZVXreoVZVXtimeout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162292;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_rack_skb_timeout")
int BPF_KPROBE(KtprobetcpVZVXrackVZVXskbVZVXtimeout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162294;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_rack_skb_timeout")
int BPF_KRETPROBE(KtretprobetcpVZVXrackVZVXskbVZVXtimeout)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162294;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_rack_update_reo_wnd")
int BPF_KPROBE(KtprobetcpVZVXrackVZVXupdateVZVXreoVZVXwnd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162296;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_rack_update_reo_wnd")
int BPF_KRETPROBE(KtretprobetcpVZVXrackVZVXupdateVZVXreoVZVXwnd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162296;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_retrans_try_collapse")
int BPF_KPROBE(KtprobetcpVZVXretransVZVXtryVZVXcollapse)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162299;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_retrans_try_collapse")
int BPF_KRETPROBE(KtretprobetcpVZVXretransVZVXtryVZVXcollapse)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162299;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_retransmit_skb")
int BPF_KPROBE(KtprobetcpVZVXretransmitVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162300;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_retransmit_skb")
int BPF_KRETPROBE(KtretprobetcpVZVXretransmitVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162300;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_rtx_synack")
int BPF_KPROBE(KtprobetcpVZVXrtxVZVXsynack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162302;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_rtx_synack")
int BPF_KRETPROBE(KtretprobetcpVZVXrtxVZVXsynack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162302;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_schedule_loss_probe")
int BPF_KPROBE(KtprobetcpVZVXscheduleVZVXlossVZVXprobe)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162304;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_schedule_loss_probe")
int BPF_KRETPROBE(KtretprobetcpVZVXscheduleVZVXlossVZVXprobe)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162304;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_select_initial_window")
int BPF_KPROBE(KtprobetcpVZVXselectVZVXinitialVZVXwindow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162306;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_select_initial_window")
int BPF_KRETPROBE(KtretprobetcpVZVXselectVZVXinitialVZVXwindow)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162306;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_send_ack")
int BPF_KPROBE(KtprobetcpVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162307;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_send_ack")
int BPF_KRETPROBE(KtretprobetcpVZVXsendVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162307;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_send_active_reset")
int BPF_KPROBE(KtprobetcpVZVXsendVZVXactiveVZVXreset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162309;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_send_active_reset")
int BPF_KRETPROBE(KtretprobetcpVZVXsendVZVXactiveVZVXreset)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162309;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_send_delayed_ack")
int BPF_KPROBE(KtprobetcpVZVXsendVZVXdelayedVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162310;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_send_delayed_ack")
int BPF_KRETPROBE(KtretprobetcpVZVXsendVZVXdelayedVZVXack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162310;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_send_fin")
int BPF_KPROBE(KtprobetcpVZVXsendVZVXfin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162311;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_send_fin")
int BPF_KRETPROBE(KtretprobetcpVZVXsendVZVXfin)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162311;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_send_loss_probe")
int BPF_KPROBE(KtprobetcpVZVXsendVZVXlossVZVXprobe)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162312;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_send_loss_probe")
int BPF_KRETPROBE(KtretprobetcpVZVXsendVZVXlossVZVXprobe)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162312;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_send_probe0")
int BPF_KPROBE(KtprobetcpVZVXsendVZVXprobe0)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162313;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_send_probe0")
int BPF_KRETPROBE(KtretprobetcpVZVXsendVZVXprobe0)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162313;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_send_syn_data")
int BPF_KPROBE(KtprobetcpVZVXsendVZVXsynVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162315;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_send_syn_data")
int BPF_KRETPROBE(KtretprobetcpVZVXsendVZVXsynVZVXdata)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162315;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_send_synack")
int BPF_KPROBE(KtprobetcpVZVXsendVZVXsynack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162316;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_send_synack")
int BPF_KRETPROBE(KtretprobetcpVZVXsendVZVXsynack)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162316;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_send_window_probe")
int BPF_KPROBE(KtprobetcpVZVXsendVZVXwindowVZVXprobe)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162317;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_send_window_probe")
int BPF_KRETPROBE(KtretprobetcpVZVXsendVZVXwindowVZVXprobe)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162317;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_skb_collapse_tstamp")
int BPF_KPROBE(KtprobetcpVZVXskbVZVXcollapseVZVXtstamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162319;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_skb_collapse_tstamp")
int BPF_KRETPROBE(KtretprobetcpVZVXskbVZVXcollapseVZVXtstamp)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162319;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_syn_options")
int BPF_KPROBE(KtprobetcpVZVXsynVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162320;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_syn_options")
int BPF_KRETPROBE(KtretprobetcpVZVXsynVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162320;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_synack_options")
int BPF_KPROBE(KtprobetcpVZVXsynackVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162322;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_synack_options")
int BPF_KRETPROBE(KtretprobetcpVZVXsynackVZVXoptions)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162322;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_sync_mss")
int BPF_KPROBE(KtprobetcpVZVXsyncVZVXmss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162323;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_sync_mss")
int BPF_KRETPROBE(KtretprobetcpVZVXsyncVZVXmss)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162323;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_time_wait")
int BPF_KPROBE(KtprobetcpVZVXtimeVZVXwait)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162325;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_time_wait")
int BPF_KRETPROBE(KtretprobetcpVZVXtimeVZVXwait)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162325;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_timewait_state_process")
int BPF_KPROBE(KtprobetcpVZVXtimewaitVZVXstateVZVXprocess)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162327;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_timewait_state_process")
int BPF_KRETPROBE(KtretprobetcpVZVXtimewaitVZVXstateVZVXprocess)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162327;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_trim_head")
int BPF_KPROBE(KtprobetcpVZVXtrimVZVXhead)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162329;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_trim_head")
int BPF_KRETPROBE(KtretprobetcpVZVXtrimVZVXhead)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162329;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_tso_segs")
int BPF_KPROBE(KtprobetcpVZVXtsoVZVXsegs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162331;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_tso_segs")
int BPF_KRETPROBE(KtretprobetcpVZVXtsoVZVXsegs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162331;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_tso_should_defer")
int BPF_KPROBE(KtprobetcpVZVXtsoVZVXshouldVZVXdefer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162333;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_tso_should_defer")
int BPF_KRETPROBE(KtretprobetcpVZVXtsoVZVXshouldVZVXdefer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162333;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_tsq_write")
int BPF_KPROBE(KtprobetcpVZVXtsqVZVXwrite)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162336;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_tsq_write")
int BPF_KRETPROBE(KtretprobetcpVZVXtsqVZVXwrite)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162336;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_twsk_destructor")
int BPF_KPROBE(KtprobetcpVZVXtwskVZVXdestructor)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162337;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_twsk_destructor")
int BPF_KRETPROBE(KtretprobetcpVZVXtwskVZVXdestructor)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162337;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_update_metrics")
int BPF_KPROBE(KtprobetcpVZVXupdateVZVXmetrics)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162339;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_update_metrics")
int BPF_KRETPROBE(KtretprobetcpVZVXupdateVZVXmetrics)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162339;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_update_skb_after_send")
int BPF_KPROBE(KtprobetcpVZVXupdateVZVXskbVZVXafterVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162341;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_update_skb_after_send")
int BPF_KRETPROBE(KtretprobetcpVZVXupdateVZVXskbVZVXafterVZVXsend)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162341;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_wfree")
int BPF_KPROBE(KtprobetcpVZVXwfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162342;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_wfree")
int BPF_KRETPROBE(KtretprobetcpVZVXwfree)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162342;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_write_wakeup")
int BPF_KPROBE(KtprobetcpVZVXwriteVZVXwakeup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162344;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_write_wakeup")
int BPF_KRETPROBE(KtretprobetcpVZVXwriteVZVXwakeup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162344;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_write_xmit")
int BPF_KPROBE(KtprobetcpVZVXwriteVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162346;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_write_xmit")
int BPF_KRETPROBE(KtretprobetcpVZVXwriteVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162346;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_xmit_probe_skb")
int BPF_KPROBE(KtprobetcpVZVXxmitVZVXprobeVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162348;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_xmit_probe_skb")
int BPF_KRETPROBE(KtretprobetcpVZVXxmitVZVXprobeVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162348;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcp_xmit_retransmit_queue")
int BPF_KPROBE(KtprobetcpVZVXxmitVZVXretransmitVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162349;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcp_xmit_retransmit_queue")
int BPF_KRETPROBE(KtretprobetcpVZVXxmitVZVXretransmitVZVXqueue)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162349;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tcpm_suck_dst")
int BPF_KPROBE(KtprobetcpmVZVXsuckVZVXdst)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162353;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tcpm_suck_dst")
int BPF_KRETPROBE(KtretprobetcpmVZVXsuckVZVXdst)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162353;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tls_client_hello_psk")
int BPF_KPROBE(KtprobetlsVZVXclientVZVXhelloVZVXpsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162551;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tls_client_hello_psk")
int BPF_KRETPROBE(KtretprobetlsVZVXclientVZVXhelloVZVXpsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162551;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tls_server_hello_psk")
int BPF_KPROBE(KtprobetlsVZVXserverVZVXhelloVZVXpsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162558;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tls_server_hello_psk")
int BPF_KRETPROBE(KtretprobetlsVZVXserverVZVXhelloVZVXpsk)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=162558;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/try_fill_recv")
int BPF_KPROBE(KtprobetryVZVXfillVZVXrecv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163494;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/try_fill_recv")
int BPF_KRETPROBE(KtretprobetryVZVXfillVZVXrecv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163494;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/tsconfig_send_reply")
int BPF_KPROBE(KtprobetsconfigVZVXsendVZVXreply)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163535;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/tsconfig_send_reply")
int BPF_KRETPROBE(KtretprobetsconfigVZVXsendVZVXreply)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163535;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp6_ehashfn")
int BPF_KPROBE(Ktprobeudp6VZVXehashfn)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163693;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp6_ehashfn")
int BPF_KRETPROBE(Ktretprobeudp6VZVXehashfn)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163693;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp6_gro_complete")
int BPF_KPROBE(Ktprobeudp6VZVXgroVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163694;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp6_gro_complete")
int BPF_KRETPROBE(Ktretprobeudp6VZVXgroVZVXcomplete)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163694;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp6_gro_receive")
int BPF_KPROBE(Ktprobeudp6VZVXgroVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163695;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp6_gro_receive")
int BPF_KRETPROBE(Ktretprobeudp6VZVXgroVZVXreceive)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163695;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp6_lib_lookup")
int BPF_KPROBE(Ktprobeudp6VZVXlibVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163697;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp6_lib_lookup")
int BPF_KRETPROBE(Ktretprobeudp6VZVXlibVZVXlookup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163697;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp6_lib_lookup2")
int BPF_KPROBE(Ktprobeudp6VZVXlibVZVXlookup2)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163699;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp6_lib_lookup2")
int BPF_KRETPROBE(Ktretprobeudp6VZVXlibVZVXlookup2)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163699;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp6_lib_lookup_skb")
int BPF_KPROBE(Ktprobeudp6VZVXlibVZVXlookupVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163700;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp6_lib_lookup_skb")
int BPF_KRETPROBE(Ktretprobeudp6VZVXlibVZVXlookupVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163700;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp6_proc_exit")
int BPF_KPROBE(Ktprobeudp6VZVXprocVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163701;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp6_proc_exit")
int BPF_KRETPROBE(Ktretprobeudp6VZVXprocVZVXexit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163701;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp6_proc_init")
int BPF_KPROBE(Ktprobeudp6VZVXprocVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163702;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp6_proc_init")
int BPF_KRETPROBE(Ktretprobeudp6VZVXprocVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163702;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp6_ufo_fragment")
int BPF_KPROBE(Ktprobeudp6VZVXufoVZVXfragment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163704;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp6_ufo_fragment")
int BPF_KRETPROBE(Ktretprobeudp6VZVXufoVZVXfragment)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163704;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp6_unicast_rcv_skb")
int BPF_KPROBE(Ktprobeudp6VZVXunicastVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163705;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp6_unicast_rcv_skb")
int BPF_KRETPROBE(Ktretprobeudp6VZVXunicastVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163705;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_v6_early_demux")
int BPF_KPROBE(KtprobeudpVZVXv6VZVXearlyVZVXdemux)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163706;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_v6_early_demux")
int BPF_KRETPROBE(KtretprobeudpVZVXv6VZVXearlyVZVXdemux)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163706;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_v6_get_port")
int BPF_KPROBE(KtprobeudpVZVXv6VZVXgetVZVXport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163707;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_v6_get_port")
int BPF_KRETPROBE(KtretprobeudpVZVXv6VZVXgetVZVXport)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163707;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_v6_push_pending_frames")
int BPF_KPROBE(KtprobeudpVZVXv6VZVXpushVZVXpendingVZVXframes)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163708;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_v6_push_pending_frames")
int BPF_KRETPROBE(KtretprobeudpVZVXv6VZVXpushVZVXpendingVZVXframes)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163708;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_v6_rehash")
int BPF_KPROBE(KtprobeudpVZVXv6VZVXrehash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163709;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_v6_rehash")
int BPF_KRETPROBE(KtretprobeudpVZVXv6VZVXrehash)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163709;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udp_v6_send_skb")
int BPF_KPROBE(KtprobeudpVZVXv6VZVXsendVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163711;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udp_v6_send_skb")
int BPF_KRETPROBE(KtretprobeudpVZVXv6VZVXsendVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163711;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udplite4_proc_exit_net")
int BPF_KPROBE(Ktprobeudplite4VZVXprocVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163712;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udplite4_proc_exit_net")
int BPF_KRETPROBE(Ktretprobeudplite4VZVXprocVZVXexitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163712;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udplite4_proc_init_net")
int BPF_KPROBE(Ktprobeudplite4VZVXprocVZVXinitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163713;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udplite4_proc_init_net")
int BPF_KRETPROBE(Ktretprobeudplite4VZVXprocVZVXinitVZVXnet)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163713;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udplite_err")
int BPF_KPROBE(KtprobeudpliteVZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163715;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udplite_err")
int BPF_KRETPROBE(KtretprobeudpliteVZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163715;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udplite_rcv")
int BPF_KPROBE(KtprobeudpliteVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163716;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udplite_rcv")
int BPF_KRETPROBE(KtretprobeudpliteVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163716;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udplite_sk_init")
int BPF_KPROBE(KtprobeudpliteVZVXskVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163717;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udplite_sk_init")
int BPF_KRETPROBE(KtretprobeudpliteVZVXskVZVXinit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163717;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udpv6_connect")
int BPF_KPROBE(Ktprobeudpv6VZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163718;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udpv6_connect")
int BPF_KRETPROBE(Ktretprobeudpv6VZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163718;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udpv6_destroy_sock")
int BPF_KPROBE(Ktprobeudpv6VZVXdestroyVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163719;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udpv6_destroy_sock")
int BPF_KRETPROBE(Ktretprobeudpv6VZVXdestroyVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163719;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udpv6_destruct_sock")
int BPF_KPROBE(Ktprobeudpv6VZVXdestructVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163720;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udpv6_destruct_sock")
int BPF_KRETPROBE(Ktretprobeudpv6VZVXdestructVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163720;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udpv6_err")
int BPF_KPROBE(Ktprobeudpv6VZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163722;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udpv6_err")
int BPF_KRETPROBE(Ktretprobeudpv6VZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163722;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udpv6_getsockopt")
int BPF_KPROBE(Ktprobeudpv6VZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163724;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udpv6_getsockopt")
int BPF_KRETPROBE(Ktretprobeudpv6VZVXgetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163724;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udpv6_init_sock")
int BPF_KPROBE(Ktprobeudpv6VZVXinitVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163726;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udpv6_init_sock")
int BPF_KRETPROBE(Ktretprobeudpv6VZVXinitVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163726;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udpv6_pre_connect")
int BPF_KPROBE(Ktprobeudpv6VZVXpreVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163729;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udpv6_pre_connect")
int BPF_KRETPROBE(Ktretprobeudpv6VZVXpreVZVXconnect)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163729;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udpv6_queue_rcv_one_skb")
int BPF_KPROBE(Ktprobeudpv6VZVXqueueVZVXrcvVZVXoneVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163730;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udpv6_queue_rcv_one_skb")
int BPF_KRETPROBE(Ktretprobeudpv6VZVXqueueVZVXrcvVZVXoneVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163730;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udpv6_queue_rcv_skb")
int BPF_KPROBE(Ktprobeudpv6VZVXqueueVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163731;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udpv6_queue_rcv_skb")
int BPF_KRETPROBE(Ktretprobeudpv6VZVXqueueVZVXrcvVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163731;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udpv6_rcv")
int BPF_KPROBE(Ktprobeudpv6VZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163732;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udpv6_rcv")
int BPF_KRETPROBE(Ktretprobeudpv6VZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163732;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udpv6_recvmsg")
int BPF_KPROBE(Ktprobeudpv6VZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163733;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udpv6_recvmsg")
int BPF_KRETPROBE(Ktretprobeudpv6VZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163733;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udpv6_setsockopt")
int BPF_KPROBE(Ktprobeudpv6VZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163735;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udpv6_setsockopt")
int BPF_KRETPROBE(Ktretprobeudpv6VZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163735;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/udpv6_splice_eof")
int BPF_KPROBE(Ktprobeudpv6VZVXspliceVZVXeof)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163736;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/udpv6_splice_eof")
int BPF_KRETPROBE(Ktretprobeudpv6VZVXspliceVZVXeof)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163736;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/unix_dgram_recvmsg")
int BPF_KPROBE(KtprobeunixVZVXdgramVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163793;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/unix_dgram_recvmsg")
int BPF_KRETPROBE(KtretprobeunixVZVXdgramVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163793;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/unix_dgram_sendmsg")
int BPF_KPROBE(KtprobeunixVZVXdgramVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163794;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/unix_dgram_sendmsg")
int BPF_KRETPROBE(KtretprobeunixVZVXdgramVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163794;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/unix_read_skb")
int BPF_KPROBE(KtprobeunixVZVXreadVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163815;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/unix_read_skb")
int BPF_KRETPROBE(KtretprobeunixVZVXreadVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163815;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/unix_release_sock")
int BPF_KPROBE(KtprobeunixVZVXreleaseVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163818;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/unix_release_sock")
int BPF_KRETPROBE(KtretprobeunixVZVXreleaseVZVXsock)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163818;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/unix_seqpacket_recvmsg")
int BPF_KPROBE(KtprobeunixVZVXseqpacketVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163825;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/unix_seqpacket_recvmsg")
int BPF_KRETPROBE(KtretprobeunixVZVXseqpacketVZVXrecvmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163825;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/unix_seqpacket_sendmsg")
int BPF_KPROBE(KtprobeunixVZVXseqpacketVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163826;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/unix_seqpacket_sendmsg")
int BPF_KRETPROBE(KtretprobeunixVZVXseqpacketVZVXsendmsg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163826;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/unix_setsockopt")
int BPF_KPROBE(KtprobeunixVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163827;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/unix_setsockopt")
int BPF_KRETPROBE(KtretprobeunixVZVXsetsockopt)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163827;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/unix_sock_destructor")
int BPF_KPROBE(KtprobeunixVZVXsockVZVXdestructor)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163832;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/unix_sock_destructor")
int BPF_KRETPROBE(KtretprobeunixVZVXsockVZVXdestructor)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163832;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/unix_socketpair")
int BPF_KPROBE(KtprobeunixVZVXsocketpair)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163833;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/unix_socketpair")
int BPF_KRETPROBE(KtretprobeunixVZVXsocketpair)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163833;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/unix_stream_read_skb")
int BPF_KPROBE(KtprobeunixVZVXstreamVZVXreadVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163841;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/unix_stream_read_skb")
int BPF_KRETPROBE(KtretprobeunixVZVXstreamVZVXreadVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163841;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/unix_stream_recv_urg")
int BPF_KPROBE(KtprobeunixVZVXstreamVZVXrecvVZVXurg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163843;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/unix_stream_recv_urg")
int BPF_KRETPROBE(KtretprobeunixVZVXstreamVZVXrecvVZVXurg)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=163843;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/validate_xmit_skb")
int BPF_KPROBE(KtprobevalidateVZVXxmitVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164284;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/validate_xmit_skb")
int BPF_KRETPROBE(KtretprobevalidateVZVXxmitVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164284;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/validate_xmit_skb_list")
int BPF_KPROBE(KtprobevalidateVZVXxmitVZVXskbVZVXlist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164285;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/validate_xmit_skb_list")
int BPF_KRETPROBE(KtretprobevalidateVZVXxmitVZVXskbVZVXlist)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164285;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/validate_xmit_xfrm")
int BPF_KPROBE(KtprobevalidateVZVXxmitVZVXxfrm)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164287;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/validate_xmit_xfrm")
int BPF_KRETPROBE(KtretprobevalidateVZVXxmitVZVXxfrm)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164287;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/virtnet_receive_xsk_bufs")
int BPF_KPROBE(KtprobevirtnetVZVXreceiveVZVXxskVZVXbufs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164558;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/virtnet_receive_xsk_bufs")
int BPF_KRETPROBE(KtretprobevirtnetVZVXreceiveVZVXxskVZVXbufs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164558;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/virtnet_rq_bind_xsk_pool")
int BPF_KPROBE(KtprobevirtnetVZVXrqVZVXbindVZVXxskVZVXpool)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164564;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/virtnet_rq_bind_xsk_pool")
int BPF_KRETPROBE(KtretprobevirtnetVZVXrqVZVXbindVZVXxskVZVXpool)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164564;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/virtnet_send_command_reply")
int BPF_KPROBE(KtprobevirtnetVZVXsendVZVXcommandVZVXreply)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164578;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/virtnet_send_command_reply")
int BPF_KRETPROBE(KtretprobevirtnetVZVXsendVZVXcommandVZVXreply)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164578;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/virtnet_send_ctrl_coal_vq_cmd")
int BPF_KPROBE(KtprobevirtnetVZVXsendVZVXctrlVZVXcoalVZVXvqVZVXcmd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164580;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/virtnet_send_ctrl_coal_vq_cmd")
int BPF_KRETPROBE(KtretprobevirtnetVZVXsendVZVXctrlVZVXcoalVZVXvqVZVXcmd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164580;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/virtnet_send_rx_ctrl_coal_vq_cmd")
int BPF_KPROBE(KtprobevirtnetVZVXsendVZVXrxVZVXctrlVZVXcoalVZVXvqVZVXcmd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164582;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/virtnet_send_rx_ctrl_coal_vq_cmd")
int BPF_KRETPROBE(KtretprobevirtnetVZVXsendVZVXrxVZVXctrlVZVXcoalVZVXvqVZVXcmd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164582;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/virtnet_send_rx_notf_coal_cmds")
int BPF_KPROBE(KtprobevirtnetVZVXsendVZVXrxVZVXnotfVZVXcoalVZVXcmds)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164584;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/virtnet_send_rx_notf_coal_cmds")
int BPF_KRETPROBE(KtretprobevirtnetVZVXsendVZVXrxVZVXnotfVZVXcoalVZVXcmds)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164584;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/virtnet_send_tx_ctrl_coal_vq_cmd")
int BPF_KPROBE(KtprobevirtnetVZVXsendVZVXtxVZVXctrlVZVXcoalVZVXvqVZVXcmd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164585;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/virtnet_send_tx_ctrl_coal_vq_cmd")
int BPF_KRETPROBE(KtretprobevirtnetVZVXsendVZVXtxVZVXctrlVZVXcoalVZVXvqVZVXcmd)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164585;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/virtnet_skb_append_frag")
int BPF_KPROBE(KtprobevirtnetVZVXskbVZVXappendVZVXfrag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164606;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/virtnet_skb_append_frag")
int BPF_KRETPROBE(KtretprobevirtnetVZVXskbVZVXappendVZVXfrag)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164606;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/virtnet_sq_bind_xsk_pool")
int BPF_KPROBE(KtprobevirtnetVZVXsqVZVXbindVZVXxskVZVXpool)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164608;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/virtnet_sq_bind_xsk_pool")
int BPF_KRETPROBE(KtretprobevirtnetVZVXsqVZVXbindVZVXxskVZVXpool)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164608;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/virtnet_xdp_xmit")
int BPF_KPROBE(KtprobevirtnetVZVXxdpVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164631;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/virtnet_xdp_xmit")
int BPF_KRETPROBE(KtretprobevirtnetVZVXxdpVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164631;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/virtnet_xsk_pool_enable")
int BPF_KPROBE(KtprobevirtnetVZVXxskVZVXpoolVZVXenable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164633;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/virtnet_xsk_pool_enable")
int BPF_KRETPROBE(KtretprobevirtnetVZVXxskVZVXpoolVZVXenable)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164633;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/virtnet_xsk_wakeup")
int BPF_KPROBE(KtprobevirtnetVZVXxskVZVXwakeup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164635;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/virtnet_xsk_wakeup")
int BPF_KRETPROBE(KtretprobevirtnetVZVXxskVZVXwakeup)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164635;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/virtnet_xsk_xmit")
int BPF_KPROBE(KtprobevirtnetVZVXxskVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164637;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/virtnet_xsk_xmit")
int BPF_KRETPROBE(KtretprobevirtnetVZVXxskVZVXxmit)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164637;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/wireless_send_event")
int BPF_KPROBE(KtprobewirelessVZVXsendVZVXevent)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164852;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/wireless_send_event")
int BPF_KRETPROBE(KtretprobewirelessVZVXsendVZVXevent)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=164852;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xfrm4_ipcomp_err")
int BPF_KPROBE(Ktprobexfrm4VZVXipcompVZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=165192;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xfrm4_ipcomp_err")
int BPF_KRETPROBE(Ktretprobexfrm4VZVXipcompVZVXerr)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=165192;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xfrm4_ipcomp_rcv")
int BPF_KPROBE(Ktprobexfrm4VZVXipcompVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=165193;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xfrm4_ipcomp_rcv")
int BPF_KRETPROBE(Ktretprobexfrm4VZVXipcompVZVXrcv)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=165193;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xmit_skb")
int BPF_KPROBE(KtprobexmitVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=165388;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xmit_skb")
int BPF_KRETPROBE(KtretprobexmitVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=165388;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_append_merge_buffer")
int BPF_KPROBE(KtprobexskVZVXappendVZVXmergeVZVXbuffer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=165391;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_append_merge_buffer")
int BPF_KRETPROBE(KtretprobexskVZVXappendVZVXmergeVZVXbuffer)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=165391;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_construct_skb")
int BPF_KPROBE(KtprobexskVZVXconstructVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=165393;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_construct_skb")
int BPF_KRETPROBE(KtretprobexskVZVXconstructVZVXskb)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=165393;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/xsk_drop_follow_bufs")
int BPF_KPROBE(KtprobexskVZVXdropVZVXfollowVZVXbufs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=165395;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/xsk_drop_follow_bufs")
int BPF_KRETPROBE(KtretprobexskVZVXdropVZVXfollowVZVXbufs)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=165395;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}

SEC("kprobe/zerocopy_fill_skb_from_iter")
int BPF_KPROBE(KtprobezerocopyVZVXfillVZVXskbVZVXfromVZVXiter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=165434;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
SEC("kretprobe/zerocopy_fill_skb_from_iter")
int BPF_KRETPROBE(KtretprobezerocopyVZVXfillVZVXskbVZVXfromVZVXiter)
{
    struct SkProbe *data = bpf_ringbuf_reserve(&events, sizeof(struct SkProbe), 0);
    if(!data){return 0;}
    data->FuncID=165434;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    bpf_ringbuf_submit(data, 0);
    return 0;
}
